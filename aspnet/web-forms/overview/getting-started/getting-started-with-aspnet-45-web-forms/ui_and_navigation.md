---
uid: web-forms/overview/getting-started/getting-started-with-aspnet-45-web-forms/ui_and_navigation
title: Interface utilisateur et navigation | Microsoft Docs
author: Erikre
description: Cette série de didacticiels vous apprend les bases de la création d’une application ASP.NET Web Forms à l’aide de ASP.NET 4,5 et Microsoft Visual Studio Express 2013 pour nous...
ms.author: riande
ms.date: 09/08/2014
ms.assetid: 5c76891d-e515-4885-b576-76bd2c494efe
msc.legacyurl: /web-forms/overview/getting-started/getting-started-with-aspnet-45-web-forms/ui_and_navigation
msc.type: authoredcontent
ms.openlocfilehash: ac1dcaf1ba911fdcaeb3845c6836ec771733d93e
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 03/06/2020
ms.locfileid: "78643555"
---
# <a name="ui-and-navigation"></a><span data-ttu-id="6fadd-103">Interface utilisateur et navigation</span><span class="sxs-lookup"><span data-stu-id="6fadd-103">UI and Navigation</span></span>

<span data-ttu-id="6fadd-104">par [Erik Reitan](https://github.com/Erikre)</span><span class="sxs-lookup"><span data-stu-id="6fadd-104">by [Erik Reitan](https://github.com/Erikre)</span></span>

<span data-ttu-id="6fadd-105">[Télécharger l’exemple de projet WingtipC#Toys ()](https://go.microsoft.com/fwlink/?LinkID=389434&clcid=0x409) ou [Télécharger le livre électronique (PDF)](https://download.microsoft.com/download/0/F/B/0FBFAA46-2BFD-478F-8E56-7BF3C672DF9D/Getting%20Started%20with%20ASP.NET%204.5%20Web%20Forms%20and%20Visual%20Studio%202013.pdf)</span><span class="sxs-lookup"><span data-stu-id="6fadd-105">[Download Wingtip Toys Sample Project (C#)](https://go.microsoft.com/fwlink/?LinkID=389434&clcid=0x409) or [Download E-book (PDF)](https://download.microsoft.com/download/0/F/B/0FBFAA46-2BFD-478F-8E56-7BF3C672DF9D/Getting%20Started%20with%20ASP.NET%204.5%20Web%20Forms%20and%20Visual%20Studio%202013.pdf)</span></span>

> <span data-ttu-id="6fadd-106">Cette série de didacticiels vous apprend les bases de la création d’une application ASP.NET Web Forms à l’aide de ASP.NET 4,5 et Microsoft Visual Studio Express 2013 pour le Web.</span><span class="sxs-lookup"><span data-stu-id="6fadd-106">This tutorial series will teach you the basics of building an ASP.NET Web Forms application using ASP.NET 4.5 and Microsoft Visual Studio Express 2013 for Web.</span></span> <span data-ttu-id="6fadd-107">Un [projet Visual Studio 2013 avec C# le code source](https://go.microsoft.com/fwlink/?LinkID=389434&clcid=0x409) est disponible pour accompagner cette série de didacticiels.</span><span class="sxs-lookup"><span data-stu-id="6fadd-107">A Visual Studio 2013 [project with C# source code](https://go.microsoft.com/fwlink/?LinkID=389434&clcid=0x409) is available to accompany this tutorial series.</span></span>

<span data-ttu-id="6fadd-108">Dans ce didacticiel, vous allez modifier l’interface utilisateur de l’application Web par défaut afin de prendre en charge les fonctionnalités de l’application Windows Store pour Wingtip Toys.</span><span class="sxs-lookup"><span data-stu-id="6fadd-108">In this tutorial, you will modify the UI of the default Web application to support features of the Wingtip Toys store front application.</span></span> <span data-ttu-id="6fadd-109">En outre, vous allez ajouter des navigations simples et liées aux données.</span><span class="sxs-lookup"><span data-stu-id="6fadd-109">Also, you will add simple and data bound navigation.</span></span> <span data-ttu-id="6fadd-110">Ce didacticiel s’appuie sur le didacticiel précédent « créer la couche d’accès aux données » et fait partie de la série de didacticiels sur Wingtip Toys.</span><span class="sxs-lookup"><span data-stu-id="6fadd-110">This tutorial builds on the previous tutorial "Create the Data Access Layer" and is part of the Wingtip Toys tutorial series.</span></span>

## <a name="what-youll-learn"></a><span data-ttu-id="6fadd-111">Ce que vous allez apprendre :</span><span class="sxs-lookup"><span data-stu-id="6fadd-111">What you'll learn:</span></span>

- <span data-ttu-id="6fadd-112">Comment modifier l’interface utilisateur pour prendre en charge les fonctionnalités de l’application Windows Store pour Wingtip Toys.</span><span class="sxs-lookup"><span data-stu-id="6fadd-112">How to change the UI to support features of the Wingtip Toys store front application.</span></span>
- <span data-ttu-id="6fadd-113">Comment configurer un élément HTML5 pour inclure la navigation entre les pages.</span><span class="sxs-lookup"><span data-stu-id="6fadd-113">How to configure an HTML5 element to include page navigation.</span></span>
- <span data-ttu-id="6fadd-114">Comment créer un contrôle piloté par les données pour accéder à des données de produit spécifiques.</span><span class="sxs-lookup"><span data-stu-id="6fadd-114">How to create a data-driven control to navigate to specific product data.</span></span>
- <span data-ttu-id="6fadd-115">Comment afficher les données d’une base de données créée à l’aide de Entity Framework Code First.</span><span class="sxs-lookup"><span data-stu-id="6fadd-115">How to display data from a database created using Entity Framework Code First.</span></span>

<span data-ttu-id="6fadd-116">ASP.NET Web Forms vous permettent de créer du contenu dynamique pour votre application Web.</span><span class="sxs-lookup"><span data-stu-id="6fadd-116">ASP.NET Web Forms allow you to create dynamic content for your Web application.</span></span> <span data-ttu-id="6fadd-117">Chaque page Web ASP.NET est créée d’une manière similaire à une page Web HTML statique (une page qui n’inclut pas le traitement basé sur le serveur), mais la page Web ASP.NET inclut des éléments supplémentaires que ASP.NET reconnaît et traite pour générer du code HTML lorsque la page s’exécute.</span><span class="sxs-lookup"><span data-stu-id="6fadd-117">Each ASP.NET Web page is created in a manner similar to a static HTML Web page (a page that does not include server-based processing), but ASP.NET Web page includes extra elements that ASP.NET recognizes and processes to generate HTML when the page runs.</span></span>

<span data-ttu-id="6fadd-118">Avec une page HTML statique (fichier *. htm* ou *. html* ), le serveur effectue une demande de `Web` en lisant le fichier et en l’envoyant tel quel au navigateur.</span><span class="sxs-lookup"><span data-stu-id="6fadd-118">With a static HTML page (*.htm* or *.html* file), the server fulfills a `Web` request by reading the file and sending it as-is to the browser.</span></span> <span data-ttu-id="6fadd-119">En revanche, lorsqu’un utilisateur demande une page Web ASP.NET (fichier *. aspx* ), la page s’exécute en tant que programme sur le serveur Web.</span><span class="sxs-lookup"><span data-stu-id="6fadd-119">In contrast, when someone requests an ASP.NET Web page (*.aspx* file), the page runs as a program on the Web server.</span></span> <span data-ttu-id="6fadd-120">Lorsque la page est en cours d’exécution, elle peut exécuter n’importe quelle tâche dont votre site Web a besoin, y compris le calcul des valeurs, la lecture ou l’écriture d’informations sur la base de données ou l’appel d’autres programmes.</span><span class="sxs-lookup"><span data-stu-id="6fadd-120">While the page is running, it can perform any task that your Web site requires, including calculating values, reading or writing database information, or calling other programs.</span></span> <span data-ttu-id="6fadd-121">Comme sortie, la page produit dynamiquement le balisage (par exemple, les éléments en HTML) et envoie cette sortie dynamique au navigateur.</span><span class="sxs-lookup"><span data-stu-id="6fadd-121">As its output, the page dynamically produces markup (such as elements in HTML) and sends this dynamic output to the browser.</span></span>

## <a name="modifying-the-ui"></a><span data-ttu-id="6fadd-122">Modification de l’interface utilisateur</span><span class="sxs-lookup"><span data-stu-id="6fadd-122">Modifying the UI</span></span>

<span data-ttu-id="6fadd-123">Vous allez poursuivre cette série de didacticiels en modifiant la page *default. aspx* .</span><span class="sxs-lookup"><span data-stu-id="6fadd-123">You'll continue this tutorial series by modifying the *Default.aspx* page.</span></span> <span data-ttu-id="6fadd-124">Vous allez modifier l’interface utilisateur qui est déjà établie par le modèle par défaut utilisé pour créer l’application.</span><span class="sxs-lookup"><span data-stu-id="6fadd-124">You will modify the UI that's already established by the default template used to create the application.</span></span> <span data-ttu-id="6fadd-125">Le type de modifications que vous effectuerez est normal lors de la création d’une application Web Forms.</span><span class="sxs-lookup"><span data-stu-id="6fadd-125">The type of modifications you'll do are typical when creating any Web Forms application.</span></span> <span data-ttu-id="6fadd-126">Pour ce faire, vous devez modifier le titre, remplacer du contenu et supprimer le contenu par défaut superflu.</span><span class="sxs-lookup"><span data-stu-id="6fadd-126">You'll do this by changing the title, replacing some content, and removing unneeded default content.</span></span>

1. <span data-ttu-id="6fadd-127">Ouvrez ou basculez vers la page *default. aspx* .</span><span class="sxs-lookup"><span data-stu-id="6fadd-127">Open or switch to the *Default.aspx* page.</span></span>
2. <span data-ttu-id="6fadd-128">Si la page s’affiche en mode **conception** , basculez en mode **source** .</span><span class="sxs-lookup"><span data-stu-id="6fadd-128">If the page appears in **Design** view, switch to **Source** view.</span></span>
3. <span data-ttu-id="6fadd-129">En haut de la page de la `@Page` directive, remplacez l’attribut `Title` par « Welcome », comme indiqué en jaune ci-dessous.</span><span class="sxs-lookup"><span data-stu-id="6fadd-129">At the top of the page in the `@Page` directive, change the `Title` attribute to "Welcome", as shown highlighted in yellow below.</span></span> 

    [!code-aspx[Main](ui_and_navigation/samples/sample1.aspx?highlight=1)]
4. <span data-ttu-id="6fadd-130">Également sur la page *default. aspx* , remplacez tout le contenu par défaut contenu dans la balise `<asp:Content>` afin que le balisage apparaisse comme indiqué ci-dessous.</span><span class="sxs-lookup"><span data-stu-id="6fadd-130">Also on the *Default.aspx* page, replace all of the default content contained in the `<asp:Content>` tag so that the markup appears as below.</span></span> 

    [!code-aspx[Main](ui_and_navigation/samples/sample2.aspx)]
5. <span data-ttu-id="6fadd-131">Enregistrez la page *default. aspx* en sélectionnant **Enregistrer default. aspx** dans le menu **fichier** .</span><span class="sxs-lookup"><span data-stu-id="6fadd-131">Save the *Default.aspx* page by selecting **Save Default.aspx** from the **File** menu.</span></span>

   <span data-ttu-id="6fadd-132">La page *default. aspx* qui en résulte s’affiche comme suit :</span><span class="sxs-lookup"><span data-stu-id="6fadd-132">The resulting *Default.aspx* page will appear as follows:</span></span> 

[!code-aspx[Main](ui_and_navigation/samples/sample3.aspx)]

<span data-ttu-id="6fadd-133">Dans l’exemple, vous avez défini l’attribut `Title` de la directive `@Page`.</span><span class="sxs-lookup"><span data-stu-id="6fadd-133">In the example, you have set the `Title` attribute of the `@Page` directive.</span></span> <span data-ttu-id="6fadd-134">Lorsque le code HTML est affiché dans un navigateur, le `<%: Page.Title %>` de code du serveur correspond au contenu de l’attribut `Title`.</span><span class="sxs-lookup"><span data-stu-id="6fadd-134">When the HTML is displayed in a browser, the server code `<%: Page.Title %>` resolves to the content contained in the `Title` attribute.</span></span>

<span data-ttu-id="6fadd-135">La page d’exemple comprend les éléments de base qui constituent une page Web ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="6fadd-135">The example page includes the basic elements that constitute an ASP.NET Web page.</span></span> <span data-ttu-id="6fadd-136">La page contient du texte statique comme vous pouvez le faire dans une page HTML, ainsi que des éléments spécifiques à ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="6fadd-136">The page contains static text as you might have in an HTML page, along with elements that are specific to ASP.NET.</span></span> <span data-ttu-id="6fadd-137">Le contenu de la page *default. aspx* sera intégré au contenu de la page maître, ce qui sera expliqué plus loin dans ce didacticiel.</span><span class="sxs-lookup"><span data-stu-id="6fadd-137">The content contained in the *Default.aspx* page will be integrated with the master page content, which will be explained later in this tutorial.</span></span>

### <a name="page-directive"></a><span data-ttu-id="6fadd-138">@Page directive</span><span class="sxs-lookup"><span data-stu-id="6fadd-138">@Page Directive</span></span>

<span data-ttu-id="6fadd-139">Les Web Forms ASP.NET contiennent généralement des directives qui vous permettent de spécifier les propriétés de page et les informations de configuration de la page.</span><span class="sxs-lookup"><span data-stu-id="6fadd-139">ASP.NET Web Forms usually contain directives that allow you to specify page properties and configuration information for the page.</span></span> <span data-ttu-id="6fadd-140">Les directives sont utilisées par ASP.NET comme instructions sur le traitement de la page, mais elles ne sont pas rendues dans le balisage envoyé au navigateur.</span><span class="sxs-lookup"><span data-stu-id="6fadd-140">The directives are used by ASP.NET as instructions for how to process the page, but they are not rendered as part of the markup that is sent to the browser.</span></span>

<span data-ttu-id="6fadd-141">La directive la plus couramment utilisée est la directive `@Page`, qui vous permet de spécifier de nombreuses options de configuration pour la page, y compris les suivantes :</span><span class="sxs-lookup"><span data-stu-id="6fadd-141">The most commonly used directive is the `@Page` directive, which allows you to specify many configuration options for the page, including the following:</span></span>

1. <span data-ttu-id="6fadd-142">Langage de programmation serveur du code dans la page, tel que C#.</span><span class="sxs-lookup"><span data-stu-id="6fadd-142">The server programming language for code in the page, such as C#.</span></span>
2. <span data-ttu-id="6fadd-143">Si la page est une page avec du code serveur directement dans la page, appelée page à fichier unique, ou s’il s’agit d’une page contenant du code dans un fichier de classe distinct, appelé page code-behind.</span><span class="sxs-lookup"><span data-stu-id="6fadd-143">Whether the page is a page with server code directly in the page, which is called a single-file page, or whether it is a page with code in a separate class file, which is called a code-behind page.</span></span>
3. <span data-ttu-id="6fadd-144">Indique si la page est associée à une page maître et doit donc être traitée comme une page de contenu.</span><span class="sxs-lookup"><span data-stu-id="6fadd-144">Whether the page has an associated master page and should therefore be treated as a content page.</span></span>
4. <span data-ttu-id="6fadd-145">Options de débogage et de traçage.</span><span class="sxs-lookup"><span data-stu-id="6fadd-145">Debugging and tracing options.</span></span>

<span data-ttu-id="6fadd-146">Si vous n’incluez pas de directive `@Page` dans la page, ou si la directive n’inclut pas de paramètre spécifique, un paramètre est hérité du fichier de configuration *Web. config* ou du fichier de configuration *machine. config* .</span><span class="sxs-lookup"><span data-stu-id="6fadd-146">If you do not include an `@Page` directive in the page, or if the directive does not include a specific setting, a setting will be inherited from the *Web.config* configuration file or from the *Machine.config* configuration file.</span></span> <span data-ttu-id="6fadd-147">Le fichier *machine. config* fournit des paramètres de configuration supplémentaires pour toutes les applications qui s’exécutent sur un ordinateur.</span><span class="sxs-lookup"><span data-stu-id="6fadd-147">The *Machine.config* file provides additional configuration settings to all applications running on a machine.</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="6fadd-148">Le *fichier machine. config* fournit également des détails sur tous les paramètres de configuration possibles.</span><span class="sxs-lookup"><span data-stu-id="6fadd-148">The *Machine.config* also provides details about all possible configuration settings.</span></span>

### <a name="web-server-controls"></a><span data-ttu-id="6fadd-149">Contrôles serveur Web</span><span class="sxs-lookup"><span data-stu-id="6fadd-149">Web Server Controls</span></span>

<span data-ttu-id="6fadd-150">Dans la plupart des ASP.NET Web Forms applications, vous allez ajouter des contrôles qui permettent à l’utilisateur d’interagir avec la page, comme des boutons, des zones de texte, des listes, etc.</span><span class="sxs-lookup"><span data-stu-id="6fadd-150">In most ASP.NET Web Forms applications, you will add controls that allow the user to interact with the page, such as buttons, text boxes, lists, and so on.</span></span> <span data-ttu-id="6fadd-151">Ces contrôles serveur Web sont similaires aux boutons HTML et aux éléments d’entrée.</span><span class="sxs-lookup"><span data-stu-id="6fadd-151">These Web server controls are similar to HTML buttons and input elements.</span></span> <span data-ttu-id="6fadd-152">Toutefois, ils sont traités sur le serveur, ce qui vous permet d’utiliser le code serveur pour définir leurs propriétés.</span><span class="sxs-lookup"><span data-stu-id="6fadd-152">However, they are processed on the server, allowing you to use server code to set their properties.</span></span> <span data-ttu-id="6fadd-153">Ces contrôles déclenchent également des événements que vous pouvez gérer dans le code serveur.</span><span class="sxs-lookup"><span data-stu-id="6fadd-153">These controls also raise events that you can handle in server code.</span></span>

<span data-ttu-id="6fadd-154">Les contrôles serveur utilisent une syntaxe spéciale que ASP.NET reconnaît lorsque la page s’exécute.</span><span class="sxs-lookup"><span data-stu-id="6fadd-154">Server controls use a special syntax that ASP.NET recognizes when the page runs.</span></span> <span data-ttu-id="6fadd-155">Le nom de balise pour les contrôles serveur ASP.NET commence par un préfixe `asp:`.</span><span class="sxs-lookup"><span data-stu-id="6fadd-155">The tag name for ASP.NET server controls starts with an `asp:` prefix.</span></span> <span data-ttu-id="6fadd-156">Cela permet à ASP.NET de reconnaître et de traiter ces contrôles serveur.</span><span class="sxs-lookup"><span data-stu-id="6fadd-156">This allows ASP.NET to recognize and process these server controls.</span></span> <span data-ttu-id="6fadd-157">Le préfixe peut être différent si le contrôle ne fait pas partie du .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="6fadd-157">The prefix might be different if the control is not part of the .NET Framework.</span></span> <span data-ttu-id="6fadd-158">Outre le préfixe `asp:`, les contrôles serveur ASP.NET incluent également l’attribut `runat="server"` et un `ID` que vous pouvez utiliser pour référencer le contrôle dans le code serveur.</span><span class="sxs-lookup"><span data-stu-id="6fadd-158">In addition to the `asp:` prefix, ASP.NET server controls also include the `runat="server"` attribute and an `ID` that you can use to reference the control in server code.</span></span>

<span data-ttu-id="6fadd-159">Lorsque la page s’exécute, ASP.NET identifie les contrôles serveur et exécute le code associé à ces contrôles.</span><span class="sxs-lookup"><span data-stu-id="6fadd-159">When the page runs, ASP.NET identifies the server controls and runs the code that is associated with those controls.</span></span> <span data-ttu-id="6fadd-160">De nombreux contrôles affichent du code HTML ou autre balisage dans la page lorsqu’il est affiché dans un navigateur.</span><span class="sxs-lookup"><span data-stu-id="6fadd-160">Many controls render some HTML or other markup into the page when it is displayed in a browser.</span></span>

### <a name="server-code"></a><span data-ttu-id="6fadd-161">Code du serveur</span><span class="sxs-lookup"><span data-stu-id="6fadd-161">Server Code</span></span>

<span data-ttu-id="6fadd-162">La plupart des applications ASP.NET Web Forms incluent du code qui s’exécute sur le serveur lors du traitement de la page.</span><span class="sxs-lookup"><span data-stu-id="6fadd-162">Most ASP.NET Web Forms applications include code that runs on the server when the page is processed.</span></span> <span data-ttu-id="6fadd-163">Comme indiqué ci-dessus, le code serveur peut être utilisé pour effectuer diverses opérations, telles que l’ajout de données à un contrôle ListView.</span><span class="sxs-lookup"><span data-stu-id="6fadd-163">As mentioned above, server code can be used to do a variety of things, such as adding data to a ListView control.</span></span> <span data-ttu-id="6fadd-164">ASP.NET prend en charge de nombreux langages à exécuter sur C#le serveur, notamment, Visual Basic, J# et d’autres.</span><span class="sxs-lookup"><span data-stu-id="6fadd-164">ASP.NET supports many languages to run on the server, including C#, Visual Basic, J#, and others.</span></span>

<span data-ttu-id="6fadd-165">ASP.NET prend en charge deux modèles pour l’écriture de code serveur pour une page Web.</span><span class="sxs-lookup"><span data-stu-id="6fadd-165">ASP.NET supports two models for writing server code for a Web page.</span></span> <span data-ttu-id="6fadd-166">Dans le modèle à fichier unique, le code de la page se trouve dans un élément script dans lequel la balise d’ouverture comprend l’attribut `runat="server"`.</span><span class="sxs-lookup"><span data-stu-id="6fadd-166">In the single-file model, the code for the page is in a script element where the opening tag includes the `runat="server"` attribute.</span></span> <span data-ttu-id="6fadd-167">Vous pouvez également créer le code de la page dans un fichier de classe distinct, appelé modèle code-behind.</span><span class="sxs-lookup"><span data-stu-id="6fadd-167">Alternatively, you can create the code for the page in a separate class file, which is referred to as the code-behind model.</span></span> <span data-ttu-id="6fadd-168">Dans ce cas, la page de Web Forms ASP.NET ne contient généralement aucun code serveur.</span><span class="sxs-lookup"><span data-stu-id="6fadd-168">In this case, the ASP.NET Web Forms page generally contains no server code.</span></span> <span data-ttu-id="6fadd-169">Au lieu de cela, la directive `@Page` contient des informations qui relient la page *. aspx* avec son fichier code-behind associé.</span><span class="sxs-lookup"><span data-stu-id="6fadd-169">Instead, the `@Page` directive includes information that links the *.aspx* page with its associated code-behind file.</span></span>

<span data-ttu-id="6fadd-170">L’attribut `CodeBehind` contenu dans la directive `@Page` spécifie le nom du fichier de classe distinct, et l’attribut `Inherits` spécifie le nom de la classe dans le fichier code-behind qui correspond à la page.</span><span class="sxs-lookup"><span data-stu-id="6fadd-170">The `CodeBehind` attribute contained in the `@Page` directive specifies the name of the separate class file, and the `Inherits` attribute specifies the name of the class within the code-behind file that corresponds to the page.</span></span>

### <a name="updating-the-master-page"></a><span data-ttu-id="6fadd-171">Mise à jour de la page maître</span><span class="sxs-lookup"><span data-stu-id="6fadd-171">Updating the Master Page</span></span>

<span data-ttu-id="6fadd-172">Dans ASP.NET Web Forms, les pages maîtres vous permettent de créer une mise en page homogène pour les pages de votre application.</span><span class="sxs-lookup"><span data-stu-id="6fadd-172">In ASP.NET Web Forms, master pages allow you to create a consistent layout for the pages in your application.</span></span> <span data-ttu-id="6fadd-173">Une seule page maître définit la présentation et le comportement standard voulus pour toutes les pages (ou un groupe de pages) dans votre application.</span><span class="sxs-lookup"><span data-stu-id="6fadd-173">A single master page defines the look and feel and standard behavior that you want for all of the pages (or a group of pages) in your application.</span></span> <span data-ttu-id="6fadd-174">Vous pouvez ensuite créer des pages de contenu individuelles qui contiennent le contenu que vous souhaitez afficher, comme expliqué ci-dessus.</span><span class="sxs-lookup"><span data-stu-id="6fadd-174">You can then create individual content pages that contain the content you want to display, as explained above.</span></span> <span data-ttu-id="6fadd-175">Lorsque les utilisateurs demandent les pages de contenu, ASP.NET les fusionne avec la page maître pour produire une sortie qui associe la disposition de la page maître et le contenu de la page de contenu.</span><span class="sxs-lookup"><span data-stu-id="6fadd-175">When users request the content pages, ASP.NET merges them with the master page to produce output that combines the layout of the master page with the content from the content page.</span></span>

<span data-ttu-id="6fadd-176">Le nouveau site a besoin d’un seul logo à afficher sur chaque page.</span><span class="sxs-lookup"><span data-stu-id="6fadd-176">The new site needs a single logo to display on every page.</span></span> <span data-ttu-id="6fadd-177">Pour ajouter ce logo, vous pouvez modifier le code HTML sur la page maître.</span><span class="sxs-lookup"><span data-stu-id="6fadd-177">To add this logo, you can modify the HTML on the master page.</span></span>

1. <span data-ttu-id="6fadd-178">Dans l' **Explorateur de solutions**, recherchez et ouvrez la page **Site.Master** .</span><span class="sxs-lookup"><span data-stu-id="6fadd-178">In **Solution Explorer**, find and open the **Site.Master** page.</span></span>
2. <span data-ttu-id="6fadd-179">Si la page est en mode **conception** , basculez en mode **source** .</span><span class="sxs-lookup"><span data-stu-id="6fadd-179">If the page is in **Design** view, switch to **Source** view.</span></span>
3. <span data-ttu-id="6fadd-180">Mettez à jour la page maître en **modifiant ou en ajoutant** le balisage mis en surbrillance en jaune :</span><span class="sxs-lookup"><span data-stu-id="6fadd-180">Update the master page by **modifying or adding** the markup highlighted in yellow:</span></span> 

    [!code-aspx[Main](ui_and_navigation/samples/sample4.aspx?highlight=9,49,76-81,87)]

<span data-ttu-id="6fadd-181">Ce code HTML affiche l’image nommée *logo. jpg* à partir du dossier *images* de l’application Web, que vous ajouterez ultérieurement.</span><span class="sxs-lookup"><span data-stu-id="6fadd-181">This HTML will display the image named *logo.jpg* from the *Images* folder of the Web application, which you'll add later.</span></span> <span data-ttu-id="6fadd-182">Quand une page qui utilise la page maître s’affiche dans un navigateur, le logo s’affiche.</span><span class="sxs-lookup"><span data-stu-id="6fadd-182">When a page that uses the master page is displayed in a browser, the logo will be displayed.</span></span> <span data-ttu-id="6fadd-183">Si un utilisateur clique sur le logo, l’utilisateur accède à la page *default. aspx* .</span><span class="sxs-lookup"><span data-stu-id="6fadd-183">If a user clicks on the logo, the user will navigate back to the *Default.aspx* page.</span></span> <span data-ttu-id="6fadd-184">La balise d’ancrage HTML `<a>` encapsule le contrôle serveur d’images et autorise l’inclusion de l’image dans le cadre du lien.</span><span class="sxs-lookup"><span data-stu-id="6fadd-184">The HTML anchor tag `<a>` wraps the image server control and allows the image to be included as part of the link.</span></span> <span data-ttu-id="6fadd-185">L’attribut `href` pour la balise d’ancrage spécifie le «`~/`» racine du site Web en tant qu’emplacement du lien.</span><span class="sxs-lookup"><span data-stu-id="6fadd-185">The `href` attribute for the anchor tag specifies the root "`~/`" of the Web site as the link location.</span></span> <span data-ttu-id="6fadd-186">Par défaut, la page *default. aspx* s’affiche lorsque l’utilisateur accède à la racine du site Web.</span><span class="sxs-lookup"><span data-stu-id="6fadd-186">By default, the *Default.aspx* page is displayed when the user navigates to the root of the Web site.</span></span> <span data-ttu-id="6fadd-187">L' **Image** `<asp:Image>` contrôle serveur comprend des propriétés d’addition, telles que des `BorderStyle`, qui s’affichent au format html lorsqu’elles sont affichées dans un navigateur.</span><span class="sxs-lookup"><span data-stu-id="6fadd-187">The **Image** `<asp:Image>` server control includes addition properties, such as `BorderStyle`, that render as HTML when displayed in a browser.</span></span>

### <a name="master-pages"></a><span data-ttu-id="6fadd-188">Pages maîtres</span><span class="sxs-lookup"><span data-stu-id="6fadd-188">Master Pages</span></span>

<span data-ttu-id="6fadd-189">Une page maître est un fichier ASP.NET avec l’extension. Master (par exemple, *site. Master*) avec une disposition prédéfinie qui peut inclure du texte statique, des éléments HTML et des contrôles serveur.</span><span class="sxs-lookup"><span data-stu-id="6fadd-189">A master page is an ASP.NET file with the extension .master (for example, *Site.Master*) with a predefined layout that can include static text, HTML elements, and server controls.</span></span> <span data-ttu-id="6fadd-190">La page maître est identifiée par une directive de `@Master` spéciale qui remplace la directive `@Page` utilisée pour les pages *. aspx* ordinaires.</span><span class="sxs-lookup"><span data-stu-id="6fadd-190">The master page is identified by a special `@Master` directive that replaces the `@Page` directive that is used for ordinary *.aspx* pages.</span></span>

<span data-ttu-id="6fadd-191">En plus de la directive `@Master`, la page maître contient également tous les éléments HTML de niveau supérieur d’une page, comme `html`, `head`et `form`.</span><span class="sxs-lookup"><span data-stu-id="6fadd-191">In addition to the `@Master` directive, the master page also contains all of the top-level HTML elements for a page, such as `html`, `head`, and `form`.</span></span> <span data-ttu-id="6fadd-192">Par exemple, sur la page maître que vous avez ajoutée ci-dessus, vous utilisez un `table` HTML pour la disposition, un élément `img` pour le logo de l’entreprise, du texte statique et des contrôles serveur pour gérer l’appartenance commune à votre site.</span><span class="sxs-lookup"><span data-stu-id="6fadd-192">For example, on the master page you added above, you use an HTML `table` for the layout, an `img` element for the company logo, static text, and server controls to handle common membership for your site.</span></span> <span data-ttu-id="6fadd-193">Vous pouvez utiliser n’importe quel élément HTML et tout élément ASP.NET dans le cadre de votre page maître.</span><span class="sxs-lookup"><span data-stu-id="6fadd-193">You can use any HTML and any ASP.NET elements as part of your master page.</span></span>

<span data-ttu-id="6fadd-194">En plus du texte statique et des contrôles qui s’affichent sur toutes les pages, la page maître comprend également un ou plusieurs contrôles **ContentPlaceHolder** .</span><span class="sxs-lookup"><span data-stu-id="6fadd-194">In addition to static text and controls that will appear on all pages, the master page also includes one or more **ContentPlaceHolder** controls.</span></span> <span data-ttu-id="6fadd-195">Ces contrôles d’espace réservé définissent les régions dans lesquelles le contenu remplaçable s’affichera.</span><span class="sxs-lookup"><span data-stu-id="6fadd-195">These placeholder controls define regions where replaceable content will appear.</span></span> <span data-ttu-id="6fadd-196">Le contenu remplaçable est ensuite défini dans les pages de contenu, par exemple *default. aspx*, à l’aide du contrôle **content** Server.</span><span class="sxs-lookup"><span data-stu-id="6fadd-196">In turn, the replaceable content is defined in content pages, such as *Default.aspx*, using the **Content** server control.</span></span>

#### <a name="adding-image-files"></a><span data-ttu-id="6fadd-197">Ajout de fichiers image</span><span class="sxs-lookup"><span data-stu-id="6fadd-197">Adding Image Files</span></span>

<span data-ttu-id="6fadd-198">L’image de logo référencée ci-dessus, ainsi que toutes les images de produit, doivent être ajoutées à l’application Web afin qu’elles puissent être affichées lorsque le projet est affiché dans un navigateur.</span><span class="sxs-lookup"><span data-stu-id="6fadd-198">The logo image that is referenced above, along with all the product images, must be added to the Web application so that they can be seen when the project is displayed in a browser.</span></span>

#### <a name="download-from-msdn-samples-site"></a><span data-ttu-id="6fadd-199">Télécharger à partir du site d’exemples MSDN :</span><span class="sxs-lookup"><span data-stu-id="6fadd-199">Download from MSDN Samples site:</span></span>

<span data-ttu-id="6fadd-200">[Prise en main avec ASP.NET 4,5 Web Forms et Visual Studio 2013-Wingtip Toys](https://go.microsoft.com/fwlink/?LinkID=389434&clcid=0x409) (C#)</span><span class="sxs-lookup"><span data-stu-id="6fadd-200">[Getting Started with ASP.NET 4.5 Web Forms and Visual Studio 2013 - Wingtip Toys](https://go.microsoft.com/fwlink/?LinkID=389434&clcid=0x409) (C#)</span></span>

<span data-ttu-id="6fadd-201">Le téléchargement comprend des ressources dans le dossier *WingtipToys-biens* utilisées pour créer l’exemple d’application.</span><span class="sxs-lookup"><span data-stu-id="6fadd-201">The download includes resources in the *WingtipToys-Assets* folder that are used to create the sample application.</span></span>

1. <span data-ttu-id="6fadd-202">Si vous ne l’avez pas déjà fait, téléchargez les exemples de fichiers compressés à l’aide du lien ci-dessus à partir du site d’exemples MSDN.</span><span class="sxs-lookup"><span data-stu-id="6fadd-202">If you haven't already done so, download the compressed sample files using the above link from the MSDN Samples site.</span></span>
2. <span data-ttu-id="6fadd-203">Une fois téléchargé, ouvrez le fichier. zip et copiez le contenu dans un dossier local sur votre ordinateur.</span><span class="sxs-lookup"><span data-stu-id="6fadd-203">Once downloaded, open the .zip file and copy the contents to a local folder on your machine.</span></span>
3. <span data-ttu-id="6fadd-204">Recherchez et ouvrez le dossier *WingtipToys-biens* .</span><span class="sxs-lookup"><span data-stu-id="6fadd-204">Find and open the *WingtipToys-Assets* folder.</span></span>
4. <span data-ttu-id="6fadd-205">Par glisser-déplacer, copiez le dossier de *catalogue* à partir de votre dossier local vers la racine du projet d’application Web dans le **Explorateur de solutions** de Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="6fadd-205">By dragging and dropping, copy the *Catalog* folder from your local folder to the root of the Web application project in the **Solution Explorer** of Visual Studio.</span></span> 

    ![Interface utilisateur et navigation-copier des fichiers](ui_and_navigation/_static/image1.png)
5. <span data-ttu-id="6fadd-207">Ensuite, créez un dossier nommé *images* en cliquant avec le bouton droit sur le projet **WingtipToys** dans **Explorateur de solutions** et en sélectionnant **Ajouter** -&gt; **nouveau dossier**.</span><span class="sxs-lookup"><span data-stu-id="6fadd-207">Next, create a new folder named *Images* by right-clicking the **WingtipToys** project in **Solution Explorer** and selecting **Add** -&gt; **New Folder**.</span></span>
6. <span data-ttu-id="6fadd-208">Copiez le fichier *logo. jpg* du dossier *WingtipToys-biens* de l' **Explorateur de fichiers** dans le dossier *Images* du projet d’application Web dans **Explorateur de solutions** de Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="6fadd-208">Copy the *logo.jpg* file from the *WingtipToys-Assets* folder in **File Explorer** to the *Images* folder of the Web application project in **Solution Explorer** of Visual Studio.</span></span>
7. <span data-ttu-id="6fadd-209">Cliquez sur l’option **Afficher tous les fichiers** en haut de **Explorateur de solutions** pour mettre à jour la liste des fichiers si vous ne voyez pas les nouveaux fichiers.</span><span class="sxs-lookup"><span data-stu-id="6fadd-209">Click the **Show All Files** option at the top of **Solution Explorer** to update the list of files if you don't see the new files.</span></span>  
  
    <span data-ttu-id="6fadd-210">**Explorateur de solutions** affiche maintenant les fichiers de projet mis à jour.</span><span class="sxs-lookup"><span data-stu-id="6fadd-210">**Solution Explorer** now shows the updated project files.</span></span> 

    ![Interface utilisateur et navigation-Explorateur de solutions](ui_and_navigation/_static/image2.png)

### <a name="adding-pages"></a><span data-ttu-id="6fadd-212">Ajout de pages</span><span class="sxs-lookup"><span data-stu-id="6fadd-212">Adding Pages</span></span>

<span data-ttu-id="6fadd-213">Avant d’ajouter la navigation à l’application Web, vous devez d’abord ajouter deux nouvelles pages vers lesquelles vous allez accéder.</span><span class="sxs-lookup"><span data-stu-id="6fadd-213">Before adding navigation to the Web application, you'll first add two new pages that you'll navigate to.</span></span> <span data-ttu-id="6fadd-214">Plus loin dans cette série de didacticiels, vous afficherez les produits et les détails du produit sur ces nouvelles pages.</span><span class="sxs-lookup"><span data-stu-id="6fadd-214">Later in this tutorial series, you'll display products and product details on these new pages.</span></span>

1. <span data-ttu-id="6fadd-215">Dans **Explorateur de solutions**, cliquez avec le bouton droit sur **WingtipToys**, cliquez sur **Ajouter**, puis sur **nouvel élément**.</span><span class="sxs-lookup"><span data-stu-id="6fadd-215">In **Solution Explorer**, right-click **WingtipToys**, click **Add**, and then click **New Item**.</span></span>   
 <span data-ttu-id="6fadd-216">La boîte de dialogue **Ajouter un nouvel élément** s’affiche.</span><span class="sxs-lookup"><span data-stu-id="6fadd-216">The **Add New Item** dialog box is displayed.</span></span>
2. <span data-ttu-id="6fadd-217">Sélectionnez le **groupe C# Visual** -&gt; des modèles **Web** sur la gauche.</span><span class="sxs-lookup"><span data-stu-id="6fadd-217">Select the **Visual C#** -&gt; **Web** templates group on the left.</span></span> <span data-ttu-id="6fadd-218">Ensuite, sélectionnez **Web Form avec page maître** dans la liste du milieu, puis nommez-le *ProductList. aspx*.</span><span class="sxs-lookup"><span data-stu-id="6fadd-218">Then, select **Web Form with Master Page** from the middle list and name it *ProductList.aspx*.</span></span> 

    ![Interface utilisateur et navigation-boîte de dialogue Ajouter un nouvel élément](ui_and_navigation/_static/image3.png)
3. <span data-ttu-id="6fadd-220">Sélectionnez **site. Master** pour attacher la page maître à la page *. aspx* nouvellement créée.</span><span class="sxs-lookup"><span data-stu-id="6fadd-220">Select **Site.Master** to attach the master page to the newly created *.aspx* page.</span></span> 

    ![Interface utilisateur et navigation-sélectionner la page maître](ui_and_navigation/_static/image4.png)
4. <span data-ttu-id="6fadd-222">Ajoutez une page supplémentaire nommée *ProductDetails. aspx* en suivant la même procédure.</span><span class="sxs-lookup"><span data-stu-id="6fadd-222">Add an additional page named *ProductDetails.aspx* by following these same steps.</span></span>

### <a name="updating-bootstrap"></a><span data-ttu-id="6fadd-223">Mise à jour de bootstrap</span><span class="sxs-lookup"><span data-stu-id="6fadd-223">Updating Bootstrap</span></span>

<span data-ttu-id="6fadd-224">Les modèles de projet Visual Studio 2013 utilisent [bootstrap](http://getbootstrap.com/), une disposition et une infrastructure de thèmes créés par Twitter.</span><span class="sxs-lookup"><span data-stu-id="6fadd-224">The Visual Studio 2013 project templates use [Bootstrap](http://getbootstrap.com/), a layout and theming framework created by Twitter.</span></span> <span data-ttu-id="6fadd-225">Bootstrap utilise CSS3 pour fournir une conception réactive, ce qui signifie que les dispositions peuvent s’adapter de manière dynamique à différentes tailles de fenêtre de navigateur.</span><span class="sxs-lookup"><span data-stu-id="6fadd-225">Bootstrap uses CSS3 to provide responsive design, which means layouts can dynamically adapt to different browser window sizes.</span></span> <span data-ttu-id="6fadd-226">Vous pouvez également utiliser la fonctionnalité de thèmes d’amorçage pour effectuer facilement une modification de l’apparence de l’application.</span><span class="sxs-lookup"><span data-stu-id="6fadd-226">You can also use Bootstrap's theming feature to easily effect a change in the application's look and feel.</span></span> <span data-ttu-id="6fadd-227">Par défaut, le modèle d’application Web ASP.NET dans Visual Studio 2013 comprend le démarrage en tant que package NuGet.</span><span class="sxs-lookup"><span data-stu-id="6fadd-227">By default, the ASP.NET Web Application template in Visual Studio 2013 includes Bootstrap as a NuGet package.</span></span>

<span data-ttu-id="6fadd-228">Dans ce didacticiel, vous allez modifier l’apparence de l’application Wingtip Toys en remplaçant les fichiers de démarrage CSS.</span><span class="sxs-lookup"><span data-stu-id="6fadd-228">In this tutorial, you will change look and feel of the Wingtip Toys application by replacing the Bootstrap CSS files.</span></span>

1. <span data-ttu-id="6fadd-229">Dans **Explorateur de solutions**, ouvrez le dossier *content* .</span><span class="sxs-lookup"><span data-stu-id="6fadd-229">In **Solution Explorer**, open the *Content* folder.</span></span>
2. <span data-ttu-id="6fadd-230">Cliquez avec le bouton droit sur le fichier *bootstrap. CSS* et renommez-le *bootstrap-original. CSS*.</span><span class="sxs-lookup"><span data-stu-id="6fadd-230">Right-click the *bootstrap.css* file and rename it to *bootstrap-original.css*.</span></span>
3. <span data-ttu-id="6fadd-231">Renommez le *bootstrap. min. CSS* en *bootstrap-original. min. CSS*.</span><span class="sxs-lookup"><span data-stu-id="6fadd-231">Rename the *bootstrap.min.css* to *bootstrap-original.min.css*.</span></span>
4. <span data-ttu-id="6fadd-232">Dans **Explorateur de solutions**, cliquez avec le bouton droit sur le dossier *content* et sélectionnez **ouvrir le dossier dans l’Explorateur de fichiers**.</span><span class="sxs-lookup"><span data-stu-id="6fadd-232">In **Solution Explorer**, right-click the *Content* folder and select **Open Folder in File Explorer**.</span></span>  
   <span data-ttu-id="6fadd-233">L’Explorateur de fichiers s’affiche.</span><span class="sxs-lookup"><span data-stu-id="6fadd-233">The File Explorer will be displayed.</span></span> <span data-ttu-id="6fadd-234">Vous allez enregistrer un fichier CSS de démarrage téléchargé à cet emplacement.</span><span class="sxs-lookup"><span data-stu-id="6fadd-234">You will save a downloaded bootstrap CSS files to this location.</span></span>
5. <span data-ttu-id="6fadd-235">Dans votre navigateur, accédez à [https://bootswatch.com/3/](https://bootswatch.com/3/).</span><span class="sxs-lookup"><span data-stu-id="6fadd-235">In your browser, go to [https://bootswatch.com/3/](https://bootswatch.com/3/).</span></span>
6. <span data-ttu-id="6fadd-236">Faites défiler la fenêtre du navigateur jusqu’à ce que le thème Cerulean s’affiche.</span><span class="sxs-lookup"><span data-stu-id="6fadd-236">Scroll the browser window until you see the Cerulean theme.</span></span> 

    ![Interface utilisateur et navigation-thème Cerulean](ui_and_navigation/_static/image5.png)
7. <span data-ttu-id="6fadd-238">Téléchargez le fichier *bootstrap. CSS* et le fichier *bootstrap. min. CSS* dans le dossier *content* .</span><span class="sxs-lookup"><span data-stu-id="6fadd-238">Download both the *bootstrap.css* file and the *bootstrap.min.css* file to the *Content* folder.</span></span> <span data-ttu-id="6fadd-239">Utilisez le chemin d’accès au dossier de contenu qui s’affiche dans la fenêtre de l' **Explorateur de fichiers** que vous avez ouverte précédemment.</span><span class="sxs-lookup"><span data-stu-id="6fadd-239">Use the path to the content folder that is displayed in the **File Explorer** window that you previously opened.</span></span>
8. <span data-ttu-id="6fadd-240">Dans **Visual Studio** en haut de **Explorateur de solutions**, sélectionnez l’option **Afficher tous les fichiers** pour afficher les nouveaux fichiers dans le dossier Content.</span><span class="sxs-lookup"><span data-stu-id="6fadd-240">In **Visual Studio** at the top of **Solution Explorer**, select the **Show All Files** option to display the new files in the Content folder.</span></span> 

    ![Interface utilisateur et navigation-Explorateur de solutions](ui_and_navigation/_static/image6.png)

   <span data-ttu-id="6fadd-242">Vous verrez les deux nouveaux fichiers CSS dans le dossier **content** , mais notez que l’icône en regard de chaque nom de fichier est grisée. Cela signifie que le fichier n’a pas encore été ajouté au projet.</span><span class="sxs-lookup"><span data-stu-id="6fadd-242">You will see the two new CSS files in the **Content** folder, but notice that the icon next to each file name is grayed out. This means that the file has not yet been added to the project.</span></span>
9. <span data-ttu-id="6fadd-243">Cliquez avec le bouton droit sur les fichiers *bootstrap. CSS* et *bootstrap. min. CSS* et sélectionnez **inclure dans le projet**.</span><span class="sxs-lookup"><span data-stu-id="6fadd-243">Right-click the *bootstrap.css* and the *bootstrap.min.css* files and select **Include In Project**.</span></span>   
   <span data-ttu-id="6fadd-244">Quand vous exécutez l’application Wingtip Toys plus loin dans ce didacticiel, la nouvelle interface utilisateur s’affiche.</span><span class="sxs-lookup"><span data-stu-id="6fadd-244">When you run the Wingtip Toys application later in this tutorial, the new UI will be displayed.</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="6fadd-245">Le modèle d’application Web ASP.NET utilise le fichier *bundle. config* à la racine du projet pour stocker le chemin d’accès aux fichiers CSS de démarrage.</span><span class="sxs-lookup"><span data-stu-id="6fadd-245">The ASP.NET Web Application template uses the *Bundle.config* file at the root of the project to store the path of the Bootstrap CSS files.</span></span>

### <a name="modifying-the-default-navigation"></a><span data-ttu-id="6fadd-246">Modification de la navigation par défaut</span><span class="sxs-lookup"><span data-stu-id="6fadd-246">Modifying the Default Navigation</span></span>

<span data-ttu-id="6fadd-247">La navigation par défaut pour chaque page de l’application peut être modifiée en modifiant l’élément de la liste de navigation non triée qui se trouve dans la page *site. Master* .</span><span class="sxs-lookup"><span data-stu-id="6fadd-247">The default navigation for every page in the application can be modified by changing the unordered navigation list element that's in the *Site.Master* page.</span></span>

1. <span data-ttu-id="6fadd-248">Dans **Explorateur de solutions**, recherchez et ouvrez la page *site. Master* .</span><span class="sxs-lookup"><span data-stu-id="6fadd-248">In **Solution Explorer**, locate and open the *Site.Master* page.</span></span>
2. <span data-ttu-id="6fadd-249">Ajoutez le lien de navigation supplémentaire mis en surbrillance en jaune à la liste non triée affichée ci-dessous :</span><span class="sxs-lookup"><span data-stu-id="6fadd-249">Add the additional navigation link highlighted in yellow to the unordered list shown below:</span></span>   

    [!code-html[Main](ui_and_navigation/samples/sample5.html?highlight=5)]

<span data-ttu-id="6fadd-250">Comme vous pouvez le voir dans le code HTML ci-dessus, vous avez modifié chaque élément de ligne `<li>` contenant une balise d’ancrage `<a>` avec un attribut `href` de lien.</span><span class="sxs-lookup"><span data-stu-id="6fadd-250">As you can see in the above HTML, you modified each line item `<li>` containing an anchor tag `<a>` with a link `href` attribute.</span></span> <span data-ttu-id="6fadd-251">Chaque `href` pointe vers une page de l’application Web.</span><span class="sxs-lookup"><span data-stu-id="6fadd-251">Each `href` points to a page in the Web application.</span></span> <span data-ttu-id="6fadd-252">Dans le navigateur, lorsqu’un utilisateur clique sur l’un de ces liens (tels que **Products**), il accède à la page contenue dans le `href` (par exemple, **ProductList. aspx**).</span><span class="sxs-lookup"><span data-stu-id="6fadd-252">In the browser, when a user clicks on one of these links (such as **Products**), they will navigate to the page contained in the `href` (such as **ProductList.aspx**).</span></span> <span data-ttu-id="6fadd-253">Vous allez exécuter l’application à la fin de ce didacticiel.</span><span class="sxs-lookup"><span data-stu-id="6fadd-253">You will run the application at the end of this tutorial.</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="6fadd-254">Le caractère tilde (`~`) est utilisé pour spécifier que le chemin d’accès `href` commence à la racine du projet.</span><span class="sxs-lookup"><span data-stu-id="6fadd-254">The tilde (`~`) character is used to specify that the `href` path begins at the root of the project.</span></span>

### <a name="adding-a-data-control-to-display-navigation-data"></a><span data-ttu-id="6fadd-255">Ajout d’un contrôle de données pour afficher les données de navigation</span><span class="sxs-lookup"><span data-stu-id="6fadd-255">Adding a Data Control to Display Navigation Data</span></span>

<span data-ttu-id="6fadd-256">Ensuite, vous allez ajouter un contrôle pour afficher toutes les catégories de la base de données.</span><span class="sxs-lookup"><span data-stu-id="6fadd-256">Next, you'll add a control to display all of the categories from the database.</span></span> <span data-ttu-id="6fadd-257">Chaque catégorie fera office de lien vers la page *ProductList. aspx* .</span><span class="sxs-lookup"><span data-stu-id="6fadd-257">Each category will act as a link to the *ProductList.aspx* page.</span></span> <span data-ttu-id="6fadd-258">Lorsqu’un utilisateur clique sur un lien de catégorie dans le navigateur, il accède à la page produits et affiche uniquement les produits associés à la catégorie sélectionnée.</span><span class="sxs-lookup"><span data-stu-id="6fadd-258">When a user clicks on a category link in the browser, they will navigate to the products page and see only the products associated with the selected category.</span></span>

<span data-ttu-id="6fadd-259">Vous utiliserez un contrôle **ListView** pour afficher toutes les catégories contenues dans la base de données.</span><span class="sxs-lookup"><span data-stu-id="6fadd-259">You'll use a **ListView** control to display all the categories contained in the database.</span></span> <span data-ttu-id="6fadd-260">Pour ajouter un contrôle **ListView** à la page maître :</span><span class="sxs-lookup"><span data-stu-id="6fadd-260">To add a **ListView** control to the master page:</span></span>

1. <span data-ttu-id="6fadd-261">Dans la page *site. Master* , ajoutez l’élément `<div>` en surbrillance suivant **après** l’élément `<div>` contenant le `id="TitleContent"` que vous avez ajouté précédemment :</span><span class="sxs-lookup"><span data-stu-id="6fadd-261">In the *Site.Master* page, add the following highlighted `<div>` element **after** the `<div>` element containing the `id="TitleContent"` that you added earlier:</span></span>  

    [!code-aspx[Main](ui_and_navigation/samples/sample6.aspx?highlight=7-21)]

<span data-ttu-id="6fadd-262">Ce code affiche toutes les catégories de la base de données.</span><span class="sxs-lookup"><span data-stu-id="6fadd-262">This code will display all the categories from the database.</span></span> <span data-ttu-id="6fadd-263">Le contrôle **ListView** affiche chaque nom de catégorie sous forme de texte de lien et comprend un lien vers la page *ProductList. aspx* avec une valeur de chaîne de requête contenant le `ID` de la catégorie.</span><span class="sxs-lookup"><span data-stu-id="6fadd-263">The **ListView** control displays each category name as link text and includes a link to the *ProductList.aspx* page with a query-string value containing the `ID` of the category.</span></span> <span data-ttu-id="6fadd-264">En définissant la propriété `ItemType` dans le contrôle **ListView** , l’expression de liaison de données `Item` est disponible dans le nœud `ItemTemplate` et le contrôle est fortement typé.</span><span class="sxs-lookup"><span data-stu-id="6fadd-264">By setting the `ItemType` property in the **ListView** control, the data-binding expression `Item` is available within the `ItemTemplate` node and the control becomes strongly typed.</span></span> <span data-ttu-id="6fadd-265">Vous pouvez sélectionner les détails de l’objet `Item` à l’aide d’IntelliSense, tels que la spécification du `CategoryName`.</span><span class="sxs-lookup"><span data-stu-id="6fadd-265">You can select details of the `Item` object using IntelliSense, such as specifying the `CategoryName`.</span></span> <span data-ttu-id="6fadd-266">Ce code est contenu à l’intérieur du conteneur `<%#: %>` qui marque une expression de liaison de données.</span><span class="sxs-lookup"><span data-stu-id="6fadd-266">This code is contained inside the container `<%#: %>` that marks a data-binding expression.</span></span> <span data-ttu-id="6fadd-267">En ajoutant le ( :) à la fin du préfixe `<%#`, le résultat de l’expression de liaison de données est encodé au format HTML.</span><span class="sxs-lookup"><span data-stu-id="6fadd-267">By adding the (:) to the end of the `<%#` prefix, the result of the data-binding expression is HTML-encoded.</span></span> <span data-ttu-id="6fadd-268">Lorsque le résultat est encodé au format HTML, votre application est mieux protégée contre les attaques par injection de script entre sites (XSS) et par injection de code HTML.</span><span class="sxs-lookup"><span data-stu-id="6fadd-268">When the result is HTML-encoded, your application is better protected against cross-site script injection (XSS) and HTML injection attacks.</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="6fadd-269">**Conseil**</span><span class="sxs-lookup"><span data-stu-id="6fadd-269">**Tip**</span></span>
> 
> <span data-ttu-id="6fadd-270">Quand vous ajoutez du code en tapant lors du développement, vous pouvez être certain qu’un membre valide d’un objet est trouvé, car les contrôles de données fortement typés affichent les membres disponibles en fonction d’IntelliSense.</span><span class="sxs-lookup"><span data-stu-id="6fadd-270">When you add code by typing during development, you can be certain that a valid member of an object is found because strongly typed data controls show the available members based on IntelliSense.</span></span> <span data-ttu-id="6fadd-271">IntelliSense offre des choix de code contextuels à mesure que vous tapez du code, comme des propriétés, des méthodes et des objets.</span><span class="sxs-lookup"><span data-stu-id="6fadd-271">IntelliSense offers context-appropriate code choices as you type code, such as properties, methods, and objects.</span></span>

<span data-ttu-id="6fadd-272">À l’étape suivante, vous allez implémenter la méthode `GetCategories` pour récupérer des données.</span><span class="sxs-lookup"><span data-stu-id="6fadd-272">In the next step, you will implement the `GetCategories` method to retrieve data.</span></span>

### <a name="linking-the-data-control-to-the-database"></a><span data-ttu-id="6fadd-273">Liaison du contrôle de données à la base de données</span><span class="sxs-lookup"><span data-stu-id="6fadd-273">Linking the Data Control to the Database</span></span>

<span data-ttu-id="6fadd-274">Avant de pouvoir afficher des données dans le contrôle Data, vous devez lier le contrôle Data à la base de données.</span><span class="sxs-lookup"><span data-stu-id="6fadd-274">Before you can display data in the data control, you need to link the data control to the database.</span></span> <span data-ttu-id="6fadd-275">Pour créer le lien, vous pouvez modifier le code-behind du fichier *site.Master.cs* .</span><span class="sxs-lookup"><span data-stu-id="6fadd-275">To make the link, you can modify the code behind of the *Site.Master.cs* file.</span></span>

1. <span data-ttu-id="6fadd-276">Dans **Explorateur de solutions**, cliquez avec le bouton droit sur la page *site. Master* , puis cliquez sur **afficher le code**.</span><span class="sxs-lookup"><span data-stu-id="6fadd-276">In **Solution Explorer**, right-click the *Site.Master* page and then click **View Code**.</span></span> <span data-ttu-id="6fadd-277">Le fichier *site.Master.cs* s’ouvre dans l’éditeur.</span><span class="sxs-lookup"><span data-stu-id="6fadd-277">The *Site.Master.cs* file is opened in the editor.</span></span>
2. <span data-ttu-id="6fadd-278">Vers le début du fichier *site.Master.cs* , ajoutez deux espaces de noms supplémentaires afin que tous les espaces de noms inclus apparaissent comme suit :</span><span class="sxs-lookup"><span data-stu-id="6fadd-278">Near the beginning of the *Site.Master.cs* file, add two additional namespaces so that all the included namespaces appear as follows:</span></span>  

    [!code-csharp[Main](ui_and_navigation/samples/sample7.cs?highlight=8-9)]
3. <span data-ttu-id="6fadd-279">Ajoutez la méthode `GetCategories` en surbrillance après le gestionnaire d’événements `Page_Load` comme suit :</span><span class="sxs-lookup"><span data-stu-id="6fadd-279">Add the highlighted `GetCategories` method after the `Page_Load` event handler as follows:</span></span>  

    [!code-csharp[Main](ui_and_navigation/samples/sample8.cs?highlight=6-11)]

<span data-ttu-id="6fadd-280">Le code ci-dessus est exécuté lorsqu’une page qui utilise la page maître est chargée dans le navigateur.</span><span class="sxs-lookup"><span data-stu-id="6fadd-280">The above code is executed when any page that uses the master page is loaded in the browser.</span></span> <span data-ttu-id="6fadd-281">Le contrôle `ListView` (nommé « categoryList ») que vous avez ajouté précédemment dans ce didacticiel utilise la liaison de modèle pour sélectionner des données.</span><span class="sxs-lookup"><span data-stu-id="6fadd-281">The `ListView` control (named "categoryList") that you added earlier in this tutorial uses model binding to select data.</span></span> <span data-ttu-id="6fadd-282">Dans le balisage du contrôle `ListView`, vous avez défini la propriété `SelectMethod` du contrôle sur la méthode `GetCategories`, comme indiqué ci-dessus.</span><span class="sxs-lookup"><span data-stu-id="6fadd-282">In the markup of the `ListView` control you set the control's `SelectMethod` property to the `GetCategories` method, shown above.</span></span> <span data-ttu-id="6fadd-283">Le contrôle `ListView` appelle la méthode `GetCategories` au moment approprié dans le cycle de vie de la page et lie automatiquement les données retournées.</span><span class="sxs-lookup"><span data-stu-id="6fadd-283">The `ListView` control calls the `GetCategories` method at the appropriate time in the page life cycle and automatically binds the returned data.</span></span> <span data-ttu-id="6fadd-284">Vous en apprendrez davantage sur la liaison de données dans le didacticiel suivant.</span><span class="sxs-lookup"><span data-stu-id="6fadd-284">You will learn more about binding data in the next tutorial.</span></span>

### <a name="running-the-application-and-creating-the-database"></a><span data-ttu-id="6fadd-285">Exécution de l’application et création de la base de données</span><span class="sxs-lookup"><span data-stu-id="6fadd-285">Running the Application and Creating the Database</span></span>

<span data-ttu-id="6fadd-286">Plus haut dans cette série de didacticiels, vous avez créé une classe d’initialiseur (nommée « ProductDatabaseInitializer ») et spécifié cette classe dans le fichier *global.asax.cs* .</span><span class="sxs-lookup"><span data-stu-id="6fadd-286">Earlier in this tutorial series you created an initializer class (named "ProductDatabaseInitializer") and specified this class in the *global.asax.cs* file.</span></span> <span data-ttu-id="6fadd-287">Le Entity Framework générera la base de données lorsque l’application sera exécutée la première fois, car la méthode `Application_Start` contenue dans le fichier *global.asax.cs* appellera la classe d’initialiseur.</span><span class="sxs-lookup"><span data-stu-id="6fadd-287">The Entity Framework will generate the database when the application is run the first time because the `Application_Start` method contained in the *global.asax.cs* file will call the initializer class.</span></span> <span data-ttu-id="6fadd-288">La classe d’initialiseur utilisera les classes de modèle (`Category` et `Product`) que vous avez ajoutées précédemment dans cette série de didacticiels pour créer la base de données.</span><span class="sxs-lookup"><span data-stu-id="6fadd-288">The initializer class will use the model classes (`Category` and `Product`) that you added earlier in this tutorial series to create the database.</span></span>

1. <span data-ttu-id="6fadd-289">Dans **Explorateur de solutions**, cliquez avec le bouton droit sur la page *default. aspx* et sélectionnez **définir comme page de démarrage**.</span><span class="sxs-lookup"><span data-stu-id="6fadd-289">In **Solution Explorer**, right-click the *Default.aspx* page and select **Set As Start Page**.</span></span>
2. <span data-ttu-id="6fadd-290">Dans Visual Studio, appuyez sur **F5**.</span><span class="sxs-lookup"><span data-stu-id="6fadd-290">In Visual Studio press **F5**.</span></span>   
 <span data-ttu-id="6fadd-291">Il faut un peu de temps pour configurer tout ce qui se passe lors de la première exécution.</span><span class="sxs-lookup"><span data-stu-id="6fadd-291">It will take a little time to set everything up during this first run.</span></span>   
    <span data-ttu-id="6fadd-292">![l’interface utilisateur et la navigation-fenêtres du navigateur](ui_and_navigation/_static/image7.png)</span><span class="sxs-lookup"><span data-stu-id="6fadd-292">![UI and Navigation - Browser Windows](ui_and_navigation/_static/image7.png)</span></span>  
 <span data-ttu-id="6fadd-293">Lorsque vous exécutez l’application, l’application est compilée et la base de données nommée *wingtiptoys. mdf* est créée dans le dossier de données de l' *application\_* .</span><span class="sxs-lookup"><span data-stu-id="6fadd-293">When you run the application, the application will be compiled and the database named *wingtiptoys.mdf* will be created in the *App\_Data* folder.</span></span> <span data-ttu-id="6fadd-294">Dans le navigateur, vous verrez un menu de navigation de catégorie.</span><span class="sxs-lookup"><span data-stu-id="6fadd-294">In the browser, you will see a category navigation menu.</span></span> <span data-ttu-id="6fadd-295">Ce menu a été généré en extrayant les catégories de la base de données.</span><span class="sxs-lookup"><span data-stu-id="6fadd-295">This menu was generated by retrieving the categories from the database.</span></span> <span data-ttu-id="6fadd-296">Dans le didacticiel suivant, vous allez implémenter la navigation.</span><span class="sxs-lookup"><span data-stu-id="6fadd-296">In the next tutorial, you will implement the navigation.</span></span>
3. <span data-ttu-id="6fadd-297">Fermez le navigateur pour arrêter l’application en cours d’exécution.</span><span class="sxs-lookup"><span data-stu-id="6fadd-297">Close the browser to stop the running application.</span></span>

### <a name="reviewing-the-database"></a><span data-ttu-id="6fadd-298">Vérification de la base de données</span><span class="sxs-lookup"><span data-stu-id="6fadd-298">Reviewing the Database</span></span>

<span data-ttu-id="6fadd-299">Ouvrez le fichier *Web. config* et examinez la section de la chaîne de connexion.</span><span class="sxs-lookup"><span data-stu-id="6fadd-299">Open the *Web.config* file and look at the connection string section.</span></span> <span data-ttu-id="6fadd-300">Vous pouvez voir que la valeur `AttachDbFilename` dans la chaîne de connexion pointe vers la `DataDirectory` pour le projet d’application Web.</span><span class="sxs-lookup"><span data-stu-id="6fadd-300">You can see that the `AttachDbFilename` value in the connection string points to the `DataDirectory` for the Web application project.</span></span> <span data-ttu-id="6fadd-301">La valeur `|DataDirectory|` est une valeur réservée qui représente le dossier de données de l' *application\_* dans le projet.</span><span class="sxs-lookup"><span data-stu-id="6fadd-301">The value `|DataDirectory|` is a reserved value that represents the *App\_Data* folder in the project.</span></span> <span data-ttu-id="6fadd-302">Ce dossier est l’emplacement où se trouve la base de données qui a été créée à partir de vos classes d’entité.</span><span class="sxs-lookup"><span data-stu-id="6fadd-302">This folder is where the database that was created from your entity classes is located.</span></span>

[!code-xml[Main](ui_and_navigation/samples/sample9.xml)]

> [!NOTE] 
> 
> <span data-ttu-id="6fadd-303">Si le dossier de données de l' *application\_* n’est pas visible ou si le dossier est vide, sélectionnez l’icône d' **actualisation** , puis l’icône **Afficher tous les fichiers** en haut de la fenêtre de **Explorateur de solutions** .</span><span class="sxs-lookup"><span data-stu-id="6fadd-303">If the *App\_Data* folder is not visible or if the folder is empty, select the **Refresh** icon and then the **Show All Files** icon at the top of the **Solution Explorer** window.</span></span> <span data-ttu-id="6fadd-304">L’agrandissement de la largeur des fenêtres de **Explorateur de solutions** peut être nécessaire pour afficher toutes les icônes disponibles.</span><span class="sxs-lookup"><span data-stu-id="6fadd-304">Expanding the width of the **Solution Explorer** windows may be required to show all available icons.</span></span>

<span data-ttu-id="6fadd-305">Vous pouvez maintenant inspecter les données contenues dans le fichier de base de données *wingtiptoys. mdf* à l’aide de la fenêtre **Explorateur de serveurs** .</span><span class="sxs-lookup"><span data-stu-id="6fadd-305">Now you can inspect the data contained in the *wingtiptoys.mdf* database file by using the **Server Explorer** window.</span></span>

1. <span data-ttu-id="6fadd-306">Développez le dossier données de l' *application\_* .</span><span class="sxs-lookup"><span data-stu-id="6fadd-306">Expand the *App\_Data* folder.</span></span> <span data-ttu-id="6fadd-307">Si le dossier de données de l' *application\_* n’est pas visible, consultez la remarque ci-dessus.</span><span class="sxs-lookup"><span data-stu-id="6fadd-307">If the *App\_Data* folder is not visible, see the note above.</span></span>
2. <span data-ttu-id="6fadd-308">Si le fichier de base de données *wingtiptoys. mdf* n’est pas visible, sélectionnez l’icône d' **actualisation** , puis l’icône **Afficher tous les fichiers** en haut de la fenêtre de **Explorateur de solutions** .</span><span class="sxs-lookup"><span data-stu-id="6fadd-308">If the *wingtiptoys.mdf* database file is not visible, select the **Refresh** icon and then the **Show All Files** icon at the top of the **Solution Explorer** window.</span></span>
3. <span data-ttu-id="6fadd-309">Cliquez avec le bouton droit sur le fichier de base de données *wingtiptoys. mdf* , puis sélectionnez **ouvrir**.</span><span class="sxs-lookup"><span data-stu-id="6fadd-309">Right-click the *wingtiptoys.mdf* database file and select **Open**.</span></span>  
    <span data-ttu-id="6fadd-310">**Explorateur de serveurs** s’affiche.</span><span class="sxs-lookup"><span data-stu-id="6fadd-310">**Server Explorer** is displayed.</span></span> 

    ![Interface utilisateur et navigation-Explorateur de serveurs](ui_and_navigation/_static/image8.png)
4. <span data-ttu-id="6fadd-312">Développez le dossier *Tables* .</span><span class="sxs-lookup"><span data-stu-id="6fadd-312">Expand the *Tables* folder.</span></span>
5. <span data-ttu-id="6fadd-313">Cliquez avec le bouton droit sur la table **Products**et sélectionnez **afficher les données**de la table.</span><span class="sxs-lookup"><span data-stu-id="6fadd-313">Right-click the **Products**table and select **Show Table Data**.</span></span>  
 <span data-ttu-id="6fadd-314">La table **Products** s’affiche.</span><span class="sxs-lookup"><span data-stu-id="6fadd-314">The **Products** table is displayed.</span></span> 

    ![Interface utilisateur et navigation-table des produits](ui_and_navigation/_static/image9.png)
6. <span data-ttu-id="6fadd-316">Cette vue vous permet de visualiser et de modifier les données de la table **Products** à la main.</span><span class="sxs-lookup"><span data-stu-id="6fadd-316">This view lets you see and modify the data in the **Products** table by hand.</span></span>
7. <span data-ttu-id="6fadd-317">Fermez la fenêtre de la table **Products** .</span><span class="sxs-lookup"><span data-stu-id="6fadd-317">Close the **Products** table window.</span></span>
8. <span data-ttu-id="6fadd-318">Dans le **Explorateur de serveurs**, cliquez à nouveau avec le bouton droit sur la table **Products** et sélectionnez **ouvrir la définition de table**.</span><span class="sxs-lookup"><span data-stu-id="6fadd-318">In the **Server Explorer**, right-click the **Products** table again and select **Open Table Definition**.</span></span>  
 <span data-ttu-id="6fadd-319">La conception des données pour la table **Products** s’affiche.</span><span class="sxs-lookup"><span data-stu-id="6fadd-319">The data design for the **Products** table is displayed.</span></span> 

    ![Interface utilisateur et navigation-conception des produits](ui_and_navigation/_static/image10.png)
9. <span data-ttu-id="6fadd-321">Dans l’onglet **T-SQL** , vous verrez l’instruction DDL SQL qui a été utilisée pour créer la table.</span><span class="sxs-lookup"><span data-stu-id="6fadd-321">In the **T-SQL** tab you will see the SQL DDL statement that was used to create the table.</span></span> <span data-ttu-id="6fadd-322">Vous pouvez également utiliser l’interface utilisateur sous l’onglet **conception** pour modifier le schéma.</span><span class="sxs-lookup"><span data-stu-id="6fadd-322">You can also use the UI in the **Design** tab to modify the schema.</span></span>
10. <span data-ttu-id="6fadd-323">Dans le **Explorateur de serveurs**, cliquez avec le bouton droit sur base de données **WingtipToys** , puis sélectionnez **Fermer la connexion**.</span><span class="sxs-lookup"><span data-stu-id="6fadd-323">In the **Server Explorer**, right-click **WingtipToys** database and select **Close Connection**.</span></span>   
 <span data-ttu-id="6fadd-324">Si vous détachez la base de données de Visual Studio, le schéma de base de données pourra être modifié plus tard dans cette série de didacticiels.</span><span class="sxs-lookup"><span data-stu-id="6fadd-324">By detaching the database from Visual Studio, the database schema will be able to be modified later in this tutorial series.</span></span>
11. <span data-ttu-id="6fadd-325">Revenez à **Explorateur de solutions**en sélectionnant l’onglet **Explorateur de solutions** au bas de la fenêtre de **Explorateur de serveurs** .</span><span class="sxs-lookup"><span data-stu-id="6fadd-325">Return to **Solution Explorer**by selecting the **Solution Explorer** tab at the bottom of the **Server Explorer** window.</span></span>

## <a name="summary"></a><span data-ttu-id="6fadd-326">Récapitulatif</span><span class="sxs-lookup"><span data-stu-id="6fadd-326">Summary</span></span>

<span data-ttu-id="6fadd-327">Dans ce didacticiel de la série, vous avez ajouté une interface utilisateur de base, des graphiques, des pages et une navigation.</span><span class="sxs-lookup"><span data-stu-id="6fadd-327">In this tutorial of the series you have added some basic UI, graphics, pages, and navigation.</span></span> <span data-ttu-id="6fadd-328">En outre, vous avez exécuté l’application Web, qui a créé la base de données à partir des classes de données que vous avez ajoutées dans le didacticiel précédent.</span><span class="sxs-lookup"><span data-stu-id="6fadd-328">Additionally, you ran the Web application, which created the database from the data classes that you added in the previous tutorial.</span></span> <span data-ttu-id="6fadd-329">Vous pouvez également afficher le contenu de la table *Products* de la base de données en consultant directement la base de données.</span><span class="sxs-lookup"><span data-stu-id="6fadd-329">You also viewed the contents of the *Products* table of the database by viewing the database directly.</span></span> <span data-ttu-id="6fadd-330">Dans le didacticiel suivant, vous allez afficher les éléments de données et les détails de la base de données.</span><span class="sxs-lookup"><span data-stu-id="6fadd-330">In the next tutorial, you'll display data items and details from the database.</span></span>

## <a name="additional-resources"></a><span data-ttu-id="6fadd-331">Ressources supplémentaires</span><span class="sxs-lookup"><span data-stu-id="6fadd-331">Additional Resources</span></span>

<span data-ttu-id="6fadd-332">[Introduction à la programmation de pages Web ASP.NET](https://msdn.microsoft.com/library/ms178125.aspx) </span><span class="sxs-lookup"><span data-stu-id="6fadd-332">[Introduction to Programming ASP.NET Web Pages](https://msdn.microsoft.com/library/ms178125.aspx) </span></span>  
<span data-ttu-id="6fadd-333">[Vue d’ensemble des contrôles serveur Web ASP.NET](https://msdn.microsoft.com/library/zsyt68f1.aspx) </span><span class="sxs-lookup"><span data-stu-id="6fadd-333">[ASP.NET Web Server Controls Overview](https://msdn.microsoft.com/library/zsyt68f1.aspx) </span></span>  
[<span data-ttu-id="6fadd-334">Didacticiel CSS</span><span class="sxs-lookup"><span data-stu-id="6fadd-334">CSS Tutorial</span></span>](http://www.w3schools.com/css/default.asp)

> [!div class="step-by-step"]
> <span data-ttu-id="6fadd-335">[Précédent](create_the_data_access_layer.md)
> [Suivant](display_data_items_and_details.md)</span><span class="sxs-lookup"><span data-stu-id="6fadd-335">[Previous](create_the_data_access_layer.md)
[Next](display_data_items_and_details.md)</span></span>
