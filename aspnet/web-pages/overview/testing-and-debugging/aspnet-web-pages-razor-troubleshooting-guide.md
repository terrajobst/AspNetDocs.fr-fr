---
uid: web-pages/overview/testing-and-debugging/aspnet-web-pages-razor-troubleshooting-guide
title: ASP.NET Web Pages (Razor) Troubleshooting Guide | Microsoft Docs
author: Rick-Anderson
description: Cet article décrit les problèmes que vous pourriez rencontrer lorsque vous travaillez avec ASP.NET Web Pages (Razor) et certaines solutions suggérées. Versions logicielles ASP.NET Web Pag...
ms.author: riande
ms.date: 02/10/2014
ms.assetid: 2a2c1833-0bfe-4e2e-9cc0-341b52c7b121
msc.legacyurl: /web-pages/overview/testing-and-debugging/aspnet-web-pages-razor-troubleshooting-guide
msc.type: authoredcontent
ms.openlocfilehash: adbaa5cbda4a60a8b222ba49bb148b28b2e214cc
ms.sourcegitcommit: 0f1119340e4464720cfd16d0ff15764746ea1fea
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 04/17/2019
ms.locfileid: "59389204"
---
# <a name="aspnet-web-pages-razor-troubleshooting-guide"></a><span data-ttu-id="cd8b5-104">ASP.NET Web Pages (Razor) - Guide de résolution des problèmes</span><span class="sxs-lookup"><span data-stu-id="cd8b5-104">ASP.NET Web Pages (Razor) Troubleshooting Guide</span></span>

<span data-ttu-id="cd8b5-105">par [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="cd8b5-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="cd8b5-106">Cet article décrit les problèmes que vous pourriez rencontrer lorsque vous travaillez avec ASP.NET Web Pages (Razor) et certaines solutions suggérées.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-106">This article describes issues that you might have when working with ASP.NET Web Pages (Razor) and some suggested solutions.</span></span>
> 
> ## <a name="software-versions"></a><span data-ttu-id="cd8b5-107">Versions des logiciels</span><span class="sxs-lookup"><span data-stu-id="cd8b5-107">Software versions</span></span>
> 
> 
> - <span data-ttu-id="cd8b5-108">ASP.NET Web Pages (Razor) 3</span><span class="sxs-lookup"><span data-stu-id="cd8b5-108">ASP.NET Web Pages (Razor) 3</span></span>
>   
> 
> <span data-ttu-id="cd8b5-109">Ce didacticiel fonctionne également avec ASP.NET Web Pages 2 et ASP.NET Web Pages 1.0.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-109">This tutorial also works with ASP.NET Web Pages 2 and ASP.NET Web Pages 1.0.</span></span>


<span data-ttu-id="cd8b5-110">Cette rubrique contient les sections suivantes :</span><span class="sxs-lookup"><span data-stu-id="cd8b5-110">This topic contains the following sections:</span></span>

- [<span data-ttu-id="cd8b5-111">Problèmes avec les Pages en cours d’exécution</span><span class="sxs-lookup"><span data-stu-id="cd8b5-111">Issues with Running Pages</span></span>](#Issues_Running_.cshtml_Pages)
- [<span data-ttu-id="cd8b5-112">Problèmes avec le Code Razor</span><span class="sxs-lookup"><span data-stu-id="cd8b5-112">Issues with Razor Code</span></span>](#IssuesWithRazorCode)
- [<span data-ttu-id="cd8b5-113">Problèmes de sécurité et d’appartenance</span><span class="sxs-lookup"><span data-stu-id="cd8b5-113">Issues with Security and Membership</span></span>](#membership)
- [<span data-ttu-id="cd8b5-114">Problèmes liés à l’envoi de courrier électronique</span><span class="sxs-lookup"><span data-stu-id="cd8b5-114">Issues with Sending Email</span></span>](#email)
- [<span data-ttu-id="cd8b5-115">Ressources supplémentaires pour MSBuild</span><span class="sxs-lookup"><span data-stu-id="cd8b5-115">Additional Resources</span></span>](#AdditionalResources)

<span data-ttu-id="cd8b5-116">Pour les questions générales, consultez [ASP.NET Web Pages (Razor) FAQ](https://go.microsoft.com/fwlink/?LinkId=253000).</span><span class="sxs-lookup"><span data-stu-id="cd8b5-116">For general questions, see [ASP.NET Web Pages (Razor) FAQ](https://go.microsoft.com/fwlink/?LinkId=253000).</span></span>

<a id="Issues_Running_.cshtml_Pages"></a>
## <a name="issues-with-running-pages"></a><span data-ttu-id="cd8b5-117">Problèmes avec les Pages en cours d’exécution</span><span class="sxs-lookup"><span data-stu-id="cd8b5-117">Issues with Running Pages</span></span>

<span data-ttu-id="cd8b5-118">De nombreux problèmes peut empêcher *.cshtml* et *.vbhtml* pages de s’exécuter correctement.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-118">A variety of issues can prevent *.cshtml* and *.vbhtml* pages from running properly.</span></span> <span data-ttu-id="cd8b5-119">Cette section répertorie les messages d’erreur courants et les causes probables.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-119">This section lists common error messages and likely causes.</span></span>

### <a name="http-error-403---forbidden-access-is-denied"></a><span data-ttu-id="cd8b5-120">Erreur HTTP 403 - Interdit : Accès refusé</span><span class="sxs-lookup"><span data-stu-id="cd8b5-120">HTTP Error 403 - Forbidden: Access is denied</span></span>

<span data-ttu-id="cd8b5-121">*Vous n’êtes pas autorisé à afficher ce répertoire ou cette page en utilisant les informations d’identification que vous avez fournies.*</span><span class="sxs-lookup"><span data-stu-id="cd8b5-121">*You do not have permission to view this directory or page using the credentials that you supplied.*</span></span>

<span data-ttu-id="cd8b5-122">Cette erreur peut se produire si le serveur n’exécute pas la version correcte du .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-122">This error can occur if the server is not running the correct version of the .NET Framework.</span></span> <span data-ttu-id="cd8b5-123">Assurez-vous que l’ordinateur qui exécute le serveur (localement ou à distance) dispose au moins .NET Framework 4 est installé.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-123">Make sure that the computer that's running the server (locally or remotely) has at least the .NET Framework 4 installed.</span></span> <span data-ttu-id="cd8b5-124">Assurez-vous également que l’application elle-même est configurée pour exécuter la bonne version.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-124">Also make sure that the application itself is configured to run the right version.</span></span>

<span data-ttu-id="cd8b5-125">Si ce problème localement tout en travaillant dans WebMatrix, cliquez sur le **Site** espace de travail et, dans l’arborescence, cliquez sur **paramètres**.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-125">If you see this problem locally while working in WebMatrix, click the **Site** workspace, and then in the treeview click **Settings**.</span></span> <span data-ttu-id="cd8b5-126">Dans le **sélectionnez .NET Framework Version** , sélectionnez **.NET 4 (intégré)**.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-126">In the **Select .NET Framework Version** list, select **.NET 4 (Integrated)**.</span></span> <span data-ttu-id="cd8b5-127">Si cette version est déjà définie, essayez d’exécuter WebMatrix en tant qu’administrateur.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-127">If this version is already set, try running WebMatrix as an administrator.</span></span>

<span data-ttu-id="cd8b5-128">Assurez-vous que la racine de votre site Web dispose d’au moins un *.cshtml* fichier qu’il contient.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-128">Make sure that the root of your website has at least one *.cshtml* file in it.</span></span>

<span data-ttu-id="cd8b5-129">Si vous voyez cette erreur lorsque le serveur web se trouve sur un serveur distant, contactez l’administrateur du serveur.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-129">If you see this error when the web server is on a remote server, contact the server administrator.</span></span> <span data-ttu-id="cd8b5-130">Assurez-vous que le serveur possède le .NET Framework 4 ou version ultérieure, installé.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-130">Make sure that the server has the .NET Framework 4 or later installed.</span></span> <span data-ttu-id="cd8b5-131">Assurez-vous également que l’application s’exécute dans un pool d’applications est configuré pour utiliser cette version du.NET Framework.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-131">Also make sure that the application is running in an application pool that's configured to use that version of the.NET Framework.</span></span>

<span data-ttu-id="cd8b5-132">Si vous avez un contrôle sur le serveur, assurez-vous qu’il exécute la version correcte du .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-132">If you have control over the server, make sure it's running the correct version of the .NET Framework.</span></span> <span data-ttu-id="cd8b5-133">Vous pouvez également essayer de réparer l’installation en exécutant le `aspnet_regiis -iru` commande.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-133">You might also try repairing the installation by running the `aspnet_regiis -iru` command.</span></span> <span data-ttu-id="cd8b5-134">(Par exemple, si vous installez IIS après avoir installé le .NET Framework, IIS ne sera pas être correctement configurée pour exécuter des pages ASP.NET.) Pour plus d’informations, consultez [outil ASP.NET IIS Registration (Aspnet\_regiis.exe)](https://msdn.microsoft.com/library/k6h9cz8h(v=vs.100).aspx).</span><span class="sxs-lookup"><span data-stu-id="cd8b5-134">(For example, if you install IIS after you install the .NET Framework, IIS will not be correctly configured to run ASP.NET pages.) For more information, see [ASP.NET IIS Registration Tool (Aspnet\_regiis.exe)](https://msdn.microsoft.com/library/k6h9cz8h(v=vs.100).aspx).</span></span>

### <a name="http-error-40314---forbidden"></a><span data-ttu-id="cd8b5-135">Erreur HTTP 403.14 - interdit</span><span class="sxs-lookup"><span data-stu-id="cd8b5-135">HTTP Error 403.14 - Forbidden</span></span>

<span data-ttu-id="cd8b5-136">*Le serveur Web est configuré pour ne pas afficher le contenu de ce répertoire.*</span><span class="sxs-lookup"><span data-stu-id="cd8b5-136">*The Web server is configured to not list the contents of this directory.*</span></span>

<span data-ttu-id="cd8b5-137">Cette erreur peut se produire si vous demandez une ressource protégée (comme le *Web.config* fichier) ou qui se trouve dans un dossier protégé (comme *application\_données* ou *application\_Code*).</span><span class="sxs-lookup"><span data-stu-id="cd8b5-137">This error can occur if you request a resource that's protected (like the *Web.config* file) or that's in a folder that's protected (like *App\_Data* or *App\_Code*).</span></span>

### <a name="http-error-40417---not-found"></a><span data-ttu-id="cd8b5-138">Erreur HTTP 404.17 - introuvable</span><span class="sxs-lookup"><span data-stu-id="cd8b5-138">HTTP Error 404.17 - Not Found</span></span>

<span data-ttu-id="cd8b5-139">*Le contenu demandé semble être le script et n’est pas traitée par le Gestionnaire de fichiers statiques.*</span><span class="sxs-lookup"><span data-stu-id="cd8b5-139">*The requested content appears to be script and will not be served by the static file handler.*</span></span>

<span data-ttu-id="cd8b5-140">Cette erreur peut se produire si le serveur n’est pas configuré correctement pour utiliser le .NET Framework 4 ou version ultérieure et par conséquent ne reconnaît pas le code dans `@{ }` blocs.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-140">This error can occur if the server is not configured correctly to use the .NET Framework 4 or later, and therefore does not recognize the code in `@{ }` blocks.</span></span> <span data-ttu-id="cd8b5-141">Consultez la description de précédemment *HTTP erreur 403 - Interdit : L’accès est refusé*.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-141">See the description earlier for *HTTP Error 403 - Forbidden: Access is denied*.</span></span>

### <a name="http-error-4047---not-found"></a><span data-ttu-id="cd8b5-142">Erreur HTTP 404.7 - non trouvé</span><span class="sxs-lookup"><span data-stu-id="cd8b5-142">HTTP Error 404.7 - Not Found</span></span>

<span data-ttu-id="cd8b5-143">*Le module de filtrage des demandes sont configuré pour refuser l’extension de fichier*</span><span class="sxs-lookup"><span data-stu-id="cd8b5-143">*The request filtering module is configured to deny the file extension*</span></span>

<span data-ttu-id="cd8b5-144">Cette erreur peut se produire si *.cshtml* ou *.vbhtml* extensions ont été explicitement bloquées sur le serveur.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-144">This error can occur if *.cshtml* or *.vbhtml* extensions have been explicitly blocked on the server.</span></span> <span data-ttu-id="cd8b5-145">Un symptôme de ce problème est que le travail URL lorsqu’ils n’incluent pas l’extension, mais les URL qui incluent *.cshtml* ou *.vbhtml* ne fonctionnent pas.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-145">A symptom of this problem is that URLs work when they do not include the extension, but URLs that include *.cshtml* or *.vbhtml* do not work.</span></span> <span data-ttu-id="cd8b5-146">Une solution possible consiste à réactiver les extensions dans le site *Web.config* fichier.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-146">A possible solution is to re-enable the extensions in the site's *Web.config* file.</span></span> <span data-ttu-id="cd8b5-147">L’exemple suivant montre comment activer le *.cshtml* extension.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-147">The following example shows how to enable the *.cshtml* extension.</span></span>

[!code-xml[Main](aspnet-web-pages-razor-troubleshooting-guide/samples/sample1.xml?highlight=5-6)]

### <a name="http-error-4048---not-found"></a><span data-ttu-id="cd8b5-148">Erreur HTTP 404.8 - non trouvé</span><span class="sxs-lookup"><span data-stu-id="cd8b5-148">HTTP Error 404.8 - Not Found</span></span>

<span data-ttu-id="cd8b5-149">*Le module de filtrage des demandes sont configuré pour refuser un chemin d’accès dans l’URL qui contient une section hiddenSegment.*</span><span class="sxs-lookup"><span data-stu-id="cd8b5-149">*The request filtering module is configured to deny a path in the URL that contains a hiddenSegment section.*</span></span>

<span data-ttu-id="cd8b5-150">Cette erreur peut se produire si vous demandez une ressource protégée (comme le *Web.config* fichier) ou qui se trouve dans un dossier protégé (comme *application\_données* ou *application\_Code*).</span><span class="sxs-lookup"><span data-stu-id="cd8b5-150">This error can occur if you request a resource that's protected (like the *Web.config* file) or that's in a folder that's protected (like *App\_Data* or *App\_Code*).</span></span>

### <a name="this-type-of-page-is-not-served-server-error-in--application"></a><span data-ttu-id="cd8b5-151">Ce type de page non pris en charge (erreur de serveur dans l’Application '/')</span><span class="sxs-lookup"><span data-stu-id="cd8b5-151">This type of page is not served (Server Error in '/' Application)</span></span>

<span data-ttu-id="cd8b5-152">Consultez la description précédemment pour 404.17 d’erreur HTTP.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-152">See the description earlier for HTTP Error 404.17.</span></span>

<a id="IssuesWithRazorCode"></a>
## <a name="issues-with-razor-code"></a><span data-ttu-id="cd8b5-153">Problèmes avec le code Razor</span><span class="sxs-lookup"><span data-stu-id="cd8b5-153">Issues with Razor code</span></span>

### <a name="the-name-class-does-not-exist-in-the-current-context"></a><span data-ttu-id="cd8b5-154">Le nom '*classe*' n’existe pas dans le contexte actuel</span><span class="sxs-lookup"><span data-stu-id="cd8b5-154">The name '*class*' does not exist in the current context</span></span>

<span data-ttu-id="cd8b5-155">Souvent, une raison que vous voyez cette erreur est que `class` références d’une assistance, mais l’application d’assistance n’est pas installé.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-155">Often, a reason you see this error is that `class` references a helper, but the helper is not installed.</span></span> <span data-ttu-id="cd8b5-156">Par exemple, si vous essayez d’utiliser un programme d’assistance, mais si vous n’avez pas installé le package à partir de NuGet, vous verrez cette erreur.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-156">For example, if you try to use a helper, but if you haven't installed the package from NuGet, you'll see this error.</span></span> <span data-ttu-id="cd8b5-157">Utiliser la galerie dans WebMatrix pour rechercher et installer l’application d’assistance.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-157">Use the Gallery in WebMatrix to find and install the helper.</span></span>

<span data-ttu-id="cd8b5-158">Si l’application d’assistance est installée, mais la page toujours ne reconnaît pas, essayez d’ajouter ajouter un `using` instruction au code.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-158">If the helper is installed, but the page still doesn't recognize it, try adding add a `using` statement to the code.</span></span> <span data-ttu-id="cd8b5-159">Dans la `using` instruction, référence de l’espace de noms qui inclut l’application d’assistance.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-159">In the `using` statement, reference the namespace that includes the helper.</span></span> <span data-ttu-id="cd8b5-160">Par exemple, les programmes d’assistance base qui se trouvent dans le package de programmes d’assistance ASP.NET Web sont dans le `System.Web.Helpers` espace de noms.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-160">For example, the basic helpers that are in the ASP.NET Web Helpers package are in the `System.Web.Helpers` namespace.</span></span> <span data-ttu-id="cd8b5-161">En haut de la page où vous souhaitez utiliser l’application d’assistance, ajoutez cette ligne :</span><span class="sxs-lookup"><span data-stu-id="cd8b5-161">At the top of the page where you want to use the helper, add this line:</span></span>

`@using Microsoft.Web.Helpers;`

<a id="membership"></a>
## <a name="issues-with-security-and-membership"></a><span data-ttu-id="cd8b5-162">Problèmes de sécurité et d’appartenance</span><span class="sxs-lookup"><span data-stu-id="cd8b5-162">Issues with Security and Membership</span></span>

<span data-ttu-id="cd8b5-163">Si vous utilisez le système de sécurité intégrés (appartenance) dans ASP.NET Web Pages (Razor), vous pouvez rencontrer les problèmes suivants.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-163">If you are using the built-in security (membership) system in ASP.NET Web Pages (Razor), you might encounter the following issues.</span></span>

### <a name="to-call-this-method-the-membershipprovider-property-must-be-an-instance-of-extendedmembershipprovider"></a><span data-ttu-id="cd8b5-164">Pour appeler cette méthode, la propriété « Membership.Provider » doit être une instance de « ExtendedMembershipProvider »</span><span class="sxs-lookup"><span data-stu-id="cd8b5-164">To call this method, the "Membership.Provider" property must be an instance of "ExtendedMembershipProvider"</span></span>

<span data-ttu-id="cd8b5-165">Cette erreur peut indiquer qu’aucun `AspNetSqlMembershipProvider` classe est configurée.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-165">This error can indicate that no `AspNetSqlMembershipProvider` class is configured.</span></span> <span data-ttu-id="cd8b5-166">(Un symptôme est que le site fonctionne correctement localement, mais génère cette erreur lorsque vous publiez celui-ci au serveur d’un fournisseur d’hébergement.) Un correctif pour résoudre ce problème consiste à activer explicitement l’appartenance simple en ajoutant le code suivant sur le site *Web.config* fichier :</span><span class="sxs-lookup"><span data-stu-id="cd8b5-166">(A symptom is that the site works fine locally but throws this error when you publish it to a hosting provider's server.) One fix for this problem is to explicitly enable simple membership by adding the following to the site's *Web.config* file:</span></span>

[!code-xml[Main](aspnet-web-pages-razor-troubleshooting-guide/samples/sample2.xml?highlight=6)]

<a id="email"></a>
## <a name="issues-with-sending-email"></a><span data-ttu-id="cd8b5-167">Problèmes liés à l’envoi de courrier électronique</span><span class="sxs-lookup"><span data-stu-id="cd8b5-167">Issues with Sending Email</span></span>

<span data-ttu-id="cd8b5-168">Problèmes liés à l’envoi de courrier électronique peuvent être difficile à déboguer.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-168">Problems with sending email can be challenging to debug.</span></span> <span data-ttu-id="cd8b5-169">Un problème initial peut être que vous ne pouvez pas vous connecter au serveur SMTP.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-169">An initial problem can be that you can't connect to the SMTP server.</span></span> <span data-ttu-id="cd8b5-170">Si la connexion est établie, ASP.NET transmet le message au serveur SMTP.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-170">If the connection is successful, ASP.NET hands the message off to the SMTP server.</span></span> <span data-ttu-id="cd8b5-171">Toutefois, il peut y avoir des problèmes avec le message lui-même qui empêche le serveur SMTP à partir de l’envoyer.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-171">However, there can be problems with the message itself that prevents the SMTP server from sending it.</span></span>

<span data-ttu-id="cd8b5-172">Si votre application n’envoie pas correctement le courrier électronique, essayez ce qui suit :</span><span class="sxs-lookup"><span data-stu-id="cd8b5-172">If your application does not successfully send email, try the following:</span></span>

- <span data-ttu-id="cd8b5-173">Le nom du serveur SMTP est souvent quelque chose comme `smtp.provider.com` ou `smtp.provider.net`.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-173">The SMTP server name is often something like `smtp.provider.com` or `smtp.provider.net`.</span></span> <span data-ttu-id="cd8b5-174">Toutefois, si vous publiez votre site sur un fournisseur d’hébergement, le nom du serveur SMTP à ce stade peut être `localhost`.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-174">However, if you publish your site to a hosting provider, the SMTP server name at that point might be `localhost`.</span></span> <span data-ttu-id="cd8b5-175">Cette situation se produit, car une fois que vous avez publié et que votre site est en cours d’exécution sur le serveur du fournisseur, le serveur SMTP peut être local du point de vue de votre application.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-175">This situation occurs because after you've published and your site is running on the provider's server, the SMTP server might be local from the perspective of your application.</span></span> <span data-ttu-id="cd8b5-176">Cette modification dans les noms de serveur peut signifier que vous devez modifier le nom du serveur SMTP dans le cadre de votre processus de publication.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-176">This change in server names might mean that you have to change the SMTP server name as part of your publishing process.</span></span>
- <span data-ttu-id="cd8b5-177">Le numéro de port est généralement 25.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-177">The port number is usually 25.</span></span> <span data-ttu-id="cd8b5-178">Toutefois, certains fournisseurs vous obligent à utiliser port 587 ou un autre port.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-178">However, some providers require you to use port 587 or some other port.</span></span> <span data-ttu-id="cd8b5-179">Contactez le propriétaire du serveur SMTP quel numéro de port qu’elles attendent vous permettent d’utiliser.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-179">Check with the owner of the SMTP server what port number they expect you to use.</span></span>
- <span data-ttu-id="cd8b5-180">Assurez-vous que vous utilisez les informations d’identification correctes.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-180">Make sure that you use the right credentials.</span></span> <span data-ttu-id="cd8b5-181">Si vous avez publié votre site vers un fournisseur d’hébergement, utilisez les informations d’identification que le fournisseur a spécifiquement sont pour le courrier électronique.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-181">If you've published your site to a hosting provider, use the credentials that the provider has specifically indicated are for email.</span></span> <span data-ttu-id="cd8b5-182">Ces informations d’identification peuvent différer les informations d’identification que vous utilisez pour publier.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-182">These credentials might be different from the credentials you use to publish.</span></span>
- <span data-ttu-id="cd8b5-183">Parfois, vous n’avez pas besoin d’informations d’identification du tout.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-183">Sometimes you don't need credentials at all.</span></span> <span data-ttu-id="cd8b5-184">Si vous envoyez un e-mail à l’aide de votre fournisseur de services Internet, votre fournisseur de messagerie déjà connaissez peut-être vos informations d’identification.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-184">If you're sending email by using your personal ISP, your email provider might already know your credentials.</span></span> <span data-ttu-id="cd8b5-185">Une fois que vous publiez, vous devrez peut-être utiliser différentes informations d’identification que lorsque vous testez sur votre ordinateur local.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-185">After you publish, you might need to use different credentials than when you test on your local computer.</span></span>
- <span data-ttu-id="cd8b5-186">Si votre fournisseur de messagerie utilise le chiffrement, définissez `WebMail.EnableSsl` à `true`.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-186">If your email provider uses encryption, set `WebMail.EnableSsl` to `true`.</span></span>

<span data-ttu-id="cd8b5-187">S’il existe une erreur d’envoi de courrier électronique, vous pouvez voir un message d’erreur ASP.NET standard, qui ressemble à ceci :</span><span class="sxs-lookup"><span data-stu-id="cd8b5-187">If there is an error sending email, you might see a standard ASP.NET error message, which looks like this:</span></span>

![Message d’erreur ASP.NET lorsqu’il existe un problème avec l’adresse e-mail](aspnet-web-pages-razor-troubleshooting-guide/_static/image1.png)

<span data-ttu-id="cd8b5-189">Vous pouvez également déboguer des problèmes liés à l’envoi de courrier électronique à l’aide un `try-catch` bloc, comme dans l’exemple suivant.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-189">You can also debug problems with sending email by using a `try-catch` block, as in the following example.</span></span> <span data-ttu-id="cd8b5-190">Lorsque vous utilisez un `try-catch` bloc, ASP.NET n’affiche pas ses messages d’erreur standard.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-190">When you use a `try-catch` block, ASP.NET does not display its standard error messages.</span></span> <span data-ttu-id="cd8b5-191">Au lieu de cela, vous pouvez capturer l’erreur dans la `catch` partie du bloc.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-191">Instead, you can capture the error in the `catch` portion of the block.</span></span>

[!code-cshtml[Main](aspnet-web-pages-razor-troubleshooting-guide/samples/sample3.cshtml)]

<span data-ttu-id="cd8b5-192">Remplacez les valeurs appropriées pour `your-SMTP-server-name`, et ainsi de suite.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-192">Substitute the appropriate values for `your-SMTP-server-name`, and so on.</span></span> <span data-ttu-id="cd8b5-193">Voici quelques-unes des messages d’erreur que peut s’afficher de cette façon :</span><span class="sxs-lookup"><span data-stu-id="cd8b5-193">Some of the error messages you might see this way include the following:</span></span>

- <span data-ttu-id="cd8b5-194">*Échec de l’envoi de courrier.*</span><span class="sxs-lookup"><span data-stu-id="cd8b5-194">*Failure sending mail.*</span></span>

    <span data-ttu-id="cd8b5-195">- ou -</span><span class="sxs-lookup"><span data-stu-id="cd8b5-195">-or-</span></span>

    <span data-ttu-id="cd8b5-196">*Une tentative de connexion a échoué car la partie connectée n’a pas répondu convenablement après une période de temps ou la connexion établie a échoué, car l’hôte connecté n’a pas répondu*</span><span class="sxs-lookup"><span data-stu-id="cd8b5-196">*A connection attempt failed because the connected party did not properly respond after a period of time, or established connection failed because connected host has failed to respond*</span></span>

    <span data-ttu-id="cd8b5-197">Cette erreur signifie généralement que l’application ne peut pas se connecter au serveur SMTP.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-197">This error usually means that the application could not connect to the SMTP server.</span></span> <span data-ttu-id="cd8b5-198">Vérifiez le nom du serveur et le numéro de port.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-198">Check the server name and port number.</span></span>
- <span data-ttu-id="cd8b5-199">*La boîte aux lettres non disponible. La réponse du serveur était : 5.1.0 &lt; someuser@invaliddomain &gt; expéditeur rejeté : domaine de l’expéditeur non valide*</span><span class="sxs-lookup"><span data-stu-id="cd8b5-199">*Mailbox unavailable. The server response was: 5.1.0 &lt;someuser@invaliddomain&gt; sender rejected : invalid sender domain*</span></span>

    <span data-ttu-id="cd8b5-200">Ce message peut indiquer que le `From` adresse n’est pas correcte ou est manquant.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-200">This message can indicate that the `From` address is not correct or is missing.</span></span>
- <span data-ttu-id="cd8b5-201">*La chaîne spécifiée n’est pas sous la forme requise pour une adresse de messagerie.*</span><span class="sxs-lookup"><span data-stu-id="cd8b5-201">*The specified string is not in the form required for an email address.*</span></span>

    <span data-ttu-id="cd8b5-202">Cette erreur peut indiquer que la valeur de la `To` ou `From` propriétés ne sont pas reconnues comme adresses de messagerie.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-202">This error might indicate that the value of the `To` or `From` properties are not recognized as email addresses.</span></span> <span data-ttu-id="cd8b5-203">(ASP.NET ne peut pas vérifier que l’adresse de messagerie est valide, uniquement si elle 's dans le format correct, comme *name@domain.com*.)</span><span class="sxs-lookup"><span data-stu-id="cd8b5-203">(ASP.NET cannot check that the email address is valid, only that it's in the correct format, like *name@domain.com*.)</span></span>

> [!NOTE]
> <span data-ttu-id="cd8b5-204">Supprimez la balise qui affiche l’erreur (`@errorMessage`) avant de publier la page sur un site de production.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-204">Remove the markup that displays the error (`@errorMessage`) before you publish the page to a live site.</span></span> <span data-ttu-id="cd8b5-205">Il n’est pas une bonne idée pour permettre aux utilisateurs de voir les messages d’erreur que vous obtenez à partir d’un serveur.</span><span class="sxs-lookup"><span data-stu-id="cd8b5-205">It's not a good idea to let users see error messages that you get from a server.</span></span>


<a id="AdditionalResources"></a>
## <a name="additional-resources"></a><span data-ttu-id="cd8b5-206">Ressources supplémentaires</span><span class="sxs-lookup"><span data-stu-id="cd8b5-206">Additional Resources</span></span>

[<span data-ttu-id="cd8b5-207">Pages web ASP.NET - Questions fréquentes (FAQ) (Razor)</span><span class="sxs-lookup"><span data-stu-id="cd8b5-207">ASP.NET Web Pages (Razor) FAQ</span></span>](https://go.microsoft.com/fwlink/?LinkId=253000)

<span data-ttu-id="cd8b5-208">[WebMatrix et ASP.NET Web Pages](https://forums.asp.net/1224.aspx/1?WebMatrix) forum sur le site Web ASP.NET</span><span class="sxs-lookup"><span data-stu-id="cd8b5-208">[WebMatrix and ASP.NET Web Pages](https://forums.asp.net/1224.aspx/1?WebMatrix) forum on the ASP.NET website</span></span>
