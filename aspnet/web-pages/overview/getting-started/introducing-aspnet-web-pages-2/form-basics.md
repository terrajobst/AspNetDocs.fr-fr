---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
title: Présentation des Pages Web ASP.NET - principes de base de formulaire HTML | Microsoft Docs
author: Rick-Anderson
description: Ce didacticiel vous montre les principes fondamentaux de la création d’un formulaire d’entrée et comment gérer l’entrée de l’utilisateur lorsque vous utilisez les Pages Web ASP.NET (Razor). Et maintenant que vous avez...
ms.author: riande
ms.date: 05/28/2015
ms.assetid: 81ed82bf-b940-44f1-b94a-555d0cb7cc98
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
msc.type: authoredcontent
ms.openlocfilehash: 28385ac2244ab0bfb38ee5fcbc64e6e11804612b
ms.sourcegitcommit: 24b1f6decbb17bb22a45166e5fdb0845c65af498
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 03/01/2019
ms.locfileid: "57028216"
---
<a name="introducing-aspnet-web-pages---html-form-basics"></a><span data-ttu-id="16fd3-104">Présentation des Pages Web ASP.NET - principes de base de formulaire HTML</span><span class="sxs-lookup"><span data-stu-id="16fd3-104">Introducing ASP.NET Web Pages - HTML Form Basics</span></span>
====================
<span data-ttu-id="16fd3-105">par [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="16fd3-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="16fd3-106">Ce didacticiel vous montre les principes fondamentaux de la création d’un formulaire d’entrée et comment gérer l’entrée de l’utilisateur lorsque vous utilisez les Pages Web ASP.NET (Razor).</span><span class="sxs-lookup"><span data-stu-id="16fd3-106">This tutorial shows you the basics of how to create an input form and how to handle the user's input when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="16fd3-107">Et maintenant que vous avez une base de données, vous allez utiliser vos compétences en matière de formulaire pour permettre aux utilisateurs de rechercher des films spécifiques dans la base de données.</span><span class="sxs-lookup"><span data-stu-id="16fd3-107">And now that you've got a database, you'll use your form skills to let users find specific movies in the database.</span></span> <span data-ttu-id="16fd3-108">Il part du principe que vous avez terminé la série via [Introduction à affichage des données à l’aide de ASP.NET Web Pages](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data).</span><span class="sxs-lookup"><span data-stu-id="16fd3-108">It assumes you have completed the series through [Introduction to Displaying Data Using ASP.NET Web Pages](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data).</span></span>
> 
> <span data-ttu-id="16fd3-109">Ce que vous allez apprendre :</span><span class="sxs-lookup"><span data-stu-id="16fd3-109">What you'll learn:</span></span>
> 
> - <span data-ttu-id="16fd3-110">Comment créer un formulaire à l’aide des éléments HTML standards.</span><span class="sxs-lookup"><span data-stu-id="16fd3-110">How to create a form by using standard HTML elements.</span></span>
> - <span data-ttu-id="16fd3-111">Guide pratique pour lire l’utilisateur d’entrée dans un formulaire.</span><span class="sxs-lookup"><span data-stu-id="16fd3-111">How to read the user's input in a form.</span></span>
> - <span data-ttu-id="16fd3-112">Comment créer une requête SQL que sélectivement Obtient des données à l’aide d’une recherche de terme que l’utilisateur fournit.</span><span class="sxs-lookup"><span data-stu-id="16fd3-112">How to create a SQL query that selectively gets data by using a search term that the user supplies.</span></span>
> - <span data-ttu-id="16fd3-113">Comment faire en sorte de champs dans la page « mémoriser » l’entrée utilisateur.</span><span class="sxs-lookup"><span data-stu-id="16fd3-113">How to have fields in the page "remember" what the user entered.</span></span>
>   
> 
> <span data-ttu-id="16fd3-114">Fonctionnalités technologies abordées :</span><span class="sxs-lookup"><span data-stu-id="16fd3-114">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="16fd3-115">Objet `Request`.</span><span class="sxs-lookup"><span data-stu-id="16fd3-115">The `Request` object.</span></span>
> - <span data-ttu-id="16fd3-116">Le code SQL `Where` clause.</span><span class="sxs-lookup"><span data-stu-id="16fd3-116">The SQL `Where` clause.</span></span>


## <a name="what-youll-build"></a><span data-ttu-id="16fd3-117">Ce que vous allez générer</span><span class="sxs-lookup"><span data-stu-id="16fd3-117">What You'll Build</span></span>

<span data-ttu-id="16fd3-118">Dans le didacticiel précédent, vous créé une base de données, ajouté des données à ce dernier, puis utilisé la `WebGrid` helper pour afficher les données.</span><span class="sxs-lookup"><span data-stu-id="16fd3-118">In the previous tutorial, you created a database, added data to it, and then used the `WebGrid` helper to display the data.</span></span> <span data-ttu-id="16fd3-119">Dans ce didacticiel, vous allez ajouter une zone de recherche qui vous permet de rechercher les films d’un genre spécifique ou dont le titre contient le mot.</span><span class="sxs-lookup"><span data-stu-id="16fd3-119">In this tutorial, you'll add a search box that lets you find movies of a specific genre or whose title contains whatever word you enter.</span></span> <span data-ttu-id="16fd3-120">(Par exemple, vous serez en mesure de trouver tous les films dont le genre est « Action » ou dont le titre contient « Harry » ou « Adventure ».)</span><span class="sxs-lookup"><span data-stu-id="16fd3-120">(For example, you'll be able to find all movies whose genre is "Action" or whose title contains "Harry" or "Adventure.")</span></span>

<span data-ttu-id="16fd3-121">Lorsque vous avez terminé ce didacticiel, vous avez une page similaire à celle-ci :</span><span class="sxs-lookup"><span data-stu-id="16fd3-121">When you're done with this tutorial, you'll have a page like this one:</span></span>

![Page de films avec recherche de Genre et titre](form-basics/_static/image1.png)

<span data-ttu-id="16fd3-123">La partie liste de la page est identique à celle du dernier didacticiel &mdash; une grille.</span><span class="sxs-lookup"><span data-stu-id="16fd3-123">The listing part of the page is the same as in the last tutorial &mdash; a grid.</span></span> <span data-ttu-id="16fd3-124">La différence est que la grille affichera uniquement les films que vous avez recherchées.</span><span class="sxs-lookup"><span data-stu-id="16fd3-124">The difference will be that the grid will show only the movies that you searched for.</span></span>

## <a name="about-html-forms"></a><span data-ttu-id="16fd3-125">Sur les formulaires HTML</span><span class="sxs-lookup"><span data-stu-id="16fd3-125">About HTML Forms</span></span>

<span data-ttu-id="16fd3-126">(Si vous avez une expérience avec la création de formulaires HTML et la différence entre `GET` et `POST`, vous pouvez ignorer cette section.)</span><span class="sxs-lookup"><span data-stu-id="16fd3-126">(If you've got experience with creating HTML forms and with the difference between `GET` and `POST`, you can skip this section.)</span></span>

<span data-ttu-id="16fd3-127">Un formulaire a des éléments d’entrée utilisateur &mdash; zones de texte, boutons, boutons radio, les cases à cocher, listes déroulantes et ainsi de suite.</span><span class="sxs-lookup"><span data-stu-id="16fd3-127">A form has user input elements &mdash; text boxes, buttons, radio buttons, check boxes, drop-down lists, and so on.</span></span> <span data-ttu-id="16fd3-128">Les utilisateurs renseignez ces contrôles ou effectuer des sélections et puis envoyez le formulaire en cliquant sur un bouton.</span><span class="sxs-lookup"><span data-stu-id="16fd3-128">Users fill in these controls or make selections and then submit the form by clicking a button.</span></span>

<span data-ttu-id="16fd3-129">La syntaxe HTML de base d’un formulaire est illustrée dans cet exemple :</span><span class="sxs-lookup"><span data-stu-id="16fd3-129">The basic HTML syntax of a form is illustrated by this example:</span></span>

[!code-html[Main](form-basics/samples/sample1.html)]

<span data-ttu-id="16fd3-130">Lorsque ce balisage s’exécute dans une page, il crée un formulaire simple qui ressemble à cette illustration :</span><span class="sxs-lookup"><span data-stu-id="16fd3-130">When this markup runs in a page, it creates a simple form that looks like this illustration:</span></span>

![Formulaire HTML de base comme affiché dans le navigateur](form-basics/_static/image2.png)

<span data-ttu-id="16fd3-132">Le `<form>` élément englobe des éléments HTML à être envoyées.</span><span class="sxs-lookup"><span data-stu-id="16fd3-132">The `<form>` element encloses HTML elements to be submitted.</span></span> <span data-ttu-id="16fd3-133">(Une erreur facile qui consiste à effectuer consiste à ajouter des éléments à la page, mais oublient de les placer à l’intérieur d’un `<form>` élément.</span><span class="sxs-lookup"><span data-stu-id="16fd3-133">(An easy mistake to make is to add elements to the page but then forget to put them inside a `<form>` element.</span></span> <span data-ttu-id="16fd3-134">Dans ce cas, rien n’est envoyé.) Le `method` attribut indique au navigateur comment soumettre l’entrée utilisateur.</span><span class="sxs-lookup"><span data-stu-id="16fd3-134">In that case, nothing is submitted.) The `method` attribute tells the browser how to submit the user input.</span></span> <span data-ttu-id="16fd3-135">Vous affectez la valeur `post` si vous effectuez une mise à jour sur le serveur ou à `get` si vous êtes simplement extraire des données à partir du serveur.</span><span class="sxs-lookup"><span data-stu-id="16fd3-135">You set this to `post` if you're performing an update on the server or to `get` if you're just fetching data from the server.</span></span>

<a id="GET,_POST,_and_HTTP_Verb_Safety"></a>

> [!TIP] 
> 
> <span data-ttu-id="16fd3-136">**GET, POST et sécurité du verbe HTTP**</span><span class="sxs-lookup"><span data-stu-id="16fd3-136">**GET, POST, and HTTP Verb Safety**</span></span>
> 
> <span data-ttu-id="16fd3-137">HTTP, navigateurs et les serveurs utilisent pour échanger des informations, le protocole est remarquablement simple dans ses opérations de base.</span><span class="sxs-lookup"><span data-stu-id="16fd3-137">HTTP, the protocol that browsers and servers use to exchange information, is remarkably simple in its basic operations.</span></span> <span data-ttu-id="16fd3-138">Navigateurs utilisent uniquement quelques verbes pour effectuer des demandes aux serveurs.</span><span class="sxs-lookup"><span data-stu-id="16fd3-138">Browsers use only a few verbs to make requests to servers.</span></span> <span data-ttu-id="16fd3-139">Lorsque vous écrivez du code pour le web, il est utile de comprendre ces verbes et la façon dont le navigateur et le serveur les utiliser.</span><span class="sxs-lookup"><span data-stu-id="16fd3-139">When you write code for the web, it's helpful to understand these verbs and how the browser and server use them.</span></span> <span data-ttu-id="16fd3-140">Et éloignés les verbes couramment utilisés sont ces :</span><span class="sxs-lookup"><span data-stu-id="16fd3-140">Far and away the most commonly used verbs are these:</span></span>
> 
> - <span data-ttu-id="16fd3-141">`GET`.</span><span class="sxs-lookup"><span data-stu-id="16fd3-141">`GET`.</span></span> <span data-ttu-id="16fd3-142">Le navigateur utilise ce verbe pour extraire un élément à partir du serveur.</span><span class="sxs-lookup"><span data-stu-id="16fd3-142">The browser uses this verb to fetch something from the server.</span></span> <span data-ttu-id="16fd3-143">Par exemple, lorsque vous tapez une URL dans votre navigateur, le navigateur effectue une `GET` opération visant à demander la page qui vous intéresse.</span><span class="sxs-lookup"><span data-stu-id="16fd3-143">For example, when you type a URL into your browser, the browser performs a `GET` operation to request the page you want.</span></span> <span data-ttu-id="16fd3-144">Si la page inclut des graphiques, le navigateur effectue supplémentaires `GET` opérations pour obtenir les images.</span><span class="sxs-lookup"><span data-stu-id="16fd3-144">If the page includes graphics, the browser performs additional `GET` operations to get the images.</span></span> <span data-ttu-id="16fd3-145">Si le `GET` opération a passer des informations sur le serveur, les informations sont transmises en tant que partie de l’URL dans la chaîne de requête.</span><span class="sxs-lookup"><span data-stu-id="16fd3-145">If the `GET` operation has to pass information to the server, the information is passed as part of the URL in the query string.</span></span>
> - <span data-ttu-id="16fd3-146">`POST`.</span><span class="sxs-lookup"><span data-stu-id="16fd3-146">`POST`.</span></span> <span data-ttu-id="16fd3-147">Le navigateur envoie une `POST` demande afin d’envoyer des données pour être ajoutés ou modifiés sur le serveur.</span><span class="sxs-lookup"><span data-stu-id="16fd3-147">The browser sends a `POST` request in order to submit data to be added or changed on the server.</span></span> <span data-ttu-id="16fd3-148">Par exemple, le `POST` verbe est utilisé pour créer des enregistrements dans une base de données ou modifier existants.</span><span class="sxs-lookup"><span data-stu-id="16fd3-148">For example, the `POST` verb is used to create records in a database or change existing ones.</span></span> <span data-ttu-id="16fd3-149">La plupart du temps, lorsque vous remplissez un formulaire et cliquez sur le bouton Envoyer, le navigateur effectue une `POST` opération.</span><span class="sxs-lookup"><span data-stu-id="16fd3-149">Most of the time, when you fill in a form and click the submit button, the browser performs a `POST` operation.</span></span> <span data-ttu-id="16fd3-150">Dans un `POST` opération, les données transmises sur le serveur sont dans le corps de la page.</span><span class="sxs-lookup"><span data-stu-id="16fd3-150">In a `POST` operation, the data being passed to the server is in the body of the page.</span></span>
> 
> <span data-ttu-id="16fd3-151">Une différence importante entre ces verbes est qu’un `GET` opération n’est pas censé pour modifier quoi que ce soit sur le serveur, ou à le placer dans une méthode légèrement plus abstraite, un `GET` opération n’entraîne pas une modification d’état sur le serveur.</span><span class="sxs-lookup"><span data-stu-id="16fd3-151">An important distinction between these verbs is that a `GET` operation is not supposed to change anything on the server — or to put it in a slightly more abstract way, a `GET` operation does not result in a change in state on the server.</span></span> <span data-ttu-id="16fd3-152">Vous pouvez effectuer un `GET` opération sur les mêmes ressources autant de fois que vous voulez, et ne modifiez pas ces ressources.</span><span class="sxs-lookup"><span data-stu-id="16fd3-152">You can perform a `GET` operation on the same resources as many times as you like, and those resources don't change.</span></span> <span data-ttu-id="16fd3-153">(Un `GET` opération dit souvent « sécurisée », ou pour utiliser un terme technique, est *idempotent*.) En revanche, bien sûr, un `POST` demande change quelque chose sur le serveur chaque fois que vous effectuez l’opération.</span><span class="sxs-lookup"><span data-stu-id="16fd3-153">(A `GET` operation is often said to be "safe," or to use a technical term, is *idempotent*.) In contrast, of course, a `POST` request changes something on the server each time you perform the operation.</span></span>
> 
> <span data-ttu-id="16fd3-154">Deux exemples permet d’illustrer cette distinction.</span><span class="sxs-lookup"><span data-stu-id="16fd3-154">Two examples will help illustrate this distinction.</span></span> <span data-ttu-id="16fd3-155">Lorsque vous effectuez une recherche à l’aide d’un moteur tel que Bing ou Google, vous remplissez un formulaire qui se compose d’une zone de texte, puis vous cliquez sur le bouton de recherche.</span><span class="sxs-lookup"><span data-stu-id="16fd3-155">When you perform a search using an engine like Bing or Google, you fill in a form that consists of one text box, and then you click the search button.</span></span> <span data-ttu-id="16fd3-156">Le navigateur effectue une `GET` opération, avec la valeur que vous avez entré dans la zone transmise en tant que partie de l’URL.</span><span class="sxs-lookup"><span data-stu-id="16fd3-156">The browser performs a `GET` operation, with the value you entered into the box passed as part of the URL.</span></span> <span data-ttu-id="16fd3-157">À l’aide un `GET` opération pour ce type de formulaire est parfait, car une opération de recherche ne change pas toutes les ressources sur le serveur, il extrait uniquement les informations.</span><span class="sxs-lookup"><span data-stu-id="16fd3-157">Using a `GET` operation for this type of form is fine, because a search operation doesn't change any resources on the server, it just fetches information.</span></span>
> 
> <span data-ttu-id="16fd3-158">Examinons à présent le processus de classement quelque chose en ligne.</span><span class="sxs-lookup"><span data-stu-id="16fd3-158">Now consider the process of ordering something online.</span></span> <span data-ttu-id="16fd3-159">Vous remplissez les détails de l’ordre et puis cliquez sur le bouton Envoyer.</span><span class="sxs-lookup"><span data-stu-id="16fd3-159">You fill in the order details and then click the submit button.</span></span> <span data-ttu-id="16fd3-160">Cette opération doit être un `POST` demander, étant donné que l’opération entraîne des modifications sur le serveur, comme un nouvel enregistrement de commande, une modification dans les informations de votre compte et peut-être nombre d’autres modifications.</span><span class="sxs-lookup"><span data-stu-id="16fd3-160">This operation will be a `POST` request, because the operation will result in changes on the server, such as a new order record, a change in your account information, and perhaps many other changes.</span></span> <span data-ttu-id="16fd3-161">Contrairement à la `GET` opération, vous ne pouvez pas répéter votre `POST` demande — si vous l’avez fait, chaque fois que vous soumis de nouveau la demande, vous génère une nouvelle commande sur le serveur.</span><span class="sxs-lookup"><span data-stu-id="16fd3-161">Unlike the `GET` operation, you cannot repeat your `POST` request — if you did, each time you resubmitted the request, you'd generate a new order on the server.</span></span> <span data-ttu-id="16fd3-162">(Dans ce cas, sites Web souvent vous avertira ne pas à cliquez plusieurs fois sur un bouton d’envoi ou désactive le bouton Envoyer afin que vous ne renvoyez pas accidentellement le formulaire.)</span><span class="sxs-lookup"><span data-stu-id="16fd3-162">(In cases like this, websites will often warn you not to click a submit button more than once, or will disable the submit button so that you don't resubmit the form accidentally.)</span></span>
> 
> <span data-ttu-id="16fd3-163">Au cours de ce didacticiel, vous allez utiliser à la fois un `GET` opération et un `POST` opération pour travailler avec des formulaires HTML.</span><span class="sxs-lookup"><span data-stu-id="16fd3-163">In the course of this tutorial, you'll use both a `GET` operation and a `POST` operation to work with HTML forms.</span></span> <span data-ttu-id="16fd3-164">Nous expliquerons dans chaque cas pourquoi le verbe que vous utilisez est celui qui convient.</span><span class="sxs-lookup"><span data-stu-id="16fd3-164">We'll explain in each case why the verb you use is the appropriate one.</span></span>
> 
> <span data-ttu-id="16fd3-165">(Pour en savoir plus sur les verbes HTTP, consultez le [définitions de méthode](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article sur le site du W3C.)</span><span class="sxs-lookup"><span data-stu-id="16fd3-165">(To learn more about HTTP verbs, see the [Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site.)</span></span>


<span data-ttu-id="16fd3-166">La plupart des éléments d’entrée utilisateur sont HTML `<input>` éléments.</span><span class="sxs-lookup"><span data-stu-id="16fd3-166">Most user input elements are HTML `<input>` elements.</span></span> <span data-ttu-id="16fd3-167">Ils ressemblent `<input type="type" name="name">,` où *type* indique le type de contrôle d’entrée d’utilisateur souhaité.</span><span class="sxs-lookup"><span data-stu-id="16fd3-167">They look like `<input type="type" name="name">,` where *type* indicates the kind of user input control you want.</span></span> <span data-ttu-id="16fd3-168">Ces éléments sont les plus courants :</span><span class="sxs-lookup"><span data-stu-id="16fd3-168">These elements are the common ones:</span></span>

- <span data-ttu-id="16fd3-169">Zone de texte : `<input type="text">`</span><span class="sxs-lookup"><span data-stu-id="16fd3-169">Text box: `<input type="text">`</span></span>
- <span data-ttu-id="16fd3-170">Case à cocher : `<input type="check">`</span><span class="sxs-lookup"><span data-stu-id="16fd3-170">Check box: `<input type="check">`</span></span>
- <span data-ttu-id="16fd3-171">Case d’option : `<input type="radio">`</span><span class="sxs-lookup"><span data-stu-id="16fd3-171">Radio button: `<input type="radio">`</span></span>
- <span data-ttu-id="16fd3-172">Bouton : `<input type="button">`</span><span class="sxs-lookup"><span data-stu-id="16fd3-172">Button: `<input type="button">`</span></span>
- <span data-ttu-id="16fd3-173">Bouton d’envoi : `<input type="submit">`</span><span class="sxs-lookup"><span data-stu-id="16fd3-173">Submit button: `<input type="submit">`</span></span>

<span data-ttu-id="16fd3-174">Vous pouvez également utiliser le `<textarea>` élément pour créer une zone de texte multiligne et `<select>` élément pour créer une liste déroulante ou une liste déroulante.</span><span class="sxs-lookup"><span data-stu-id="16fd3-174">You can also use the `<textarea>` element to create a multiline text box and the `<select>` element to create a drop-down list or scrollable list.</span></span> <span data-ttu-id="16fd3-175">(Pour plus d’informations sur HTML forment des éléments, consultez [formulaires HTML et entrée](http://www.w3schools.com/html/html_forms.asp) sur le site W3Schools.)</span><span class="sxs-lookup"><span data-stu-id="16fd3-175">(For more about HTML form elements, see [HTML Forms and Input](http://www.w3schools.com/html/html_forms.asp) on the W3Schools site.)</span></span>

<span data-ttu-id="16fd3-176">Le `name` attribut est très important, car le nom est comment vous allez obtenir la valeur de l’élément dans une version ultérieure, comme vous le verrez bientôt.</span><span class="sxs-lookup"><span data-stu-id="16fd3-176">The `name` attribute is very important, because the name is how you'll get the value of the element later, as you'll see shortly.</span></span>

<span data-ttu-id="16fd3-177">La partie intéressante est ce que vous, le développeur de pages, faire avec l’entrée d’utilisateur.</span><span class="sxs-lookup"><span data-stu-id="16fd3-177">The interesting part is what you, the page developer, do with the user's input.</span></span> <span data-ttu-id="16fd3-178">Il n’existe aucun comportement intégré associé à ces éléments.</span><span class="sxs-lookup"><span data-stu-id="16fd3-178">There's no built-in behavior associated with these elements.</span></span> <span data-ttu-id="16fd3-179">Au lieu de cela, vous devez obtenir les valeurs que l’utilisateur a entré ou sélectionné et faire quelque chose avec eux.</span><span class="sxs-lookup"><span data-stu-id="16fd3-179">Instead, you have to get the values that the user has entered or selected and do something with them.</span></span> <span data-ttu-id="16fd3-180">C’est ce que vous allez apprendre dans ce didacticiel.</span><span class="sxs-lookup"><span data-stu-id="16fd3-180">That's what you'll learn in this tutorial.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="16fd3-181">**HTML5 et les formulaires de saisie**</span><span class="sxs-lookup"><span data-stu-id="16fd3-181">**HTML5 and Input Forms**</span></span>
> 
> <span data-ttu-id="16fd3-182">Comme vous le savez peut-être, HTML est en transition et la dernière version (HTML5) inclut la prise en charge de manière plus intuitive pour les utilisateurs à entrer des informations.</span><span class="sxs-lookup"><span data-stu-id="16fd3-182">As you might know, HTML is in transition and the latest version (HTML5) includes support for more intuitive ways for users to enter information.</span></span> <span data-ttu-id="16fd3-183">Par exemple, en HTML5, vous (le développeur de pages) pouvez indiquer la page que vous souhaitez que l’utilisateur à entrer une date.</span><span class="sxs-lookup"><span data-stu-id="16fd3-183">For example, in HTML5, you (the page developer) can tell the page that you want the user to enter a date.</span></span> <span data-ttu-id="16fd3-184">Le navigateur peut afficher automatiquement puis d’un calendrier au lieu d’obliger l’utilisateur à entrer une date manuellement.</span><span class="sxs-lookup"><span data-stu-id="16fd3-184">The browser can then automatically display a calendar rather than requiring the user to enter a date manually.</span></span> <span data-ttu-id="16fd3-185">Toutefois, HTML5 est nouveau et ne prend pas en charge tous les navigateurs encore.</span><span class="sxs-lookup"><span data-stu-id="16fd3-185">However, HTML5 is new and is not supported in all browsers yet.</span></span>
> 
> <span data-ttu-id="16fd3-186">Les Pages Web ASP.NET prend en charge HTML5 d’entrée dans la mesure où le navigateur de l’utilisateur est.</span><span class="sxs-lookup"><span data-stu-id="16fd3-186">ASP.NET Web Pages supports HTML5 input to the extent that the user's browser does.</span></span> <span data-ttu-id="16fd3-187">Pour avoir une idée des nouveaux attributs pour le `<input>` élément en HTML5, consultez [HTML &lt;d’entrée&gt; attribut type](http://www.w3schools.com/html/html_form_input_types.asp) sur le site W3Schools.</span><span class="sxs-lookup"><span data-stu-id="16fd3-187">For an idea of the new attributes for the `<input>` element in HTML5, see [HTML &lt;input&gt; type Attribute](http://www.w3schools.com/html/html_form_input_types.asp) on the W3Schools site.</span></span>


## <a name="creating-the-form"></a><span data-ttu-id="16fd3-188">Création du formulaire</span><span class="sxs-lookup"><span data-stu-id="16fd3-188">Creating the Form</span></span>

<span data-ttu-id="16fd3-189">Dans WebMatrix, dans le **fichiers** espace de travail, ouvrez le *Movies.cshtml* page.</span><span class="sxs-lookup"><span data-stu-id="16fd3-189">In WebMatrix, in the **Files** workspace, open the *Movies.cshtml* page.</span></span>

<span data-ttu-id="16fd3-190">Après la fermeture `</h1>` balise et avant l’ouverture `<div>` balise de le `grid.GetHtml` appeler, ajoutez le balisage suivant :</span><span class="sxs-lookup"><span data-stu-id="16fd3-190">After the closing `</h1>` tag and before the opening `<div>` tag of the `grid.GetHtml` call, add the following markup:</span></span>

[!code-html[Main](form-basics/samples/sample2.html)]

<span data-ttu-id="16fd3-191">Ce balisage crée un formulaire qui a une zone de texte nommée `searchGenre` et un bouton Envoyer.</span><span class="sxs-lookup"><span data-stu-id="16fd3-191">This markup creates a form that has a text box named `searchGenre` and a submit button.</span></span> <span data-ttu-id="16fd3-192">Le texte de zone et d’envoyer le bouton sont placés entre un `<form>` élément dont `method` attribut a la valeur `get`.</span><span class="sxs-lookup"><span data-stu-id="16fd3-192">The text box and submit button are enclosed in a `<form>` element whose `method` attribute is set to `get`.</span></span> <span data-ttu-id="16fd3-193">(N’oubliez pas que si vous ne placez la zone de texte et bouton à l’intérieur d’envoyer un `<form>` élément, rien n’est envoyée lorsque vous cliquez sur le bouton.) Vous utilisez le `GET` verbe ici, car vous créez un formulaire qui n’apporte aucune modification sur le serveur, il en résulte simplement une recherche.</span><span class="sxs-lookup"><span data-stu-id="16fd3-193">(Remember that if you don't put the text box and submit button inside a `<form>` element, nothing will be submitted when you click the button.) You use the `GET` verb here because you're creating a form that does not make any changes on the server — it just results in a search.</span></span> <span data-ttu-id="16fd3-194">(Dans le didacticiel précédent, vous avez utilisé un `post` (méthode), qui est la manière dont vous soumettez des modifications sur le serveur.</span><span class="sxs-lookup"><span data-stu-id="16fd3-194">(In the previous tutorial, you used a `post` method, which is how you submit changes to the server.</span></span> <span data-ttu-id="16fd3-195">Vous verrez que dans le didacticiel suivant à nouveau.)</span><span class="sxs-lookup"><span data-stu-id="16fd3-195">You'll see that in the next tutorial again.)</span></span>

<span data-ttu-id="16fd3-196">Exécutez la page.</span><span class="sxs-lookup"><span data-stu-id="16fd3-196">Run the page.</span></span> <span data-ttu-id="16fd3-197">Bien que vous n’avez pas défini de comportement pour le formulaire, vous pouvez voir à quoi elle ressemble :</span><span class="sxs-lookup"><span data-stu-id="16fd3-197">Although you haven't defined any behavior for the form, you can see what it looks like:</span></span>

![Page de films avec la zone de recherche pour Genre](form-basics/_static/image3.png)

<span data-ttu-id="16fd3-199">Entrez une valeur dans la zone de texte, tels que « Comédie ».</span><span class="sxs-lookup"><span data-stu-id="16fd3-199">Enter a value into the text box, like "Comedy."</span></span> <span data-ttu-id="16fd3-200">Puis cliquez sur **recherche Genre**.</span><span class="sxs-lookup"><span data-stu-id="16fd3-200">Then click **Search Genre**.</span></span>

<span data-ttu-id="16fd3-201">Prenez note de l’URL de la page.</span><span class="sxs-lookup"><span data-stu-id="16fd3-201">Take note of the URL of the page.</span></span> <span data-ttu-id="16fd3-202">Étant donné que vous définissez le `<form>` l’élément `method` attribut `get`, la valeur que vous avez entré fait désormais partie de la chaîne de requête dans l’URL, comme ceci :</span><span class="sxs-lookup"><span data-stu-id="16fd3-202">Because you set the `<form>` element's `method` attribute to `get`, the value you entered is now part of the query string in the URL, like this:</span></span>

`http://localhost:45661/Movies.cshtml?searchGenre=Comedy`

## <a name="reading-form-values"></a><span data-ttu-id="16fd3-203">Valeurs de formulaire de lecture</span><span class="sxs-lookup"><span data-stu-id="16fd3-203">Reading Form Values</span></span>

<span data-ttu-id="16fd3-204">La page contient déjà du code qui obtient des données de base de données et affiche les résultats dans une grille.</span><span class="sxs-lookup"><span data-stu-id="16fd3-204">The page already contains some code that gets database data and displays the results in a grid.</span></span> <span data-ttu-id="16fd3-205">Vous devez maintenant ajouter du code qui lit la valeur de la zone de texte afin de pouvoir exécuter une requête SQL qui inclut le terme de recherche.</span><span class="sxs-lookup"><span data-stu-id="16fd3-205">Now you have to add some code that reads the value of the text box so you can run a SQL query that includes the search term.</span></span>

<span data-ttu-id="16fd3-206">Étant donné que vous définissez la méthode du formulaire sur `get`, vous pouvez lire la valeur qui a été entrée dans la zone de texte à l’aide de code comme suit :</span><span class="sxs-lookup"><span data-stu-id="16fd3-206">Because you set the form's method to `get`, you can read the value that was entered into the text box by using code like the following:</span></span>

`var searchTerm = Request.QueryString["searchGenre"];`

<span data-ttu-id="16fd3-207">Le `Request.QueryString` objet (la `QueryString` propriété de la `Request` objet) inclut les valeurs des éléments qui ont été soumises dans le cadre de la `GET` opération.</span><span class="sxs-lookup"><span data-stu-id="16fd3-207">The `Request.QueryString` object (the `QueryString` property of the `Request` object) includes the values of elements that were submitted as part of the `GET` operation.</span></span> <span data-ttu-id="16fd3-208">Le `Request.QueryString` propriété contient un *collection* (liste) des valeurs qui sont envoyées dans le formulaire.</span><span class="sxs-lookup"><span data-stu-id="16fd3-208">The `Request.QueryString` property contains a *collection* (a list) of the values that are submitted in the form.</span></span> <span data-ttu-id="16fd3-209">Pour obtenir toute valeur individuelle, vous spécifiez le nom de l’élément que vous souhaitez.</span><span class="sxs-lookup"><span data-stu-id="16fd3-209">To get any individual value, you specify the name of the element that you want.</span></span> <span data-ttu-id="16fd3-210">C’est pourquoi vous devez avoir un `name` d’attribut sur le `<input>` élément (`searchTerm`) qui crée la zone de texte.</span><span class="sxs-lookup"><span data-stu-id="16fd3-210">That's why you have to have a `name` attribute on the `<input>` element (`searchTerm`) that creates the text box.</span></span> <span data-ttu-id="16fd3-211">(Pour plus d’informations sur la `Request` d’objets, consultez le [encadré](#BKMK_TheRequestObject) ultérieurement.)</span><span class="sxs-lookup"><span data-stu-id="16fd3-211">(For more about the `Request` object, see the [sidebar](#BKMK_TheRequestObject) later.)</span></span>

<span data-ttu-id="16fd3-212">Il est suffisamment simple pour lire la valeur de la zone de texte.</span><span class="sxs-lookup"><span data-stu-id="16fd3-212">It's simple enough to read the value of the text box.</span></span> <span data-ttu-id="16fd3-213">Toutefois, si l’utilisateur n’entrez rien du tout dans la zone de texte, qu’il a cliqué sur **recherche** tout de même, vous pouvez ignorer ce clic, dans la mesure où il n’y a rien à rechercher.</span><span class="sxs-lookup"><span data-stu-id="16fd3-213">But if the user didn't enter anything at all in the text box but clicked **Search** anyway, you can ignore that click, since there's nothing to search.</span></span>

<span data-ttu-id="16fd3-214">Le code suivant est un exemple qui montre comment implémenter ces conditions.</span><span class="sxs-lookup"><span data-stu-id="16fd3-214">The following code is an example that shows how to implement these conditions.</span></span> <span data-ttu-id="16fd3-215">(Vous n’êtes pas obligé d’ajouter ce code encore ; vous pouvez utiliser dans un instant).</span><span class="sxs-lookup"><span data-stu-id="16fd3-215">(You don't have to add this code yet; you'll do that in a moment.)</span></span>

[!code-csharp[Main](form-basics/samples/sample3.cs)]

<span data-ttu-id="16fd3-216">Le test se décompose ainsi :</span><span class="sxs-lookup"><span data-stu-id="16fd3-216">The test breaks down in this way:</span></span>

- <span data-ttu-id="16fd3-217">Obtenir la valeur de `Request.QueryString["searchGenre"]`, à savoir la valeur qui a été entrée dans le `<input>` élément nommé `searchGenre`.</span><span class="sxs-lookup"><span data-stu-id="16fd3-217">Get the value of `Request.QueryString["searchGenre"]`, namely the value that was entered into the `<input>` element named `searchGenre`.</span></span>
- <span data-ttu-id="16fd3-218">Découvrez si elle est vide à l’aide de la `IsEmpty` (méthode).</span><span class="sxs-lookup"><span data-stu-id="16fd3-218">Find out if it's empty by using the `IsEmpty` method.</span></span> <span data-ttu-id="16fd3-219">Cette méthode est la méthode standard pour déterminer si quelque chose (par exemple, un élément de formulaire) contient une valeur.</span><span class="sxs-lookup"><span data-stu-id="16fd3-219">This method is the standard way to determine whether something (for example, a form element) contains a value.</span></span> <span data-ttu-id="16fd3-220">Mais, vous fiche uniquement si elle a *pas* vide, par conséquent...</span><span class="sxs-lookup"><span data-stu-id="16fd3-220">But really, you care only if it's *not* empty, therefore ...</span></span>
- <span data-ttu-id="16fd3-221">Ajouter le `!` opérateur devant le `IsEmpty` de test.</span><span class="sxs-lookup"><span data-stu-id="16fd3-221">Add the `!` operator in front of the `IsEmpty` test.</span></span> <span data-ttu-id="16fd3-222">(Le `!` opérateur signifie NOT logique).</span><span class="sxs-lookup"><span data-stu-id="16fd3-222">(The `!` operator means logical NOT).</span></span>

<span data-ttu-id="16fd3-223">En clair, l’ensemble `if` condition se traduit par les éléments suivants : *Si l’élément de searchGenre du formulaire n’est pas vide, puis...*</span><span class="sxs-lookup"><span data-stu-id="16fd3-223">In plain English, the entire `if` condition translates into the following: *If the form's searchGenre element is not empty, then ...*</span></span>

<span data-ttu-id="16fd3-224">Ce bloc définit l’étape de création d’une requête qui utilise le terme de recherche.</span><span class="sxs-lookup"><span data-stu-id="16fd3-224">This block sets the stage for creating a query that uses the search term.</span></span> <span data-ttu-id="16fd3-225">Vous pouvez utiliser dans la section suivante.</span><span class="sxs-lookup"><span data-stu-id="16fd3-225">You'll do that in the next section.</span></span>

<a id="BKMK_TheRequestObject"></a>

> [!TIP] 
> 
> <span data-ttu-id="16fd3-226">**L’objet de requête**</span><span class="sxs-lookup"><span data-stu-id="16fd3-226">**The Request Object**</span></span>
> 
> <span data-ttu-id="16fd3-227">Le `Request` objet contient toutes les informations que le navigateur envoie à votre application lorsqu’une page est demandée ou soumise.</span><span class="sxs-lookup"><span data-stu-id="16fd3-227">The `Request` object contains all the information that the browser sends to your application when a page is requested or submitted.</span></span> <span data-ttu-id="16fd3-228">Cet objet inclut toutes les informations fournies par l’utilisateur, telles que les valeurs de zone de texte ou un fichier à charger.</span><span class="sxs-lookup"><span data-stu-id="16fd3-228">This object includes any information that the user provides, like text box values or a file to upload.</span></span> <span data-ttu-id="16fd3-229">Il inclut également toutes sortes d’informations supplémentaires, telles que les cookies, les valeurs dans la chaîne de requête URL (le cas échéant), le chemin d’accès de fichier de la page qui est en cours d’exécution, le type de navigateur à l’aide de l’utilisateur, la liste des langues qui sont définies dans le navigateur et bien plus encore.</span><span class="sxs-lookup"><span data-stu-id="16fd3-229">It also includes all sorts of additional information, like cookies, values in the URL query string (if any), the file path of the page that is running, the type of browser that the user is using, the list of languages that are set in the browser, and much more.</span></span>
> 
> <span data-ttu-id="16fd3-230">Le `Request` objet est un *collection* (liste) de valeurs.</span><span class="sxs-lookup"><span data-stu-id="16fd3-230">The `Request` object is a *collection* (list) of values.</span></span> <span data-ttu-id="16fd3-231">Vous obtenez une valeur individuelle dans la collection en spécifiant son nom :</span><span class="sxs-lookup"><span data-stu-id="16fd3-231">You get an individual value out of the collection by specifying its name:</span></span>
> 
> `var someValue = Request["name"];`
> 
> <span data-ttu-id="16fd3-232">Le `Request` objet expose en fait plusieurs sous-ensembles.</span><span class="sxs-lookup"><span data-stu-id="16fd3-232">The `Request` object actually exposes several subsets.</span></span> <span data-ttu-id="16fd3-233">Exemple :</span><span class="sxs-lookup"><span data-stu-id="16fd3-233">For example:</span></span>
> 
> - <span data-ttu-id="16fd3-234">`Request.Form` vous donne les valeurs à partir des éléments à l’intérieur de l’élément soumis `<form>` élément si la demande est un `POST` demande.</span><span class="sxs-lookup"><span data-stu-id="16fd3-234">`Request.Form` gives you values from elements inside the submitted `<form>` element if the request is a `POST` request.</span></span>
> - <span data-ttu-id="16fd3-235">`Request.QueryString` permet que les valeurs dans la chaîne de requête URL.</span><span class="sxs-lookup"><span data-stu-id="16fd3-235">`Request.QueryString` gives you just the values in the URL's query string.</span></span> <span data-ttu-id="16fd3-236">(Dans une URL telle que `http://mysite/myapp/page?searchGenre=action&page=2`, le `?searchGenre=action&page=2` section de l’URL est la chaîne de requête.)</span><span class="sxs-lookup"><span data-stu-id="16fd3-236">(In a URL like `http://mysite/myapp/page?searchGenre=action&page=2`, the `?searchGenre=action&page=2` section of the URL is the query string.)</span></span>
> - <span data-ttu-id="16fd3-237">`Request.Cookies` collection vous permet d’accéder aux cookies du navigateur a envoyé.</span><span class="sxs-lookup"><span data-stu-id="16fd3-237">`Request.Cookies` collection gives you access to cookies that the browser has sent.</span></span>
> 
> <span data-ttu-id="16fd3-238">Pour obtenir une valeur dont vous savez qu’il est dans le formulaire envoyé, vous pouvez utiliser `Request["name"]`.</span><span class="sxs-lookup"><span data-stu-id="16fd3-238">To get a value that you know is in the submitted form, you can use `Request["name"]`.</span></span> <span data-ttu-id="16fd3-239">Vous pouvez également utiliser les versions plus spécifiques `Request.Form["name"]` (pour `POST` demandes) ou `Request.QueryString["name"]` (pour `GET` demandes).</span><span class="sxs-lookup"><span data-stu-id="16fd3-239">Alternatively, you can use the more specific versions `Request.Form["name"]` (for `POST` requests) or `Request.QueryString["name"]` (for `GET` requests).</span></span> <span data-ttu-id="16fd3-240">Bien sûr, *nom* est le nom de l’élément à obtenir.</span><span class="sxs-lookup"><span data-stu-id="16fd3-240">Of course, *name* is the name of the item to get.</span></span>
> 
> <span data-ttu-id="16fd3-241">Le nom de l’élément que vous souhaitez obtenir doit être unique au sein de la collection que vous utilisez.</span><span class="sxs-lookup"><span data-stu-id="16fd3-241">The name of the item you want to get has to be unique within the collection you're using.</span></span> <span data-ttu-id="16fd3-242">C’est pourquoi le `Request` objet fournit des sous-ensembles telles que `Request.Form` et `Request.QueryString`.</span><span class="sxs-lookup"><span data-stu-id="16fd3-242">That's why the `Request` object provides the subsets like `Request.Form` and `Request.QueryString`.</span></span> <span data-ttu-id="16fd3-243">Supposons que votre page contient un élément de formulaire nommé `userName` et *également* contient un cookie nommé `userName`.</span><span class="sxs-lookup"><span data-stu-id="16fd3-243">Suppose that your page contains a form element named `userName` and *also* contains a cookie named `userName`.</span></span> <span data-ttu-id="16fd3-244">Si vous obtenez `Request["userName"]`, si vous souhaitez la valeur de formulaire ou le cookie est ambigu.</span><span class="sxs-lookup"><span data-stu-id="16fd3-244">If you get `Request["userName"]`, it's ambiguous whether you want the form value or the cookie.</span></span> <span data-ttu-id="16fd3-245">Toutefois, si vous obtenez `Request.Form["userName"]` ou `Request.Cookie["userName"]`, vous êtes soient explicites sur la valeur à obtenir.</span><span class="sxs-lookup"><span data-stu-id="16fd3-245">However, if you get `Request.Form["userName"]` or `Request.Cookie["userName"]`, you're being explicit about which value to get.</span></span>
> 
> <span data-ttu-id="16fd3-246">Il est conseillé d’être précis et utiliser le sous-ensemble de `Request` qui vous intéresse, comme `Request.Form` ou `Request.QueryString`.</span><span class="sxs-lookup"><span data-stu-id="16fd3-246">It's a good practice to be specific and use the subset of `Request` that you're interested in, like `Request.Form` or `Request.QueryString`.</span></span> <span data-ttu-id="16fd3-247">Pour les pages simples que vous créez dans ce didacticiel, il probablement ne vraiment faire la différence.</span><span class="sxs-lookup"><span data-stu-id="16fd3-247">For the simple pages that you're creating in this tutorial, it probably doesn't really make any difference.</span></span> <span data-ttu-id="16fd3-248">Toutefois, lorsque vous créez des pages plus complexes, à l’aide de la version explicite `Request.Form` ou `Request.QueryString` peut vous aider à éviter les problèmes qui peuvent survenir lors de la page contient un formulaire (ou plusieurs formulaires), les cookies, les valeurs de chaîne de requête et ainsi de suite.</span><span class="sxs-lookup"><span data-stu-id="16fd3-248">However, as you create more complex pages, using the explicit version `Request.Form` or `Request.QueryString` can help you avoid problems that can arise when the page contains a form (or multiple forms), cookies, query string values, and so on.</span></span>


## <a name="creating-a-query-by-using-a-search-term"></a><span data-ttu-id="16fd3-249">Création d’une requête à l’aide d’un terme de recherche</span><span class="sxs-lookup"><span data-stu-id="16fd3-249">Creating a Query by Using a Search Term</span></span>

<span data-ttu-id="16fd3-250">Maintenant que vous savez comment obtenir le terme de recherche entré par l’utilisateur, vous pouvez créer une requête qui l’utilise.</span><span class="sxs-lookup"><span data-stu-id="16fd3-250">Now that you know how to get the search term that the user entered, you can create a query that uses it.</span></span> <span data-ttu-id="16fd3-251">N’oubliez pas que pour obtenir tous les éléments film de la base de données, vous utilisez une requête SQL qui ressemble à cette instruction :</span><span class="sxs-lookup"><span data-stu-id="16fd3-251">Remember that to get all the movie items out of the database, you're using a SQL query that looks like this statement:</span></span>

`SELECT * FROM Movies`

<span data-ttu-id="16fd3-252">Pour obtenir uniquement certains films, vous devez utiliser une requête qui inclut un `Where` clause.</span><span class="sxs-lookup"><span data-stu-id="16fd3-252">To get only certain movies, you have to use a query that includes a `Where` clause.</span></span> <span data-ttu-id="16fd3-253">Cette clause vous permet de définir une condition sur laquelle les lignes sont retournées par la requête.</span><span class="sxs-lookup"><span data-stu-id="16fd3-253">This clause lets you set a condition on which rows are returned by the query.</span></span> <span data-ttu-id="16fd3-254">Voici un exemple :</span><span class="sxs-lookup"><span data-stu-id="16fd3-254">Here's an example:</span></span>

`SELECT * FROM Movies WHERE Genre = 'Action'`

<span data-ttu-id="16fd3-255">Le format de base est `WHERE column = value`.</span><span class="sxs-lookup"><span data-stu-id="16fd3-255">The basic format is `WHERE column = value`.</span></span> <span data-ttu-id="16fd3-256">Vous pouvez utiliser différents opérateurs outre juste `=`, comme `>` (supérieur à), `<` (inférieur à), `<>` (différent de), `<=` (inférieur ou égal à), etc., selon ce que vous cherchez.</span><span class="sxs-lookup"><span data-stu-id="16fd3-256">You can use different operators besides just `=`, like `>` (greater than), `<` (less than), `<>` (not equal to), `<=` (less than or equal to), etc., depending on what you're looking for.</span></span>

<span data-ttu-id="16fd3-257">Au cas où vous vous poseriez la question, les instructions SQL ne sont pas sensible à la casse &mdash; `SELECT` est identique à `Select` (voire `select`).</span><span class="sxs-lookup"><span data-stu-id="16fd3-257">In case you're wondering, SQL statements are not case sensitive &mdash; `SELECT` is the same as `Select` (or even `select`).</span></span> <span data-ttu-id="16fd3-258">Toutefois, personnes souvent Tirez profit des mots clés dans une instruction SQL, comme `SELECT` et `WHERE`, pour le rendre plus facile à lire.</span><span class="sxs-lookup"><span data-stu-id="16fd3-258">However, people often capitalize keywords in a SQL statement, like `SELECT` and `WHERE`, to make it easier to read.</span></span>

### <a name="passing-the-search-term-as-a-parameter"></a><span data-ttu-id="16fd3-259">En passant le terme de recherche en tant que paramètre</span><span class="sxs-lookup"><span data-stu-id="16fd3-259">Passing the search term as a parameter</span></span>

<span data-ttu-id="16fd3-260">Si vous recherchez un genre spécifique est assez facile (`WHERE Genre = 'Action'`), mais que vous souhaitez être en mesure de rechercher n’importe quel genre de l’utilisateur entre.</span><span class="sxs-lookup"><span data-stu-id="16fd3-260">Searching for a specific genre is easy enough (`WHERE Genre = 'Action'`), but you want to be able to search for any genre that the user enters.</span></span> <span data-ttu-id="16fd3-261">Pour ce faire, vous créez en tant que requête SQL qui inclut un espace réservé pour la valeur à rechercher.</span><span class="sxs-lookup"><span data-stu-id="16fd3-261">To do that, you create as SQL query that includes a placeholder for the value to search.</span></span> <span data-ttu-id="16fd3-262">Il ressemblera à cette commande :</span><span class="sxs-lookup"><span data-stu-id="16fd3-262">It will look like this command:</span></span>

`SELECT * FROM Movies WHERE Genre = @0`

<span data-ttu-id="16fd3-263">L’espace réservé est le `@` caractères suivis de zéro.</span><span class="sxs-lookup"><span data-stu-id="16fd3-263">The placeholder is the `@` character followed by zero.</span></span> <span data-ttu-id="16fd3-264">Comme vous pouvez le deviner, une requête peut contenir des espaces réservés, et ils se nommerait `@0`, `@1`, `@2`, etc.</span><span class="sxs-lookup"><span data-stu-id="16fd3-264">As you might guess, a query can contain multiple placeholders, and they'd be named `@0`, `@1`, `@2`, etc.</span></span>

<span data-ttu-id="16fd3-265">Pour définir la requête et transmet en réalité la valeur, vous utilisez le code comme suit :</span><span class="sxs-lookup"><span data-stu-id="16fd3-265">To set up the query and actually pass it the value, you use the code like the following:</span></span>

[!code-sql[Main](form-basics/samples/sample4.sql)]

<span data-ttu-id="16fd3-266">Ce code est similaire à ce que vous avez déjà fait pour afficher des données dans la grille.</span><span class="sxs-lookup"><span data-stu-id="16fd3-266">This code is similar to what you've already done to display data in the grid.</span></span> <span data-ttu-id="16fd3-267">Les seules différences sont :</span><span class="sxs-lookup"><span data-stu-id="16fd3-267">The only differences are:</span></span>

- <span data-ttu-id="16fd3-268">La requête contient un espace réservé (`WHERE Genre = @0"`).</span><span class="sxs-lookup"><span data-stu-id="16fd3-268">The query contains a placeholder (`WHERE Genre = @0"`).</span></span>
- <span data-ttu-id="16fd3-269">La requête est placée dans une variable (`selectCommand`) ; avant, vous avez passé la requête directement à la `db.Query` (méthode).</span><span class="sxs-lookup"><span data-stu-id="16fd3-269">The query is put into a variable (`selectCommand`); before, you passed the query directly to the `db.Query` method.</span></span>
- <span data-ttu-id="16fd3-270">Lorsque vous appelez le `db.Query` (méthode), vous passez la requête et la valeur à utiliser pour l’espace réservé.</span><span class="sxs-lookup"><span data-stu-id="16fd3-270">When you call the `db.Query` method, you pass both the query and the value to use for the placeholder.</span></span> <span data-ttu-id="16fd3-271">(Si la requête a dû à plusieurs espaces réservés, vous pouvez transmettre les tout en tant que valeurs distincts à la méthode.)</span><span class="sxs-lookup"><span data-stu-id="16fd3-271">(If the query had multiple placeholders, you'd pass them all as separate values to the method.)</span></span>

<span data-ttu-id="16fd3-272">Si vous placez tous ces éléments ensemble, vous obtenez le code suivant :</span><span class="sxs-lookup"><span data-stu-id="16fd3-272">If you put all these elements together, you get the following code:</span></span>

[!code-csharp[Main](form-basics/samples/sample5.cs)]

> [!NOTE] 
> 
> <span data-ttu-id="16fd3-273">**Important !**</span><span class="sxs-lookup"><span data-stu-id="16fd3-273">**Important!**</span></span> <span data-ttu-id="16fd3-274">À l’aide des espaces réservés (tels que `@0`) pour transmettre des valeurs à une commande SQL est *extrêmement important* pour la sécurité.</span><span class="sxs-lookup"><span data-stu-id="16fd3-274">Using placeholders (like `@0`) to pass values to a SQL command is *extremely important* for security.</span></span> <span data-ttu-id="16fd3-275">La même façon qu’ici, avec des espaces réservés pour les données de variable, est la seule façon, vous devez construire des commandes SQL.</span><span class="sxs-lookup"><span data-stu-id="16fd3-275">The way you see it here, with placeholders for variable data, is the only way you should construct SQL commands.</span></span>
> 
> <span data-ttu-id="16fd3-276">Jamais construire une instruction SQL en rassemblant des texte littéral (concaténer) et les valeurs que vous obtenez à partir de l’utilisateur.</span><span class="sxs-lookup"><span data-stu-id="16fd3-276">Never construct a SQL statement by putting together (concatenating) literal text and values you get from the user.</span></span> <span data-ttu-id="16fd3-277">Concaténation de l’entrée utilisateur dans une instruction SQL s’ouvre votre site vers un *attaque par injection SQL* où un utilisateur malveillant soumet les valeurs à votre page de pirater votre base de données.</span><span class="sxs-lookup"><span data-stu-id="16fd3-277">Concatenating user input into a SQL statement opens your site to a *SQL injection attack* where a malicious user submits values to your page that hack your database.</span></span> <span data-ttu-id="16fd3-278">(Vous trouverez plus d’informations dans l’article [Injection SQL](https://msdn.microsoft.com/library/ms161953.aspx) le site Web MSDN.)</span><span class="sxs-lookup"><span data-stu-id="16fd3-278">(You can read more in the article [SQL Injection](https://msdn.microsoft.com/library/ms161953.aspx) the MSDN website.)</span></span>


## <a name="updating-the-movies-page-with-search-code"></a><span data-ttu-id="16fd3-279">Mise à jour de la Page de films avec rechercher du Code</span><span class="sxs-lookup"><span data-stu-id="16fd3-279">Updating the Movies Page with Search Code</span></span>

<span data-ttu-id="16fd3-280">Maintenant vous pouvez mettre à jour le code dans le *Movies.cshtml* fichier.</span><span class="sxs-lookup"><span data-stu-id="16fd3-280">Now you can update the code in the *Movies.cshtml* file.</span></span> <span data-ttu-id="16fd3-281">Pour commencer, remplacez le code dans le bloc de code en haut de la page avec ce code :</span><span class="sxs-lookup"><span data-stu-id="16fd3-281">To begin, replace the code in the code block at the top of the page with this code:</span></span>

[!code-csharp[Main](form-basics/samples/sample6.cs)]

<span data-ttu-id="16fd3-282">La différence ici est que vous avez créé la requête dans le `selectCommand` variable, ce qui vous passerez à `db.Query` plus tard.</span><span class="sxs-lookup"><span data-stu-id="16fd3-282">The difference here is that you've put the query into the `selectCommand` variable, which you'll pass to `db.Query` later.</span></span> <span data-ttu-id="16fd3-283">Placement de l’instruction SQL dans une variable vous permet de modifier l’instruction, ce qui est ce que vous allez faire pour effectuer la recherche.</span><span class="sxs-lookup"><span data-stu-id="16fd3-283">Putting the SQL statement into a variable lets you change the statement, which is what you'll do to perform the search.</span></span>

<span data-ttu-id="16fd3-284">Vous avez également supprimé ces deux lignes, vous devez remettre dans ultérieurement :</span><span class="sxs-lookup"><span data-stu-id="16fd3-284">You've also removed these two lines, which you'll put back in later:</span></span>

[!code-csharp[Main](form-basics/samples/sample7.cs)]

<span data-ttu-id="16fd3-285">Vous ne souhaitez pas encore exécuté la requête (autrement dit, appelez `db.Query`) et vous ne souhaitez pas initialiser le `WebGrid` helper encore soit.</span><span class="sxs-lookup"><span data-stu-id="16fd3-285">You don't want to run the query yet (that is, call `db.Query`) and you don't want to initialize the `WebGrid` helper yet either.</span></span> <span data-ttu-id="16fd3-286">Vous pouvez utiliser ces éléments une fois que vous avez compris comment fonctionne l’instruction SQL doit s’exécuter.</span><span class="sxs-lookup"><span data-stu-id="16fd3-286">You'll do those things after you've figured out which SQL statement has to run.</span></span>

<span data-ttu-id="16fd3-287">Après ce bloc réécrit, vous pouvez ajouter la nouvelle logique pour la gestion de la recherche.</span><span class="sxs-lookup"><span data-stu-id="16fd3-287">After this rewritten block, you can add the new logic for handling the search.</span></span> <span data-ttu-id="16fd3-288">Le code complet se présente comme suit.</span><span class="sxs-lookup"><span data-stu-id="16fd3-288">The completed code will look like the following.</span></span> <span data-ttu-id="16fd3-289">Mettre à jour le code de votre page afin qu’il corresponde à cet exemple :</span><span class="sxs-lookup"><span data-stu-id="16fd3-289">Update the code in your page so it matches this example:</span></span>

[!code-cshtml[Main](form-basics/samples/sample8.cshtml)]

<span data-ttu-id="16fd3-290">La page fonctionne désormais comme suit.</span><span class="sxs-lookup"><span data-stu-id="16fd3-290">The page now works like this.</span></span> <span data-ttu-id="16fd3-291">Chaque fois que la page s’exécute, le code s’ouvre à la base de données et la `selectCommand` variable est définie à l’instruction SQL qui obtient tous les enregistrements à partir de la `Movies` table.</span><span class="sxs-lookup"><span data-stu-id="16fd3-291">Every time the page runs, the code opens the database and the `selectCommand` variable is set to the SQL statement that gets all the records from the `Movies` table.</span></span> <span data-ttu-id="16fd3-292">Le code initialise également le `searchTerm` variable.</span><span class="sxs-lookup"><span data-stu-id="16fd3-292">The code also initializes the `searchTerm` variable.</span></span>

<span data-ttu-id="16fd3-293">Toutefois, si la requête actuelle inclut une valeur pour le `searchGenre` élément, le code définit `selectCommand` à une autre requête, à savoir, pour qu’il inclut le `Where` clause pour rechercher un genre.</span><span class="sxs-lookup"><span data-stu-id="16fd3-293">However, if the current request includes a value for the `searchGenre` element, the code sets `selectCommand` to a different query — namely, to one that includes the `Where` clause to search for a genre.</span></span> <span data-ttu-id="16fd3-294">Il définit également `searchTerm` à tout ce qui a été transmis pour la zone de recherche (qui peut être nothing).</span><span class="sxs-lookup"><span data-stu-id="16fd3-294">It also sets `searchTerm` to whatever was passed for the search box (which might be nothing).</span></span>

<span data-ttu-id="16fd3-295">Quel que soit l’élément SQL instruction se trouve dans `selectCommand`, le code appelle ensuite `db.Query` pour exécuter la requête, en lui passant l’est dans `searchTerm`.</span><span class="sxs-lookup"><span data-stu-id="16fd3-295">Regardless of which SQL statement is in `selectCommand`, the code then calls `db.Query` to run the query, passing it whatever is in `searchTerm`.</span></span> <span data-ttu-id="16fd3-296">Si aucun élément ne `searchTerm`, peu importe, car dans ce cas il n’existe aucun paramètre à passer la valeur à `selectCommand` quand même.</span><span class="sxs-lookup"><span data-stu-id="16fd3-296">If there's nothing in `searchTerm`, it doesn't matter, because in that case there's no parameter to pass the value to `selectCommand` anyway.</span></span>

<span data-ttu-id="16fd3-297">Enfin, le code initialise le `WebGrid` application d’assistance à l’aide des résultats de la requête, comme avant.</span><span class="sxs-lookup"><span data-stu-id="16fd3-297">Finally, the code initializes the `WebGrid` helper by using the query results, just like before.</span></span>

<span data-ttu-id="16fd3-298">Vous pouvez voir qu’en plaçant l’instruction SQL et le terme de recherche dans des variables, vous avez ajouté de la flexibilité au code.</span><span class="sxs-lookup"><span data-stu-id="16fd3-298">You can see that by putting the SQL statement and the search term into variables, you've added flexibility to the code.</span></span> <span data-ttu-id="16fd3-299">Comme vous le verrez plus loin dans ce didacticiel, vous pouvez utiliser cette infrastructure de base et continuez d’ajouter une logique pour différents types de recherche.</span><span class="sxs-lookup"><span data-stu-id="16fd3-299">As you'll see later in this tutorial, you can use this basic framework and keep adding logic for different types of searches.</span></span>

## <a name="testing-the-search-by-genre-feature"></a><span data-ttu-id="16fd3-300">La fonctionnalité de recherche par Genre de test</span><span class="sxs-lookup"><span data-stu-id="16fd3-300">Testing the Search-by-Genre Feature</span></span>

<span data-ttu-id="16fd3-301">Dans WebMatrix, exécutez le *Movies.cshtml* page.</span><span class="sxs-lookup"><span data-stu-id="16fd3-301">In WebMatrix, run the *Movies.cshtml* page.</span></span> <span data-ttu-id="16fd3-302">Vous voyez la page avec la zone de texte pour le genre.</span><span class="sxs-lookup"><span data-stu-id="16fd3-302">You see the page with the text box for genre.</span></span>

<span data-ttu-id="16fd3-303">Entrez un genre que vous avez entré pour l’une de vos enregistrements de test, puis cliquez sur **recherche**.</span><span class="sxs-lookup"><span data-stu-id="16fd3-303">Enter a genre that you've entered for one of your test records, then click **Search**.</span></span> <span data-ttu-id="16fd3-304">Cette fois-ci vous obtenir la liste de simplement les films qui correspondent à ce genre s’affiche :</span><span class="sxs-lookup"><span data-stu-id="16fd3-304">This time you see a listing of just the movies that match that genre:</span></span>

![Page de films répertoriant après une recherche de genre 'Comedies'](form-basics/_static/image4.png)

<span data-ttu-id="16fd3-306">Entrez un autre genre et relancez la recherche.</span><span class="sxs-lookup"><span data-stu-id="16fd3-306">Enter a different genre and search again.</span></span> <span data-ttu-id="16fd3-307">Essayez d’entrer le genre à l’aide de toutes les lettres minuscules ou majuscules afin que vous puissiez voir que la recherche ne respecte pas la casse.</span><span class="sxs-lookup"><span data-stu-id="16fd3-307">Try entering the genre by using all lowercase or all uppercase letters so that you can see that the search is not case sensitive.</span></span>

## <a name="remembering-what-the-user-entered"></a><span data-ttu-id="16fd3-308">« Mémoriser » l’entrée utilisateur</span><span class="sxs-lookup"><span data-stu-id="16fd3-308">"Remembering" What the User Entered</span></span>

<span data-ttu-id="16fd3-309">Vous avez peut-être remarqué qu’une fois que vous avez entré un genre et que vous avez cliqué sur **recherche Genre**, vous avez vu une liste pour ce genre s’affiche.</span><span class="sxs-lookup"><span data-stu-id="16fd3-309">You might have noticed that after you entered a genre and clicked **Search Genre**, you saw a listing for that genre.</span></span> <span data-ttu-id="16fd3-310">Toutefois, la zone de texte de recherche était vide &mdash; en d’autres termes, il n’a pas n’oubliez pas ce que vous aviez entré.</span><span class="sxs-lookup"><span data-stu-id="16fd3-310">However, the search text box was empty &mdash; in other words, it didn't remember what you'd entered.</span></span>

<span data-ttu-id="16fd3-311">Il est important de comprendre pourquoi ce comportement se produit.</span><span class="sxs-lookup"><span data-stu-id="16fd3-311">It's important to understand why this behavior occurs.</span></span> <span data-ttu-id="16fd3-312">Lorsque vous envoyez une page, le navigateur envoie une demande au serveur web.</span><span class="sxs-lookup"><span data-stu-id="16fd3-312">When you submit a page, the browser sends a request to the web server.</span></span> <span data-ttu-id="16fd3-313">Lorsqu’ASP.NET reçoit la demande, il crée une toute nouvelle instance de la page, exécute le code qu’il contient, puis affiche de nouveau la page au navigateur.</span><span class="sxs-lookup"><span data-stu-id="16fd3-313">When ASP.NET gets the request, it creates a brand-new instance of the page, runs the code in it, and then renders the page to the browser again.</span></span> <span data-ttu-id="16fd3-314">En effet, cependant, la page ne sait pas que vous travailliez avec une version précédente de lui-même.</span><span class="sxs-lookup"><span data-stu-id="16fd3-314">In effect, though, the page doesn't know that you were just working with a previous version of itself.</span></span> <span data-ttu-id="16fd3-315">Les qu'il sait qu’il est obtenu d’une demande ayant certaines données de formulaire qu’il contient.</span><span class="sxs-lookup"><span data-stu-id="16fd3-315">All it knows is that it got a request that had some form data in it.</span></span>

<span data-ttu-id="16fd3-316">Chaque fois que vous demandez une page &mdash; s’il faut pour la première fois ou en le soumettant &mdash; vous obtenez une nouvelle page.</span><span class="sxs-lookup"><span data-stu-id="16fd3-316">Every time you request a page &mdash; whether for the first time or by submitting it &mdash; you're getting a new page.</span></span> <span data-ttu-id="16fd3-317">Le serveur web n’a aucun mémoire de votre dernière demande.</span><span class="sxs-lookup"><span data-stu-id="16fd3-317">The web server has no memory of your last request.</span></span> <span data-ttu-id="16fd3-318">Pas plus qu’ASP.NET, et pas plus que le navigateur.</span><span class="sxs-lookup"><span data-stu-id="16fd3-318">Neither does ASP.NET, and neither does the browser.</span></span> <span data-ttu-id="16fd3-319">La seule connexion entre ces instances distinctes de la page est toutes les données que vous transmettez entre eux.</span><span class="sxs-lookup"><span data-stu-id="16fd3-319">The only connection between these separate instances of the page is any data that you transmit between them.</span></span> <span data-ttu-id="16fd3-320">Par exemple, si vous envoyez une page, la nouvelle instance de page permettre obtenir les données de formulaire qui a été envoyées par l’instance antérieure.</span><span class="sxs-lookup"><span data-stu-id="16fd3-320">If you submit a page, for example, the new page instance can get the form data that was sent by the earlier instance.</span></span> <span data-ttu-id="16fd3-321">(Un autre pour passer des données entre les pages consiste à utiliser des cookies.)</span><span class="sxs-lookup"><span data-stu-id="16fd3-321">(Another way to pass data between pages is to use cookies.)</span></span>

<span data-ttu-id="16fd3-322">Une méthode formelle pour décrire cette situation est à dire que les pages web sont *sans état*.</span><span class="sxs-lookup"><span data-stu-id="16fd3-322">A formal way to describe this situation is to say that web pages are *stateless*.</span></span> <span data-ttu-id="16fd3-323">Serveurs Web et les pages elles-mêmes et les éléments dans la page ne conservent pas toutes les informations concernant l’état précédent d’une page.</span><span class="sxs-lookup"><span data-stu-id="16fd3-323">Web servers and the pages themselves and the elements in the page do not maintain any information about the previous state of a page.</span></span> <span data-ttu-id="16fd3-324">Le web a été conçu de cette façon, car la gestion de l’état des requêtes individuelles est rapidement épuiser les ressources de serveurs web, ce qui est souvent gérer des milliers, peut-être même des centaines de milliers de demandes par seconde.</span><span class="sxs-lookup"><span data-stu-id="16fd3-324">The web was designed this way because maintaining state for individual requests would quickly exhaust the resources of web servers, which often handle thousands, maybe even hundreds of thousands, of requests per second.</span></span>

<span data-ttu-id="16fd3-325">C’est pourquoi la zone de texte était vide.</span><span class="sxs-lookup"><span data-stu-id="16fd3-325">So that's why the text box was empty.</span></span> <span data-ttu-id="16fd3-326">Une fois que vous avez envoyé la page, ASP.NET créé une nouvelle instance de la page et s’est exécutée dans le code et le balisage.</span><span class="sxs-lookup"><span data-stu-id="16fd3-326">After you submitted the page, ASP.NET created a new instance of the page and ran through the code and markup.</span></span> <span data-ttu-id="16fd3-327">Lors de rien que du code qui a dit ASP.NET pour placer une valeur dans la zone de texte.</span><span class="sxs-lookup"><span data-stu-id="16fd3-327">There was nothing in that code that told ASP.NET to put a value into the text box.</span></span> <span data-ttu-id="16fd3-328">Par conséquent, ASP.NET n’ai rien fait, et la zone de texte a été restituée sans une valeur qu’il contient.</span><span class="sxs-lookup"><span data-stu-id="16fd3-328">So ASP.NET didn't do anything, and the text box was rendered without a value in it.</span></span>

<span data-ttu-id="16fd3-329">Il est en fait un moyen simple de contourner ce problème.</span><span class="sxs-lookup"><span data-stu-id="16fd3-329">There's actually an easy way to get around this issue.</span></span> <span data-ttu-id="16fd3-330">Le genre que vous avez entré dans la zone de texte *est* à votre disposition dans le code &mdash; il se trouve dans `Request.QueryString["searchGenre"]`.</span><span class="sxs-lookup"><span data-stu-id="16fd3-330">The genre that you entered into the text box *is* available to you in code &mdash; it's in `Request.QueryString["searchGenre"]`.</span></span>

<span data-ttu-id="16fd3-331">Mettre à jour le balisage de la zone de texte afin que le `value` attribut obtient sa valeur de `searchTerm`, comme dans cet exemple :</span><span class="sxs-lookup"><span data-stu-id="16fd3-331">Update the markup for the text box so that the `value` attribute gets its value from `searchTerm`, like this example:</span></span>

[!code-html[Main](form-basics/samples/sample9.html?highlight=1)]

<span data-ttu-id="16fd3-332">Dans cette page, vous pouvez aussi définir également la `value` attribut le `searchTerm` variable, étant donné que cette variable contient également le genre que vous avez entré.</span><span class="sxs-lookup"><span data-stu-id="16fd3-332">In this page, you could have also set the `value` attribute to the `searchTerm` variable, since that variable also contains the genre you entered.</span></span> <span data-ttu-id="16fd3-333">Mais l’utilisation du `Request` objet à définir le `value` attribut comme indiqué ici est la méthode standard pour accomplir cette tâche.</span><span class="sxs-lookup"><span data-stu-id="16fd3-333">But using the `Request` object to set the `value` attribute as shown here is the standard way to accomplish this task.</span></span> <span data-ttu-id="16fd3-334">(En supposant que vous souhaitez encore cela &mdash; dans certaines situations, vous souhaiterez restituer la page *sans* valeurs dans les champs.</span><span class="sxs-lookup"><span data-stu-id="16fd3-334">(Assuming you even want to do this &mdash; in some situations, you might want to render the page *without* values in the fields.</span></span> <span data-ttu-id="16fd3-335">Tout dépend de ce qui se passait avec votre application.)</span><span class="sxs-lookup"><span data-stu-id="16fd3-335">It all depends on what's going on with your app.)</span></span>

> [!NOTE]
> <span data-ttu-id="16fd3-336">Vous ne pouvez pas de « mémoriser » la valeur d’une zone de texte qui est utilisée pour les mots de passe.</span><span class="sxs-lookup"><span data-stu-id="16fd3-336">You can't "remember" the value of a text box that's used for passwords.</span></span> <span data-ttu-id="16fd3-337">Il serait une faille de sécurité pour permettre aux utilisateurs de remplir un champ de mot de passe à l’aide de code.</span><span class="sxs-lookup"><span data-stu-id="16fd3-337">It would be a security hole to allow people to fill in a password field by using code.</span></span>


<span data-ttu-id="16fd3-338">Réexécutez la page, entrez un genre, puis cliquez sur **recherche Genre**.</span><span class="sxs-lookup"><span data-stu-id="16fd3-338">Run the page again, enter a genre, and click **Search Genre**.</span></span> <span data-ttu-id="16fd3-339">Cette fois mais non seulement voir les résultats de la recherche, mais souvient de la zone de texte que vous avez entré la dernière fois :</span><span class="sxs-lookup"><span data-stu-id="16fd3-339">This time not only do you see the results of the search, but the text box remembers what you entered last time:</span></span>

![Page montrant que la zone de texte a « mémorisé » l’entrée précédente](form-basics/_static/image5.png)

## <a name="searching-for-any-word-in-the-title"></a><span data-ttu-id="16fd3-341">Recherche un mot dans le titre</span><span class="sxs-lookup"><span data-stu-id="16fd3-341">Searching for Any Word in the Title</span></span>

<span data-ttu-id="16fd3-342">Vous pouvez maintenant rechercher n’importe quel genre, mais vous souhaiterez également rechercher un titre.</span><span class="sxs-lookup"><span data-stu-id="16fd3-342">You can now search for any genre, but you might also want to search for a title.</span></span> <span data-ttu-id="16fd3-343">Il est difficile d’obtenir un titre brancher correctement lorsque vous recherchez, par conséquent, au lieu de cela que vous pouvez rechercher un mot qui apparaît n’importe où à l’intérieur d’un titre.</span><span class="sxs-lookup"><span data-stu-id="16fd3-343">It's hard to get a title exactly right when you search, so instead you can search for a word that appears anywhere inside a title.</span></span> <span data-ttu-id="16fd3-344">Pour ce faire dans SQL, vous utilisez le `LIKE` opérateur et la syntaxe comme suit :</span><span class="sxs-lookup"><span data-stu-id="16fd3-344">To do that in SQL, you use the `LIKE` operator and syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Title LIKE '%adventure%'`

<span data-ttu-id="16fd3-345">Cette commande Obtient tous les films dont le titre contient « adventure ».</span><span class="sxs-lookup"><span data-stu-id="16fd3-345">This command gets all the movies whose titles contain "adventure".</span></span> <span data-ttu-id="16fd3-346">Lorsque vous utilisez le `LIKE` opérateur, vous incluez le caractère générique `%` en tant que partie du terme recherché.</span><span class="sxs-lookup"><span data-stu-id="16fd3-346">When you use the `LIKE` operator, you include the wildcard character `%` as part of the search term.</span></span> <span data-ttu-id="16fd3-347">La recherche `LIKE 'adventure%'` signifie « commençant par « adventure » ».</span><span class="sxs-lookup"><span data-stu-id="16fd3-347">The search `LIKE 'adventure%'` means "starting with 'adventure'".</span></span> <span data-ttu-id="16fd3-348">(Techniquement, cela signifie que « La chaîne « adventure », suivi par aucun élément ».) De même, le terme de recherche `LIKE '%adventure'` signifie « quoi que ce soit suivi de la chaîne « adventure » », qui est une autre façon de dire « terminant par « adventure » ».</span><span class="sxs-lookup"><span data-stu-id="16fd3-348">(Technically, it means "The string 'adventure' followed by anything.") Similarly, the search term `LIKE '%adventure'` means "anything followed by the string 'adventure'", which is another way to say "ending with 'adventure'".</span></span>

<span data-ttu-id="16fd3-349">Le terme de recherche `LIKE '%adventure%'` signifie par conséquent « avec « adventure » n’importe où dans le titre. »</span><span class="sxs-lookup"><span data-stu-id="16fd3-349">The search term `LIKE '%adventure%'` therefore means "with 'adventure' anywhere in the title."</span></span> <span data-ttu-id="16fd3-350">(Techniquement, « quoi que ce soit dans le titre, suivi de « adventure », suivie de quoi que ce soit. »)</span><span class="sxs-lookup"><span data-stu-id="16fd3-350">(Technically, "anything in the title, followed by 'adventure', followed by anything.")</span></span>

<span data-ttu-id="16fd3-351">À l’intérieur de la `<form>` élément, ajoutez le balisage suivant juste en dessous la fermeture `</div>` balise pour la recherche de genre (juste avant la fermeture `</form>` élément) :</span><span class="sxs-lookup"><span data-stu-id="16fd3-351">Inside the `<form>` element, add the following markup right under the closing `</div>` tag for the genre search (just before the closing `</form>` element):</span></span>

[!code-html[Main](form-basics/samples/sample10.html)]

<span data-ttu-id="16fd3-352">Le code pour gérer cette recherche est similaire au code de la recherche de genre, à ceci près que vous ayez à assembler le `LIKE` recherche.</span><span class="sxs-lookup"><span data-stu-id="16fd3-352">The code to handle this search is similar to the code for the genre search, except that you have to assemble the `LIKE` search.</span></span> <span data-ttu-id="16fd3-353">Dans le bloc de code en haut de la page, ajoutez ceci `if` bloquer juste après le `if` bloc pour la recherche du genre :</span><span class="sxs-lookup"><span data-stu-id="16fd3-353">Inside the code block at the top of the page, add this `if` block just after the `if` block for the genre search:</span></span>

[!code-csharp[Main](form-basics/samples/sample11.cs)]

<span data-ttu-id="16fd3-354">Ce code utilise la même logique que vous avez vu précédemment, à ceci près que la recherche utilise un `LIKE` opérateur et la place de code «`%`» avant et après le terme de recherche.</span><span class="sxs-lookup"><span data-stu-id="16fd3-354">This code uses the same logic you saw earlier, except that the search uses a `LIKE` operator and the code puts "`%`" before and after the search term.</span></span>

<span data-ttu-id="16fd3-355">Remarquez comment il était facile d’ajouter une autre recherche à la page.</span><span class="sxs-lookup"><span data-stu-id="16fd3-355">Notice how it was easy to add another search to the page.</span></span> <span data-ttu-id="16fd3-356">Il vous suffisait à faire était :</span><span class="sxs-lookup"><span data-stu-id="16fd3-356">All you had to do was:</span></span>

- <span data-ttu-id="16fd3-357">Créer un `if` bloc testé pour voir si la zone de recherche pertinentes avait une valeur.</span><span class="sxs-lookup"><span data-stu-id="16fd3-357">Create an `if` block that tested to see whether the relevant search box had a value.</span></span>
- <span data-ttu-id="16fd3-358">Définir le `selectCommand` variable à une nouvelle instruction SQL.</span><span class="sxs-lookup"><span data-stu-id="16fd3-358">Set the `selectCommand` variable to a new SQL statement.</span></span>
- <span data-ttu-id="16fd3-359">Définir le `searchTerm` variable à la valeur à passer à la requête.</span><span class="sxs-lookup"><span data-stu-id="16fd3-359">Set the `searchTerm` variable to the value to pass to the query.</span></span>

<span data-ttu-id="16fd3-360">Voici le bloc de code complet, qui contient la nouvelle logique d’une recherche de titre :</span><span class="sxs-lookup"><span data-stu-id="16fd3-360">Here's the complete code block, which contains the new logic for a title search:</span></span>

[!code-cshtml[Main](form-basics/samples/sample12.cshtml)]

<span data-ttu-id="16fd3-361">Voici un résumé de ce que fait ce code :</span><span class="sxs-lookup"><span data-stu-id="16fd3-361">Here's a summary of what this code does:</span></span>

- <span data-ttu-id="16fd3-362">Les variables `searchTerm` et `selectCommand` sont initialisés en haut.</span><span class="sxs-lookup"><span data-stu-id="16fd3-362">The variables `searchTerm` and `selectCommand` are initialized at the top.</span></span> <span data-ttu-id="16fd3-363">Vous allez définir ces variables au terme de recherche approprié (le cas échéant) et la commande SQL appropriée selon ce que fait l’utilisateur dans la page.</span><span class="sxs-lookup"><span data-stu-id="16fd3-363">You're going to set these variables to the appropriate search term (if any) and appropriate SQL command based on what the user does in the page.</span></span> <span data-ttu-id="16fd3-364">La recherche par défaut est le cas simple d’obtenir tous les films à partir de la base de données.</span><span class="sxs-lookup"><span data-stu-id="16fd3-364">The default search is the simple case of getting all the movies from the database.</span></span>
- <span data-ttu-id="16fd3-365">Dans les tests pour `searchGenre` et `searchTitle`, le code définit `searchTerm` à la valeur que vous souhaitez rechercher.</span><span class="sxs-lookup"><span data-stu-id="16fd3-365">In the tests for `searchGenre` and `searchTitle`, the code sets `searchTerm` to the value you want to search for.</span></span> <span data-ttu-id="16fd3-366">Ces blocs de code est également défini `selectCommand` à une commande SQL appropriée pour cette recherche.</span><span class="sxs-lookup"><span data-stu-id="16fd3-366">Those code blocks also set `selectCommand` to an appropriate SQL command for that search.</span></span>
- <span data-ttu-id="16fd3-367">Le `db.Query` méthode est appelée une seule fois, à l’aide de toute autre commande SQL dans `selectedCommand` et quelle que soit la valeur se trouve dans `searchTerm`.</span><span class="sxs-lookup"><span data-stu-id="16fd3-367">The `db.Query` method is invoked only once, using whatever SQL command is in `selectedCommand` and whatever value is in `searchTerm`.</span></span> <span data-ttu-id="16fd3-368">S’il n’existe aucun terme de recherche (aucun genre et aucun mot de titre), la valeur de `searchTerm` est une chaîne vide.</span><span class="sxs-lookup"><span data-stu-id="16fd3-368">If there is no search term (no genre and no title word), the value of `searchTerm` is an empty string.</span></span> <span data-ttu-id="16fd3-369">Toutefois, qui importe peu, car dans ce cas la requête ne nécessite pas un paramètre.</span><span class="sxs-lookup"><span data-stu-id="16fd3-369">However, that doesn't matter, because in that case the query doesn't require a parameter.</span></span>

## <a name="testing-the-title-search-feature"></a><span data-ttu-id="16fd3-370">Test de la fonctionnalité de recherche de titre</span><span class="sxs-lookup"><span data-stu-id="16fd3-370">Testing the Title Search Feature</span></span>

<span data-ttu-id="16fd3-371">Vous pouvez maintenant tester votre page de recherche terminée.</span><span class="sxs-lookup"><span data-stu-id="16fd3-371">Now you can test your completed search page.</span></span> <span data-ttu-id="16fd3-372">Exécutez *Movies.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="16fd3-372">Run *Movies.cshtml*.</span></span>

<span data-ttu-id="16fd3-373">Entrez un genre et cliquez sur **recherche Genre**.</span><span class="sxs-lookup"><span data-stu-id="16fd3-373">Enter a genre and click **Search Genre**.</span></span> <span data-ttu-id="16fd3-374">La grille affiche les films de ce genre s’affiche, comme avant.</span><span class="sxs-lookup"><span data-stu-id="16fd3-374">The grid displays movies of that genre, like before.</span></span>

<span data-ttu-id="16fd3-375">Entrez un mot de titre et cliquez sur **titre de la recherche**.</span><span class="sxs-lookup"><span data-stu-id="16fd3-375">Enter a title word and click **Search Title**.</span></span> <span data-ttu-id="16fd3-376">La grille affiche les films qui ont ce mot dans le titre.</span><span class="sxs-lookup"><span data-stu-id="16fd3-376">The grid displays movies that have that word in the title.</span></span>

![Page de films répertoriant après une recherche pour « Le » dans le titre](form-basics/_static/image6.png)

<span data-ttu-id="16fd3-378">Ne renseignez pas les deux zones de texte et cliquez sur l’un des boutons.</span><span class="sxs-lookup"><span data-stu-id="16fd3-378">Leave both text boxes blank and click either button.</span></span> <span data-ttu-id="16fd3-379">La grille affiche tous les films.</span><span class="sxs-lookup"><span data-stu-id="16fd3-379">The grid displays all the movies.</span></span>

## <a name="combining-the-queries"></a><span data-ttu-id="16fd3-380">Combinant les requêtes</span><span class="sxs-lookup"><span data-stu-id="16fd3-380">Combining the Queries</span></span>

<span data-ttu-id="16fd3-381">Vous pouvez remarquer que les recherches, que vous pouvez effectuer sont exclusifs.</span><span class="sxs-lookup"><span data-stu-id="16fd3-381">You might notice that the searches you can perform are exclusive.</span></span> <span data-ttu-id="16fd3-382">Vous ne pouvez pas rechercher le titre et le genre en même temps, même si les deux zones de recherche contiennent des valeurs.</span><span class="sxs-lookup"><span data-stu-id="16fd3-382">You can't search the title and the genre at the same time, even if both search boxes have values in them.</span></span> <span data-ttu-id="16fd3-383">Par exemple, vous ne pouvez pas rechercher pour tous les films d’action dont le titre contient « Adventure ».</span><span class="sxs-lookup"><span data-stu-id="16fd3-383">For example, you can't search for all action movies whose title contains "Adventure".</span></span> <span data-ttu-id="16fd3-384">(La page est codé à présent, si vous entrez des valeurs pour le genre et le titre, la recherche de titre obtient priorité). Pour créer une recherche qui combine les conditions, vous devrez créer une requête SQL qui a une syntaxe similaire à celle-ci :</span><span class="sxs-lookup"><span data-stu-id="16fd3-384">(As the page is coded now, if you enter values for both genre and title, the title search gets precedence.) To create a search that combines the conditions, you would have to create a SQL query that has syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Genre = @0 AND Title LIKE @1`

<span data-ttu-id="16fd3-385">Et vous seriez obligé d’exécuter la requête en utilisant une instruction comme suit (les grandes lignes) :</span><span class="sxs-lookup"><span data-stu-id="16fd3-385">And you'd have to run the query by using a statement like the following (roughly speaking):</span></span>

`var selectedData = db.Query(selectCommand, searchGenre, searchTitle);`

<span data-ttu-id="16fd3-386">Création d’une logique permettant de nombreuses permutations de critères de recherche peut obtenir un peu impliqué, comme vous pouvez le voir.</span><span class="sxs-lookup"><span data-stu-id="16fd3-386">Creating logic to allow many permutations of search criteria can get a bit involved, as you can see.</span></span> <span data-ttu-id="16fd3-387">Par conséquent, nous arrêtons ici.</span><span class="sxs-lookup"><span data-stu-id="16fd3-387">Therefore, we'll stop here.</span></span>

## <a name="coming-up-next"></a><span data-ttu-id="16fd3-388">Prochaine</span><span class="sxs-lookup"><span data-stu-id="16fd3-388">Coming Up Next</span></span>

<span data-ttu-id="16fd3-389">Dans le didacticiel suivant, vous allez créer une page qui utilise un formulaire pour permettre aux utilisateurs d’ajouter des films à la base de données.</span><span class="sxs-lookup"><span data-stu-id="16fd3-389">In the next tutorial, you'll create a page that uses a form to let users add movies to the database.</span></span>

## <a name="complete-listing-for-movie-page-updated-with-search"></a><span data-ttu-id="16fd3-390">Intégralité de la Page de film (mis à jour avec recherche)</span><span class="sxs-lookup"><span data-stu-id="16fd3-390">Complete Listing for Movie Page (Updated with Search)</span></span>

[!code-cshtml[Main](form-basics/samples/sample13.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="16fd3-391">Ressources supplémentaires</span><span class="sxs-lookup"><span data-stu-id="16fd3-391">Additional Resources</span></span>

- [<span data-ttu-id="16fd3-392">Introduction à la programmation Web ASP.NET à l’aide de la syntaxe Razor</span><span class="sxs-lookup"><span data-stu-id="16fd3-392">Introduction to ASP.NET Web Programming Using the Razor Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkID=202890)
- <span data-ttu-id="16fd3-393">[Une Clause SQL WHERE](http://www.w3schools.com/sql/sql_where.asp) sur le site W3Schools</span><span class="sxs-lookup"><span data-stu-id="16fd3-393">[SQL WHERE Clause](http://www.w3schools.com/sql/sql_where.asp) on the W3Schools site</span></span>
- <span data-ttu-id="16fd3-394">[Définitions de méthode](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article sur le site du W3C</span><span class="sxs-lookup"><span data-stu-id="16fd3-394">[Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="16fd3-395">[Précédent](displaying-data.md)
> [Suivant](entering-data.md)</span><span class="sxs-lookup"><span data-stu-id="16fd3-395">[Previous](displaying-data.md)
[Next](entering-data.md)</span></span>
