---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
title: Présentation de la pages Web ASP.NET-notions de base des formulaires HTML | Microsoft Docs
author: Rick-Anderson
description: Ce didacticiel vous montre les principes fondamentaux de la création d’un formulaire d’entrée et de la gestion de l’entrée de l’utilisateur lorsque vous utilisez pages Web ASP.NET (Razor). Et maintenant...
ms.author: riande
ms.date: 05/28/2015
ms.assetid: 81ed82bf-b940-44f1-b94a-555d0cb7cc98
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
msc.type: authoredcontent
ms.openlocfilehash: f57661077ec3bb13f3d4ec41b130bda4d2fb9070
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 03/06/2020
ms.locfileid: "78574283"
---
# <a name="introducing-aspnet-web-pages---html-form-basics"></a><span data-ttu-id="2289e-104">Présentation des notions de base des formulaires pages Web ASP.NET-HTML</span><span class="sxs-lookup"><span data-stu-id="2289e-104">Introducing ASP.NET Web Pages - HTML Form Basics</span></span>

<span data-ttu-id="2289e-105">par [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="2289e-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="2289e-106">Ce didacticiel vous montre les principes fondamentaux de la création d’un formulaire d’entrée et de la gestion de l’entrée de l’utilisateur lorsque vous utilisez pages Web ASP.NET (Razor).</span><span class="sxs-lookup"><span data-stu-id="2289e-106">This tutorial shows you the basics of how to create an input form and how to handle the user's input when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="2289e-107">Et maintenant que vous disposez d’une base de données, vous utiliserez vos compétences de formulaire pour permettre aux utilisateurs de rechercher des films spécifiques dans la base de données.</span><span class="sxs-lookup"><span data-stu-id="2289e-107">And now that you've got a database, you'll use your form skills to let users find specific movies in the database.</span></span> <span data-ttu-id="2289e-108">Il part du principe que vous avez terminé la série jusqu’à la [Présentation de l’affichage des données à l’aide de pages Web ASP.net](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data).</span><span class="sxs-lookup"><span data-stu-id="2289e-108">It assumes you have completed the series through [Introduction to Displaying Data Using ASP.NET Web Pages](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data).</span></span>
> 
> <span data-ttu-id="2289e-109">Ce que vous allez apprendre :</span><span class="sxs-lookup"><span data-stu-id="2289e-109">What you'll learn:</span></span>
> 
> - <span data-ttu-id="2289e-110">Comment créer un formulaire à l’aide d’éléments HTML standard.</span><span class="sxs-lookup"><span data-stu-id="2289e-110">How to create a form by using standard HTML elements.</span></span>
> - <span data-ttu-id="2289e-111">Comment lire l’entrée de l’utilisateur dans un formulaire.</span><span class="sxs-lookup"><span data-stu-id="2289e-111">How to read the user's input in a form.</span></span>
> - <span data-ttu-id="2289e-112">Comment créer une requête SQL qui obtient des données de manière sélective à l’aide d’un terme de recherche fourni par l’utilisateur.</span><span class="sxs-lookup"><span data-stu-id="2289e-112">How to create a SQL query that selectively gets data by using a search term that the user supplies.</span></span>
> - <span data-ttu-id="2289e-113">Comment faire en sorte que les champs de la page « se souviennent » de ce que l’utilisateur a entré.</span><span class="sxs-lookup"><span data-stu-id="2289e-113">How to have fields in the page "remember" what the user entered.</span></span>
>   
> 
> <span data-ttu-id="2289e-114">Fonctionnalités/technologies présentées :</span><span class="sxs-lookup"><span data-stu-id="2289e-114">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="2289e-115">Objet `Request`.</span><span class="sxs-lookup"><span data-stu-id="2289e-115">The `Request` object.</span></span>
> - <span data-ttu-id="2289e-116">Clause SQL `Where`.</span><span class="sxs-lookup"><span data-stu-id="2289e-116">The SQL `Where` clause.</span></span>

## <a name="what-youll-build"></a><span data-ttu-id="2289e-117">Contenu</span><span class="sxs-lookup"><span data-stu-id="2289e-117">What You'll Build</span></span>

<span data-ttu-id="2289e-118">Dans le didacticiel précédent, vous avez créé une base de données, vous y avez ajouté des données, puis utilisé le programme d’assistance `WebGrid` pour afficher les données.</span><span class="sxs-lookup"><span data-stu-id="2289e-118">In the previous tutorial, you created a database, added data to it, and then used the `WebGrid` helper to display the data.</span></span> <span data-ttu-id="2289e-119">Dans ce didacticiel, vous allez ajouter une zone de recherche qui vous permet de trouver des films d’un genre spécifique ou dont le titre contient le mot que vous entrez.</span><span class="sxs-lookup"><span data-stu-id="2289e-119">In this tutorial, you'll add a search box that lets you find movies of a specific genre or whose title contains whatever word you enter.</span></span> <span data-ttu-id="2289e-120">(Par exemple, vous pourrez trouver tous les films dont le genre est « action » ou dont le titre contient « Harry » ou « Adventure »)</span><span class="sxs-lookup"><span data-stu-id="2289e-120">(For example, you'll be able to find all movies whose genre is "Action" or whose title contains "Harry" or "Adventure.")</span></span>

<span data-ttu-id="2289e-121">Lorsque vous avez terminé ce didacticiel, vous disposez d’une page similaire à celle-ci :</span><span class="sxs-lookup"><span data-stu-id="2289e-121">When you're done with this tutorial, you'll have a page like this one:</span></span>

![Page films avec recherche de genre et de titre](form-basics/_static/image1.png)

<span data-ttu-id="2289e-123">La partie listing de la page est la même que dans le dernier didacticiel &mdash; une grille.</span><span class="sxs-lookup"><span data-stu-id="2289e-123">The listing part of the page is the same as in the last tutorial &mdash; a grid.</span></span> <span data-ttu-id="2289e-124">La différence est que la grille n’affiche que les films que vous avez recherchés.</span><span class="sxs-lookup"><span data-stu-id="2289e-124">The difference will be that the grid will show only the movies that you searched for.</span></span>

## <a name="about-html-forms"></a><span data-ttu-id="2289e-125">À propos des formulaires HTML</span><span class="sxs-lookup"><span data-stu-id="2289e-125">About HTML Forms</span></span>

<span data-ttu-id="2289e-126">(Si vous avez de l’expérience dans la création de formulaires HTML et avec la différence entre `GET` et `POST`, vous pouvez ignorer cette section.)</span><span class="sxs-lookup"><span data-stu-id="2289e-126">(If you've got experience with creating HTML forms and with the difference between `GET` and `POST`, you can skip this section.)</span></span>

<span data-ttu-id="2289e-127">Un formulaire contient des éléments d’entrée utilisateur &mdash; des zones de texte, des boutons, des cases d’option, des cases à cocher, des listes déroulantes, etc.</span><span class="sxs-lookup"><span data-stu-id="2289e-127">A form has user input elements &mdash; text boxes, buttons, radio buttons, check boxes, drop-down lists, and so on.</span></span> <span data-ttu-id="2289e-128">Les utilisateurs remplissent ces contrôles ou effectuent des sélections, puis envoient le formulaire en cliquant sur un bouton.</span><span class="sxs-lookup"><span data-stu-id="2289e-128">Users fill in these controls or make selections and then submit the form by clicking a button.</span></span>

<span data-ttu-id="2289e-129">La syntaxe HTML de base d’un formulaire est illustrée dans cet exemple :</span><span class="sxs-lookup"><span data-stu-id="2289e-129">The basic HTML syntax of a form is illustrated by this example:</span></span>

[!code-html[Main](form-basics/samples/sample1.html)]

<span data-ttu-id="2289e-130">Quand ce balisage s’exécute dans une page, il crée un formulaire simple qui ressemble à l’illustration suivante :</span><span class="sxs-lookup"><span data-stu-id="2289e-130">When this markup runs in a page, it creates a simple form that looks like this illustration:</span></span>

![Formulaire HTML de base tel qu’il est affiché dans le navigateur](form-basics/_static/image2.png)

<span data-ttu-id="2289e-132">L’élément `<form>` encadre les éléments HTML à envoyer.</span><span class="sxs-lookup"><span data-stu-id="2289e-132">The `<form>` element encloses HTML elements to be submitted.</span></span> <span data-ttu-id="2289e-133">(Une erreur facile à effectuer consiste à ajouter des éléments à la page, mais à oublier de les placer à l’intérieur d’un élément `<form>`.</span><span class="sxs-lookup"><span data-stu-id="2289e-133">(An easy mistake to make is to add elements to the page but then forget to put them inside a `<form>` element.</span></span> <span data-ttu-id="2289e-134">Dans ce cas, rien n’est envoyé.) L’attribut `method` indique au navigateur comment envoyer l’entrée utilisateur.</span><span class="sxs-lookup"><span data-stu-id="2289e-134">In that case, nothing is submitted.) The `method` attribute tells the browser how to submit the user input.</span></span> <span data-ttu-id="2289e-135">Vous définissez cette valeur sur `post` si vous effectuez une mise à jour sur le serveur ou sur `get` si vous récupérez simplement des données du serveur.</span><span class="sxs-lookup"><span data-stu-id="2289e-135">You set this to `post` if you're performing an update on the server or to `get` if you're just fetching data from the server.</span></span>

<a id="GET,_POST,_and_HTTP_Verb_Safety"></a>

> [!TIP] 
> 
> <span data-ttu-id="2289e-136">**RÉCUPÉRATION, publication et sécurité des verbes HTTP**</span><span class="sxs-lookup"><span data-stu-id="2289e-136">**GET, POST, and HTTP Verb Safety**</span></span>
> 
> <span data-ttu-id="2289e-137">HTTP, le protocole que les navigateurs et les serveurs utilisent pour échanger des informations, est remarquablement simple dans ses opérations de base.</span><span class="sxs-lookup"><span data-stu-id="2289e-137">HTTP, the protocol that browsers and servers use to exchange information, is remarkably simple in its basic operations.</span></span> <span data-ttu-id="2289e-138">Les navigateurs n’utilisent que quelques verbes pour effectuer des demandes aux serveurs.</span><span class="sxs-lookup"><span data-stu-id="2289e-138">Browsers use only a few verbs to make requests to servers.</span></span> <span data-ttu-id="2289e-139">Lorsque vous écrivez du code pour le Web, il est utile de comprendre ces verbes et la façon dont le navigateur et le serveur les utilisent.</span><span class="sxs-lookup"><span data-stu-id="2289e-139">When you write code for the web, it's helpful to understand these verbs and how the browser and server use them.</span></span> <span data-ttu-id="2289e-140">Les verbes les plus couramment utilisés sont les suivants :</span><span class="sxs-lookup"><span data-stu-id="2289e-140">Far and away the most commonly used verbs are these:</span></span>
> 
> - <span data-ttu-id="2289e-141">`GET`.</span><span class="sxs-lookup"><span data-stu-id="2289e-141">`GET`.</span></span> <span data-ttu-id="2289e-142">Le navigateur utilise ce verbe pour extraire un résultat du serveur.</span><span class="sxs-lookup"><span data-stu-id="2289e-142">The browser uses this verb to fetch something from the server.</span></span> <span data-ttu-id="2289e-143">Par exemple, lorsque vous tapez une URL dans votre navigateur, le navigateur effectue une opération de `GET` pour demander la page souhaitée.</span><span class="sxs-lookup"><span data-stu-id="2289e-143">For example, when you type a URL into your browser, the browser performs a `GET` operation to request the page you want.</span></span> <span data-ttu-id="2289e-144">Si la page comprend des graphiques, le navigateur effectue des opérations de `GET` supplémentaires pour obtenir les images.</span><span class="sxs-lookup"><span data-stu-id="2289e-144">If the page includes graphics, the browser performs additional `GET` operations to get the images.</span></span> <span data-ttu-id="2289e-145">Si l’opération de `GET` doit passer des informations au serveur, les informations sont transmises dans le cadre de l’URL dans la chaîne de requête.</span><span class="sxs-lookup"><span data-stu-id="2289e-145">If the `GET` operation has to pass information to the server, the information is passed as part of the URL in the query string.</span></span>
> - <span data-ttu-id="2289e-146">`POST`.</span><span class="sxs-lookup"><span data-stu-id="2289e-146">`POST`.</span></span> <span data-ttu-id="2289e-147">Le navigateur envoie une demande de `POST` pour envoyer des données à ajouter ou à modifier sur le serveur.</span><span class="sxs-lookup"><span data-stu-id="2289e-147">The browser sends a `POST` request in order to submit data to be added or changed on the server.</span></span> <span data-ttu-id="2289e-148">Par exemple, le verbe `POST` est utilisé pour créer des enregistrements dans une base de données ou pour modifier des enregistrements existants.</span><span class="sxs-lookup"><span data-stu-id="2289e-148">For example, the `POST` verb is used to create records in a database or change existing ones.</span></span> <span data-ttu-id="2289e-149">La plupart du temps, lorsque vous remplissez un formulaire et cliquez sur le bouton envoyer, le navigateur effectue une opération de `POST`.</span><span class="sxs-lookup"><span data-stu-id="2289e-149">Most of the time, when you fill in a form and click the submit button, the browser performs a `POST` operation.</span></span> <span data-ttu-id="2289e-150">Dans une opération de `POST`, les données passées au serveur se trouvent dans le corps de la page.</span><span class="sxs-lookup"><span data-stu-id="2289e-150">In a `POST` operation, the data being passed to the server is in the body of the page.</span></span>
> 
> <span data-ttu-id="2289e-151">Une distinction importante entre ces verbes est qu’une opération de `GET` n’est pas censée modifier quoi que ce soit sur le serveur, ou pour la mettre de manière légèrement plus abstraite, une opération de `GET` n’entraîne pas de modification de l’État sur le serveur.</span><span class="sxs-lookup"><span data-stu-id="2289e-151">An important distinction between these verbs is that a `GET` operation is not supposed to change anything on the server — or to put it in a slightly more abstract way, a `GET` operation does not result in a change in state on the server.</span></span> <span data-ttu-id="2289e-152">Vous pouvez effectuer une opération de `GET` sur les mêmes ressources autant de fois que vous le souhaitez, et ces ressources ne changent pas.</span><span class="sxs-lookup"><span data-stu-id="2289e-152">You can perform a `GET` operation on the same resources as many times as you like, and those resources don't change.</span></span> <span data-ttu-id="2289e-153">(Une opération de `GET` est souvent considérée comme « sûre », ou pour utiliser un terme technique, est *idempotent*). À l’inverse, bien entendu, une demande de `POST` modifie une valeur sur le serveur chaque fois que vous effectuez l’opération.</span><span class="sxs-lookup"><span data-stu-id="2289e-153">(A `GET` operation is often said to be "safe," or to use a technical term, is *idempotent*.) In contrast, of course, a `POST` request changes something on the server each time you perform the operation.</span></span>
> 
> <span data-ttu-id="2289e-154">Deux exemples vous aideront à illustrer cette distinction.</span><span class="sxs-lookup"><span data-stu-id="2289e-154">Two examples will help illustrate this distinction.</span></span> <span data-ttu-id="2289e-155">Lorsque vous effectuez une recherche à l’aide d’un moteur tel que Bing ou Google, vous remplissez un formulaire qui se compose d’une zone de texte, puis vous cliquez sur le bouton de recherche.</span><span class="sxs-lookup"><span data-stu-id="2289e-155">When you perform a search using an engine like Bing or Google, you fill in a form that consists of one text box, and then you click the search button.</span></span> <span data-ttu-id="2289e-156">Le navigateur effectue une opération de `GET`, avec la valeur que vous avez entrée dans la zone transmise dans le cadre de l’URL.</span><span class="sxs-lookup"><span data-stu-id="2289e-156">The browser performs a `GET` operation, with the value you entered into the box passed as part of the URL.</span></span> <span data-ttu-id="2289e-157">L’utilisation d’une opération de `GET` pour ce type de formulaire est correcte, car une opération de recherche ne modifie aucune ressource sur le serveur, elle récupère simplement les informations.</span><span class="sxs-lookup"><span data-stu-id="2289e-157">Using a `GET` operation for this type of form is fine, because a search operation doesn't change any resources on the server, it just fetches information.</span></span>
> 
> <span data-ttu-id="2289e-158">Considérons à présent le processus de classement d’un contenu en ligne.</span><span class="sxs-lookup"><span data-stu-id="2289e-158">Now consider the process of ordering something online.</span></span> <span data-ttu-id="2289e-159">Renseignez les détails de la commande, puis cliquez sur le bouton Envoyer.</span><span class="sxs-lookup"><span data-stu-id="2289e-159">You fill in the order details and then click the submit button.</span></span> <span data-ttu-id="2289e-160">Cette opération est une demande de `POST`, car l’opération entraîne des modifications sur le serveur, telles qu’un nouvel enregistrement de commande, une modification de vos informations de compte et peut-être de nombreuses autres modifications.</span><span class="sxs-lookup"><span data-stu-id="2289e-160">This operation will be a `POST` request, because the operation will result in changes on the server, such as a new order record, a change in your account information, and perhaps many other changes.</span></span> <span data-ttu-id="2289e-161">Contrairement à l’opération `GET`, vous ne pouvez pas répéter votre demande de `POST`. Si vous l’avez fait, chaque fois que vous avez envoyé la demande, vous générez une nouvelle commande sur le serveur.</span><span class="sxs-lookup"><span data-stu-id="2289e-161">Unlike the `GET` operation, you cannot repeat your `POST` request — if you did, each time you resubmitted the request, you'd generate a new order on the server.</span></span> <span data-ttu-id="2289e-162">(Dans les cas de ce type, les sites Web vous avertissent souvent de ne pas cliquer plusieurs fois sur un bouton envoyer, ou ils désactivent le bouton Envoyer pour ne pas renvoyer le formulaire par inadvertance.)</span><span class="sxs-lookup"><span data-stu-id="2289e-162">(In cases like this, websites will often warn you not to click a submit button more than once, or will disable the submit button so that you don't resubmit the form accidentally.)</span></span>
> 
> <span data-ttu-id="2289e-163">Au cours de ce didacticiel, vous allez utiliser une opération de `GET` et une opération `POST` pour travailler avec des formulaires HTML.</span><span class="sxs-lookup"><span data-stu-id="2289e-163">In the course of this tutorial, you'll use both a `GET` operation and a `POST` operation to work with HTML forms.</span></span> <span data-ttu-id="2289e-164">Nous expliquerons dans chaque cas pourquoi le verbe que vous utilisez est celui qui convient.</span><span class="sxs-lookup"><span data-stu-id="2289e-164">We'll explain in each case why the verb you use is the appropriate one.</span></span>
> 
> <span data-ttu-id="2289e-165">(Pour en savoir plus sur les verbes HTTP, consultez l’article [définitions de méthode](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) sur le site W3C.)</span><span class="sxs-lookup"><span data-stu-id="2289e-165">(To learn more about HTTP verbs, see the [Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site.)</span></span>

<span data-ttu-id="2289e-166">La plupart des éléments d’entrée utilisateur sont des éléments de `<input>` HTML.</span><span class="sxs-lookup"><span data-stu-id="2289e-166">Most user input elements are HTML `<input>` elements.</span></span> <span data-ttu-id="2289e-167">Elles ressemblent à `<input type="type" name="name">,` où *type* indique le type de contrôle d’entrée utilisateur souhaité.</span><span class="sxs-lookup"><span data-stu-id="2289e-167">They look like `<input type="type" name="name">,` where *type* indicates the kind of user input control you want.</span></span> <span data-ttu-id="2289e-168">Ces éléments sont les plus courants :</span><span class="sxs-lookup"><span data-stu-id="2289e-168">These elements are the common ones:</span></span>

- <span data-ttu-id="2289e-169">Zone de texte : `<input type="text">`</span><span class="sxs-lookup"><span data-stu-id="2289e-169">Text box: `<input type="text">`</span></span>
- <span data-ttu-id="2289e-170">Case à cocher : `<input type="check">`</span><span class="sxs-lookup"><span data-stu-id="2289e-170">Check box: `<input type="check">`</span></span>
- <span data-ttu-id="2289e-171">Case d’option : `<input type="radio">`</span><span class="sxs-lookup"><span data-stu-id="2289e-171">Radio button: `<input type="radio">`</span></span>
- <span data-ttu-id="2289e-172">Bouton : `<input type="button">`</span><span class="sxs-lookup"><span data-stu-id="2289e-172">Button: `<input type="button">`</span></span>
- <span data-ttu-id="2289e-173">Bouton Envoyer : `<input type="submit">`</span><span class="sxs-lookup"><span data-stu-id="2289e-173">Submit button: `<input type="submit">`</span></span>

<span data-ttu-id="2289e-174">Vous pouvez également utiliser l’élément `<textarea>` pour créer une zone de texte multiligne et l’élément `<select>` pour créer une liste déroulante ou une liste déroulante.</span><span class="sxs-lookup"><span data-stu-id="2289e-174">You can also use the `<textarea>` element to create a multiline text box and the `<select>` element to create a drop-down list or scrollable list.</span></span> <span data-ttu-id="2289e-175">(Pour plus d’informations sur les éléments de formulaire HTML, consultez [formulaires HTML et entrée](http://www.w3schools.com/html/html_forms.asp) sur le site W3Schools.)</span><span class="sxs-lookup"><span data-stu-id="2289e-175">(For more about HTML form elements, see [HTML Forms and Input](http://www.w3schools.com/html/html_forms.asp) on the W3Schools site.)</span></span>

<span data-ttu-id="2289e-176">L’attribut `name` est très important, car le nom est la manière dont vous obtiendrez la valeur de l’élément ultérieurement, comme vous le verrez bientôt.</span><span class="sxs-lookup"><span data-stu-id="2289e-176">The `name` attribute is very important, because the name is how you'll get the value of the element later, as you'll see shortly.</span></span>

<span data-ttu-id="2289e-177">La partie intéressante est ce que vous, le développeur de pages, faites avec l’entrée de l’utilisateur.</span><span class="sxs-lookup"><span data-stu-id="2289e-177">The interesting part is what you, the page developer, do with the user's input.</span></span> <span data-ttu-id="2289e-178">Aucun comportement intégré n’est associé à ces éléments.</span><span class="sxs-lookup"><span data-stu-id="2289e-178">There's no built-in behavior associated with these elements.</span></span> <span data-ttu-id="2289e-179">Au lieu de cela, vous devez obtenir les valeurs que l’utilisateur a entrées ou sélectionnées et faire un compte.</span><span class="sxs-lookup"><span data-stu-id="2289e-179">Instead, you have to get the values that the user has entered or selected and do something with them.</span></span> <span data-ttu-id="2289e-180">C’est ce que vous allez apprendre dans ce didacticiel.</span><span class="sxs-lookup"><span data-stu-id="2289e-180">That's what you'll learn in this tutorial.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="2289e-181">**HTML5 et formulaires d’entrée**</span><span class="sxs-lookup"><span data-stu-id="2289e-181">**HTML5 and Input Forms**</span></span>
> 
> <span data-ttu-id="2289e-182">Comme vous le savez peut-être, le code HTML est en transition et la version la plus récente (HTML5) prend en charge des méthodes plus intuitives permettant aux utilisateurs d’entrer des informations.</span><span class="sxs-lookup"><span data-stu-id="2289e-182">As you might know, HTML is in transition and the latest version (HTML5) includes support for more intuitive ways for users to enter information.</span></span> <span data-ttu-id="2289e-183">Par exemple, dans HTML5, vous (le développeur de pages) pouvez indiquer à la page que vous souhaitez que l’utilisateur entre une date.</span><span class="sxs-lookup"><span data-stu-id="2289e-183">For example, in HTML5, you (the page developer) can tell the page that you want the user to enter a date.</span></span> <span data-ttu-id="2289e-184">Le navigateur peut ensuite afficher automatiquement un calendrier au lieu de demander à l’utilisateur d’entrer une date manuellement.</span><span class="sxs-lookup"><span data-stu-id="2289e-184">The browser can then automatically display a calendar rather than requiring the user to enter a date manually.</span></span> <span data-ttu-id="2289e-185">Toutefois, HTML5 est nouveau et n’est pas encore pris en charge dans tous les navigateurs.</span><span class="sxs-lookup"><span data-stu-id="2289e-185">However, HTML5 is new and is not supported in all browsers yet.</span></span>
> 
> <span data-ttu-id="2289e-186">Pages Web ASP.NET prend en charge l’entrée HTML5 dans l’étendue du navigateur de l’utilisateur.</span><span class="sxs-lookup"><span data-stu-id="2289e-186">ASP.NET Web Pages supports HTML5 input to the extent that the user's browser does.</span></span> <span data-ttu-id="2289e-187">Pour obtenir une idée des nouveaux attributs de l’élément `<input>` dans HTML5, consultez [attribut de type HTML &lt;entrée&gt;](http://www.w3schools.com/html/html_form_input_types.asp) sur le site W3Schools.</span><span class="sxs-lookup"><span data-stu-id="2289e-187">For an idea of the new attributes for the `<input>` element in HTML5, see [HTML &lt;input&gt; type Attribute](http://www.w3schools.com/html/html_form_input_types.asp) on the W3Schools site.</span></span>

## <a name="creating-the-form"></a><span data-ttu-id="2289e-188">Création du formulaire</span><span class="sxs-lookup"><span data-stu-id="2289e-188">Creating the Form</span></span>

<span data-ttu-id="2289e-189">Dans WebMatrix, dans l’espace de travail **fichiers** , ouvrez la page *movies. cshtml* .</span><span class="sxs-lookup"><span data-stu-id="2289e-189">In WebMatrix, in the **Files** workspace, open the *Movies.cshtml* page.</span></span>

<span data-ttu-id="2289e-190">Après la balise `</h1>` fermante et avant la balise `<div>` ouvrante de l’appel `grid.GetHtml`, ajoutez le balisage suivant :</span><span class="sxs-lookup"><span data-stu-id="2289e-190">After the closing `</h1>` tag and before the opening `<div>` tag of the `grid.GetHtml` call, add the following markup:</span></span>

[!code-html[Main](form-basics/samples/sample2.html)]

<span data-ttu-id="2289e-191">Ce balisage crée un formulaire qui a une zone de texte nommée `searchGenre` et un bouton Envoyer.</span><span class="sxs-lookup"><span data-stu-id="2289e-191">This markup creates a form that has a text box named `searchGenre` and a submit button.</span></span> <span data-ttu-id="2289e-192">La zone de texte et le bouton Envoyer sont placés dans un élément `<form>` dont l’attribut `method` a la valeur `get`.</span><span class="sxs-lookup"><span data-stu-id="2289e-192">The text box and submit button are enclosed in a `<form>` element whose `method` attribute is set to `get`.</span></span> <span data-ttu-id="2289e-193">(N’oubliez pas que si vous ne placez pas la zone de texte et le bouton Envoyer à l’intérieur d’un élément `<form>`, rien n’est envoyé lorsque vous cliquez sur le bouton.) Vous utilisez le verbe `GET` ici, car vous créez un formulaire qui n’apporte aucune modification sur le serveur : il se contente d’effectuer une recherche.</span><span class="sxs-lookup"><span data-stu-id="2289e-193">(Remember that if you don't put the text box and submit button inside a `<form>` element, nothing will be submitted when you click the button.) You use the `GET` verb here because you're creating a form that does not make any changes on the server — it just results in a search.</span></span> <span data-ttu-id="2289e-194">(Dans le didacticiel précédent, vous avez utilisé une méthode `post`, qui vous permet d’envoyer des modifications au serveur.</span><span class="sxs-lookup"><span data-stu-id="2289e-194">(In the previous tutorial, you used a `post` method, which is how you submit changes to the server.</span></span> <span data-ttu-id="2289e-195">Vous verrez que dans le didacticiel suivant.)</span><span class="sxs-lookup"><span data-stu-id="2289e-195">You'll see that in the next tutorial again.)</span></span>

<span data-ttu-id="2289e-196">Exécutez la page.</span><span class="sxs-lookup"><span data-stu-id="2289e-196">Run the page.</span></span> <span data-ttu-id="2289e-197">Bien que vous n’ayez défini aucun comportement pour le formulaire, vous pouvez voir à quoi il ressemble :</span><span class="sxs-lookup"><span data-stu-id="2289e-197">Although you haven't defined any behavior for the form, you can see what it looks like:</span></span>

![Page films avec zone de recherche pour genre](form-basics/_static/image3.png)

<span data-ttu-id="2289e-199">Entrez une valeur dans la zone de texte, par exemple « comédie ».</span><span class="sxs-lookup"><span data-stu-id="2289e-199">Enter a value into the text box, like "Comedy."</span></span> <span data-ttu-id="2289e-200">Cliquez ensuite sur **Rechercher un genre**.</span><span class="sxs-lookup"><span data-stu-id="2289e-200">Then click **Search Genre**.</span></span>

<span data-ttu-id="2289e-201">Prenez note de l’URL de la page.</span><span class="sxs-lookup"><span data-stu-id="2289e-201">Take note of the URL of the page.</span></span> <span data-ttu-id="2289e-202">Étant donné que vous affectez à l’attribut `method` de l’élément `<form>` la valeur `get`, la valeur que vous avez entrée fait maintenant partie de la chaîne de requête dans l’URL, comme suit :</span><span class="sxs-lookup"><span data-stu-id="2289e-202">Because you set the `<form>` element's `method` attribute to `get`, the value you entered is now part of the query string in the URL, like this:</span></span>

`http://localhost:45661/Movies.cshtml?searchGenre=Comedy`

## <a name="reading-form-values"></a><span data-ttu-id="2289e-203">Lecture des valeurs de formulaire</span><span class="sxs-lookup"><span data-stu-id="2289e-203">Reading Form Values</span></span>

<span data-ttu-id="2289e-204">La page contient déjà du code qui récupère les données de la base de données et affiche les résultats dans une grille.</span><span class="sxs-lookup"><span data-stu-id="2289e-204">The page already contains some code that gets database data and displays the results in a grid.</span></span> <span data-ttu-id="2289e-205">À présent, vous devez ajouter du code qui lit la valeur de la zone de texte afin de pouvoir exécuter une requête SQL qui comprend le terme recherché.</span><span class="sxs-lookup"><span data-stu-id="2289e-205">Now you have to add some code that reads the value of the text box so you can run a SQL query that includes the search term.</span></span>

<span data-ttu-id="2289e-206">Étant donné que vous définissez la méthode du formulaire sur `get`, vous pouvez lire la valeur qui a été entrée dans la zone de texte à l’aide d’un code similaire à ce qui suit :</span><span class="sxs-lookup"><span data-stu-id="2289e-206">Because you set the form's method to `get`, you can read the value that was entered into the text box by using code like the following:</span></span>

`var searchTerm = Request.QueryString["searchGenre"];`

<span data-ttu-id="2289e-207">L’objet `Request.QueryString` (la propriété `QueryString` de l’objet `Request`) comprend les valeurs des éléments qui ont été envoyés dans le cadre de l’opération de `GET`.</span><span class="sxs-lookup"><span data-stu-id="2289e-207">The `Request.QueryString` object (the `QueryString` property of the `Request` object) includes the values of elements that were submitted as part of the `GET` operation.</span></span> <span data-ttu-id="2289e-208">La propriété `Request.QueryString` contient une *collection* (une liste) des valeurs soumises dans le formulaire.</span><span class="sxs-lookup"><span data-stu-id="2289e-208">The `Request.QueryString` property contains a *collection* (a list) of the values that are submitted in the form.</span></span> <span data-ttu-id="2289e-209">Pour obtenir une valeur individuelle, vous spécifiez le nom de l’élément souhaité.</span><span class="sxs-lookup"><span data-stu-id="2289e-209">To get any individual value, you specify the name of the element that you want.</span></span> <span data-ttu-id="2289e-210">C’est la raison pour laquelle vous devez avoir un attribut `name` sur l’élément `<input>` (`searchTerm`) qui crée la zone de texte.</span><span class="sxs-lookup"><span data-stu-id="2289e-210">That's why you have to have a `name` attribute on the `<input>` element (`searchTerm`) that creates the text box.</span></span> <span data-ttu-id="2289e-211">(Pour plus d’informations sur l’objet `Request`, consultez la [barre latérale](#BKMK_TheRequestObject) ultérieurement.)</span><span class="sxs-lookup"><span data-stu-id="2289e-211">(For more about the `Request` object, see the [sidebar](#BKMK_TheRequestObject) later.)</span></span>

<span data-ttu-id="2289e-212">Il est assez simple de lire la valeur de la zone de texte.</span><span class="sxs-lookup"><span data-stu-id="2289e-212">It's simple enough to read the value of the text box.</span></span> <span data-ttu-id="2289e-213">Toutefois, si l’utilisateur n’a rien entré dans la zone de texte, mais a cliqué sur **Rechercher** quand même, vous pouvez ignorer ce clic, puisqu’il n’y a rien à rechercher.</span><span class="sxs-lookup"><span data-stu-id="2289e-213">But if the user didn't enter anything at all in the text box but clicked **Search** anyway, you can ignore that click, since there's nothing to search.</span></span>

<span data-ttu-id="2289e-214">Le code suivant est un exemple qui montre comment implémenter ces conditions.</span><span class="sxs-lookup"><span data-stu-id="2289e-214">The following code is an example that shows how to implement these conditions.</span></span> <span data-ttu-id="2289e-215">(Vous n’avez pas encore ajouté ce code ; vous le ferez dans un moment.)</span><span class="sxs-lookup"><span data-stu-id="2289e-215">(You don't have to add this code yet; you'll do that in a moment.)</span></span>

[!code-csharp[Main](form-basics/samples/sample3.cs)]

<span data-ttu-id="2289e-216">Le test s’arrête de cette façon :</span><span class="sxs-lookup"><span data-stu-id="2289e-216">The test breaks down in this way:</span></span>

- <span data-ttu-id="2289e-217">Obtient la valeur de `Request.QueryString["searchGenre"]`, à savoir la valeur qui a été entrée dans l’élément `<input>` nommé `searchGenre`.</span><span class="sxs-lookup"><span data-stu-id="2289e-217">Get the value of `Request.QueryString["searchGenre"]`, namely the value that was entered into the `<input>` element named `searchGenre`.</span></span>
- <span data-ttu-id="2289e-218">Déterminez si elle est vide à l’aide de la méthode `IsEmpty`.</span><span class="sxs-lookup"><span data-stu-id="2289e-218">Find out if it's empty by using the `IsEmpty` method.</span></span> <span data-ttu-id="2289e-219">Cette méthode est le moyen standard de déterminer si un élément (par exemple, un élément de formulaire) contient une valeur.</span><span class="sxs-lookup"><span data-stu-id="2289e-219">This method is the standard way to determine whether something (for example, a form element) contains a value.</span></span> <span data-ttu-id="2289e-220">Mais vraiment, vous vous souciez uniquement s’il ne s’agit *pas* d’un vide, par conséquent...</span><span class="sxs-lookup"><span data-stu-id="2289e-220">But really, you care only if it's *not* empty, therefore ...</span></span>
- <span data-ttu-id="2289e-221">Ajoutez l’opérateur `!` devant le test `IsEmpty`.</span><span class="sxs-lookup"><span data-stu-id="2289e-221">Add the `!` operator in front of the `IsEmpty` test.</span></span> <span data-ttu-id="2289e-222">(L’opérateur `!` signifie NOT logique).</span><span class="sxs-lookup"><span data-stu-id="2289e-222">(The `!` operator means logical NOT).</span></span>

<span data-ttu-id="2289e-223">En anglais, l’intégralité de la condition de `if` se traduit comme suit : *si l’élément searchGenre du formulaire n’est pas vide, alors...*</span><span class="sxs-lookup"><span data-stu-id="2289e-223">In plain English, the entire `if` condition translates into the following: *If the form's searchGenre element is not empty, then ...*</span></span>

<span data-ttu-id="2289e-224">Ce bloc définit la phase de création d’une requête qui utilise le terme de recherche.</span><span class="sxs-lookup"><span data-stu-id="2289e-224">This block sets the stage for creating a query that uses the search term.</span></span> <span data-ttu-id="2289e-225">Vous le ferez dans la section suivante.</span><span class="sxs-lookup"><span data-stu-id="2289e-225">You'll do that in the next section.</span></span>

<a id="BKMK_TheRequestObject"></a>

> [!TIP] 
> 
> <span data-ttu-id="2289e-226">**Objet de la requête**</span><span class="sxs-lookup"><span data-stu-id="2289e-226">**The Request Object**</span></span>
> 
> <span data-ttu-id="2289e-227">L’objet `Request` contient toutes les informations que le navigateur envoie à votre application lorsqu’une page est demandée ou envoyée.</span><span class="sxs-lookup"><span data-stu-id="2289e-227">The `Request` object contains all the information that the browser sends to your application when a page is requested or submitted.</span></span> <span data-ttu-id="2289e-228">Cet objet inclut toutes les informations fournies par l’utilisateur, telles que les valeurs de zone de texte ou un fichier à charger.</span><span class="sxs-lookup"><span data-stu-id="2289e-228">This object includes any information that the user provides, like text box values or a file to upload.</span></span> <span data-ttu-id="2289e-229">Il comprend également toutes sortes d’informations supplémentaires, telles que les cookies, les valeurs de la chaîne de requête d’URL (le cas échéant), le chemin d’accès de la page en cours d’exécution, le type de navigateur utilisé par l’utilisateur, la liste des langues définies dans le navigateur. , et bien plus encore.</span><span class="sxs-lookup"><span data-stu-id="2289e-229">It also includes all sorts of additional information, like cookies, values in the URL query string (if any), the file path of the page that is running, the type of browser that the user is using, the list of languages that are set in the browser, and much more.</span></span>
> 
> <span data-ttu-id="2289e-230">L’objet `Request` est une *collection* (liste) de valeurs.</span><span class="sxs-lookup"><span data-stu-id="2289e-230">The `Request` object is a *collection* (list) of values.</span></span> <span data-ttu-id="2289e-231">Vous récupérez une valeur individuelle de la collection en spécifiant son nom :</span><span class="sxs-lookup"><span data-stu-id="2289e-231">You get an individual value out of the collection by specifying its name:</span></span>
> 
> `var someValue = Request["name"];`
> 
> <span data-ttu-id="2289e-232">L’objet `Request` expose en fait plusieurs sous-ensembles.</span><span class="sxs-lookup"><span data-stu-id="2289e-232">The `Request` object actually exposes several subsets.</span></span> <span data-ttu-id="2289e-233">Exemple :</span><span class="sxs-lookup"><span data-stu-id="2289e-233">For example:</span></span>
> 
> - <span data-ttu-id="2289e-234">`Request.Form` vous donne des valeurs à partir d’éléments à l’intérieur de l’élément `<form>` envoyé si la requête est une requête `POST`.</span><span class="sxs-lookup"><span data-stu-id="2289e-234">`Request.Form` gives you values from elements inside the submitted `<form>` element if the request is a `POST` request.</span></span>
> - <span data-ttu-id="2289e-235">`Request.QueryString` vous donne uniquement les valeurs de la chaîne de requête de l’URL.</span><span class="sxs-lookup"><span data-stu-id="2289e-235">`Request.QueryString` gives you just the values in the URL's query string.</span></span> <span data-ttu-id="2289e-236">(Dans une URL telle que `http://mysite/myapp/page?searchGenre=action&page=2`, la section `?searchGenre=action&page=2` de l’URL est la chaîne de requête.)</span><span class="sxs-lookup"><span data-stu-id="2289e-236">(In a URL like `http://mysite/myapp/page?searchGenre=action&page=2`, the `?searchGenre=action&page=2` section of the URL is the query string.)</span></span>
> - <span data-ttu-id="2289e-237">`Request.Cookies` collection vous donne accès aux cookies envoyés par le navigateur.</span><span class="sxs-lookup"><span data-stu-id="2289e-237">`Request.Cookies` collection gives you access to cookies that the browser has sent.</span></span>
> 
> <span data-ttu-id="2289e-238">Pour obtenir une valeur que vous connaissez dans le formulaire envoyé, vous pouvez utiliser `Request["name"]`.</span><span class="sxs-lookup"><span data-stu-id="2289e-238">To get a value that you know is in the submitted form, you can use `Request["name"]`.</span></span> <span data-ttu-id="2289e-239">Vous pouvez également utiliser les versions plus spécifiques `Request.Form["name"]` (pour les demandes `POST`) ou `Request.QueryString["name"]` (pour les demandes `GET`).</span><span class="sxs-lookup"><span data-stu-id="2289e-239">Alternatively, you can use the more specific versions `Request.Form["name"]` (for `POST` requests) or `Request.QueryString["name"]` (for `GET` requests).</span></span> <span data-ttu-id="2289e-240">Bien entendu, *Name* est le nom de l’élément à récupérer.</span><span class="sxs-lookup"><span data-stu-id="2289e-240">Of course, *name* is the name of the item to get.</span></span>
> 
> <span data-ttu-id="2289e-241">Le nom de l’élément que vous souhaitez obtenir doit être unique au sein de la collection que vous utilisez.</span><span class="sxs-lookup"><span data-stu-id="2289e-241">The name of the item you want to get has to be unique within the collection you're using.</span></span> <span data-ttu-id="2289e-242">C’est la raison pour laquelle l’objet `Request` fournit les sous-ensembles comme `Request.Form` et `Request.QueryString`.</span><span class="sxs-lookup"><span data-stu-id="2289e-242">That's why the `Request` object provides the subsets like `Request.Form` and `Request.QueryString`.</span></span> <span data-ttu-id="2289e-243">Supposons que votre page contient un élément de formulaire nommé `userName` et qu’elle contienne *également* un cookie nommé `userName`.</span><span class="sxs-lookup"><span data-stu-id="2289e-243">Suppose that your page contains a form element named `userName` and *also* contains a cookie named `userName`.</span></span> <span data-ttu-id="2289e-244">Si vous recevez `Request["userName"]`, il est ambigu si vous souhaitez la valeur de formulaire ou le cookie.</span><span class="sxs-lookup"><span data-stu-id="2289e-244">If you get `Request["userName"]`, it's ambiguous whether you want the form value or the cookie.</span></span> <span data-ttu-id="2289e-245">Toutefois, si vous recevez `Request.Form["userName"]` ou `Request.Cookie["userName"]`, vous êtes explicite en ce qui concerne la valeur à obtenir.</span><span class="sxs-lookup"><span data-stu-id="2289e-245">However, if you get `Request.Form["userName"]` or `Request.Cookie["userName"]`, you're being explicit about which value to get.</span></span>
> 
> <span data-ttu-id="2289e-246">Il est recommandé d’être spécifique et d’utiliser le sous-ensemble de `Request` qui vous intéresse, comme `Request.Form` ou `Request.QueryString`.</span><span class="sxs-lookup"><span data-stu-id="2289e-246">It's a good practice to be specific and use the subset of `Request` that you're interested in, like `Request.Form` or `Request.QueryString`.</span></span> <span data-ttu-id="2289e-247">Pour les pages simples que vous créez dans ce didacticiel, cela ne fait probablement aucune différence.</span><span class="sxs-lookup"><span data-stu-id="2289e-247">For the simple pages that you're creating in this tutorial, it probably doesn't really make any difference.</span></span> <span data-ttu-id="2289e-248">Toutefois, lorsque vous créez des pages plus complexes, l’utilisation de la version explicite `Request.Form` ou `Request.QueryString` peut vous aider à éviter les problèmes qui peuvent survenir lorsque la page contient un formulaire (ou plusieurs formulaires), des cookies, des valeurs de chaîne de requête, etc.</span><span class="sxs-lookup"><span data-stu-id="2289e-248">However, as you create more complex pages, using the explicit version `Request.Form` or `Request.QueryString` can help you avoid problems that can arise when the page contains a form (or multiple forms), cookies, query string values, and so on.</span></span>

## <a name="creating-a-query-by-using-a-search-term"></a><span data-ttu-id="2289e-249">Création d’une requête à l’aide d’un terme de recherche</span><span class="sxs-lookup"><span data-stu-id="2289e-249">Creating a Query by Using a Search Term</span></span>

<span data-ttu-id="2289e-250">Maintenant que vous savez comment obtenir le terme de recherche entré par l’utilisateur, vous pouvez créer une requête qui l’utilise.</span><span class="sxs-lookup"><span data-stu-id="2289e-250">Now that you know how to get the search term that the user entered, you can create a query that uses it.</span></span> <span data-ttu-id="2289e-251">N’oubliez pas que pour récupérer tous les éléments de film de la base de données, vous utilisez une requête SQL qui ressemble à l’instruction suivante :</span><span class="sxs-lookup"><span data-stu-id="2289e-251">Remember that to get all the movie items out of the database, you're using a SQL query that looks like this statement:</span></span>

`SELECT * FROM Movies`

<span data-ttu-id="2289e-252">Pour obtenir uniquement certains films, vous devez utiliser une requête qui comprend une clause `Where`.</span><span class="sxs-lookup"><span data-stu-id="2289e-252">To get only certain movies, you have to use a query that includes a `Where` clause.</span></span> <span data-ttu-id="2289e-253">Cette clause vous permet de définir une condition sur laquelle les lignes sont retournées par la requête.</span><span class="sxs-lookup"><span data-stu-id="2289e-253">This clause lets you set a condition on which rows are returned by the query.</span></span> <span data-ttu-id="2289e-254">Voici un exemple :</span><span class="sxs-lookup"><span data-stu-id="2289e-254">Here's an example:</span></span>

`SELECT * FROM Movies WHERE Genre = 'Action'`

<span data-ttu-id="2289e-255">Le format de base est `WHERE column = value`.</span><span class="sxs-lookup"><span data-stu-id="2289e-255">The basic format is `WHERE column = value`.</span></span> <span data-ttu-id="2289e-256">Vous pouvez utiliser différents opérateurs en plus de `=`, comme `>` (supérieur à), `<` (inférieur à), `<>` (différent de), `<=` (inférieur ou égal à), etc., en fonction de ce que vous recherchez.</span><span class="sxs-lookup"><span data-stu-id="2289e-256">You can use different operators besides just `=`, like `>` (greater than), `<` (less than), `<>` (not equal to), `<=` (less than or equal to), etc., depending on what you're looking for.</span></span>

<span data-ttu-id="2289e-257">Au cas où vous vous poseriez la question, les instructions SQL ne respectent pas la casse &mdash; `SELECT` est identique à `Select` (ou même `select`).</span><span class="sxs-lookup"><span data-stu-id="2289e-257">In case you're wondering, SQL statements are not case sensitive &mdash; `SELECT` is the same as `Select` (or even `select`).</span></span> <span data-ttu-id="2289e-258">Toutefois, les gens mettent souvent en majuscules les mots clés dans une instruction SQL, comme `SELECT` et `WHERE`, pour faciliter leur lecture.</span><span class="sxs-lookup"><span data-stu-id="2289e-258">However, people often capitalize keywords in a SQL statement, like `SELECT` and `WHERE`, to make it easier to read.</span></span>

### <a name="passing-the-search-term-as-a-parameter"></a><span data-ttu-id="2289e-259">Passage du terme de recherche en tant que paramètre</span><span class="sxs-lookup"><span data-stu-id="2289e-259">Passing the search term as a parameter</span></span>

<span data-ttu-id="2289e-260">La recherche d’un genre spécifique est suffisamment simple (`WHERE Genre = 'Action'`), mais vous souhaitez pouvoir Rechercher le genre que l’utilisateur entre.</span><span class="sxs-lookup"><span data-stu-id="2289e-260">Searching for a specific genre is easy enough (`WHERE Genre = 'Action'`), but you want to be able to search for any genre that the user enters.</span></span> <span data-ttu-id="2289e-261">Pour ce faire, vous créez en tant que requête SQL qui comprend un espace réservé pour la valeur à rechercher.</span><span class="sxs-lookup"><span data-stu-id="2289e-261">To do that, you create as SQL query that includes a placeholder for the value to search.</span></span> <span data-ttu-id="2289e-262">Elle doit ressembler à la commande suivante :</span><span class="sxs-lookup"><span data-stu-id="2289e-262">It will look like this command:</span></span>

`SELECT * FROM Movies WHERE Genre = @0`

<span data-ttu-id="2289e-263">L’espace réservé est le caractère `@` suivi de zéro.</span><span class="sxs-lookup"><span data-stu-id="2289e-263">The placeholder is the `@` character followed by zero.</span></span> <span data-ttu-id="2289e-264">Comme vous pouvez le deviner, une requête peut contenir plusieurs espaces réservés et se nommer `@0`, `@1`, `@2`, etc.</span><span class="sxs-lookup"><span data-stu-id="2289e-264">As you might guess, a query can contain multiple placeholders, and they'd be named `@0`, `@1`, `@2`, etc.</span></span>

<span data-ttu-id="2289e-265">Pour configurer la requête et lui transmettre réellement la valeur, vous utilisez le code comme suit :</span><span class="sxs-lookup"><span data-stu-id="2289e-265">To set up the query and actually pass it the value, you use the code like the following:</span></span>

[!code-sql[Main](form-basics/samples/sample4.sql)]

<span data-ttu-id="2289e-266">Ce code est similaire à ce que vous avez déjà fait pour afficher des données dans la grille.</span><span class="sxs-lookup"><span data-stu-id="2289e-266">This code is similar to what you've already done to display data in the grid.</span></span> <span data-ttu-id="2289e-267">Les seules différences sont les suivantes :</span><span class="sxs-lookup"><span data-stu-id="2289e-267">The only differences are:</span></span>

- <span data-ttu-id="2289e-268">La requête contient un espace réservé (`WHERE Genre = @0"`).</span><span class="sxs-lookup"><span data-stu-id="2289e-268">The query contains a placeholder (`WHERE Genre = @0"`).</span></span>
- <span data-ttu-id="2289e-269">La requête est placée dans une variable (`selectCommand`); avant, vous avez passé directement la requête à la méthode `db.Query`.</span><span class="sxs-lookup"><span data-stu-id="2289e-269">The query is put into a variable (`selectCommand`); before, you passed the query directly to the `db.Query` method.</span></span>
- <span data-ttu-id="2289e-270">Lorsque vous appelez la méthode `db.Query`, vous transmettez à la fois la requête et la valeur à utiliser pour l’espace réservé.</span><span class="sxs-lookup"><span data-stu-id="2289e-270">When you call the `db.Query` method, you pass both the query and the value to use for the placeholder.</span></span> <span data-ttu-id="2289e-271">(Si la requête comportait plusieurs espaces réservés, transmettez-les tous sous forme de valeurs séparées à la méthode.)</span><span class="sxs-lookup"><span data-stu-id="2289e-271">(If the query had multiple placeholders, you'd pass them all as separate values to the method.)</span></span>

<span data-ttu-id="2289e-272">Si vous placez tous ces éléments ensemble, vous recevez le code suivant :</span><span class="sxs-lookup"><span data-stu-id="2289e-272">If you put all these elements together, you get the following code:</span></span>

[!code-csharp[Main](form-basics/samples/sample5.cs)]

> [!NOTE] 
> 
> <span data-ttu-id="2289e-273">**Important !**</span><span class="sxs-lookup"><span data-stu-id="2289e-273">**Important!**</span></span> <span data-ttu-id="2289e-274">L’utilisation d’espaces réservés (comme `@0`) pour passer des valeurs à une commande SQL est *extrêmement importante* pour la sécurité.</span><span class="sxs-lookup"><span data-stu-id="2289e-274">Using placeholders (like `@0`) to pass values to a SQL command is *extremely important* for security.</span></span> <span data-ttu-id="2289e-275">La façon dont vous le voyez ici, avec des espaces réservés pour les données variables, est le seul moyen de construire des commandes SQL.</span><span class="sxs-lookup"><span data-stu-id="2289e-275">The way you see it here, with placeholders for variable data, is the only way you should construct SQL commands.</span></span>
> 
> <span data-ttu-id="2289e-276">Ne construisez jamais une instruction SQL en rassemblant (concaténation) le texte littéral et les valeurs que vous recevez de l’utilisateur.</span><span class="sxs-lookup"><span data-stu-id="2289e-276">Never construct a SQL statement by putting together (concatenating) literal text and values you get from the user.</span></span> <span data-ttu-id="2289e-277">La concaténation de l’entrée d’utilisateur dans une instruction SQL ouvre votre site à une *attaque par injection SQL* où un utilisateur malveillant soumet des valeurs à votre page qui piratent votre base de données.</span><span class="sxs-lookup"><span data-stu-id="2289e-277">Concatenating user input into a SQL statement opens your site to a *SQL injection attack* where a malicious user submits values to your page that hack your database.</span></span> <span data-ttu-id="2289e-278">(Pour plus d’informations, consultez l’article [injection SQL](https://msdn.microsoft.com/library/ms161953.aspx) sur le site Web MSDN.)</span><span class="sxs-lookup"><span data-stu-id="2289e-278">(You can read more in the article [SQL Injection](https://msdn.microsoft.com/library/ms161953.aspx) the MSDN website.)</span></span>

## <a name="updating-the-movies-page-with-search-code"></a><span data-ttu-id="2289e-279">Mise à jour de la page films avec code de recherche</span><span class="sxs-lookup"><span data-stu-id="2289e-279">Updating the Movies Page with Search Code</span></span>

<span data-ttu-id="2289e-280">Vous pouvez maintenant mettre à jour le code dans le fichier *movies. cshtml* .</span><span class="sxs-lookup"><span data-stu-id="2289e-280">Now you can update the code in the *Movies.cshtml* file.</span></span> <span data-ttu-id="2289e-281">Pour commencer, remplacez le code dans le bloc de code en haut de la page par le code suivant :</span><span class="sxs-lookup"><span data-stu-id="2289e-281">To begin, replace the code in the code block at the top of the page with this code:</span></span>

[!code-csharp[Main](form-basics/samples/sample6.cs)]

<span data-ttu-id="2289e-282">La différence ici est que vous avez placé la requête dans la variable `selectCommand`, que vous passerez à `db.Query` ultérieurement.</span><span class="sxs-lookup"><span data-stu-id="2289e-282">The difference here is that you've put the query into the `selectCommand` variable, which you'll pass to `db.Query` later.</span></span> <span data-ttu-id="2289e-283">En plaçant l’instruction SQL dans une variable, vous pouvez modifier l’instruction, ce qui vous permet d’effectuer la recherche.</span><span class="sxs-lookup"><span data-stu-id="2289e-283">Putting the SQL statement into a variable lets you change the statement, which is what you'll do to perform the search.</span></span>

<span data-ttu-id="2289e-284">Vous avez également supprimé ces deux lignes, que vous allez recopier plus tard :</span><span class="sxs-lookup"><span data-stu-id="2289e-284">You've also removed these two lines, which you'll put back in later:</span></span>

[!code-csharp[Main](form-basics/samples/sample7.cs)]

<span data-ttu-id="2289e-285">Vous ne souhaitez pas encore exécuter la requête (autrement dit, appeler `db.Query`) et vous ne souhaitez pas non plus initialiser le `WebGrid` Helper.</span><span class="sxs-lookup"><span data-stu-id="2289e-285">You don't want to run the query yet (that is, call `db.Query`) and you don't want to initialize the `WebGrid` helper yet either.</span></span> <span data-ttu-id="2289e-286">Vous effectuerez ces opérations après avoir déterminé l’instruction SQL à exécuter.</span><span class="sxs-lookup"><span data-stu-id="2289e-286">You'll do those things after you've figured out which SQL statement has to run.</span></span>

<span data-ttu-id="2289e-287">Après ce bloc réécrit, vous pouvez ajouter la nouvelle logique pour gérer la recherche.</span><span class="sxs-lookup"><span data-stu-id="2289e-287">After this rewritten block, you can add the new logic for handling the search.</span></span> <span data-ttu-id="2289e-288">Le code complet ressemble à ce qui suit.</span><span class="sxs-lookup"><span data-stu-id="2289e-288">The completed code will look like the following.</span></span> <span data-ttu-id="2289e-289">Mettez à jour le code dans votre page pour qu’il corresponde à l’exemple suivant :</span><span class="sxs-lookup"><span data-stu-id="2289e-289">Update the code in your page so it matches this example:</span></span>

[!code-cshtml[Main](form-basics/samples/sample8.cshtml)]

<span data-ttu-id="2289e-290">La page fonctionne maintenant comme suit.</span><span class="sxs-lookup"><span data-stu-id="2289e-290">The page now works like this.</span></span> <span data-ttu-id="2289e-291">À chaque exécution de la page, le code ouvre la base de données et la variable `selectCommand` est définie sur l’instruction SQL qui obtient tous les enregistrements de la table `Movies`.</span><span class="sxs-lookup"><span data-stu-id="2289e-291">Every time the page runs, the code opens the database and the `selectCommand` variable is set to the SQL statement that gets all the records from the `Movies` table.</span></span> <span data-ttu-id="2289e-292">Le code initialise également la variable `searchTerm`.</span><span class="sxs-lookup"><span data-stu-id="2289e-292">The code also initializes the `searchTerm` variable.</span></span>

<span data-ttu-id="2289e-293">Toutefois, si la requête actuelle comprend une valeur pour l’élément `searchGenre`, le code définit `selectCommand` à une requête différente, à savoir, à une requête qui comprend la clause `Where` pour rechercher un genre.</span><span class="sxs-lookup"><span data-stu-id="2289e-293">However, if the current request includes a value for the `searchGenre` element, the code sets `selectCommand` to a different query — namely, to one that includes the `Where` clause to search for a genre.</span></span> <span data-ttu-id="2289e-294">Il définit également `searchTerm` sur ce qui a été passé pour la zone de recherche (qui peut être Nothing).</span><span class="sxs-lookup"><span data-stu-id="2289e-294">It also sets `searchTerm` to whatever was passed for the search box (which might be nothing).</span></span>

<span data-ttu-id="2289e-295">Quelle que soit l’instruction SQL qui se trouve dans `selectCommand`, le code appelle ensuite `db.Query` pour exécuter la requête, en lui transmettant ce qui se trouve dans `searchTerm`.</span><span class="sxs-lookup"><span data-stu-id="2289e-295">Regardless of which SQL statement is in `selectCommand`, the code then calls `db.Query` to run the query, passing it whatever is in `searchTerm`.</span></span> <span data-ttu-id="2289e-296">S’il n’y a rien dans `searchTerm`, cela n’a pas d’importance, car dans ce cas, il n’y a aucun paramètre pour transmettre la valeur à `selectCommand` quand même.</span><span class="sxs-lookup"><span data-stu-id="2289e-296">If there's nothing in `searchTerm`, it doesn't matter, because in that case there's no parameter to pass the value to `selectCommand` anyway.</span></span>

<span data-ttu-id="2289e-297">Enfin, le code initialise le programme d’assistance `WebGrid` à l’aide des résultats de la requête, comme auparavant.</span><span class="sxs-lookup"><span data-stu-id="2289e-297">Finally, the code initializes the `WebGrid` helper by using the query results, just like before.</span></span>

<span data-ttu-id="2289e-298">Vous pouvez voir qu’en plaçant l’instruction SQL et le terme de recherche dans des variables, vous avez ajouté de la flexibilité au code.</span><span class="sxs-lookup"><span data-stu-id="2289e-298">You can see that by putting the SQL statement and the search term into variables, you've added flexibility to the code.</span></span> <span data-ttu-id="2289e-299">Comme vous le verrez plus loin dans ce didacticiel, vous pouvez utiliser ce Framework de base et continuer à ajouter de la logique pour différents types de recherche.</span><span class="sxs-lookup"><span data-stu-id="2289e-299">As you'll see later in this tutorial, you can use this basic framework and keep adding logic for different types of searches.</span></span>

## <a name="testing-the-search-by-genre-feature"></a><span data-ttu-id="2289e-300">Test de la fonctionnalité de recherche par genre</span><span class="sxs-lookup"><span data-stu-id="2289e-300">Testing the Search-by-Genre Feature</span></span>

<span data-ttu-id="2289e-301">Dans WebMatrix, exécutez la page *movies. cshtml* .</span><span class="sxs-lookup"><span data-stu-id="2289e-301">In WebMatrix, run the *Movies.cshtml* page.</span></span> <span data-ttu-id="2289e-302">La page s’affiche avec la zone de texte correspondant au genre.</span><span class="sxs-lookup"><span data-stu-id="2289e-302">You see the page with the text box for genre.</span></span>

<span data-ttu-id="2289e-303">Entrez un genre que vous avez entré pour l’un de vos enregistrements de test, puis cliquez sur **Rechercher**.</span><span class="sxs-lookup"><span data-stu-id="2289e-303">Enter a genre that you've entered for one of your test records, then click **Search**.</span></span> <span data-ttu-id="2289e-304">Cette fois, vous voyez une liste des films qui correspondent à ce genre :</span><span class="sxs-lookup"><span data-stu-id="2289e-304">This time you see a listing of just the movies that match that genre:</span></span>

![Liste des pages de films après la recherche du genre « Comedies »](form-basics/_static/image4.png)

<span data-ttu-id="2289e-306">Entrez un autre genre et recommencez la recherche.</span><span class="sxs-lookup"><span data-stu-id="2289e-306">Enter a different genre and search again.</span></span> <span data-ttu-id="2289e-307">Essayez d’entrer le genre en utilisant des minuscules ou des majuscules afin de voir que la recherche ne respecte pas la casse.</span><span class="sxs-lookup"><span data-stu-id="2289e-307">Try entering the genre by using all lowercase or all uppercase letters so that you can see that the search is not case sensitive.</span></span>

## <a name="remembering-what-the-user-entered"></a><span data-ttu-id="2289e-308">« Mémorisation » de la saisie de l’utilisateur</span><span class="sxs-lookup"><span data-stu-id="2289e-308">"Remembering" What the User Entered</span></span>

<span data-ttu-id="2289e-309">Vous avez peut-être remarqué qu’une fois que vous avez entré un genre et cliqué sur le **genre de recherche**, vous avez vu une liste pour ce genre.</span><span class="sxs-lookup"><span data-stu-id="2289e-309">You might have noticed that after you entered a genre and clicked **Search Genre**, you saw a listing for that genre.</span></span> <span data-ttu-id="2289e-310">Toutefois, la zone de texte de recherche est vide &mdash; en d’autres termes, elle n’a pas oublié ce que vous avez entré.</span><span class="sxs-lookup"><span data-stu-id="2289e-310">However, the search text box was empty &mdash; in other words, it didn't remember what you'd entered.</span></span>

<span data-ttu-id="2289e-311">Il est important de comprendre pourquoi ce comportement se produit.</span><span class="sxs-lookup"><span data-stu-id="2289e-311">It's important to understand why this behavior occurs.</span></span> <span data-ttu-id="2289e-312">Lorsque vous envoyez une page, le navigateur envoie une demande au serveur Web.</span><span class="sxs-lookup"><span data-stu-id="2289e-312">When you submit a page, the browser sends a request to the web server.</span></span> <span data-ttu-id="2289e-313">Quand ASP.NET obtient la demande, il crée une nouvelle instance de la page, y exécute le code, puis restitue à nouveau la page dans le navigateur.</span><span class="sxs-lookup"><span data-stu-id="2289e-313">When ASP.NET gets the request, it creates a brand-new instance of the page, runs the code in it, and then renders the page to the browser again.</span></span> <span data-ttu-id="2289e-314">En effet, toutefois, la page ne sait pas que vous travailliez avec une version précédente d’elle-même.</span><span class="sxs-lookup"><span data-stu-id="2289e-314">In effect, though, the page doesn't know that you were just working with a previous version of itself.</span></span> <span data-ttu-id="2289e-315">Tout ce qu’il sait, c’est qu’il a reçu une requête qui contient des données de formulaire.</span><span class="sxs-lookup"><span data-stu-id="2289e-315">All it knows is that it got a request that had some form data in it.</span></span>

<span data-ttu-id="2289e-316">Chaque fois que vous demandez une page &mdash; pour la première fois ou en la soumettant &mdash; vous recevez une nouvelle page.</span><span class="sxs-lookup"><span data-stu-id="2289e-316">Every time you request a page &mdash; whether for the first time or by submitting it &mdash; you're getting a new page.</span></span> <span data-ttu-id="2289e-317">Le serveur Web n’a pas de mémoire de votre dernière demande.</span><span class="sxs-lookup"><span data-stu-id="2289e-317">The web server has no memory of your last request.</span></span> <span data-ttu-id="2289e-318">Ni ASP.NET, ni le navigateur.</span><span class="sxs-lookup"><span data-stu-id="2289e-318">Neither does ASP.NET, and neither does the browser.</span></span> <span data-ttu-id="2289e-319">La seule connexion entre ces instances distinctes de la page correspond à toutes les données que vous transmettez entre elles.</span><span class="sxs-lookup"><span data-stu-id="2289e-319">The only connection between these separate instances of the page is any data that you transmit between them.</span></span> <span data-ttu-id="2289e-320">Si vous soumettez une page, par exemple, la nouvelle instance de page peut obtenir les données de formulaire envoyées par l’instance antérieure.</span><span class="sxs-lookup"><span data-stu-id="2289e-320">If you submit a page, for example, the new page instance can get the form data that was sent by the earlier instance.</span></span> <span data-ttu-id="2289e-321">(Une autre façon de passer des données entre les pages consiste à utiliser des cookies.)</span><span class="sxs-lookup"><span data-stu-id="2289e-321">(Another way to pass data between pages is to use cookies.)</span></span>

<span data-ttu-id="2289e-322">Une façon formelle de décrire cette situation consiste à indiquer que les pages Web sont *sans état*.</span><span class="sxs-lookup"><span data-stu-id="2289e-322">A formal way to describe this situation is to say that web pages are *stateless*.</span></span> <span data-ttu-id="2289e-323">Les serveurs Web et les pages elles-mêmes et les éléments de la page ne gèrent pas les informations relatives à l’état précédent d’une page.</span><span class="sxs-lookup"><span data-stu-id="2289e-323">Web servers and the pages themselves and the elements in the page do not maintain any information about the previous state of a page.</span></span> <span data-ttu-id="2289e-324">Le Web a été conçu de cette façon, car la conservation de l’état des demandes individuelles épuiserait rapidement les ressources des serveurs Web, qui gèrent souvent des milliers, voire des centaines de milliers, de demandes par seconde.</span><span class="sxs-lookup"><span data-stu-id="2289e-324">The web was designed this way because maintaining state for individual requests would quickly exhaust the resources of web servers, which often handle thousands, maybe even hundreds of thousands, of requests per second.</span></span>

<span data-ttu-id="2289e-325">C’est pourquoi la zone de texte était vide.</span><span class="sxs-lookup"><span data-stu-id="2289e-325">So that's why the text box was empty.</span></span> <span data-ttu-id="2289e-326">Une fois la page envoyée, ASP.NET a créé une nouvelle instance de la page et exécuté le code et le balisage.</span><span class="sxs-lookup"><span data-stu-id="2289e-326">After you submitted the page, ASP.NET created a new instance of the page and ran through the code and markup.</span></span> <span data-ttu-id="2289e-327">Il n’y avait rien dans ce code qui indiquait à ASP.NET de placer une valeur dans la zone de texte.</span><span class="sxs-lookup"><span data-stu-id="2289e-327">There was nothing in that code that told ASP.NET to put a value into the text box.</span></span> <span data-ttu-id="2289e-328">ASP.NET ne faisait rien, et la zone de texte a été rendue sans valeur.</span><span class="sxs-lookup"><span data-stu-id="2289e-328">So ASP.NET didn't do anything, and the text box was rendered without a value in it.</span></span>

<span data-ttu-id="2289e-329">Il existe en fait un moyen simple de contourner ce problème.</span><span class="sxs-lookup"><span data-stu-id="2289e-329">There's actually an easy way to get around this issue.</span></span> <span data-ttu-id="2289e-330">Le genre que vous avez entré dans la zone de texte *est* disponible dans le code &mdash; il est `Request.QueryString["searchGenre"]`.</span><span class="sxs-lookup"><span data-stu-id="2289e-330">The genre that you entered into the text box *is* available to you in code &mdash; it's in `Request.QueryString["searchGenre"]`.</span></span>

<span data-ttu-id="2289e-331">Mettez à jour le balisage de la zone de texte afin que l’attribut `value` obtient sa valeur de `searchTerm`, comme dans l’exemple suivant :</span><span class="sxs-lookup"><span data-stu-id="2289e-331">Update the markup for the text box so that the `value` attribute gets its value from `searchTerm`, like this example:</span></span>

[!code-html[Main](form-basics/samples/sample9.html?highlight=1)]

<span data-ttu-id="2289e-332">Dans cette page, vous pouvez également définir l’attribut `value` sur la variable `searchTerm`, car cette variable contient également le genre que vous avez entré.</span><span class="sxs-lookup"><span data-stu-id="2289e-332">In this page, you could have also set the `value` attribute to the `searchTerm` variable, since that variable also contains the genre you entered.</span></span> <span data-ttu-id="2289e-333">Toutefois, l’utilisation de l’objet `Request` pour définir l’attribut `value` comme indiqué ici est la méthode standard pour accomplir cette tâche.</span><span class="sxs-lookup"><span data-stu-id="2289e-333">But using the `Request` object to set the `value` attribute as shown here is the standard way to accomplish this task.</span></span> <span data-ttu-id="2289e-334">(En supposant que vous souhaitez même effectuer cette &mdash; dans certains cas, vous souhaiterez peut-être afficher la page *sans* valeurs dans les champs.</span><span class="sxs-lookup"><span data-stu-id="2289e-334">(Assuming you even want to do this &mdash; in some situations, you might want to render the page *without* values in the fields.</span></span> <span data-ttu-id="2289e-335">Tout dépend de ce qui se passe avec votre application.)</span><span class="sxs-lookup"><span data-stu-id="2289e-335">It all depends on what's going on with your app.)</span></span>

> [!NOTE]
> <span data-ttu-id="2289e-336">Vous ne pouvez pas « mémoriser » la valeur d’une zone de texte utilisée pour les mots de passe.</span><span class="sxs-lookup"><span data-stu-id="2289e-336">You can't "remember" the value of a text box that's used for passwords.</span></span> <span data-ttu-id="2289e-337">Il s’agit d’une faille de sécurité pour permettre aux utilisateurs de remplir un champ de mot de passe à l’aide de code.</span><span class="sxs-lookup"><span data-stu-id="2289e-337">It would be a security hole to allow people to fill in a password field by using code.</span></span>

<span data-ttu-id="2289e-338">Réexécutez la page, entrez un genre, puis cliquez sur **Rechercher un genre**.</span><span class="sxs-lookup"><span data-stu-id="2289e-338">Run the page again, enter a genre, and click **Search Genre**.</span></span> <span data-ttu-id="2289e-339">Cette fois, vous voyez les résultats de la recherche, mais la zone de texte mémorise ce que vous avez entré la dernière fois :</span><span class="sxs-lookup"><span data-stu-id="2289e-339">This time not only do you see the results of the search, but the text box remembers what you entered last time:</span></span>

![Page indiquant que la zone de texte a’mémorisé’l’entrée précédente](form-basics/_static/image5.png)

## <a name="searching-for-any-word-in-the-title"></a><span data-ttu-id="2289e-341">Recherche de mots dans le titre</span><span class="sxs-lookup"><span data-stu-id="2289e-341">Searching for Any Word in the Title</span></span>

<span data-ttu-id="2289e-342">Vous pouvez maintenant Rechercher un genre, mais vous pouvez également rechercher un titre.</span><span class="sxs-lookup"><span data-stu-id="2289e-342">You can now search for any genre, but you might also want to search for a title.</span></span> <span data-ttu-id="2289e-343">Il est difficile d’obtenir un titre exactement juste lorsque vous recherchez. à la place, vous pouvez rechercher un mot qui apparaît n’importe où dans un titre.</span><span class="sxs-lookup"><span data-stu-id="2289e-343">It's hard to get a title exactly right when you search, so instead you can search for a word that appears anywhere inside a title.</span></span> <span data-ttu-id="2289e-344">Pour ce faire, dans SQL, vous utilisez l’opérateur `LIKE` et la syntaxe comme suit :</span><span class="sxs-lookup"><span data-stu-id="2289e-344">To do that in SQL, you use the `LIKE` operator and syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Title LIKE '%adventure%'`

<span data-ttu-id="2289e-345">Cette commande obtient tous les films dont les titres contiennent « Adventure ».</span><span class="sxs-lookup"><span data-stu-id="2289e-345">This command gets all the movies whose titles contain "adventure".</span></span> <span data-ttu-id="2289e-346">Lorsque vous utilisez l’opérateur `LIKE`, vous incluez le caractère générique `%` dans le terme de recherche.</span><span class="sxs-lookup"><span data-stu-id="2289e-346">When you use the `LIKE` operator, you include the wildcard character `%` as part of the search term.</span></span> <span data-ttu-id="2289e-347">La `LIKE 'adventure%'` de recherche signifie « commençant par «Adventure »».</span><span class="sxs-lookup"><span data-stu-id="2289e-347">The search `LIKE 'adventure%'` means "starting with 'adventure'".</span></span> <span data-ttu-id="2289e-348">(Techniquement, cela signifie « la chaîne «Adventure », suivie de n’importe quoi».) De même, le terme de recherche `LIKE '%adventure'` signifie « tout suivi par la chaîne «Adventure »», qui est une autre façon d’indiquer « se terminant par «Adventure »».</span><span class="sxs-lookup"><span data-stu-id="2289e-348">(Technically, it means "The string 'adventure' followed by anything.") Similarly, the search term `LIKE '%adventure'` means "anything followed by the string 'adventure'", which is another way to say "ending with 'adventure'".</span></span>

<span data-ttu-id="2289e-349">Le terme de recherche `LIKE '%adventure%'` signifie donc « avec «Adventure » n’importe où dans le titre.»</span><span class="sxs-lookup"><span data-stu-id="2289e-349">The search term `LIKE '%adventure%'` therefore means "with 'adventure' anywhere in the title."</span></span> <span data-ttu-id="2289e-350">(Techniquement, « tout ce qui se trouve dans le titre, suivi de «Adventure », suivi de n’importe quoi».)</span><span class="sxs-lookup"><span data-stu-id="2289e-350">(Technically, "anything in the title, followed by 'adventure', followed by anything.")</span></span>

<span data-ttu-id="2289e-351">À l’intérieur de l’élément `<form>`, ajoutez le balisage suivant sous la balise `</div>` de fermeture pour la recherche de genre (juste avant l’élément `</form>` de fermeture) :</span><span class="sxs-lookup"><span data-stu-id="2289e-351">Inside the `<form>` element, add the following markup right under the closing `</div>` tag for the genre search (just before the closing `</form>` element):</span></span>

[!code-html[Main](form-basics/samples/sample10.html)]

<span data-ttu-id="2289e-352">Le code permettant de gérer cette recherche est similaire au code de la recherche de genre, à ceci près que vous devez assembler la recherche de `LIKE`.</span><span class="sxs-lookup"><span data-stu-id="2289e-352">The code to handle this search is similar to the code for the genre search, except that you have to assemble the `LIKE` search.</span></span> <span data-ttu-id="2289e-353">Dans le bloc de code en haut de la page, ajoutez ce bloc `if` juste après le bloc `if` pour la recherche de genre :</span><span class="sxs-lookup"><span data-stu-id="2289e-353">Inside the code block at the top of the page, add this `if` block just after the `if` block for the genre search:</span></span>

[!code-csharp[Main](form-basics/samples/sample11.cs)]

<span data-ttu-id="2289e-354">Ce code utilise la même logique que celle que vous avez vue précédemment, à la différence près que la recherche utilise un opérateur `LIKE` et que le code place «`%`» avant et après le terme de recherche.</span><span class="sxs-lookup"><span data-stu-id="2289e-354">This code uses the same logic you saw earlier, except that the search uses a `LIKE` operator and the code puts "`%`" before and after the search term.</span></span>

<span data-ttu-id="2289e-355">Notez que vous pouvez facilement ajouter une autre recherche à la page.</span><span class="sxs-lookup"><span data-stu-id="2289e-355">Notice how it was easy to add another search to the page.</span></span> <span data-ttu-id="2289e-356">Tout ce que vous avez à faire était :</span><span class="sxs-lookup"><span data-stu-id="2289e-356">All you had to do was:</span></span>

- <span data-ttu-id="2289e-357">Créez un bloc `if` qui a été testé pour voir si la zone de recherche appropriée avait une valeur.</span><span class="sxs-lookup"><span data-stu-id="2289e-357">Create an `if` block that tested to see whether the relevant search box had a value.</span></span>
- <span data-ttu-id="2289e-358">Définissez la variable `selectCommand` sur une nouvelle instruction SQL.</span><span class="sxs-lookup"><span data-stu-id="2289e-358">Set the `selectCommand` variable to a new SQL statement.</span></span>
- <span data-ttu-id="2289e-359">Affectez à la variable `searchTerm` la valeur à transmettre à la requête.</span><span class="sxs-lookup"><span data-stu-id="2289e-359">Set the `searchTerm` variable to the value to pass to the query.</span></span>

<span data-ttu-id="2289e-360">Voici le bloc de code complet, qui contient la nouvelle logique pour une recherche de titres :</span><span class="sxs-lookup"><span data-stu-id="2289e-360">Here's the complete code block, which contains the new logic for a title search:</span></span>

[!code-cshtml[Main](form-basics/samples/sample12.cshtml)]

<span data-ttu-id="2289e-361">Voici un résumé de ce que fait ce code :</span><span class="sxs-lookup"><span data-stu-id="2289e-361">Here's a summary of what this code does:</span></span>

- <span data-ttu-id="2289e-362">Les variables `searchTerm` et `selectCommand` sont initialisées en haut.</span><span class="sxs-lookup"><span data-stu-id="2289e-362">The variables `searchTerm` and `selectCommand` are initialized at the top.</span></span> <span data-ttu-id="2289e-363">Vous allez définir ces variables sur le terme de recherche approprié (le cas échéant) et la commande SQL appropriée en fonction de ce que l’utilisateur fait dans la page.</span><span class="sxs-lookup"><span data-stu-id="2289e-363">You're going to set these variables to the appropriate search term (if any) and appropriate SQL command based on what the user does in the page.</span></span> <span data-ttu-id="2289e-364">La recherche par défaut est le cas simple d’obtention de tous les films de la base de données.</span><span class="sxs-lookup"><span data-stu-id="2289e-364">The default search is the simple case of getting all the movies from the database.</span></span>
- <span data-ttu-id="2289e-365">Dans les tests pour `searchGenre` et `searchTitle`, le code définit `searchTerm` à la valeur que vous souhaitez rechercher.</span><span class="sxs-lookup"><span data-stu-id="2289e-365">In the tests for `searchGenre` and `searchTitle`, the code sets `searchTerm` to the value you want to search for.</span></span> <span data-ttu-id="2289e-366">Ces blocs de code définissent également `selectCommand` à une commande SQL appropriée pour cette recherche.</span><span class="sxs-lookup"><span data-stu-id="2289e-366">Those code blocks also set `selectCommand` to an appropriate SQL command for that search.</span></span>
- <span data-ttu-id="2289e-367">La méthode `db.Query` est appelée une seule fois, en utilisant la commande SQL qui se trouve dans `selectedCommand` et toute valeur figurant dans `searchTerm`.</span><span class="sxs-lookup"><span data-stu-id="2289e-367">The `db.Query` method is invoked only once, using whatever SQL command is in `selectedCommand` and whatever value is in `searchTerm`.</span></span> <span data-ttu-id="2289e-368">S’il n’y a pas de terme de recherche (aucun genre et aucun mot de titre), la valeur de `searchTerm` est une chaîne vide.</span><span class="sxs-lookup"><span data-stu-id="2289e-368">If there is no search term (no genre and no title word), the value of `searchTerm` is an empty string.</span></span> <span data-ttu-id="2289e-369">Toutefois, cela n’a pas d’importance, car dans ce cas, la requête ne nécessite pas de paramètre.</span><span class="sxs-lookup"><span data-stu-id="2289e-369">However, that doesn't matter, because in that case the query doesn't require a parameter.</span></span>

## <a name="testing-the-title-search-feature"></a><span data-ttu-id="2289e-370">Test de la fonctionnalité de recherche de titres</span><span class="sxs-lookup"><span data-stu-id="2289e-370">Testing the Title Search Feature</span></span>

<span data-ttu-id="2289e-371">Vous pouvez maintenant tester votre page de recherche terminée.</span><span class="sxs-lookup"><span data-stu-id="2289e-371">Now you can test your completed search page.</span></span> <span data-ttu-id="2289e-372">Exécutez *movies. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="2289e-372">Run *Movies.cshtml*.</span></span>

<span data-ttu-id="2289e-373">Entrez un genre et cliquez sur **Rechercher un genre**.</span><span class="sxs-lookup"><span data-stu-id="2289e-373">Enter a genre and click **Search Genre**.</span></span> <span data-ttu-id="2289e-374">La grille affiche des films de ce genre, comme avant.</span><span class="sxs-lookup"><span data-stu-id="2289e-374">The grid displays movies of that genre, like before.</span></span>

<span data-ttu-id="2289e-375">Entrez un mot de titre et cliquez sur **Rechercher un titre**.</span><span class="sxs-lookup"><span data-stu-id="2289e-375">Enter a title word and click **Search Title**.</span></span> <span data-ttu-id="2289e-376">La grille affiche les films qui contiennent ce mot dans le titre.</span><span class="sxs-lookup"><span data-stu-id="2289e-376">The grid displays movies that have that word in the title.</span></span>

![Liste des pages de films après la recherche de « The » dans le titre](form-basics/_static/image6.png)

<span data-ttu-id="2289e-378">Laissez les deux zones de texte vides et cliquez sur l’un des boutons.</span><span class="sxs-lookup"><span data-stu-id="2289e-378">Leave both text boxes blank and click either button.</span></span> <span data-ttu-id="2289e-379">La grille affiche tous les films.</span><span class="sxs-lookup"><span data-stu-id="2289e-379">The grid displays all the movies.</span></span>

## <a name="combining-the-queries"></a><span data-ttu-id="2289e-380">Combinaison des requêtes</span><span class="sxs-lookup"><span data-stu-id="2289e-380">Combining the Queries</span></span>

<span data-ttu-id="2289e-381">Vous remarquerez peut-être que les recherches que vous pouvez effectuer sont exclusives.</span><span class="sxs-lookup"><span data-stu-id="2289e-381">You might notice that the searches you can perform are exclusive.</span></span> <span data-ttu-id="2289e-382">Vous ne pouvez pas effectuer une recherche dans le titre et le genre en même temps, même si les deux zones de recherche contiennent des valeurs.</span><span class="sxs-lookup"><span data-stu-id="2289e-382">You can't search the title and the genre at the same time, even if both search boxes have values in them.</span></span> <span data-ttu-id="2289e-383">Par exemple, vous ne pouvez pas rechercher tous les films d’action dont le titre contient « Adventure ».</span><span class="sxs-lookup"><span data-stu-id="2289e-383">For example, you can't search for all action movies whose title contains "Adventure".</span></span> <span data-ttu-id="2289e-384">(Lorsque la page est codée maintenant, si vous entrez des valeurs pour le genre et le titre, la recherche de titre est prioritaire.) Pour créer une recherche qui combine les conditions, vous devez créer une requête SQL dont la syntaxe est similaire à ce qui suit :</span><span class="sxs-lookup"><span data-stu-id="2289e-384">(As the page is coded now, if you enter values for both genre and title, the title search gets precedence.) To create a search that combines the conditions, you would have to create a SQL query that has syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Genre = @0 AND Title LIKE @1`

<span data-ttu-id="2289e-385">Et vous devez exécuter la requête à l’aide d’une instruction telle que la suivante (à peu près) :</span><span class="sxs-lookup"><span data-stu-id="2289e-385">And you'd have to run the query by using a statement like the following (roughly speaking):</span></span>

`var selectedData = db.Query(selectCommand, searchGenre, searchTitle);`

<span data-ttu-id="2289e-386">La création d’une logique pour permettre de nombreuses permutations de critères de recherche peut être un peu complexe, comme vous pouvez le voir.</span><span class="sxs-lookup"><span data-stu-id="2289e-386">Creating logic to allow many permutations of search criteria can get a bit involved, as you can see.</span></span> <span data-ttu-id="2289e-387">Par conséquent, nous allons nous arrêter ici.</span><span class="sxs-lookup"><span data-stu-id="2289e-387">Therefore, we'll stop here.</span></span>

## <a name="coming-up-next"></a><span data-ttu-id="2289e-388">À venir suivant</span><span class="sxs-lookup"><span data-stu-id="2289e-388">Coming Up Next</span></span>

<span data-ttu-id="2289e-389">Dans le didacticiel suivant, vous allez créer une page qui utilise un formulaire pour permettre aux utilisateurs d’ajouter des films à la base de données.</span><span class="sxs-lookup"><span data-stu-id="2289e-389">In the next tutorial, you'll create a page that uses a form to let users add movies to the database.</span></span>

## <a name="complete-listing-for-movie-page-updated-with-search"></a><span data-ttu-id="2289e-390">Liste complète des pages de films (mise à jour avec recherche)</span><span class="sxs-lookup"><span data-stu-id="2289e-390">Complete Listing for Movie Page (Updated with Search)</span></span>

[!code-cshtml[Main](form-basics/samples/sample13.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="2289e-391">Ressources supplémentaires</span><span class="sxs-lookup"><span data-stu-id="2289e-391">Additional Resources</span></span>

- [<span data-ttu-id="2289e-392">Présentation de la programmation Web ASP.NET à l’aide de la syntaxe Razor</span><span class="sxs-lookup"><span data-stu-id="2289e-392">Introduction to ASP.NET Web Programming Using the Razor Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkID=202890)
- <span data-ttu-id="2289e-393">[Clause SQL WHERE](http://www.w3schools.com/sql/sql_where.asp) sur le site W3Schools</span><span class="sxs-lookup"><span data-stu-id="2289e-393">[SQL WHERE Clause](http://www.w3schools.com/sql/sql_where.asp) on the W3Schools site</span></span>
- <span data-ttu-id="2289e-394">Article sur les [définitions de méthode](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) sur le site W3C</span><span class="sxs-lookup"><span data-stu-id="2289e-394">[Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="2289e-395">[Précédent](displaying-data.md)
> [Suivant](entering-data.md)</span><span class="sxs-lookup"><span data-stu-id="2289e-395">[Previous](displaying-data.md)
[Next](entering-data.md)</span></span>
