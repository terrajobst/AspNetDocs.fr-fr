---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/updating-data
title: Présentation de la mise à jour des données de la base de données de pages Web ASP.NET | Microsoft Docs
author: Rick-Anderson
description: Ce didacticiel vous montre comment mettre à jour (modifier) une entrée de base de données existante lorsque vous utilisez pages Web ASP.NET (Razor). Il part du principe que vous avez terminé la série th...
ms.author: riande
ms.date: 01/02/2018
ms.assetid: ac86ec9c-6b69-485b-b9e0-8b9127b13e6b
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/updating-data
msc.type: authoredcontent
ms.openlocfilehash: 8f8bcfb7d9d2416a2699776cadbdaae8e12415ba
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 03/06/2020
ms.locfileid: "78574227"
---
# <a name="introducing-aspnet-web-pages---updating-database-data"></a><span data-ttu-id="8d472-104">Présentation de la mise à jour des données de la base de données pages Web ASP.NET</span><span class="sxs-lookup"><span data-stu-id="8d472-104">Introducing ASP.NET Web Pages - Updating Database Data</span></span>

<span data-ttu-id="8d472-105">par [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="8d472-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="8d472-106">Ce didacticiel vous montre comment mettre à jour (modifier) une entrée de base de données existante lorsque vous utilisez pages Web ASP.NET (Razor).</span><span class="sxs-lookup"><span data-stu-id="8d472-106">This tutorial shows you how to update (change) an existing database entry when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="8d472-107">Elle suppose que vous avez terminé la série [en entrant des données à l’aide de formulaires à l’aide de pages Web ASP.net](entering-data.md).</span><span class="sxs-lookup"><span data-stu-id="8d472-107">It assumes you have completed the series through [Entering Data by Using Forms Using ASP.NET Web Pages](entering-data.md).</span></span>
> 
> <span data-ttu-id="8d472-108">Ce que vous allez apprendre :</span><span class="sxs-lookup"><span data-stu-id="8d472-108">What you'll learn:</span></span>
> 
> - <span data-ttu-id="8d472-109">Comment sélectionner un enregistrement individuel dans le `WebGrid` Helper.</span><span class="sxs-lookup"><span data-stu-id="8d472-109">How to select an individual record in the `WebGrid` helper.</span></span>
> - <span data-ttu-id="8d472-110">Comment lire un enregistrement unique à partir d’une base de données.</span><span class="sxs-lookup"><span data-stu-id="8d472-110">How to read a single record from a database.</span></span>
> - <span data-ttu-id="8d472-111">Comment précharger un formulaire avec les valeurs de l’enregistrement de base de données.</span><span class="sxs-lookup"><span data-stu-id="8d472-111">How to preload a form with values from the database record.</span></span>
> - <span data-ttu-id="8d472-112">Comment mettre à jour un enregistrement existant dans une base de données.</span><span class="sxs-lookup"><span data-stu-id="8d472-112">How to update an existing record in a database.</span></span>
> - <span data-ttu-id="8d472-113">Comment stocker des informations dans la page sans les afficher.</span><span class="sxs-lookup"><span data-stu-id="8d472-113">How to store information in the page without displaying it.</span></span>
> - <span data-ttu-id="8d472-114">Comment utiliser un champ masqué pour stocker des informations.</span><span class="sxs-lookup"><span data-stu-id="8d472-114">How to use a hidden field to store information.</span></span>
>   
> 
> <span data-ttu-id="8d472-115">Fonctionnalités/technologies présentées :</span><span class="sxs-lookup"><span data-stu-id="8d472-115">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="8d472-116">Le programme d’assistance `WebGrid`.</span><span class="sxs-lookup"><span data-stu-id="8d472-116">The `WebGrid` helper.</span></span>
> - <span data-ttu-id="8d472-117">Commande SQL `Update`.</span><span class="sxs-lookup"><span data-stu-id="8d472-117">The SQL `Update` command.</span></span>
> - <span data-ttu-id="8d472-118">Méthode `Database.Execute`</span><span class="sxs-lookup"><span data-stu-id="8d472-118">The `Database.Execute` method.</span></span>
> - <span data-ttu-id="8d472-119">Champs masqués (`<input type="hidden">`).</span><span class="sxs-lookup"><span data-stu-id="8d472-119">Hidden fields (`<input type="hidden">`).</span></span>

## <a name="what-youll-build"></a><span data-ttu-id="8d472-120">Contenu</span><span class="sxs-lookup"><span data-stu-id="8d472-120">What You'll Build</span></span>

<span data-ttu-id="8d472-121">Dans le didacticiel précédent, vous avez appris à ajouter un enregistrement à une base de données.</span><span class="sxs-lookup"><span data-stu-id="8d472-121">In the previous tutorial, you learned how to add a record to a database.</span></span> <span data-ttu-id="8d472-122">Ici, vous allez apprendre à afficher un enregistrement à des fins de modification.</span><span class="sxs-lookup"><span data-stu-id="8d472-122">Here, you'll learn how to display a record for editing.</span></span> <span data-ttu-id="8d472-123">Dans la page *films* , vous allez mettre à jour le `WebGrid` Helper afin qu’il affiche un lien de **modification** en regard de chaque film :</span><span class="sxs-lookup"><span data-stu-id="8d472-123">In the *Movies* page, you'll update the `WebGrid` helper so that it displays an **Edit** link next to each movie:</span></span>

![Affichage de WebGrid incluant un lien « Modifier » pour chaque film](updating-data/_static/image1.png)

<span data-ttu-id="8d472-125">Lorsque vous cliquez sur le lien **modifier** , vous accédez à une page différente, où les informations sur le film se trouvent déjà dans un formulaire :</span><span class="sxs-lookup"><span data-stu-id="8d472-125">When you click the **Edit** link, it takes you to a different page, where the movie information is already in a form:</span></span>

![Page de modification de la vidéo présentant le film à modifier](updating-data/_static/image2.png)

<span data-ttu-id="8d472-127">Vous pouvez modifier n’importe laquelle des valeurs.</span><span class="sxs-lookup"><span data-stu-id="8d472-127">You can change any of the values.</span></span> <span data-ttu-id="8d472-128">Lorsque vous envoyez les modifications, le code de la page met à jour la base de données et vous ramène à la liste des films.</span><span class="sxs-lookup"><span data-stu-id="8d472-128">When you submit the changes, the code in the page updates the database and takes you back to the movie listing.</span></span>

<span data-ttu-id="8d472-129">Cette partie du processus fonctionne presque exactement comme la page *AddMovie. cshtml* que vous avez créée dans le didacticiel précédent, si bien que la majeure partie de ce didacticiel vous sera familière.</span><span class="sxs-lookup"><span data-stu-id="8d472-129">This part of the process works almost exactly like the *AddMovie.cshtml* page you created in the previous tutorial, so much of this tutorial will be familiar.</span></span>

<span data-ttu-id="8d472-130">Il existe plusieurs façons d’implémenter une méthode de modification d’un film individuel.</span><span class="sxs-lookup"><span data-stu-id="8d472-130">There are several ways you could implement a way to edit an individual movie.</span></span> <span data-ttu-id="8d472-131">L’approche indiquée a été choisie, car elle est facile à implémenter et facile à comprendre.</span><span class="sxs-lookup"><span data-stu-id="8d472-131">The approach shown was chosen because it's easy to implement and easy to understand.</span></span>

## <a name="adding-an-edit-link-to-the-movie-listing"></a><span data-ttu-id="8d472-132">Ajout d’un lien modifier à la liste des films</span><span class="sxs-lookup"><span data-stu-id="8d472-132">Adding an Edit Link to the Movie Listing</span></span>

<span data-ttu-id="8d472-133">Pour commencer, vous allez mettre à jour la page *films* afin que chaque liste de films contienne également un lien **modifier** .</span><span class="sxs-lookup"><span data-stu-id="8d472-133">To begin, you'll update the *Movies* page so that each movie listing also contains an **Edit** link.</span></span>

<span data-ttu-id="8d472-134">Ouvrez le fichier *movies. cshtml* .</span><span class="sxs-lookup"><span data-stu-id="8d472-134">Open the *Movies.cshtml* file.</span></span>

<span data-ttu-id="8d472-135">Dans le corps de la page, modifiez le balisage `WebGrid` en ajoutant une colonne.</span><span class="sxs-lookup"><span data-stu-id="8d472-135">In the body of the page, change the `WebGrid` markup by adding a column.</span></span> <span data-ttu-id="8d472-136">Voici le balisage modifié :</span><span class="sxs-lookup"><span data-stu-id="8d472-136">Here's the modified markup:</span></span>

[!code-html[Main](updating-data/samples/sample1.html?highlight=6)]

<span data-ttu-id="8d472-137">La nouvelle colonne est celle-ci :</span><span class="sxs-lookup"><span data-stu-id="8d472-137">The new column is this one:</span></span>

[!code-html[Main](updating-data/samples/sample2.html)]

<span data-ttu-id="8d472-138">Le point de cette colonne est d’afficher un lien (élément`<a>`) dont le texte indique « modifier ».</span><span class="sxs-lookup"><span data-stu-id="8d472-138">The point of this column is to show a link (`<a>` element) whose text says "Edit".</span></span> <span data-ttu-id="8d472-139">Nous allons ensuite créer un lien ressemblant à ce qui suit lors de l’exécution de la page, avec la valeur `id` différente pour chaque film :</span><span class="sxs-lookup"><span data-stu-id="8d472-139">What we're after is to create a link that looks like the following when the page runs, with the `id` value different for each movie:</span></span>

[!code-css[Main](updating-data/samples/sample3.css)]

<span data-ttu-id="8d472-140">Ce lien appellera une page nommée *EditMovie*et passera la chaîne de requête `?id=7` à cette page.</span><span class="sxs-lookup"><span data-stu-id="8d472-140">This link will invoke a page named *EditMovie*, and it will pass the query string `?id=7` to that page.</span></span>

<span data-ttu-id="8d472-141">La syntaxe de la nouvelle colonne peut paraître un peu complexe, mais c’est uniquement parce qu’elle regroupe plusieurs éléments.</span><span class="sxs-lookup"><span data-stu-id="8d472-141">The syntax for the new column might look a bit complex, but that's only because it puts together several elements.</span></span> <span data-ttu-id="8d472-142">Chaque élément individuel est simple.</span><span class="sxs-lookup"><span data-stu-id="8d472-142">Each individual element is straightforward.</span></span> <span data-ttu-id="8d472-143">Si vous vous concentrez uniquement sur l’élément `<a>`, vous voyez le balisage suivant :</span><span class="sxs-lookup"><span data-stu-id="8d472-143">If you concentrate on just the `<a>` element, you see this markup:</span></span>

[!code-html[Main](updating-data/samples/sample4.html)]

<span data-ttu-id="8d472-144">Informations générales sur le fonctionnement de la grille : la grille affiche des lignes, une pour chaque enregistrement de base de données, et affiche des colonnes pour chaque champ de l’enregistrement de base de données.</span><span class="sxs-lookup"><span data-stu-id="8d472-144">Some background about how the grid works: the grid displays rows, one for each database record, and it displays columns for each field in the database record.</span></span> <span data-ttu-id="8d472-145">Pendant la construction de chaque ligne de la grille, l’objet `item` contient l’enregistrement (élément) de la base de données pour cette ligne.</span><span class="sxs-lookup"><span data-stu-id="8d472-145">While each grid row is being constructed, the `item` object contains the database record (item) for that row.</span></span> <span data-ttu-id="8d472-146">Cette organisation vous donne un moyen de code pour accéder aux données de cette ligne.</span><span class="sxs-lookup"><span data-stu-id="8d472-146">This arrangement gives you a way in code to get at the data for that row.</span></span> <span data-ttu-id="8d472-147">C’est ce que vous voyez ici : l’expression `item.ID` obtient la valeur d’ID de l’élément de base de données actuel.</span><span class="sxs-lookup"><span data-stu-id="8d472-147">That's what you see here: the expression `item.ID` is getting the ID value of the current database item.</span></span> <span data-ttu-id="8d472-148">Vous pouvez récupérer les valeurs de la base de données (titre, genre ou année) de la même manière à l’aide de `item.Title`, `item.Genre`ou `item.Year`.</span><span class="sxs-lookup"><span data-stu-id="8d472-148">You could get any of the database values (title, genre, or year) the same way by using `item.Title`, `item.Genre`, or `item.Year`.</span></span>

<span data-ttu-id="8d472-149">L’expression `"~/EditMovie?id=@item.ID` combine la partie codée en dur de l’URL cible (`~/EditMovie?id=`) avec cet ID dérivé de manière dynamique.</span><span class="sxs-lookup"><span data-stu-id="8d472-149">The expression `"~/EditMovie?id=@item.ID` combines the hard-coded part of the target URL (`~/EditMovie?id=`) with this dynamically derived ID.</span></span> <span data-ttu-id="8d472-150">(Vous avez vu l’opérateur `~` dans le didacticiel précédent. il s’agit d’un opérateur ASP.NET qui représente la racine du site Web actuel.)</span><span class="sxs-lookup"><span data-stu-id="8d472-150">(You saw the `~` operator in the previous tutorial; it's an ASP.NET operator that represents the current website root.)</span></span>

<span data-ttu-id="8d472-151">Le résultat est que cette partie du balisage de la colonne produit simplement un aspect semblable au balisage suivant au moment de l’exécution :</span><span class="sxs-lookup"><span data-stu-id="8d472-151">The result is that this part of the markup in the column simply produces something like the following markup at run time:</span></span>

[!code-xml[Main](updating-data/samples/sample5.xml)]

<span data-ttu-id="8d472-152">Naturellement, la valeur réelle de `id` sera différente pour chaque ligne.</span><span class="sxs-lookup"><span data-stu-id="8d472-152">Naturally, the actual value of `id` will be different for each row.</span></span>

## <a name="creating-a-custom-display-for-a-grid-column"></a><span data-ttu-id="8d472-153">Création d’un affichage personnalisé pour une colonne de grille</span><span class="sxs-lookup"><span data-stu-id="8d472-153">Creating a Custom Display for a Grid Column</span></span>

<span data-ttu-id="8d472-154">À présent, revenez à la colonne de la grille.</span><span class="sxs-lookup"><span data-stu-id="8d472-154">Now back to the grid column.</span></span> <span data-ttu-id="8d472-155">Les trois colonnes que vous aviez initialement dans la grille affichaient uniquement des valeurs de données (titre, genre et année).</span><span class="sxs-lookup"><span data-stu-id="8d472-155">The three columns you originally had in the grid displayed only data values (title, genre, and year).</span></span> <span data-ttu-id="8d472-156">Vous avez spécifié cet affichage en passant le nom de la colonne de base de données &mdash; par exemple, `grid.Column("Title")`.</span><span class="sxs-lookup"><span data-stu-id="8d472-156">You specified this display by passing the name of the database column &mdash; for example, `grid.Column("Title")`.</span></span>

<span data-ttu-id="8d472-157">Cette nouvelle colonne de lien de **modification** est différente.</span><span class="sxs-lookup"><span data-stu-id="8d472-157">This new **Edit** link column is different.</span></span> <span data-ttu-id="8d472-158">Au lieu de spécifier un nom de colonne, vous transmettez un paramètre `format`.</span><span class="sxs-lookup"><span data-stu-id="8d472-158">Instead of specifying a column name, you're passing a `format` parameter.</span></span> <span data-ttu-id="8d472-159">Ce paramètre vous permet de définir le balisage que le programme d’assistance de `WebGrid` restituera avec la valeur `item` pour afficher les données de la colonne en gras ou en vert ou dans le format de votre choix.</span><span class="sxs-lookup"><span data-stu-id="8d472-159">This parameter lets you define markup that the `WebGrid` helper will render along with the `item` value to display the column data as bold or green or in whatever format that you want.</span></span> <span data-ttu-id="8d472-160">Par exemple, si vous souhaitez que le titre apparaisse en gras, vous pouvez créer une colonne comme dans l’exemple suivant :</span><span class="sxs-lookup"><span data-stu-id="8d472-160">For example, if you wanted the title to appear bold, you could create a column like this example:</span></span>

[!code-html[Main](updating-data/samples/sample6.html)]

<span data-ttu-id="8d472-161">(Les différents caractères de `@` que vous voyez dans la propriété `format` marquent la transition entre le balisage et une valeur de code.)</span><span class="sxs-lookup"><span data-stu-id="8d472-161">(The various `@` characters you see in the `format` property mark the transition between markup and a code value.)</span></span>

<span data-ttu-id="8d472-162">Une fois que vous connaissez la propriété de `format`, il est plus facile de comprendre comment la nouvelle colonne de lien de **modification** est regroupée :</span><span class="sxs-lookup"><span data-stu-id="8d472-162">Once you know about the `format` property, it's easier to understand how the new **Edit** link column is put together:</span></span>

[!code-html[Main](updating-data/samples/sample7.html)]

<span data-ttu-id="8d472-163">La colonne se compose *uniquement* du balisage qui restitue le lien, ainsi que d’informations (l’ID) extraites de l’enregistrement de la base de données pour la ligne.</span><span class="sxs-lookup"><span data-stu-id="8d472-163">The column consists *only* of the markup that renders the link, plus some information (the ID) that's extracted from the database record for the row.</span></span>

> [!TIP]
> 
> <span data-ttu-id="8d472-164">**Paramètres nommés et paramètres positionnels pour une méthode**</span><span class="sxs-lookup"><span data-stu-id="8d472-164">**Named Parameters and Positional Parameters for a Method**</span></span>
> 
> <span data-ttu-id="8d472-165">Souvent, lorsque vous avez appelé une méthode et que vous lui avez passé des paramètres, vous avez simplement répertorié les valeurs des paramètres en les séparant par des virgules.</span><span class="sxs-lookup"><span data-stu-id="8d472-165">Many times when you've called a method and passed parameters to it, you've simply listed the parameter values separated by commas.</span></span> <span data-ttu-id="8d472-166">Voici quelques exemples :</span><span class="sxs-lookup"><span data-stu-id="8d472-166">Here are a couple of examples:</span></span>
> 
> `db.Execute(insertCommand, title, genre, year)`
> 
> `Validation.RequireField("title", "You must enter a title")`
> 
> <span data-ttu-id="8d472-167">Nous n’avons pas mentionné le problème lorsque vous avez vu ce code pour la première fois, mais dans chaque cas, vous transmettez des paramètres aux méthodes dans un ordre spécifique &mdash;, à savoir, l’ordre dans lequel les paramètres sont définis dans cette méthode.</span><span class="sxs-lookup"><span data-stu-id="8d472-167">We didn't mention the issue when you first saw this code, but in each case, you're passing parameters to the methods in a specific order &mdash; namely, the order in which the parameters are defined in that method.</span></span> <span data-ttu-id="8d472-168">Par `db.Execute` et `Validation.RequireFields`, si vous mélangez l’ordre des valeurs que vous transmettez, vous obtenez un message d’erreur lorsque la page s’exécute ou, au moins, des résultats étranges.</span><span class="sxs-lookup"><span data-stu-id="8d472-168">For `db.Execute` and `Validation.RequireFields`, if you mixed up the order of the values you pass, you'd get an error message when the page runs, or at least some strange results.</span></span> <span data-ttu-id="8d472-169">En clair, vous devez connaître l’ordre dans lequel les paramètres doivent être transmis.</span><span class="sxs-lookup"><span data-stu-id="8d472-169">Clearly, you have to know the order to pass the parameters in.</span></span> <span data-ttu-id="8d472-170">(Dans WebMatrix, IntelliSense peut vous aider à déterminer le nom, le type et l’ordre des paramètres.)</span><span class="sxs-lookup"><span data-stu-id="8d472-170">(In WebMatrix, IntelliSense can help you learn figure out the name, type, and order of the parameters.)</span></span>
> 
> <span data-ttu-id="8d472-171">Comme alternative au passage de valeurs dans l’ordre, vous pouvez utiliser des *paramètres nommés*.</span><span class="sxs-lookup"><span data-stu-id="8d472-171">As an alternative to passing values in order, you can use *named parameters*.</span></span> <span data-ttu-id="8d472-172">(Le passage de paramètres dans l’ordre est connu sous le nom d’utilisation de *paramètres positionnels*.) Pour les paramètres nommés, vous incluez explicitement le nom du paramètre lors du passage de sa valeur.</span><span class="sxs-lookup"><span data-stu-id="8d472-172">(Passing parameters in order is known as using *positional parameters*.) For named parameters, you explicitly include the name of the parameter when passing its value.</span></span> <span data-ttu-id="8d472-173">Vous avez déjà utilisé des paramètres nommés plusieurs fois dans ces didacticiels.</span><span class="sxs-lookup"><span data-stu-id="8d472-173">You've used named parameters already a number of times in these tutorials.</span></span> <span data-ttu-id="8d472-174">Exemple :</span><span class="sxs-lookup"><span data-stu-id="8d472-174">For example:</span></span>
> 
> [!code-csharp[Main](updating-data/samples/sample8.cs)]
> 
> <span data-ttu-id="8d472-175">et</span><span class="sxs-lookup"><span data-stu-id="8d472-175">and</span></span>
> 
> [!code-css[Main](updating-data/samples/sample9.css)]
> 
> <span data-ttu-id="8d472-176">Les paramètres nommés sont pratiques pour plusieurs situations, en particulier lorsqu’une méthode utilise de nombreux paramètres.</span><span class="sxs-lookup"><span data-stu-id="8d472-176">Named parameters are handy for a couple of situations, especially when a method takes many parameters.</span></span> <span data-ttu-id="8d472-177">Vous ne pouvez passer qu’un seul ou deux paramètres, mais les valeurs que vous souhaitez passer ne sont pas parmi les premières positions de la liste de paramètres.</span><span class="sxs-lookup"><span data-stu-id="8d472-177">One is when you want to pass only one or two parameters, but the values you want to pass are not among the first positions in the parameter list.</span></span> <span data-ttu-id="8d472-178">Une autre situation est lorsque vous souhaitez rendre votre code plus lisible en passant les paramètres dans l’ordre qui vous convient le mieux.</span><span class="sxs-lookup"><span data-stu-id="8d472-178">Another situation is when you want to make your code more readable by passing the parameters in the order that makes the most sense to you.</span></span>
> 
> <span data-ttu-id="8d472-179">Évidemment, pour utiliser des paramètres nommés, vous devez connaître les noms des paramètres.</span><span class="sxs-lookup"><span data-stu-id="8d472-179">Obviously, to use named parameters, you have to know the names of the parameters.</span></span> <span data-ttu-id="8d472-180">WebMatrix IntelliSense peut vous *indiquer* les noms, mais il ne peut pas les remplir pour vous.</span><span class="sxs-lookup"><span data-stu-id="8d472-180">WebMatrix IntelliSense can *show* you the names, but it cannot currently fill them in for you.</span></span>

## <a name="creating-the-edit-page"></a><span data-ttu-id="8d472-181">Création de la page de modification</span><span class="sxs-lookup"><span data-stu-id="8d472-181">Creating the Edit Page</span></span>

<span data-ttu-id="8d472-182">Vous pouvez maintenant créer la page *EditMovie* .</span><span class="sxs-lookup"><span data-stu-id="8d472-182">Now you can create the *EditMovie* page.</span></span> <span data-ttu-id="8d472-183">Quand les utilisateurs cliquent sur le lien **modifier** , ils finissent sur cette page.</span><span class="sxs-lookup"><span data-stu-id="8d472-183">When users click the **Edit** link, they'll end up on this page.</span></span>

<span data-ttu-id="8d472-184">Créez une page nommée *EditMovie. cshtml* et remplacez ce qui se trouve dans le fichier par le balisage suivant :</span><span class="sxs-lookup"><span data-stu-id="8d472-184">Create a page named *EditMovie.cshtml* and replace what's in the file with the following markup:</span></span>

[!code-cshtml[Main](updating-data/samples/sample10.cshtml)]

<span data-ttu-id="8d472-185">Ce balisage et ce code sont semblables à ce que vous avez dans la page *AddMovie* .</span><span class="sxs-lookup"><span data-stu-id="8d472-185">This markup and code is similar to what you have in the *AddMovie* page.</span></span> <span data-ttu-id="8d472-186">Il existe une petite différence dans le texte du bouton Envoyer.</span><span class="sxs-lookup"><span data-stu-id="8d472-186">There's a small difference in the text for the submit button.</span></span> <span data-ttu-id="8d472-187">Comme avec la page *AddMovie* , il existe un appel `Html.ValidationSummary` qui affiche les erreurs de validation, le cas échéant.</span><span class="sxs-lookup"><span data-stu-id="8d472-187">As with the *AddMovie* page, there's an `Html.ValidationSummary` call that will display validation errors if there are any.</span></span> <span data-ttu-id="8d472-188">Cette fois, nous laissons des appels à `Validation.Message`, car les erreurs seront affichées dans le résumé des validations.</span><span class="sxs-lookup"><span data-stu-id="8d472-188">This time we're leaving out calls to `Validation.Message`, since errors will be displayed in the validation summary.</span></span> <span data-ttu-id="8d472-189">Comme indiqué dans le didacticiel précédent, vous pouvez utiliser le résumé des validations et les messages d’erreur individuels dans différentes combinaisons.</span><span class="sxs-lookup"><span data-stu-id="8d472-189">As noted in the previous tutorial, you can use the validation summary and the individual error messages in various combinations.</span></span>

<span data-ttu-id="8d472-190">Remarquez que l’attribut `method` de l’élément `<form>` a la valeur `post`.</span><span class="sxs-lookup"><span data-stu-id="8d472-190">Notice again that the `method` attribute of the `<form>` element is set to `post`.</span></span> <span data-ttu-id="8d472-191">Comme avec la page *AddMovie. cshtml* , cette page apporte des modifications à la base de données.</span><span class="sxs-lookup"><span data-stu-id="8d472-191">As with the *AddMovie.cshtml* page, this page makes changes to the database.</span></span> <span data-ttu-id="8d472-192">Par conséquent, ce formulaire doit effectuer une opération de `POST`.</span><span class="sxs-lookup"><span data-stu-id="8d472-192">Therefore, this form should perform a `POST` operation.</span></span> <span data-ttu-id="8d472-193">(Pour plus d’informations sur la différence entre les opérations de `GET` et de `POST`, consultez la barre latérale [sécurité des verbes obtenir, poster et http](form-basics.md#GET,_POST,_and_HTTP_Verb_Safety) dans le didacticiel sur les formulaires HTML.)</span><span class="sxs-lookup"><span data-stu-id="8d472-193">(For more about the difference between `GET` and `POST` operations, see the [GET, POST, and HTTP Verb Safety](form-basics.md#GET,_POST,_and_HTTP_Verb_Safety) sidebar in the tutorial on HTML forms.)</span></span>

<span data-ttu-id="8d472-194">Comme vous l’avez vu dans un didacticiel précédent, les attributs `value` des zones de texte sont définis avec du code Razor afin de les précharger.</span><span class="sxs-lookup"><span data-stu-id="8d472-194">As you saw in an earlier tutorial, the `value` attributes of the text boxes are being set with Razor code in order to preload them.</span></span> <span data-ttu-id="8d472-195">Cette fois, cependant, vous utilisez des variables comme `title` et `genre` pour cette tâche au lieu de `Request.Form["title"]`:</span><span class="sxs-lookup"><span data-stu-id="8d472-195">This time, though, you're using variables like `title` and `genre` for that task instead of `Request.Form["title"]`:</span></span>

`<input type="text" name="title" value="@title" />`

<span data-ttu-id="8d472-196">Comme précédemment, cette balise préchargera les valeurs de zone de texte avec les valeurs de film.</span><span class="sxs-lookup"><span data-stu-id="8d472-196">As before, this markup will preload the text box values with the movie values.</span></span> <span data-ttu-id="8d472-197">Vous verrez dans un moment pourquoi il est pratique d’utiliser des variables cette fois-ci plutôt que d’utiliser l’objet `Request`.</span><span class="sxs-lookup"><span data-stu-id="8d472-197">You'll see in a moment why it's handy to use variables this time instead of using the `Request` object.</span></span>

<span data-ttu-id="8d472-198">Cette page contient également un élément `<input type="hidden">`.</span><span class="sxs-lookup"><span data-stu-id="8d472-198">There's also a `<input type="hidden">` element on this page.</span></span> <span data-ttu-id="8d472-199">Cet élément stocke l’ID de film sans le rendre visible sur la page.</span><span class="sxs-lookup"><span data-stu-id="8d472-199">This element stores the movie ID without making it visible on the page.</span></span> <span data-ttu-id="8d472-200">L’ID est initialement passé à la page à l’aide d’une valeur de chaîne de requête (`?id=7` ou similaire dans l’URL).</span><span class="sxs-lookup"><span data-stu-id="8d472-200">The ID is initially passed to the page by using a query string value (`?id=7` or similar in the URL).</span></span> <span data-ttu-id="8d472-201">En plaçant la valeur d’ID dans un champ masqué, vous pouvez vous assurer qu’elle est disponible lorsque le formulaire est envoyé, même si vous n’avez plus accès à l’URL d’origine avec laquelle la page a été appelée.</span><span class="sxs-lookup"><span data-stu-id="8d472-201">By putting the ID value into a hidden field, you can make sure that it's available when the form is submitted, even if you no longer have access to the original URL that the page was invoked with.</span></span>

<span data-ttu-id="8d472-202">Contrairement à la page *AddMovie* , le code de la page *EditMovie* a deux fonctions distinctes.</span><span class="sxs-lookup"><span data-stu-id="8d472-202">Unlike the *AddMovie* page, the code for the *EditMovie* page has two distinct functions.</span></span> <span data-ttu-id="8d472-203">La première fonction est que, lorsque la page est affichée pour la première fois (et *seulement* ensuite), le code obtient l’ID de la vidéo à partir de la chaîne de requête.</span><span class="sxs-lookup"><span data-stu-id="8d472-203">The first function is that when the page is displayed for the first time (and *only* then), the code gets the movie ID from the query string.</span></span> <span data-ttu-id="8d472-204">Le code utilise ensuite l’ID pour lire le film correspondant dans la base de données et l’afficher (précharger) dans les zones de texte.</span><span class="sxs-lookup"><span data-stu-id="8d472-204">The code then uses the ID to read the corresponding movie out of the database and display (preload) it in the text boxes.</span></span>

<span data-ttu-id="8d472-205">La deuxième fonction est que lorsque l’utilisateur clique sur le bouton **submit changes** , le code doit lire les valeurs des zones de texte et les valider.</span><span class="sxs-lookup"><span data-stu-id="8d472-205">The second function is that when the user clicks the **Submit Changes** button, the code has to read the values of the text boxes and validate them.</span></span> <span data-ttu-id="8d472-206">Le code doit également mettre à jour l’élément de base de données avec les nouvelles valeurs.</span><span class="sxs-lookup"><span data-stu-id="8d472-206">The code also has to update the database item with the new values.</span></span> <span data-ttu-id="8d472-207">Cette technique est similaire à l’ajout d’un enregistrement, comme vous l’avez vu dans *AddMovie*.</span><span class="sxs-lookup"><span data-stu-id="8d472-207">This technique is similar to adding a record, as you saw in *AddMovie*.</span></span>

## <a name="adding-code-to-read-a-single-movie"></a><span data-ttu-id="8d472-208">Ajout de code pour lire un seul film</span><span class="sxs-lookup"><span data-stu-id="8d472-208">Adding Code to Read a Single Movie</span></span>

<span data-ttu-id="8d472-209">Pour exécuter la première fonction, ajoutez le code suivant en haut de la page :</span><span class="sxs-lookup"><span data-stu-id="8d472-209">To perform the first function, add this code to the top of the page:</span></span>

[!code-cshtml[Main](updating-data/samples/sample11.cshtml)]

<span data-ttu-id="8d472-210">La majeure partie de ce code se trouve à l’intérieur d’un bloc qui démarre `if(!IsPost)`.</span><span class="sxs-lookup"><span data-stu-id="8d472-210">Most of this code is inside a block that starts `if(!IsPost)`.</span></span> <span data-ttu-id="8d472-211">L’opérateur de `!` signifie « not ». par conséquent, l’expression signifie *si cette demande n’est pas une soumission de publication*, ce qui est un moyen indirect de dire *si cette demande est la première fois que cette page a été exécutée*.</span><span class="sxs-lookup"><span data-stu-id="8d472-211">The `!` operator means "not," so the expression means *if this request is not a post submission*, which is an indirect way of saying *if this request is the first time that this page has been run*.</span></span> <span data-ttu-id="8d472-212">Comme indiqué précédemment, ce code ne doit s’exécuter *que* lors de la première exécution de la page.</span><span class="sxs-lookup"><span data-stu-id="8d472-212">As noted earlier, this code should run *only* the first time the page runs.</span></span> <span data-ttu-id="8d472-213">Si vous n’avez pas placé le code dans `if(!IsPost)`, il s’exécute à chaque fois que la page est appelée, que la première fois ou en réponse à un clic sur un bouton.</span><span class="sxs-lookup"><span data-stu-id="8d472-213">If you didn't enclose the code in `if(!IsPost)`, it would run every time the page is invoked, whether the first time or in response to a button click.</span></span>

<span data-ttu-id="8d472-214">Notez que le code comprend un bloc `else` cette fois-ci.</span><span class="sxs-lookup"><span data-stu-id="8d472-214">Notice that the code includes an `else` block this time.</span></span> <span data-ttu-id="8d472-215">Comme nous l’avons dit quand nous avons introduit des blocs `if`, vous pouvez parfois exécuter un autre code si la condition que vous testez n’est pas vraie.</span><span class="sxs-lookup"><span data-stu-id="8d472-215">As we said when we introduced `if` blocks, sometimes you want to run alternative code if the condition you're testing isn't true.</span></span> <span data-ttu-id="8d472-216">C’est le cas ici.</span><span class="sxs-lookup"><span data-stu-id="8d472-216">That's the case here.</span></span> <span data-ttu-id="8d472-217">Si la condition est satisfaite (autrement dit, si l’ID passé à la page est OK), vous lisez une ligne de la base de données.</span><span class="sxs-lookup"><span data-stu-id="8d472-217">If the condition passes (that is, if the ID passed to the page is ok), you read a row from the database.</span></span> <span data-ttu-id="8d472-218">Toutefois, si la condition n’est pas satisfaite, le bloc `else` s’exécute et le code définit un message d’erreur.</span><span class="sxs-lookup"><span data-stu-id="8d472-218">However, if the condition doesn't pass, the `else` block runs and the code sets an error message.</span></span>

## <a name="validating-a-value-passed-to-the-page"></a><span data-ttu-id="8d472-219">Validation d’une valeur passée à la page</span><span class="sxs-lookup"><span data-stu-id="8d472-219">Validating a Value Passed to the Page</span></span>

<span data-ttu-id="8d472-220">Le code utilise `Request.QueryString["id"]` pour récupérer l’ID qui est passé à la page.</span><span class="sxs-lookup"><span data-stu-id="8d472-220">The code uses `Request.QueryString["id"]` to get the ID that's passed to the page.</span></span> <span data-ttu-id="8d472-221">Le code vérifie qu’une valeur a bien été passée pour l’ID.</span><span class="sxs-lookup"><span data-stu-id="8d472-221">The code makes sure that a value was actually passed for the ID.</span></span> <span data-ttu-id="8d472-222">Si aucune valeur n’a été passée, le code définit une erreur de validation.</span><span class="sxs-lookup"><span data-stu-id="8d472-222">If no value was passed, the code sets a validation error.</span></span>

<span data-ttu-id="8d472-223">Ce code montre une autre façon de valider des informations.</span><span class="sxs-lookup"><span data-stu-id="8d472-223">This code shows a different way to validate information.</span></span> <span data-ttu-id="8d472-224">Dans le didacticiel précédent, vous travailliez avec le programme d’assistance `Validation`.</span><span class="sxs-lookup"><span data-stu-id="8d472-224">In the previous tutorial, you worked with the `Validation` helper.</span></span> <span data-ttu-id="8d472-225">Vous avez inscrit les champs à valider et ASP.NET automatiquement la validation et les erreurs affichées à l’aide de `Html.ValidationMessage` et `Html.ValidationSummary`.</span><span class="sxs-lookup"><span data-stu-id="8d472-225">You registered fields to validate, and ASP.NET automatically did the validation and displayed errors by using `Html.ValidationMessage` and `Html.ValidationSummary`.</span></span> <span data-ttu-id="8d472-226">Dans ce cas, toutefois, vous ne validez pas véritablement l’entrée de l’utilisateur.</span><span class="sxs-lookup"><span data-stu-id="8d472-226">In this case, however, you're not really validating user input.</span></span> <span data-ttu-id="8d472-227">Au lieu de cela, vous validez une valeur qui a été transmise à la page à partir d’un autre emplacement.</span><span class="sxs-lookup"><span data-stu-id="8d472-227">Instead, you're validating a value that was passed to the page from elsewhere.</span></span> <span data-ttu-id="8d472-228">Le programme d’assistance `Validation` ne le fait pas pour vous.</span><span class="sxs-lookup"><span data-stu-id="8d472-228">The `Validation` helper doesn't do that for you.</span></span>

<span data-ttu-id="8d472-229">Par conséquent, vous vérifiez la valeur vous-même, en la testant avec `if(!Request.QueryString["ID"].IsEmpty()`).</span><span class="sxs-lookup"><span data-stu-id="8d472-229">Therefore, you check the value yourself, by testing it with `if(!Request.QueryString["ID"].IsEmpty()`).</span></span> <span data-ttu-id="8d472-230">En cas de problème, vous pouvez afficher l’erreur à l’aide de `Html.ValidationSummary`, comme vous l’avez fait avec l’application auxiliaire `Validation`.</span><span class="sxs-lookup"><span data-stu-id="8d472-230">If there's a problem, you can display the error by using `Html.ValidationSummary`, as you did with the `Validation` helper.</span></span> <span data-ttu-id="8d472-231">Pour ce faire, appelez `Validation.AddFormError` et transmettez-lui un message à afficher.</span><span class="sxs-lookup"><span data-stu-id="8d472-231">To do that, you call `Validation.AddFormError` and pass it a message to display.</span></span> <span data-ttu-id="8d472-232">`Validation.AddFormError` est une méthode intégrée qui vous permet de définir des messages personnalisés qui s’intègrent au système de validation que vous connaissez déjà.</span><span class="sxs-lookup"><span data-stu-id="8d472-232">`Validation.AddFormError` is a built-in method that lets you define custom messages that tie in with the validation system you're already familiar with.</span></span> <span data-ttu-id="8d472-233">(Plus loin dans ce didacticiel, nous décrirons comment rendre ce processus de validation un peu plus robuste.)</span><span class="sxs-lookup"><span data-stu-id="8d472-233">(Later in this tutorial we'll talk about how to make this validation process a little more robust.)</span></span>

<span data-ttu-id="8d472-234">Après avoir vérifié qu’il existe un ID pour le film, le code lit la base de données, en recherchant un seul élément de base de données.</span><span class="sxs-lookup"><span data-stu-id="8d472-234">After making sure that there's an ID for the movie, the code reads the database, looking for only a single database item.</span></span> <span data-ttu-id="8d472-235">(Vous avez probablement remarqué le modèle général pour les opérations de base de données : Ouvrez la base de données, définissez une instruction SQL, puis exécutez l’instruction.) Cette fois-ci, l’instruction SQL `Select` comprend `WHERE ID = @0`.</span><span class="sxs-lookup"><span data-stu-id="8d472-235">(You probably have noticed the general pattern for database operations: open the database, define a SQL statement, and run the statement.) This time, the SQL `Select` statement includes `WHERE ID = @0`.</span></span> <span data-ttu-id="8d472-236">Étant donné que l’ID est unique, un seul enregistrement peut être retourné.</span><span class="sxs-lookup"><span data-stu-id="8d472-236">Because the ID is unique, only one record can be returned.</span></span>

<span data-ttu-id="8d472-237">La requête est exécutée à l’aide de `db.QuerySingle` (non `db.Query`, comme vous l’avez fait pour la liste des films) et le code place le résultat dans la variable `row`.</span><span class="sxs-lookup"><span data-stu-id="8d472-237">The query is performed by using `db.QuerySingle` (not `db.Query`, as you used for the movie listing), and the code puts the result into the `row` variable.</span></span> <span data-ttu-id="8d472-238">Le nom `row` est arbitraire ; vous pouvez nommer les variables comme vous le souhaitez.</span><span class="sxs-lookup"><span data-stu-id="8d472-238">The name `row` is arbitrary; you can name the variables anything you like.</span></span> <span data-ttu-id="8d472-239">Les variables initialisées en haut sont ensuite remplies avec les détails du film afin que ces valeurs puissent être affichées dans les zones de texte.</span><span class="sxs-lookup"><span data-stu-id="8d472-239">The variables initialized at the top are then filled with the movie details so that these values can be displayed in the text boxes.</span></span>

## <a name="testing-the-edit-page-so-far"></a><span data-ttu-id="8d472-240">Test de la page de modification (jusqu’à présent)</span><span class="sxs-lookup"><span data-stu-id="8d472-240">Testing the Edit Page (So Far)</span></span>

<span data-ttu-id="8d472-241">Si vous souhaitez tester votre page, exécutez la page *films* maintenant et cliquez sur un lien **modifier** en regard de tout film.</span><span class="sxs-lookup"><span data-stu-id="8d472-241">If you'd like to test your page, run the *Movies* page now and click an **Edit** link next to any movie.</span></span> <span data-ttu-id="8d472-242">Vous verrez la page *EditMovie* avec les détails renseignés pour le film que vous avez sélectionné :</span><span class="sxs-lookup"><span data-stu-id="8d472-242">You'll see the *EditMovie* page with the details filled in for the movie you selected:</span></span>

![Page de modification de la vidéo présentant le film à modifier](updating-data/_static/image3.png)

<span data-ttu-id="8d472-244">Notez que l’URL de la page comprend des éléments tels que `?id=10` (ou un autre nombre).</span><span class="sxs-lookup"><span data-stu-id="8d472-244">Notice that the URL of the page includes something like `?id=10` (or some other number).</span></span> <span data-ttu-id="8d472-245">Jusqu’à présent, vous avez testé que les liens de **modification** dans la page de *film* fonctionnent, que votre page lit l’ID à partir de la chaîne de requête et que la requête de base de données pour obtenir un seul enregistrement de film fonctionne.</span><span class="sxs-lookup"><span data-stu-id="8d472-245">So far you've tested that **Edit** links in the *Movie* page work, that your page is reading the ID from the query string, and that the database query to get a single movie record is working.</span></span>

<span data-ttu-id="8d472-246">Vous pouvez modifier les informations du film, mais rien ne se produit lorsque vous cliquez sur **soumettre les modifications**.</span><span class="sxs-lookup"><span data-stu-id="8d472-246">You can change the movie information, but nothing happens when you click **Submit Changes**.</span></span>

## <a name="adding-code-to-update-the-movie-with-the-users-changes"></a><span data-ttu-id="8d472-247">Ajout de code pour mettre à jour le film avec les modifications de l’utilisateur</span><span class="sxs-lookup"><span data-stu-id="8d472-247">Adding Code to Update the Movie with the User's Changes</span></span>

<span data-ttu-id="8d472-248">Dans le fichier *EditMovie. cshtml* , pour implémenter la deuxième fonction (enregistrement des modifications), ajoutez le code suivant à l’intérieur de l’accolade fermante du bloc `@`.</span><span class="sxs-lookup"><span data-stu-id="8d472-248">In the *EditMovie.cshtml* file, to implement the second function (saving changes), add the following code just inside the closing brace of the `@` block.</span></span> <span data-ttu-id="8d472-249">(Si vous ne savez pas exactement où placer le code, vous pouvez consulter la [liste complète du code pour la page modifier le film](#Complete_Page_Listing_for_EditMovie) qui s’affiche à la fin de ce didacticiel.)</span><span class="sxs-lookup"><span data-stu-id="8d472-249">(If you're not sure exactly where to put the code, you can look at the [complete code listing for the Edit Movie page](#Complete_Page_Listing_for_EditMovie) that appears at the end of this tutorial.)</span></span>

[!code-csharp[Main](updating-data/samples/sample12.cs)]

<span data-ttu-id="8d472-250">Là encore, le balisage et le code sont similaires au code de *AddMovie*.</span><span class="sxs-lookup"><span data-stu-id="8d472-250">Again, this markup and code is similar to the code in *AddMovie*.</span></span> <span data-ttu-id="8d472-251">Le code se trouve dans un bloc `if(IsPost)`, car ce code s’exécute uniquement quand l’utilisateur clique sur le bouton **submit changes** &mdash; autrement dit, lorsque (et uniquement lorsque) le formulaire a été publié.</span><span class="sxs-lookup"><span data-stu-id="8d472-251">The code is in an `if(IsPost)` block, because this code runs only when the user clicks the **Submit Changes** button &mdash; that is, when (and only when) the form has been posted.</span></span> <span data-ttu-id="8d472-252">Dans ce cas, vous n’utilisez pas un test comme `if(IsPost && Validation.IsValid())`, autrement dit, vous ne combinez pas les deux tests à l’aide de et.</span><span class="sxs-lookup"><span data-stu-id="8d472-252">In this case, you're not using a test like `if(IsPost && Validation.IsValid())`— that is, you're not combining both tests by using AND.</span></span> <span data-ttu-id="8d472-253">Dans cette page, vous déterminez tout d’abord s’il existe un envoi de formulaire (`if(IsPost)`) et que vous enregistrez uniquement les champs à des fins de validation.</span><span class="sxs-lookup"><span data-stu-id="8d472-253">In this page, you first determine whether there's a form submission (`if(IsPost)`), and only then register the fields for validation.</span></span> <span data-ttu-id="8d472-254">Vous pouvez ensuite tester les résultats de la validation (`if(Validation.IsValid()`).</span><span class="sxs-lookup"><span data-stu-id="8d472-254">Then you can test the validation results (`if(Validation.IsValid()`).</span></span> <span data-ttu-id="8d472-255">Le Flow est légèrement différent de celui de la page *AddMovie. cshtml* , mais l’effet est le même.</span><span class="sxs-lookup"><span data-stu-id="8d472-255">The flow is slightly different than in the *AddMovie.cshtml* page, but the effect is the same.</span></span>

<span data-ttu-id="8d472-256">Vous pouvez obtenir les valeurs des zones de texte à l’aide de `Request.Form["title"]` et de code similaire pour les autres éléments `<input>`.</span><span class="sxs-lookup"><span data-stu-id="8d472-256">You get the values of the text boxes by using `Request.Form["title"]` and similar code for the other `<input>` elements.</span></span> <span data-ttu-id="8d472-257">Notez que cette fois-ci, le code obtient l’ID de film hors du champ masqué (`<input type="hidden">`).</span><span class="sxs-lookup"><span data-stu-id="8d472-257">Notice that this time, the code gets the movie ID out of the hidden field (`<input type="hidden">`).</span></span> <span data-ttu-id="8d472-258">Lorsque la page s’est exécutée pour la première fois, le code a obtenu l’ID de la chaîne de requête.</span><span class="sxs-lookup"><span data-stu-id="8d472-258">When the page ran the first time, the code got the ID out of the query string.</span></span> <span data-ttu-id="8d472-259">Vous obtenez la valeur du champ masqué pour vous assurer que vous obtenez l’ID du film qui a été affiché à l’origine, en cas de modification de la chaîne de requête depuis.</span><span class="sxs-lookup"><span data-stu-id="8d472-259">You get the value from the hidden field to make sure that you're getting the ID of the movie that was originally displayed, in case the query string was somehow altered since then.</span></span>

<span data-ttu-id="8d472-260">La différence très importante entre le code *AddMovie* et ce code est que dans ce code vous utilisez l’instruction SQL `Update` au lieu de l’instruction `Insert Into`.</span><span class="sxs-lookup"><span data-stu-id="8d472-260">The really important difference between the *AddMovie* code and this code is that in this code you use the SQL `Update` statement instead of the `Insert Into` statement.</span></span> <span data-ttu-id="8d472-261">L’exemple suivant illustre la syntaxe de l’instruction SQL `Update` :</span><span class="sxs-lookup"><span data-stu-id="8d472-261">The following example shows the syntax of the SQL `Update` statement:</span></span>

`UPDATE table SET col1="value", col2="value", col3="value" ... WHERE ID = value`

<span data-ttu-id="8d472-262">Vous pouvez spécifier toutes les colonnes dans n’importe quel ordre et vous n’avez pas nécessairement à mettre à jour chaque colonne pendant une opération de `Update`.</span><span class="sxs-lookup"><span data-stu-id="8d472-262">You can specify any columns in any order, and you don't necessarily have to update every column during an `Update` operation.</span></span> <span data-ttu-id="8d472-263">(Vous ne pouvez pas mettre à jour l’ID lui-même, car cela permet d’enregistrer l’enregistrement en tant que nouvel enregistrement, ce qui n’est pas autorisé pour une opération `Update`.)</span><span class="sxs-lookup"><span data-stu-id="8d472-263">(You cannot update the ID itself, because that would in effect save the record as a new record, and that's not allowed for an `Update` operation.)</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="8d472-264">**Important** La clause `Where` avec l’ID est très importante, car c’est comme cela que la base de données sait quel enregistrement de base de données vous souhaitez mettre à jour.</span><span class="sxs-lookup"><span data-stu-id="8d472-264">**Important** The `Where` clause with the ID is very important, because that's how the database knows which database record you want to update.</span></span> <span data-ttu-id="8d472-265">Si vous avez quitté la clause `Where`, la base de données met à jour *chaque* enregistrement dans la base de données.</span><span class="sxs-lookup"><span data-stu-id="8d472-265">If you left off the `Where` clause, the database would update *every* record in the database.</span></span> <span data-ttu-id="8d472-266">Dans la plupart des cas, il s’agit d’un incident.</span><span class="sxs-lookup"><span data-stu-id="8d472-266">In most cases, that would be a disaster.</span></span>

<span data-ttu-id="8d472-267">Dans le code, les valeurs à mettre à jour sont transmises à l’instruction SQL à l’aide d’espaces réservés.</span><span class="sxs-lookup"><span data-stu-id="8d472-267">In the code, the values to update are passed to the SQL statement by using placeholders.</span></span> <span data-ttu-id="8d472-268">Pour répéter ce que nous avons dit précédemment : pour des raisons de sécurité, utilisez *uniquement* des espaces réservés pour passer des valeurs à une instruction SQL.</span><span class="sxs-lookup"><span data-stu-id="8d472-268">To repeat what we've said before: for security reasons, *only* use placeholders to pass values to a SQL statement.</span></span>

<span data-ttu-id="8d472-269">Une fois que le code a utilisé `db.Execute` pour exécuter l’instruction `Update`, il redirige vers la page de liste, où vous pouvez voir les modifications.</span><span class="sxs-lookup"><span data-stu-id="8d472-269">After the code uses `db.Execute` to run the `Update` statement, it redirects back to the listing page, where you can see the changes.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="8d472-270">**Différentes instructions SQL, différentes méthodes**</span><span class="sxs-lookup"><span data-stu-id="8d472-270">**Different SQL Statements, Different Methods**</span></span>
> 
> <span data-ttu-id="8d472-271">Vous avez peut-être remarqué que vous utilisez des méthodes légèrement différentes pour exécuter différentes instructions SQL.</span><span class="sxs-lookup"><span data-stu-id="8d472-271">You might have noticed that you use slightly different methods to run different SQL statements.</span></span> <span data-ttu-id="8d472-272">Pour exécuter une requête `Select` qui retourne potentiellement plusieurs enregistrements, vous utilisez la méthode `Query`.</span><span class="sxs-lookup"><span data-stu-id="8d472-272">To run a `Select` query that potentially returns multiple records, you use the `Query` method.</span></span> <span data-ttu-id="8d472-273">Pour exécuter une requête `Select` dont vous savez qu’elle ne renverra qu’un seul élément de base de données, vous utilisez la méthode `QuerySingle`.</span><span class="sxs-lookup"><span data-stu-id="8d472-273">To run a `Select` query that you know will return only one database item, you use the `QuerySingle` method.</span></span> <span data-ttu-id="8d472-274">Pour exécuter des commandes qui apportent des modifications, mais qui ne retournent pas d’éléments de base de données, vous utilisez la méthode `Execute`.</span><span class="sxs-lookup"><span data-stu-id="8d472-274">To run commands that make changes but that don't return database items, you use the `Execute` method.</span></span>
> 
> <span data-ttu-id="8d472-275">Vous devez avoir différentes méthodes, car chacune d’elles retourne des résultats différents, comme vous l’avez vu déjà dans la différence entre les `Query` et les `QuerySingle`.</span><span class="sxs-lookup"><span data-stu-id="8d472-275">You have to have different methods because each of them returns different results, as you saw already in the difference between `Query` and `QuerySingle`.</span></span> <span data-ttu-id="8d472-276">(La méthode `Execute` retourne en fait une valeur également &mdash;, à savoir, le nombre de lignes de base de données affectées par la commande &mdash; mais vous l’avez ignorée jusqu’à présent.)</span><span class="sxs-lookup"><span data-stu-id="8d472-276">(The `Execute` method actually returns a value also &mdash; namely, the number of database rows that were affected by the command &mdash; but you've been ignoring that so far.)</span></span>
> 
> <span data-ttu-id="8d472-277">Bien entendu, la méthode `Query` ne peut retourner qu’une seule ligne de base de données.</span><span class="sxs-lookup"><span data-stu-id="8d472-277">Of course, the `Query` method might return only one database row.</span></span> <span data-ttu-id="8d472-278">Toutefois, ASP.NET traite toujours les résultats de la méthode `Query` en tant que collection.</span><span class="sxs-lookup"><span data-stu-id="8d472-278">However, ASP.NET always treats the results of the `Query` method as a collection.</span></span> <span data-ttu-id="8d472-279">Même si la méthode ne retourne qu’une seule ligne, vous devez extraire cette seule ligne de la collection.</span><span class="sxs-lookup"><span data-stu-id="8d472-279">Even if the method returns just one row, you have to extract that single row from the collection.</span></span> <span data-ttu-id="8d472-280">Par conséquent, dans les situations où vous *savez* que vous n’obtiendrez qu’une seule ligne, il est un peu plus pratique d’utiliser `QuerySingle`.</span><span class="sxs-lookup"><span data-stu-id="8d472-280">Therefore, in situations where you *know* you'll get back only one row, it's a bit more convenient to use `QuerySingle`.</span></span>
> 
> <span data-ttu-id="8d472-281">Il existe d’autres méthodes qui effectuent des types spécifiques d’opérations de base de données.</span><span class="sxs-lookup"><span data-stu-id="8d472-281">There are a few other methods that perform specific types of database operations.</span></span> <span data-ttu-id="8d472-282">Vous trouverez une liste des méthodes de base de données dans la [référence rapide de l’API pages Web ASP.net](../../api-reference/asp-net-web-pages-api-reference.md#Data).</span><span class="sxs-lookup"><span data-stu-id="8d472-282">You can find a listing of database methods in the [ASP.NET Web Pages API Quick Reference](../../api-reference/asp-net-web-pages-api-reference.md#Data).</span></span>

## <a name="making-validation-for-the-id-more-robust"></a><span data-ttu-id="8d472-283">Rendre la validation de l’ID plus robuste</span><span class="sxs-lookup"><span data-stu-id="8d472-283">Making Validation for the ID More Robust</span></span>

<span data-ttu-id="8d472-284">La première fois que la page est exécutée, vous recevez l’ID de la vidéo à partir de la chaîne de requête afin de pouvoir obtenir ce film à partir de la base de données.</span><span class="sxs-lookup"><span data-stu-id="8d472-284">The first time that the page runs, you get the movie ID from the query string so that you can go get that movie from the database.</span></span> <span data-ttu-id="8d472-285">Vous avez fait en sorte qu’il y ait une valeur à rechercher, ce que vous avez fait à l’aide de ce code :</span><span class="sxs-lookup"><span data-stu-id="8d472-285">You made sure that there actually was a value to go look for, which you did by using this code:</span></span>

[!code-csharp[Main](updating-data/samples/sample13.cs)]

<span data-ttu-id="8d472-286">Vous avez utilisé ce code pour vous assurer que si un utilisateur parviendra à la page *EditMovies* sans sélectionner d’abord un film dans la page *films* , la page afficherait un message d’erreur convivial.</span><span class="sxs-lookup"><span data-stu-id="8d472-286">You used this code to make sure that if a user gets to the *EditMovies* page without first selecting a movie in the *Movies* page, the page would display a user-friendly error message.</span></span> <span data-ttu-id="8d472-287">(Dans le cas contraire, les utilisateurs verraient une erreur qui pourrait simplement les confondre.)</span><span class="sxs-lookup"><span data-stu-id="8d472-287">(Otherwise, users would see an error that would probably just confuse them.)</span></span>

<span data-ttu-id="8d472-288">Toutefois, cette validation n’est pas très fiable.</span><span class="sxs-lookup"><span data-stu-id="8d472-288">However, this validation isn't very robust.</span></span> <span data-ttu-id="8d472-289">La page peut également être appelée avec les erreurs suivantes :</span><span class="sxs-lookup"><span data-stu-id="8d472-289">The page might also be invoked with these errors:</span></span>

- <span data-ttu-id="8d472-290">L’ID n’est pas un nombre.</span><span class="sxs-lookup"><span data-stu-id="8d472-290">The ID isn't a number.</span></span> <span data-ttu-id="8d472-291">Par exemple, la page peut être appelée avec une URL telle que `http://localhost:nnnnn/EditMovie?id=abc`.</span><span class="sxs-lookup"><span data-stu-id="8d472-291">For example, the page could be invoked with a URL like `http://localhost:nnnnn/EditMovie?id=abc`.</span></span>
- <span data-ttu-id="8d472-292">L’ID est un nombre, mais il fait référence à un film qui n’existe pas (par exemple, `http://localhost:nnnnn/EditMovie?id=100934`).</span><span class="sxs-lookup"><span data-stu-id="8d472-292">The ID is a number, but it references a movie that doesn't exist (for example, `http://localhost:nnnnn/EditMovie?id=100934`).</span></span>

<span data-ttu-id="8d472-293">Si vous êtes curieux de voir les erreurs qui résultent de ces URL, exécutez la page *films* .</span><span class="sxs-lookup"><span data-stu-id="8d472-293">If you're curious to see the errors that result from these URLs, run the *Movies* page.</span></span> <span data-ttu-id="8d472-294">Sélectionnez un film à modifier, puis remplacez l’URL de la page *EditMovie* par une URL qui contient un ID alphabétique ou l’ID d’un film inexistant.</span><span class="sxs-lookup"><span data-stu-id="8d472-294">Select a movie to edit, and then change the URL of the *EditMovie* page to a URL that contains an alphabetic ID or the ID of a non-existent movie.</span></span>

<span data-ttu-id="8d472-295">Que devez-vous faire ?</span><span class="sxs-lookup"><span data-stu-id="8d472-295">So what should you do?</span></span> <span data-ttu-id="8d472-296">Le premier correctif consiste à s’assurer que non seulement un ID est passé à la page, mais que l’ID est un entier.</span><span class="sxs-lookup"><span data-stu-id="8d472-296">The first fix is to make sure that not only is an ID passed to the page, but that the ID is an integer.</span></span> <span data-ttu-id="8d472-297">Modifiez le code du test `!IsPost` pour qu’il ressemble à l’exemple suivant :</span><span class="sxs-lookup"><span data-stu-id="8d472-297">Change the code for the `!IsPost` test to look like this example:</span></span>

[!code-csharp[Main](updating-data/samples/sample14.cs)]

<span data-ttu-id="8d472-298">Vous avez ajouté une deuxième condition au test `IsEmpty`, liée à `&&` (AND logique) :</span><span class="sxs-lookup"><span data-stu-id="8d472-298">You've added a second condition to the `IsEmpty` test, linked with `&&` (logical AND):</span></span>

[!code-csharp[Main](updating-data/samples/sample15.cs)]

<span data-ttu-id="8d472-299">Vous vous souvenez peut-être du didacticiel [Introduction à la programmation de pages Web ASP.net](../introducing-razor-syntax-c.md) que les méthodes comme `AsBool` une `AsInt` convertir une chaîne de caractères en un autre type de données.</span><span class="sxs-lookup"><span data-stu-id="8d472-299">You might remember from the [Introduction to ASP.NET Web Pages Programming](../introducing-razor-syntax-c.md) tutorial that methods like `AsBool` an `AsInt` convert a character string to some other data type.</span></span> <span data-ttu-id="8d472-300">La méthode `IsInt` (ainsi que d’autres, comme `IsBool` et `IsDateTime`) sont similaires.</span><span class="sxs-lookup"><span data-stu-id="8d472-300">The `IsInt` method (and others, like `IsBool` and `IsDateTime`) are similar.</span></span> <span data-ttu-id="8d472-301">Toutefois, ils testent uniquement si vous *pouvez* convertir la chaîne, sans réellement effectuer la conversion.</span><span class="sxs-lookup"><span data-stu-id="8d472-301">However, they test only whether you *can* convert the string, without actually performing the conversion.</span></span> <span data-ttu-id="8d472-302">Ici, vous dites essentiellement *si la valeur de la chaîne de requête peut être convertie en un entier...* .</span><span class="sxs-lookup"><span data-stu-id="8d472-302">So here you're essentially saying *If the query string value can be converted to an integer ...*.</span></span>

<span data-ttu-id="8d472-303">L’autre problème potentiel consiste à rechercher un film qui n’existe pas.</span><span class="sxs-lookup"><span data-stu-id="8d472-303">The other potential problem is looking for a movie that doesn't exist.</span></span> <span data-ttu-id="8d472-304">Le code permettant d’obtenir un film ressemble à ce code :</span><span class="sxs-lookup"><span data-stu-id="8d472-304">The code to get a movie looks like this code:</span></span>

[!code-csharp[Main](updating-data/samples/sample16.cs)]

<span data-ttu-id="8d472-305">Si vous transmettez une valeur `movieId` à la méthode `QuerySingle` qui ne correspond pas à un film réel, rien n’est retourné et les instructions qui suivent (par exemple, `title=row.Title`) génèrent des erreurs.</span><span class="sxs-lookup"><span data-stu-id="8d472-305">If you pass a `movieId` value to the `QuerySingle` method that doesn't correspond to an actual movie, nothing is returned and the statements that follow (for example, `title=row.Title`) result in errors.</span></span>

<span data-ttu-id="8d472-306">Là encore, il existe un correctif simple.</span><span class="sxs-lookup"><span data-stu-id="8d472-306">Again there's an easy fix.</span></span> <span data-ttu-id="8d472-307">Si la méthode `db.QuerySingle` ne retourne aucun résultat, la variable `row` a la valeur null.</span><span class="sxs-lookup"><span data-stu-id="8d472-307">If the `db.QuerySingle` method returns no results, the `row` variable will be null.</span></span> <span data-ttu-id="8d472-308">Ainsi, vous pouvez vérifier si la variable `row` est null avant d’essayer d’en extraire des valeurs.</span><span class="sxs-lookup"><span data-stu-id="8d472-308">So you can check whether the `row` variable is null before you try to get values from it.</span></span> <span data-ttu-id="8d472-309">Le code suivant ajoute un bloc `if` autour des instructions qui récupèrent les valeurs de l’objet `row` :</span><span class="sxs-lookup"><span data-stu-id="8d472-309">The following code adds an `if` block around the statements that get the values out of the `row` object:</span></span>

[!code-csharp[Main](updating-data/samples/sample17.cs)]

<span data-ttu-id="8d472-310">Avec ces deux tests de validation supplémentaires, la page devient plus une preuve de la liste à puces.</span><span class="sxs-lookup"><span data-stu-id="8d472-310">With these two additional validation tests, the page becomes more bullet-proof.</span></span> <span data-ttu-id="8d472-311">Le code complet de la branche `!IsPost` ressemble maintenant à l’exemple suivant :</span><span class="sxs-lookup"><span data-stu-id="8d472-311">The complete code for the `!IsPost` branch now looks like this example:</span></span>

[!code-csharp[Main](updating-data/samples/sample18.cs)]

<span data-ttu-id="8d472-312">Nous noterons une fois de plus que cette tâche est une bonne utilisation d’un bloc de `else`.</span><span class="sxs-lookup"><span data-stu-id="8d472-312">We'll note once more that this task is a good use for an `else` block.</span></span> <span data-ttu-id="8d472-313">Si les tests ne réussissent pas, les blocs de `else` définissent des messages d’erreur.</span><span class="sxs-lookup"><span data-stu-id="8d472-313">If the tests don't pass, the `else` blocks set error messages.</span></span>

## <a name="adding-a-link-to-return-to-the-movies-page"></a><span data-ttu-id="8d472-314">Ajout d’un lien pour revenir à la page films</span><span class="sxs-lookup"><span data-stu-id="8d472-314">Adding a Link to Return to the Movies Page</span></span>

<span data-ttu-id="8d472-315">Un détail final et utile consiste à ajouter un lien à la page *films* .</span><span class="sxs-lookup"><span data-stu-id="8d472-315">A final and helpful detail is to add a link back to the *Movies* page.</span></span> <span data-ttu-id="8d472-316">Dans le workflow ordinaire des événements, les utilisateurs démarrent à la page *films* et cliquent sur un lien **modifier** .</span><span class="sxs-lookup"><span data-stu-id="8d472-316">In the ordinary flow of events, users will start at the *Movies* page and click an **Edit** link.</span></span> <span data-ttu-id="8d472-317">Cela les amène à la page *EditMovie* , où ils peuvent modifier le film et cliquer sur le bouton.</span><span class="sxs-lookup"><span data-stu-id="8d472-317">That brings them to the *EditMovie* page, where they can edit the movie and click the button.</span></span> <span data-ttu-id="8d472-318">Une fois que le code a traité la modification, il redirige vers la page *films* .</span><span class="sxs-lookup"><span data-stu-id="8d472-318">After the code processes the change, it redirects back to the *Movies* page.</span></span>

<span data-ttu-id="8d472-319">Cependant :</span><span class="sxs-lookup"><span data-stu-id="8d472-319">However:</span></span>

- <span data-ttu-id="8d472-320">L’utilisateur peut décider de ne pas modifier quoi que ce soit.</span><span class="sxs-lookup"><span data-stu-id="8d472-320">The user might decide not to change anything.</span></span>
- <span data-ttu-id="8d472-321">Il se peut que l’utilisateur ait obtenu cette page sans cliquer d’abord sur un lien **modifier** dans la page *films* .</span><span class="sxs-lookup"><span data-stu-id="8d472-321">The user might have gotten to this page without first clicking an **Edit** link in the *Movies* page.</span></span>

<span data-ttu-id="8d472-322">Dans les deux cas, vous souhaitez qu’ils reviennent facilement à la liste principale.</span><span class="sxs-lookup"><span data-stu-id="8d472-322">Either way, you want to make it easy for them to return to the main listing.</span></span> <span data-ttu-id="8d472-323">Il s’agit d’un correctif simple &mdash; ajouter le balisage suivant juste après la balise de fermeture `</form>` dans le balisage :</span><span class="sxs-lookup"><span data-stu-id="8d472-323">It's an easy fix &mdash; add the following markup just after the closing `</form>` tag in the markup:</span></span>

[!code-html[Main](updating-data/samples/sample19.html)]

<span data-ttu-id="8d472-324">Ce balisage utilise la même syntaxe pour un élément `<a>` que vous avez vu ailleurs.</span><span class="sxs-lookup"><span data-stu-id="8d472-324">This markup uses the same syntax for an `<a>` element that you've seen elsewhere.</span></span> <span data-ttu-id="8d472-325">L’URL comprend `~` pour signifier « la racine du site Web ».</span><span class="sxs-lookup"><span data-stu-id="8d472-325">The URL includes `~` to mean "root of the website."</span></span>

## <a name="testing-the-movie-update-process"></a><span data-ttu-id="8d472-326">Test du processus de mise à jour des films</span><span class="sxs-lookup"><span data-stu-id="8d472-326">Testing the Movie Update Process</span></span>

<span data-ttu-id="8d472-327">Vous pouvez maintenant tester.</span><span class="sxs-lookup"><span data-stu-id="8d472-327">Now you can test.</span></span> <span data-ttu-id="8d472-328">Exécutez la page *films* , puis cliquez sur **modifier** en regard d’un film.</span><span class="sxs-lookup"><span data-stu-id="8d472-328">Run the *Movies* page, and click **Edit** next to a movie.</span></span> <span data-ttu-id="8d472-329">Lorsque la page *EditMovie* s’affiche, modifiez le film et cliquez sur **soumettre les modifications**.</span><span class="sxs-lookup"><span data-stu-id="8d472-329">When the *EditMovie* page appears, make changes to the movie and click **Submit Changes**.</span></span> <span data-ttu-id="8d472-330">Lorsque la liste des films s’affiche, vérifiez que vos modifications sont affichées.</span><span class="sxs-lookup"><span data-stu-id="8d472-330">When the movie listing appears, make sure that your changes are shown.</span></span>

<span data-ttu-id="8d472-331">Pour vous assurer que la validation fonctionne, cliquez sur **modifier** pour un autre film.</span><span class="sxs-lookup"><span data-stu-id="8d472-331">To make sure that validation is working, click **Edit** for another movie.</span></span> <span data-ttu-id="8d472-332">Lorsque vous accédez à la page *EditMovie* , effacez le champ **genre** (ou le champ **year** , ou les deux) et essayez d’envoyer vos modifications.</span><span class="sxs-lookup"><span data-stu-id="8d472-332">When you get to the *EditMovie* page, clear the **Genre** field (or **Year** field, or both) and try to submit your changes.</span></span> <span data-ttu-id="8d472-333">Une erreur s’affiche, comme vous pouvez vous y attendre :</span><span class="sxs-lookup"><span data-stu-id="8d472-333">You'll see an error, as you'd expect:</span></span>

![Page de modification de la vidéo présentant des erreurs de validation](updating-data/_static/image4.png)

<span data-ttu-id="8d472-335">Cliquez sur le lien **revenir à la liste des films** pour abandonner vos modifications et revenir à la page *films* .</span><span class="sxs-lookup"><span data-stu-id="8d472-335">Click the **Return to movie listing** link to abandon your changes and return to the *Movies* page.</span></span>

## <a name="coming-up-next"></a><span data-ttu-id="8d472-336">À venir suivant</span><span class="sxs-lookup"><span data-stu-id="8d472-336">Coming Up Next</span></span>

<span data-ttu-id="8d472-337">Dans le didacticiel suivant, vous verrez comment supprimer un enregistrement de film.</span><span class="sxs-lookup"><span data-stu-id="8d472-337">In the next tutorial, you'll see how to delete a movie record.</span></span>

## <a name="complete-listing-for-movie-page-updated-with-edit-links"></a><span data-ttu-id="8d472-338">Liste complète des pages de films (mise à jour avec les liens de modification)</span><span class="sxs-lookup"><span data-stu-id="8d472-338">Complete Listing for Movie Page (Updated with Edit Links)</span></span>

[!code-cshtml[Main](updating-data/samples/sample20.cshtml)]

<a id="Complete_Page_Listing_for_EditMovie"></a>
## <a name="complete-page-listing-for-edit-movie-page"></a><span data-ttu-id="8d472-339">Liste complète des pages pour la page modifier la vidéo</span><span class="sxs-lookup"><span data-stu-id="8d472-339">Complete Page Listing for Edit Movie Page</span></span>

[!code-cshtml[Main](updating-data/samples/sample21.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="8d472-340">Ressources supplémentaires</span><span class="sxs-lookup"><span data-stu-id="8d472-340">Additional Resources</span></span>

- [<span data-ttu-id="8d472-341">Présentation de la programmation Web ASP.NET à l’aide de la syntaxe Razor</span><span class="sxs-lookup"><span data-stu-id="8d472-341">Introduction to ASP.NET Web Programming by Using the Razor Syntax</span></span>](../../getting-started/introducing-razor-syntax-c.md)
- <span data-ttu-id="8d472-342">[Instruction SQL Update](http://www.w3schools.com/sql/sql_update.asp) sur le site W3Schools</span><span class="sxs-lookup"><span data-stu-id="8d472-342">[SQL UPDATE Statement](http://www.w3schools.com/sql/sql_update.asp) on the W3Schools site</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="8d472-343">[Précédent](entering-data.md)
> [Suivant](deleting-data.md)</span><span class="sxs-lookup"><span data-stu-id="8d472-343">[Previous](entering-data.md)
[Next](deleting-data.md)</span></span>
