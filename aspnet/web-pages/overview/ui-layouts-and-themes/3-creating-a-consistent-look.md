---
uid: web-pages/overview/ui-layouts-and-themes/3-creating-a-consistent-look
title: Création d’une disposition cohérente dans les sites pages Web ASP.NET (Razor) | Microsoft Docs
author: Rick-Anderson
description: Pour améliorer l’efficacité de la création de pages Web pour votre site, vous pouvez créer des blocs de contenu réutilisables (tels que des en-têtes et des pieds de page) pour votre site Web et c...
ms.author: riande
ms.date: 03/10/2014
ms.assetid: d7bd001b-6db2-4422-9b78-f3d08b743b00
msc.legacyurl: /web-pages/overview/ui-layouts-and-themes/3-creating-a-consistent-look
msc.type: authoredcontent
ms.openlocfilehash: 3f63ce68ae4c13970ac0df196167ace0b22b592c
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 03/06/2020
ms.locfileid: "78627448"
---
# <a name="creating-a-consistent-layout-in-aspnet-web-pages-razor-sites"></a><span data-ttu-id="981c3-103">Création d’une disposition cohérente dans les sites pages Web ASP.NET (Razor)</span><span class="sxs-lookup"><span data-stu-id="981c3-103">Creating a Consistent Layout in ASP.NET Web Pages (Razor) Sites</span></span>

<span data-ttu-id="981c3-104">par [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="981c3-104">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="981c3-105">Cet article explique comment vous pouvez utiliser les pages de disposition d’un site Web pages Web ASP.NET (Razor) pour créer des blocs de contenu réutilisables (tels que des en-têtes et des pieds de page) et créer une apparence cohérente pour toutes les pages du site.</span><span class="sxs-lookup"><span data-stu-id="981c3-105">This article explains how you can use layout pages in an ASP.NET Web Pages (Razor) website to create reusable blocks of content (like headers and footers) and to create a consistent look for all the pages in the site.</span></span>
> 
> <span data-ttu-id="981c3-106">**Ce que vous allez apprendre :**</span><span class="sxs-lookup"><span data-stu-id="981c3-106">**What you'll learn:**</span></span> 
> 
> - <span data-ttu-id="981c3-107">Comment créer des blocs de contenu réutilisables comme des en-têtes et des pieds de page.</span><span class="sxs-lookup"><span data-stu-id="981c3-107">How to create reusable blocks of content like headers and footers.</span></span>
> - <span data-ttu-id="981c3-108">Comment créer une apparence cohérente pour toutes les pages de votre site à l’aide d’une disposition.</span><span class="sxs-lookup"><span data-stu-id="981c3-108">How to create a consistent look for all the pages in your site using a layout.</span></span>
> - <span data-ttu-id="981c3-109">Comment passer des données au moment de l’exécution à une page de disposition.</span><span class="sxs-lookup"><span data-stu-id="981c3-109">How to pass data at run time to a layout page.</span></span>
> 
> <span data-ttu-id="981c3-110">Voici les fonctionnalités ASP.NET présentées dans l’article :</span><span class="sxs-lookup"><span data-stu-id="981c3-110">These are the ASP.NET features introduced in the article:</span></span>
> 
> - <span data-ttu-id="981c3-111">Blocs de contenu, qui sont des fichiers contenant du contenu au format HTML à insérer dans plusieurs pages.</span><span class="sxs-lookup"><span data-stu-id="981c3-111">Content blocks, which are files that contain HTML-formatted content to be inserted in multiple pages.</span></span>
> - <span data-ttu-id="981c3-112">Les pages de disposition, qui sont des pages qui contiennent du contenu au format HTML qui peuvent être partagées par des pages du site Web.</span><span class="sxs-lookup"><span data-stu-id="981c3-112">Layout pages, which are pages that contain HTML-formatted content that can be shared by pages on the website.</span></span>
> - <span data-ttu-id="981c3-113">Les méthodes `RenderPage`, `RenderBody`et `RenderSection`, qui indiquent à ASP.NET où insérer les éléments de page.</span><span class="sxs-lookup"><span data-stu-id="981c3-113">The `RenderPage`, `RenderBody`, and `RenderSection` methods, which tell ASP.NET where to insert page elements.</span></span>
> - <span data-ttu-id="981c3-114">Le dictionnaire `PageData` qui vous permet de partager des données entre des blocs de contenu et des pages de disposition.</span><span class="sxs-lookup"><span data-stu-id="981c3-114">The `PageData` dictionary that lets you share data between content blocks and layout pages.</span></span>
>   
> 
> ## <a name="software-versions-used-in-the-tutorial"></a><span data-ttu-id="981c3-115">Versions logicielles utilisées dans le didacticiel</span><span class="sxs-lookup"><span data-stu-id="981c3-115">Software versions used in the tutorial</span></span>
> 
> 
> - <span data-ttu-id="981c3-116">Pages Web ASP.NET (Razor) 3</span><span class="sxs-lookup"><span data-stu-id="981c3-116">ASP.NET Web Pages (Razor) 3</span></span>
>   
> 
> <span data-ttu-id="981c3-117">Ce didacticiel fonctionne également avec pages Web ASP.NET 2.</span><span class="sxs-lookup"><span data-stu-id="981c3-117">This tutorial also works with ASP.NET Web Pages 2.</span></span>

## <a name="about-layout-pages"></a><span data-ttu-id="981c3-118">À propos des pages de disposition</span><span class="sxs-lookup"><span data-stu-id="981c3-118">About Layout Pages</span></span>

<span data-ttu-id="981c3-119">De nombreux sites Web contiennent du contenu qui s’affiche sur chaque page, comme un en-tête et un pied de page, ou une zone qui indique aux utilisateurs qu’ils sont connectés.</span><span class="sxs-lookup"><span data-stu-id="981c3-119">Many websites have content that's displayed on every page, like a header and footer, or a box that tells users that they're logged in.</span></span> <span data-ttu-id="981c3-120">ASP.NET vous permet de créer un fichier distinct avec un bloc de contenu qui peut contenir du texte, du balisage et du code, tout comme une page Web standard.</span><span class="sxs-lookup"><span data-stu-id="981c3-120">ASP.NET lets you create a separate file with a content block that can contain text, markup, and code, just like a regular web page.</span></span> <span data-ttu-id="981c3-121">Vous pouvez ensuite insérer le bloc de contenu dans d’autres pages du site où vous souhaitez afficher les informations.</span><span class="sxs-lookup"><span data-stu-id="981c3-121">You can then insert the content block in other pages on the site where you want the information to appear.</span></span> <span data-ttu-id="981c3-122">De cette façon, vous n’êtes pas obligé de copier et coller le même contenu dans chaque page.</span><span class="sxs-lookup"><span data-stu-id="981c3-122">That way you don't have to copy and paste the same content into every page.</span></span> <span data-ttu-id="981c3-123">La création d’un contenu courant comme celui-ci facilite également la mise à jour de votre site.</span><span class="sxs-lookup"><span data-stu-id="981c3-123">Creating common content like this also makes it easier to update your site.</span></span> <span data-ttu-id="981c3-124">Si vous avez besoin de modifier le contenu, vous pouvez simplement mettre à jour un seul fichier, et les modifications sont reflétées partout où le contenu a été inséré.</span><span class="sxs-lookup"><span data-stu-id="981c3-124">If you need to change the content, you can just update a single file, and the changes are then reflected everywhere the content has been inserted.</span></span>

<span data-ttu-id="981c3-125">Le diagramme suivant illustre le fonctionnement des blocs de contenu.</span><span class="sxs-lookup"><span data-stu-id="981c3-125">The following diagram shows how content blocks work.</span></span> <span data-ttu-id="981c3-126">Quand un navigateur demande une page du serveur Web, ASP.NET insère les blocs de contenu au point où la méthode `RenderPage` est appelée dans la page principale.</span><span class="sxs-lookup"><span data-stu-id="981c3-126">When a browser requests a page from the web server, ASP.NET inserts the content blocks at the point where the `RenderPage` method is called in the main page.</span></span> <span data-ttu-id="981c3-127">La page terminé (fusionnée) est ensuite envoyée au navigateur.</span><span class="sxs-lookup"><span data-stu-id="981c3-127">The finished (merged) page is then sent to the browser.</span></span>

![Diagramme conceptuel montrant comment la méthode RenderPage insère une page référencée dans la page active.](3-creating-a-consistent-look/_static/image1.jpg)

<span data-ttu-id="981c3-129">Dans cette procédure, vous allez créer une page qui référence deux blocs de contenu (un en-tête et un pied de page) situés dans des fichiers distincts.</span><span class="sxs-lookup"><span data-stu-id="981c3-129">In this procedure, you'll create a page that references two content blocks (a header and a footer) that are located in separate files.</span></span> <span data-ttu-id="981c3-130">Vous pouvez utiliser ces mêmes blocs de contenu dans n’importe quelle page de votre site.</span><span class="sxs-lookup"><span data-stu-id="981c3-130">You can use these same content blocks in any page in your site.</span></span> <span data-ttu-id="981c3-131">Une fois que vous avez terminé, vous obtenez une page semblable à celle-ci :</span><span class="sxs-lookup"><span data-stu-id="981c3-131">When you're done, you'll get a page like this:</span></span>

![Capture d’écran montrant une page dans le navigateur qui résulte de l’exécution d’une page qui comprend des appels à la méthode RenderPage.](3-creating-a-consistent-look/_static/image2.png)

1. <span data-ttu-id="981c3-133">Dans le dossier racine de votre site Web, créez un fichier nommé *index. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="981c3-133">In the root folder of your website, create a file named *Index.cshtml*.</span></span>
2. <span data-ttu-id="981c3-134">Remplacez le balisage existant par le code suivant :</span><span class="sxs-lookup"><span data-stu-id="981c3-134">Replace the existing markup with the following:</span></span>

    [!code-html[Main](3-creating-a-consistent-look/samples/sample1.html)]
3. <span data-ttu-id="981c3-135">Dans le dossier racine, créez un dossier nommé *Shared*.</span><span class="sxs-lookup"><span data-stu-id="981c3-135">In the root folder, create a folder named *Shared*.</span></span>

    > [!NOTE]
    > <span data-ttu-id="981c3-136">Il est courant de stocker les fichiers partagés entre les pages Web dans un dossier nommé *Shared*.</span><span class="sxs-lookup"><span data-stu-id="981c3-136">It's common practice to store files that are shared among web pages in a folder named *Shared*.</span></span>
4. <span data-ttu-id="981c3-137">Dans le dossier *partagé* , créez un fichier nommé *\_Header. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="981c3-137">In the *Shared* folder, create a file named *\_Header.cshtml*.</span></span>
5. <span data-ttu-id="981c3-138">Remplacez tout contenu existant par ce qui suit :</span><span class="sxs-lookup"><span data-stu-id="981c3-138">Replace any existing content with the following:</span></span>

    [!code-html[Main](3-creating-a-consistent-look/samples/sample2.html)]

    <span data-ttu-id="981c3-139">Notez que le nom de fichier est *\_Header. cshtml*, avec un trait de soulignement (\_) comme préfixe.</span><span class="sxs-lookup"><span data-stu-id="981c3-139">Notice that the file name is *\_Header.cshtml*, with an underscore (\_) as a prefix.</span></span> <span data-ttu-id="981c3-140">ASP.NET n’envoie pas de page au navigateur si son nom commence par un trait de soulignement.</span><span class="sxs-lookup"><span data-stu-id="981c3-140">ASP.NET won't send a page to the browser if its name starts with an underscore.</span></span> <span data-ttu-id="981c3-141">Cela empêche les personnes de demander (par inadvertance ou autrement) ces pages directement.</span><span class="sxs-lookup"><span data-stu-id="981c3-141">This prevents people from requesting (inadvertently or otherwise) these pages directly.</span></span> <span data-ttu-id="981c3-142">Il est judicieux d’utiliser un trait de soulignement pour nommer les pages qui contiennent des blocs de contenu, car vous ne souhaitez pas vraiment que les utilisateurs soient en &#8212; mesure de demander l’insertion de ces pages dans d’autres pages.</span><span class="sxs-lookup"><span data-stu-id="981c3-142">It's a good idea to use an underscore to name pages that have content blocks in them, because you don't really want users to be able to request these pages &#8212; they exist strictly to be inserted into other pages.</span></span>
6. <span data-ttu-id="981c3-143">Dans le dossier *partagé* , créez un fichier nommé *\_footer. cshtml* et remplacez le contenu par ce qui suit :</span><span class="sxs-lookup"><span data-stu-id="981c3-143">In the *Shared* folder, create a file named *\_Footer.cshtml* and replace the content with the following:</span></span>

    [!code-html[Main](3-creating-a-consistent-look/samples/sample3.html)]
7. <span data-ttu-id="981c3-144">Dans la page *index. cshtml* , ajoutez deux appels à la méthode `RenderPage`, comme illustré ici :</span><span class="sxs-lookup"><span data-stu-id="981c3-144">In the *Index.cshtml* page, add two calls to the `RenderPage` method, as shown here:</span></span>

    [!code-html[Main](3-creating-a-consistent-look/samples/sample4.html)]

    <span data-ttu-id="981c3-145">Cela montre comment insérer un bloc de contenu dans une page Web.</span><span class="sxs-lookup"><span data-stu-id="981c3-145">This shows how to insert a content block into a web page.</span></span> <span data-ttu-id="981c3-146">Vous appelez la méthode `RenderPage` et lui transmettez le nom du fichier dont vous souhaitez insérer le contenu à ce stade.</span><span class="sxs-lookup"><span data-stu-id="981c3-146">You call the `RenderPage` method and pass it the name of the file whose contents you want to insert at that point.</span></span> <span data-ttu-id="981c3-147">Ici, vous insérez le contenu des fichiers *\_Header. cshtml* et *\_footer. cshtml* dans le fichier *index. cshtml* .</span><span class="sxs-lookup"><span data-stu-id="981c3-147">Here, you're inserting the contents of the *\_Header.cshtml* and *\_Footer.cshtml* files into the *Index.cshtml* file.</span></span>
8. <span data-ttu-id="981c3-148">Exécutez la page *index. cshtml* dans un navigateur.</span><span class="sxs-lookup"><span data-stu-id="981c3-148">Run the *Index.cshtml* page in a browser.</span></span> <span data-ttu-id="981c3-149">(Dans WebMatrix, dans l’espace de travail **fichiers** , cliquez avec le bouton droit sur le fichier, puis sélectionnez **lancer dans le navigateur**.)</span><span class="sxs-lookup"><span data-stu-id="981c3-149">(In WebMatrix, in the **Files** workspace, right-click the file and then select **Launch in browser**.)</span></span>
9. <span data-ttu-id="981c3-150">Dans le navigateur, affichez la page source.</span><span class="sxs-lookup"><span data-stu-id="981c3-150">In the browser, view the page source.</span></span> <span data-ttu-id="981c3-151">(Par exemple, dans Internet Explorer, cliquez avec le bouton droit sur la page, puis cliquez sur **afficher la source**.)</span><span class="sxs-lookup"><span data-stu-id="981c3-151">(For example, in Internet Explorer, right-click the page and then click **View Source**.)</span></span>

    <span data-ttu-id="981c3-152">Cela vous permet de voir le balisage de la page Web qui est envoyé au navigateur, qui combine le balisage de la page d’index avec les blocs de contenu.</span><span class="sxs-lookup"><span data-stu-id="981c3-152">This lets you see the web page markup that's sent to the browser, which combines the index page markup with the content blocks.</span></span> <span data-ttu-id="981c3-153">L’exemple suivant illustre la source de la page rendue pour *index. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="981c3-153">The following example shows the page source that's rendered for *Index.cshtml*.</span></span> <span data-ttu-id="981c3-154">Les appels à `RenderPage` que vous avez insérés dans *index. cshtml* ont été remplacés par le contenu réel des fichiers d’en-tête et de pied de page.</span><span class="sxs-lookup"><span data-stu-id="981c3-154">The calls to `RenderPage` that you inserted into *Index.cshtml* have been replaced with the actual contents of the header and footer files.</span></span>

    [!code-html[Main](3-creating-a-consistent-look/samples/sample5.html)]

## <a name="creating-a-consistent-look-using-layout-pages"></a><span data-ttu-id="981c3-155">Création d’une apparence cohérente à l’aide des pages de disposition</span><span class="sxs-lookup"><span data-stu-id="981c3-155">Creating a Consistent Look Using Layout Pages</span></span>

<span data-ttu-id="981c3-156">Jusqu’à présent, vous savez qu’il est facile d’inclure le même contenu sur plusieurs pages.</span><span class="sxs-lookup"><span data-stu-id="981c3-156">So far you've seen that it's easy to include the same content on multiple pages.</span></span> <span data-ttu-id="981c3-157">Une approche plus structurée de la création d’une apparence cohérente pour un site consiste à utiliser des pages de disposition.</span><span class="sxs-lookup"><span data-stu-id="981c3-157">A more structured approach to creating a consistent look for a site is to use layout pages.</span></span> <span data-ttu-id="981c3-158">Une page de disposition définit la structure d’une page Web, mais ne contient pas de contenu réel.</span><span class="sxs-lookup"><span data-stu-id="981c3-158">A layout page defines the structure of a web page, but doesn't contain any actual content.</span></span> <span data-ttu-id="981c3-159">Une fois que vous avez créé une page de disposition, vous pouvez créer des pages Web qui contiennent le contenu, puis les lier à la page de disposition.</span><span class="sxs-lookup"><span data-stu-id="981c3-159">After you've created a layout page, you can create web pages that contain the content and then link them to the layout page.</span></span> <span data-ttu-id="981c3-160">Lorsque ces pages sont affichées, elles sont formatées en fonction de la page de disposition.</span><span class="sxs-lookup"><span data-stu-id="981c3-160">When these pages are displayed, they'll be formatted according to the layout page.</span></span> <span data-ttu-id="981c3-161">(Dans ce sens, une page de disposition agit comme un type de modèle pour le contenu qui est défini dans d’autres pages.)</span><span class="sxs-lookup"><span data-stu-id="981c3-161">(In this sense, a layout page acts as a kind of template for content that's defined in other pages.)</span></span>

<span data-ttu-id="981c3-162">La page de disposition est similaire à une page HTML, à la différence qu’elle contient un appel à la méthode `RenderBody`.</span><span class="sxs-lookup"><span data-stu-id="981c3-162">The layout page is just like any HTML page, except that it contains a call to the `RenderBody` method.</span></span> <span data-ttu-id="981c3-163">La position de la méthode `RenderBody` dans la page de disposition détermine l’emplacement où les informations de la page de contenu seront incluses.</span><span class="sxs-lookup"><span data-stu-id="981c3-163">The position of the `RenderBody` method in the layout page determines where the information from the content page will be included.</span></span>

<span data-ttu-id="981c3-164">Le diagramme suivant montre comment les pages de contenu et les pages de disposition sont combinées au moment de l’exécution pour produire la page Web terminée.</span><span class="sxs-lookup"><span data-stu-id="981c3-164">The following diagram shows how content pages and layout pages are combined at run time to produce the finished web page.</span></span> <span data-ttu-id="981c3-165">Le navigateur demande une page de contenu.</span><span class="sxs-lookup"><span data-stu-id="981c3-165">The browser requests a content page.</span></span> <span data-ttu-id="981c3-166">La page de contenu contient du code qui spécifie la page de disposition à utiliser pour la structure de la page.</span><span class="sxs-lookup"><span data-stu-id="981c3-166">The content page has code in it that specifies the layout page to use for the page's structure.</span></span> <span data-ttu-id="981c3-167">Dans la page disposition, le contenu est inséré au point où la méthode `RenderBody` est appelée.</span><span class="sxs-lookup"><span data-stu-id="981c3-167">In the layout page, the content is inserted at the point where the `RenderBody` method is called.</span></span> <span data-ttu-id="981c3-168">Les blocs de contenu peuvent également être insérés dans la page de disposition en appelant la méthode `RenderPage`, comme vous l’avez fait dans la section précédente.</span><span class="sxs-lookup"><span data-stu-id="981c3-168">Content blocks can also be inserted into the layout page by calling the `RenderPage` method, the way you did in the previous section.</span></span> <span data-ttu-id="981c3-169">Une fois la page Web terminée, elle est envoyée au navigateur.</span><span class="sxs-lookup"><span data-stu-id="981c3-169">When the web page is complete, it's sent to the browser.</span></span>

![Capture d’écran montrant une page dans le navigateur qui résulte de l’exécution d’une page qui comprend des appels à la méthode RenderBody.](3-creating-a-consistent-look/_static/image3.jpg)

<span data-ttu-id="981c3-171">La procédure suivante montre comment créer une page de disposition et lier des pages de contenu à celle-ci.</span><span class="sxs-lookup"><span data-stu-id="981c3-171">The following procedure shows how to create a layout page and link content pages to it.</span></span>

1. <span data-ttu-id="981c3-172">Dans le dossier *partagé* de votre site Web, créez un fichier nommé *\_layout1. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="981c3-172">In the *Shared* folder of your website, create a file named *\_Layout1.cshtml*.</span></span>
2. <span data-ttu-id="981c3-173">Remplacez tout contenu existant par ce qui suit :</span><span class="sxs-lookup"><span data-stu-id="981c3-173">Replace any existing content with the following:</span></span>

    [!code-html[Main](3-creating-a-consistent-look/samples/sample6.html)]

    <span data-ttu-id="981c3-174">Vous utilisez la méthode `RenderPage` dans une page de disposition pour insérer des blocs de contenu.</span><span class="sxs-lookup"><span data-stu-id="981c3-174">You use the `RenderPage` method in a layout page to insert content blocks.</span></span> <span data-ttu-id="981c3-175">Une page de disposition ne peut contenir qu’un seul appel à la méthode `RenderBody`.</span><span class="sxs-lookup"><span data-stu-id="981c3-175">A layout page can contain only one call to the `RenderBody` method.</span></span>
3. <span data-ttu-id="981c3-176">Dans le dossier *partagé* , créez un fichier nommé *\_Header2. cshtml* et remplacez le contenu existant par ce qui suit :</span><span class="sxs-lookup"><span data-stu-id="981c3-176">In the *Shared* folder, create a file named *\_Header2.cshtml* and replace any existing content with the following:</span></span>

    [!code-html[Main](3-creating-a-consistent-look/samples/sample7.html)]
4. <span data-ttu-id="981c3-177">Dans le dossier racine, créez un nouveau dossier et nommez-le *styles*.</span><span class="sxs-lookup"><span data-stu-id="981c3-177">In the root folder, create a new folder and name it *Styles*.</span></span>
5. <span data-ttu-id="981c3-178">Dans le dossier *styles* , créez un fichier nommé *site. CSS* et ajoutez les définitions de style suivantes :</span><span class="sxs-lookup"><span data-stu-id="981c3-178">In the *Styles* folder, create a file named *Site.css* and add the following style definitions:</span></span>

    [!code-css[Main](3-creating-a-consistent-look/samples/sample8.css)]

    <span data-ttu-id="981c3-179">Ces définitions de style sont ici uniquement pour montrer comment les feuilles de style peuvent être utilisées avec les pages de disposition.</span><span class="sxs-lookup"><span data-stu-id="981c3-179">These style definitions are here only to show how style sheets can be used with layout pages.</span></span> <span data-ttu-id="981c3-180">Si vous le souhaitez, vous pouvez définir vos propres styles pour ces éléments.</span><span class="sxs-lookup"><span data-stu-id="981c3-180">If you want, you can define your own styles for these elements.</span></span>
6. <span data-ttu-id="981c3-181">Dans le dossier racine, créez un fichier nommé *Content1. cshtml* et remplacez le contenu existant par ce qui suit :</span><span class="sxs-lookup"><span data-stu-id="981c3-181">In the root folder, create a file named *Content1.cshtml* and replace any existing content with the following:</span></span>

    [!code-cshtml[Main](3-creating-a-consistent-look/samples/sample9.cshtml)]

    <span data-ttu-id="981c3-182">Il s’agit d’une page qui utilise une page de disposition.</span><span class="sxs-lookup"><span data-stu-id="981c3-182">This is a page that will use a layout page.</span></span> <span data-ttu-id="981c3-183">Le bloc de code en haut de la page indique la page de disposition à utiliser pour mettre en forme ce contenu.</span><span class="sxs-lookup"><span data-stu-id="981c3-183">The code block at the top of the page indicates which layout page to use to format this content.</span></span>
7. <span data-ttu-id="981c3-184">Exécutez *Content1. cshtml* dans un navigateur.</span><span class="sxs-lookup"><span data-stu-id="981c3-184">Run *Content1.cshtml* in a browser.</span></span> <span data-ttu-id="981c3-185">La page rendue utilise le format et la feuille de style définis dans *\_layout1. cshtml* et le texte (contenu) défini dans *Content1. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="981c3-185">The rendered page uses the format and style sheet defined in *\_Layout1.cshtml* and the text (content) defined in *Content1.cshtml*.</span></span>

    ![[image]](3-creating-a-consistent-look/_static/image4.png)

    <span data-ttu-id="981c3-187">Vous pouvez répéter l’étape 6 pour créer des pages de contenu supplémentaires qui peuvent ensuite partager la même page de disposition.</span><span class="sxs-lookup"><span data-stu-id="981c3-187">You can repeat step 6 to create additional content pages that can then share the same layout page.</span></span>

    > [!NOTE]
    > <span data-ttu-id="981c3-188">Vous pouvez configurer votre site pour que vous puissiez utiliser automatiquement la même page de disposition pour toutes les pages de contenu d’un dossier.</span><span class="sxs-lookup"><span data-stu-id="981c3-188">You can set up your site so that you can automatically use the same layout page for all the content pages in a folder.</span></span> <span data-ttu-id="981c3-189">Pour plus d’informations, consultez [Personnalisation du comportement à l’ensemble du site pour pages Web ASP.net](https://go.microsoft.com/fwlink/?LinkId=202906).</span><span class="sxs-lookup"><span data-stu-id="981c3-189">For details, see [Customizing Site-Wide Behavior for ASP.NET Web Pages](https://go.microsoft.com/fwlink/?LinkId=202906).</span></span>

## <a name="designing-layout-pages-that-have-multiple-content-sections"></a><span data-ttu-id="981c3-190">Conception de pages de disposition comportant plusieurs sections de contenu</span><span class="sxs-lookup"><span data-stu-id="981c3-190">Designing Layout Pages That Have Multiple Content Sections</span></span>

<span data-ttu-id="981c3-191">Une page de contenu peut avoir plusieurs sections, ce qui est utile si vous souhaitez utiliser des dispositions qui ont plusieurs zones avec du contenu remplaçable.</span><span class="sxs-lookup"><span data-stu-id="981c3-191">A content page can have multiple sections, which is useful if you want to use layouts that have multiple areas with replaceable content.</span></span> <span data-ttu-id="981c3-192">Dans la page contenu, attribuez un nom unique à chaque section.</span><span class="sxs-lookup"><span data-stu-id="981c3-192">In the content page, you give each section a unique name.</span></span> <span data-ttu-id="981c3-193">(La section par défaut n’a pas de nom.) Dans la page disposition, vous ajoutez une méthode `RenderBody` pour spécifier l’emplacement où la section sans nom (par défaut) doit apparaître.</span><span class="sxs-lookup"><span data-stu-id="981c3-193">(The default section is left unnamed.) In the layout page, you add a `RenderBody` method to specify where the unnamed (default) section should appear.</span></span> <span data-ttu-id="981c3-194">Vous ajoutez ensuite des méthodes `RenderSection` distinctes afin de rendre les sections nommées individuellement.</span><span class="sxs-lookup"><span data-stu-id="981c3-194">You then add separate `RenderSection` methods in order to render named sections individually.</span></span>

<span data-ttu-id="981c3-195">Le diagramme suivant montre comment ASP.NET gère le contenu qui est divisé en plusieurs sections.</span><span class="sxs-lookup"><span data-stu-id="981c3-195">The following diagram shows how ASP.NET handles content that's divided into multiple sections.</span></span> <span data-ttu-id="981c3-196">Chaque section nommée est contenue dans un bloc de section de la page de contenu.</span><span class="sxs-lookup"><span data-stu-id="981c3-196">Each named section is contained in a section block in the content page.</span></span> <span data-ttu-id="981c3-197">(Ils sont nommés `Header` et `List` dans l’exemple.) Le Framework insère la section de contenu dans la page de disposition à l’endroit où la méthode `RenderSection` est appelée.</span><span class="sxs-lookup"><span data-stu-id="981c3-197">(They're named `Header` and `List` in the example.) The framework inserts content section into the layout page at the point where the `RenderSection` method is called.</span></span> <span data-ttu-id="981c3-198">La section sans nom (valeur par défaut) est insérée au point où la méthode `RenderBody` est appelée, comme vous l’avez vu précédemment.</span><span class="sxs-lookup"><span data-stu-id="981c3-198">The unnamed (default) section is inserted at the point where the `RenderBody` method is called, as you saw earlier.</span></span>

![Diagramme conceptuel montrant comment la méthode RenderSection insère des sections de références dans la page actuelle.](3-creating-a-consistent-look/_static/image5.jpg)

<span data-ttu-id="981c3-200">Cette procédure indique comment créer une page de contenu qui contient plusieurs sections de contenu et comment la restituer à l’aide d’une page de disposition qui prend en charge plusieurs sections de contenu.</span><span class="sxs-lookup"><span data-stu-id="981c3-200">This procedure shows how to create a content page that has multiple content sections and how to render it using a layout page that supports multiple content sections.</span></span>

1. <span data-ttu-id="981c3-201">Dans le dossier *partagé* , créez un fichier nommé *\_Layout2. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="981c3-201">In the *Shared* folder, create a file named *\_Layout2.cshtml*.</span></span>
2. <span data-ttu-id="981c3-202">Remplacez tout contenu existant par ce qui suit :</span><span class="sxs-lookup"><span data-stu-id="981c3-202">Replace any existing content with the following:</span></span>

    [!code-html[Main](3-creating-a-consistent-look/samples/sample10.html)]

    <span data-ttu-id="981c3-203">Vous utilisez la méthode `RenderSection` pour restituer les sections d’en-tête et de liste.</span><span class="sxs-lookup"><span data-stu-id="981c3-203">You use the `RenderSection` method to render both the header and list sections.</span></span>
3. <span data-ttu-id="981c3-204">Dans le dossier racine, créez un fichier nommé *Content2. cshtml* et remplacez le contenu existant par ce qui suit :</span><span class="sxs-lookup"><span data-stu-id="981c3-204">In the root folder, create a file named *Content2.cshtml* and replace any existing content with the following:</span></span>

    [!code-cshtml[Main](3-creating-a-consistent-look/samples/sample11.cshtml)]

    <span data-ttu-id="981c3-205">Cette page de contenu contient un bloc de code en haut de la page.</span><span class="sxs-lookup"><span data-stu-id="981c3-205">This content page contains a code block at the top of the page.</span></span> <span data-ttu-id="981c3-206">Chaque section nommée est contenue dans un bloc de section.</span><span class="sxs-lookup"><span data-stu-id="981c3-206">Each named section is contained in a section block.</span></span> <span data-ttu-id="981c3-207">Le reste de la page contient la section de contenu (sans nom) par défaut.</span><span class="sxs-lookup"><span data-stu-id="981c3-207">The rest of the page contains the default (unnamed) content section.</span></span>
4. <span data-ttu-id="981c3-208">Exécutez *Content2. cshtml* dans un navigateur.</span><span class="sxs-lookup"><span data-stu-id="981c3-208">Run *Content2.cshtml* in a browser.</span></span>

    ![Capture d’écran montrant une page dans le navigateur qui résulte de l’exécution d’une page qui comprend des appels à la méthode RenderSection.](3-creating-a-consistent-look/_static/image6.png)

## <a name="making-content-sections-optional"></a><span data-ttu-id="981c3-210">Rendre les sections de contenu facultatives</span><span class="sxs-lookup"><span data-stu-id="981c3-210">Making Content Sections Optional</span></span>

<span data-ttu-id="981c3-211">Normalement, les sections que vous créez dans une page de contenu doivent correspondre aux sections définies dans la page de disposition.</span><span class="sxs-lookup"><span data-stu-id="981c3-211">Normally, the sections that you create in a content page have to match sections that are defined in the layout page.</span></span> <span data-ttu-id="981c3-212">Vous pouvez recevoir des erreurs si l’un des éléments suivants se produit :</span><span class="sxs-lookup"><span data-stu-id="981c3-212">You can get errors if any of the following occur:</span></span>

- <span data-ttu-id="981c3-213">La page de contenu contient une section qui n’a pas de section correspondante dans la page de disposition.</span><span class="sxs-lookup"><span data-stu-id="981c3-213">The content page contains a section that has no corresponding section in the layout page.</span></span>
- <span data-ttu-id="981c3-214">La page de disposition contient une section pour laquelle il n’existe aucun contenu.</span><span class="sxs-lookup"><span data-stu-id="981c3-214">The layout page contains a section for which there's no content.</span></span>
- <span data-ttu-id="981c3-215">La page de disposition comprend des appels de méthode qui essaient d’afficher plusieurs fois la même section.</span><span class="sxs-lookup"><span data-stu-id="981c3-215">The layout page includes method calls that try to render the same section more than once.</span></span>

<span data-ttu-id="981c3-216">Toutefois, vous pouvez substituer ce comportement pour une section nommée en déclarant la section comme étant facultative dans la page de disposition.</span><span class="sxs-lookup"><span data-stu-id="981c3-216">However, you can override this behavior for a named section by declaring the section to be optional in the layout page.</span></span> <span data-ttu-id="981c3-217">Cela vous permet de définir plusieurs pages de contenu qui peuvent partager une page de disposition, mais qui peuvent avoir ou non du contenu pour une section spécifique.</span><span class="sxs-lookup"><span data-stu-id="981c3-217">This lets you define multiple content pages that can share a layout page but that might or might not have content for a specific section.</span></span>

1. <span data-ttu-id="981c3-218">Ouvrez *Content2. cshtml* et supprimez la section suivante :</span><span class="sxs-lookup"><span data-stu-id="981c3-218">Open *Content2.cshtml* and remove the following section:</span></span>

    [!code-cshtml[Main](3-creating-a-consistent-look/samples/sample12.cshtml)]
2. <span data-ttu-id="981c3-219">Enregistrez la page, puis exécutez-la dans un navigateur.</span><span class="sxs-lookup"><span data-stu-id="981c3-219">Save the page and then run it in a browser.</span></span> <span data-ttu-id="981c3-220">Un message d’erreur s’affiche, car la page de contenu ne fournit pas de contenu pour une section définie dans la page de disposition, à savoir la section d’en-tête.</span><span class="sxs-lookup"><span data-stu-id="981c3-220">An error message is displayed, because the content page doesn't provide content for a section defined in the layout page, namely the header section.</span></span>

    ![Capture d’écran qui montre l’erreur qui se produit si vous exécutez une page qui appelle la méthode RenderSection, mais que la section correspondante n’est pas fournie.](3-creating-a-consistent-look/_static/image7.png)
3. <span data-ttu-id="981c3-222">Dans le dossier *partagé* , ouvrez la page *\_Layout2. cshtml* et remplacez la ligne suivante :</span><span class="sxs-lookup"><span data-stu-id="981c3-222">In the *Shared* folder, open the *\_Layout2.cshtml* page and replace this line:</span></span>

    [!code-javascript[Main](3-creating-a-consistent-look/samples/sample13.js)]

    <span data-ttu-id="981c3-223">par le code suivant :</span><span class="sxs-lookup"><span data-stu-id="981c3-223">with the following code:</span></span>

    [!code-javascript[Main](3-creating-a-consistent-look/samples/sample14.js)]

    <span data-ttu-id="981c3-224">Vous pouvez également remplacer la ligne de code précédente par le bloc de code suivant, qui produit les mêmes résultats :</span><span class="sxs-lookup"><span data-stu-id="981c3-224">As an alternative, you could replace the previous line of code with the following code block, which produces the same results:</span></span>

    [!code-cshtml[Main](3-creating-a-consistent-look/samples/sample15.cshtml)]
4. <span data-ttu-id="981c3-225">Exécutez à nouveau la page *Content2. cshtml* dans un navigateur.</span><span class="sxs-lookup"><span data-stu-id="981c3-225">Run the *Content2.cshtml* page in a browser again.</span></span> <span data-ttu-id="981c3-226">(Si cette page est toujours ouverte dans le navigateur, il vous suffit de l’actualiser.) Cette fois, la page s’affiche sans erreur, même si elle n’a pas d’en-tête.</span><span class="sxs-lookup"><span data-stu-id="981c3-226">(If you still have this page open in the browser, you can just refresh it.) This time the page is displayed with no error, even though it has no header.</span></span>

## <a name="passing-data-to-layout-pages"></a><span data-ttu-id="981c3-227">Passage de données à des pages de disposition</span><span class="sxs-lookup"><span data-stu-id="981c3-227">Passing Data to Layout Pages</span></span>

<span data-ttu-id="981c3-228">Vous pouvez avoir des données définies dans la page de contenu à laquelle vous devez faire référence dans une page de disposition.</span><span class="sxs-lookup"><span data-stu-id="981c3-228">You might have data defined in the content page that you need to refer to in a layout page.</span></span> <span data-ttu-id="981c3-229">Si c’est le cas, vous devez passer les données de la page de contenu à la page de disposition.</span><span class="sxs-lookup"><span data-stu-id="981c3-229">If so, you need to pass the data from the content page to the layout page.</span></span> <span data-ttu-id="981c3-230">Par exemple, vous souhaiterez peut-être afficher l’état de la connexion d’un utilisateur, ou vous pouvez afficher ou masquer les zones de contenu en fonction de l’entrée de l’utilisateur.</span><span class="sxs-lookup"><span data-stu-id="981c3-230">For example, you might want to display the login status of a user, or you might want to show or hide content areas based on user input.</span></span>

<span data-ttu-id="981c3-231">Pour passer des données d’une page de contenu à une page de disposition, vous pouvez placer des valeurs dans la propriété `PageData` de la page de contenu.</span><span class="sxs-lookup"><span data-stu-id="981c3-231">To pass data from a content page to a layout page, you can put values into the `PageData` property of the content page.</span></span> <span data-ttu-id="981c3-232">La propriété `PageData` est une collection de paires nom/valeur qui contiennent les données que vous souhaitez passer entre les pages.</span><span class="sxs-lookup"><span data-stu-id="981c3-232">The `PageData` property is a collection of name/value pairs that hold the data that you want to pass between pages.</span></span> <span data-ttu-id="981c3-233">Dans la page disposition, vous pouvez ensuite lire les valeurs en dehors de la propriété `PageData`.</span><span class="sxs-lookup"><span data-stu-id="981c3-233">In the layout page, you can then read values out of the `PageData` property.</span></span>

<span data-ttu-id="981c3-234">Voici un autre diagramme.</span><span class="sxs-lookup"><span data-stu-id="981c3-234">Here's another diagram.</span></span> <span data-ttu-id="981c3-235">Celui-ci montre comment ASP.NET peut utiliser la propriété `PageData` pour passer des valeurs d’une page de contenu à la page de disposition.</span><span class="sxs-lookup"><span data-stu-id="981c3-235">This one shows how ASP.NET can use the `PageData` property to pass values from a content page to the layout page.</span></span> <span data-ttu-id="981c3-236">Quand ASP.NET commence à générer la page Web, il crée la collection `PageData`.</span><span class="sxs-lookup"><span data-stu-id="981c3-236">When ASP.NET begins building the web page, it creates the `PageData` collection.</span></span> <span data-ttu-id="981c3-237">Dans la page de contenu, vous écrivez du code pour placer des données dans la collection `PageData`.</span><span class="sxs-lookup"><span data-stu-id="981c3-237">In the content page, you write code to put data in the `PageData` collection.</span></span> <span data-ttu-id="981c3-238">Les valeurs de la collection `PageData` sont également accessibles par d’autres sections de la page de contenu ou par des blocs de contenu supplémentaires.</span><span class="sxs-lookup"><span data-stu-id="981c3-238">Values in the `PageData` collection can also be accessed by other sections in the content page or by additional content blocks.</span></span>

![Diagramme conceptuel qui montre comment une page de contenu peut remplir un dictionnaire PageData et passer ces informations à la page de disposition.](3-creating-a-consistent-look/_static/image8.jpg)

<span data-ttu-id="981c3-240">La procédure suivante indique comment passer des données d’une page de contenu à une page de disposition.</span><span class="sxs-lookup"><span data-stu-id="981c3-240">The following procedure shows how to pass data from a content page to a layout page.</span></span> <span data-ttu-id="981c3-241">Lorsque la page s’exécute, elle affiche un bouton qui permet à l’utilisateur de masquer ou d’afficher une liste définie dans la page de disposition.</span><span class="sxs-lookup"><span data-stu-id="981c3-241">When the page runs, it displays a button that lets the user hide or show a list that's defined in the layout page.</span></span> <span data-ttu-id="981c3-242">Quand les utilisateurs cliquent sur le bouton, il définit une valeur true/false (Boolean) dans la propriété `PageData`.</span><span class="sxs-lookup"><span data-stu-id="981c3-242">When users click the button, it sets a true/false (Boolean) value in the `PageData` property.</span></span> <span data-ttu-id="981c3-243">La page de disposition lit cette valeur et, si elle est définie sur false, masque la liste.</span><span class="sxs-lookup"><span data-stu-id="981c3-243">The layout page reads that value, and if it's false, hides the list.</span></span> <span data-ttu-id="981c3-244">La valeur est également utilisée dans la page de contenu pour déterminer s’il faut afficher le bouton **masquer la liste** ou le bouton **afficher la liste** .</span><span class="sxs-lookup"><span data-stu-id="981c3-244">The value is also used in the content page to determine whether to display the **Hide List** button or the **Show List** button.</span></span>

![[image]](3-creating-a-consistent-look/_static/image9.jpg)

1. <span data-ttu-id="981c3-246">Dans le dossier racine, créez un fichier nommé *Content3. cshtml* et remplacez le contenu existant par ce qui suit :</span><span class="sxs-lookup"><span data-stu-id="981c3-246">In the root folder, create a file named *Content3.cshtml* and replace any existing content with the following:</span></span>

    [!code-cshtml[Main](3-creating-a-consistent-look/samples/sample16.cshtml)]

    <span data-ttu-id="981c3-247">Le code stocke deux éléments de données dans la propriété &#8212; `PageData` le titre de la page Web et true ou false pour spécifier s’il faut afficher une liste.</span><span class="sxs-lookup"><span data-stu-id="981c3-247">The code stores two pieces of data in the `PageData` property &#8212; the title of the web page and true or false to specify whether to display a list.</span></span>

    <span data-ttu-id="981c3-248">Notez que ASP.NET vous permet de placer le balisage HTML dans la page de manière conditionnelle à l’aide d’un bloc de code.</span><span class="sxs-lookup"><span data-stu-id="981c3-248">Notice that ASP.NET lets you put HTML markup into the page conditionally using a code block.</span></span> <span data-ttu-id="981c3-249">Par exemple, le bloc `if/else` dans le corps de la page détermine le formulaire à afficher selon que `PageData["ShowList"]` a la valeur true.</span><span class="sxs-lookup"><span data-stu-id="981c3-249">For example, the `if/else` block in the body of the page determines which form to display depending on whether `PageData["ShowList"]` is set to true.</span></span>
2. <span data-ttu-id="981c3-250">Dans le dossier *partagé* , créez un fichier nommé *\_Layout3. cshtml* et remplacez le contenu existant par ce qui suit :</span><span class="sxs-lookup"><span data-stu-id="981c3-250">In the *Shared* folder, create a file named *\_Layout3.cshtml* and replace any existing content with the following:</span></span>

    [!code-cshtml[Main](3-creating-a-consistent-look/samples/sample17.cshtml)]

    <span data-ttu-id="981c3-251">La page de disposition comprend une expression dans l’élément `<title>` qui obtient la valeur de titre de la propriété `PageData`.</span><span class="sxs-lookup"><span data-stu-id="981c3-251">The layout page includes an expression in the `<title>` element that gets the title value from the `PageData` property.</span></span> <span data-ttu-id="981c3-252">Elle utilise également la valeur `ShowList` de la propriété `PageData` pour déterminer s’il faut afficher le bloc de contenu de la liste.</span><span class="sxs-lookup"><span data-stu-id="981c3-252">It also uses the `ShowList` value of the `PageData` property to determine whether to display the list content block.</span></span>
3. <span data-ttu-id="981c3-253">Dans le dossier *partagé* , créez un fichier nommé *\_List. cshtml* et remplacez le contenu existant par ce qui suit :</span><span class="sxs-lookup"><span data-stu-id="981c3-253">In the *Shared* folder, create a file named *\_List.cshtml* and replace any existing content with the following:</span></span>

    [!code-html[Main](3-creating-a-consistent-look/samples/sample18.html)]
4. <span data-ttu-id="981c3-254">Exécutez la page *Content3. cshtml* dans un navigateur.</span><span class="sxs-lookup"><span data-stu-id="981c3-254">Run the *Content3.cshtml* page in a browser.</span></span> <span data-ttu-id="981c3-255">La page s’affiche avec la liste visible sur le côté gauche de la page et un bouton **masquer la liste** en bas.</span><span class="sxs-lookup"><span data-stu-id="981c3-255">The page is displayed with the list visible on the left side of the page and a **Hide List** button at the bottom.</span></span>

    ![Capture d’écran montrant la page qui comprend la liste et un bouton indiquant « masquer la liste ».](3-creating-a-consistent-look/_static/image10.png)
5. <span data-ttu-id="981c3-257">Cliquez sur **masquer la liste**.</span><span class="sxs-lookup"><span data-stu-id="981c3-257">Click **Hide List**.</span></span> <span data-ttu-id="981c3-258">La liste disparaît et le bouton devient **afficher la liste**.</span><span class="sxs-lookup"><span data-stu-id="981c3-258">The list disappears and the button changes to **Show List**.</span></span>

    ![Capture d’écran montrant la page qui n’inclut pas la liste et un bouton indiquant « afficher la liste ».](3-creating-a-consistent-look/_static/image11.png)
6. <span data-ttu-id="981c3-260">Cliquez sur le bouton **afficher la liste** pour afficher à nouveau la liste.</span><span class="sxs-lookup"><span data-stu-id="981c3-260">Click the **Show List** button, and the list is displayed again.</span></span>

## <a name="additional-resources"></a><span data-ttu-id="981c3-261">Ressources supplémentaires</span><span class="sxs-lookup"><span data-stu-id="981c3-261">Additional Resources</span></span>

[<span data-ttu-id="981c3-262">Personnalisation du comportement à l’ensemble du site pour pages Web ASP.NET</span><span class="sxs-lookup"><span data-stu-id="981c3-262">Customizing Site-Wide Behavior for ASP.NET Web Pages</span></span>](https://go.microsoft.com/fwlink/?LinkId=202906)
