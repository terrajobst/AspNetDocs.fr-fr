---
uid: mvc/overview/older-versions-1/controllers-and-routing/understanding-action-filters-cs
title: Fonctionnement des filtres d'C#action () | Microsoft Docs
author: microsoft
description: L’objectif de ce didacticiel est d’expliquer les filtres d’action. Un filtre d’action est un attribut que vous pouvez appliquer à une action de contrôleur ou à un contrôleur entier...
ms.author: riande
ms.date: 10/16/2008
ms.assetid: a94e4e81-40c1-47b7-8613-126a1a6cc93d
msc.legacyurl: /mvc/overview/older-versions-1/controllers-and-routing/understanding-action-filters-cs
msc.type: authoredcontent
ms.openlocfilehash: d1c72c2355c6122f851351a8c1e8f04fa63ae04e
ms.sourcegitcommit: 22fbd8863672c4ad6693b8388ad5c8e753fb41a2
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 11/28/2019
ms.locfileid: "74590090"
---
# <a name="understanding-action-filters-c"></a><span data-ttu-id="774f7-104">Présentation des filtres d’actions (C#)</span><span class="sxs-lookup"><span data-stu-id="774f7-104">Understanding Action Filters (C#)</span></span>

<span data-ttu-id="774f7-105">par [Microsoft](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="774f7-105">by [Microsoft](https://github.com/microsoft)</span></span>

[<span data-ttu-id="774f7-106">Télécharger PDF</span><span class="sxs-lookup"><span data-stu-id="774f7-106">Download PDF</span></span>](https://download.microsoft.com/download/e/f/3/ef3f2ff6-7424-48f7-bdaa-180ef64c3490/ASPNET_MVC_Tutorial_14_CS.pdf)

> <span data-ttu-id="774f7-107">L’objectif de ce didacticiel est d’expliquer les filtres d’action.</span><span class="sxs-lookup"><span data-stu-id="774f7-107">The goal of this tutorial is to explain action filters.</span></span> <span data-ttu-id="774f7-108">Un filtre d’action est un attribut que vous pouvez appliquer à une action de contrôleur, ou à un contrôleur entier, qui modifie la manière dont l’action est exécutée.</span><span class="sxs-lookup"><span data-stu-id="774f7-108">An action filter is an attribute that you can apply to a controller action -- or an entire controller -- that modifies the way in which the action is executed.</span></span>

## <a name="understanding-action-filters"></a><span data-ttu-id="774f7-109">Fonctionnement des filtres d’action</span><span class="sxs-lookup"><span data-stu-id="774f7-109">Understanding Action Filters</span></span>

<span data-ttu-id="774f7-110">L’objectif de ce didacticiel est d’expliquer les filtres d’action.</span><span class="sxs-lookup"><span data-stu-id="774f7-110">The goal of this tutorial is to explain action filters.</span></span> <span data-ttu-id="774f7-111">Un filtre d’action est un attribut que vous pouvez appliquer à une action de contrôleur, ou à un contrôleur entier, qui modifie la manière dont l’action est exécutée.</span><span class="sxs-lookup"><span data-stu-id="774f7-111">An action filter is an attribute that you can apply to a controller action -- or an entire controller -- that modifies the way in which the action is executed.</span></span> <span data-ttu-id="774f7-112">L’infrastructure MVC ASP.NET comprend plusieurs filtres d’action :</span><span class="sxs-lookup"><span data-stu-id="774f7-112">The ASP.NET MVC framework includes several action filters:</span></span>

- <span data-ttu-id="774f7-113">OutputCache : ce filtre d’action met en cache la sortie d’une action de contrôleur pendant un laps de temps spécifié.</span><span class="sxs-lookup"><span data-stu-id="774f7-113">OutputCache – This action filter caches the output of a controller action for a specified amount of time.</span></span>
- <span data-ttu-id="774f7-114">HandleError : ce filtre d’action gère les erreurs déclenchées lors de l’exécution d’une action de contrôleur.</span><span class="sxs-lookup"><span data-stu-id="774f7-114">HandleError – This action filter handles errors raised when a controller action executes.</span></span>
- <span data-ttu-id="774f7-115">Authorize : ce filtre d’action vous permet de restreindre l’accès à un utilisateur ou à un rôle particulier.</span><span class="sxs-lookup"><span data-stu-id="774f7-115">Authorize – This action filter enables you to restrict access to a particular user or role.</span></span>

<span data-ttu-id="774f7-116">Vous pouvez également créer vos propres filtres d’action personnalisés.</span><span class="sxs-lookup"><span data-stu-id="774f7-116">You also can create your own custom action filters.</span></span> <span data-ttu-id="774f7-117">Par exemple, vous souhaiterez peut-être créer un filtre d’action personnalisé afin d’implémenter un système d’authentification personnalisé.</span><span class="sxs-lookup"><span data-stu-id="774f7-117">For example, you might want to create a custom action filter in order to implement a custom authentication system.</span></span> <span data-ttu-id="774f7-118">Vous pouvez également créer un filtre d’action qui modifie les données d’affichage retournées par une action de contrôleur.</span><span class="sxs-lookup"><span data-stu-id="774f7-118">Or, you might want to create an action filter that modifies the view data returned by a controller action.</span></span>

<span data-ttu-id="774f7-119">Dans ce didacticiel, vous allez apprendre à créer un filtre d’action à partir de zéro.</span><span class="sxs-lookup"><span data-stu-id="774f7-119">In this tutorial, you learn how to build an action filter from the ground up.</span></span> <span data-ttu-id="774f7-120">Nous créons un filtre d’action de journal qui journalise différentes étapes du traitement d’une action dans la fenêtre sortie de Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="774f7-120">We create a Log action filter that logs different stages of the processing of an action to the Visual Studio Output window.</span></span>

### <a name="using-an-action-filter"></a><span data-ttu-id="774f7-121">Utilisation d’un filtre d’action</span><span class="sxs-lookup"><span data-stu-id="774f7-121">Using an Action Filter</span></span>

<span data-ttu-id="774f7-122">Un filtre d’action est un attribut.</span><span class="sxs-lookup"><span data-stu-id="774f7-122">An action filter is an attribute.</span></span> <span data-ttu-id="774f7-123">Vous pouvez appliquer la plupart des filtres d’action à une action de contrôleur individuel ou à un contrôleur entier.</span><span class="sxs-lookup"><span data-stu-id="774f7-123">You can apply most action filters to either an individual controller action or an entire controller.</span></span>

<span data-ttu-id="774f7-124">Par exemple, le contrôleur de données de la liste 1 expose une action nommée `Index()` qui retourne l’heure actuelle.</span><span class="sxs-lookup"><span data-stu-id="774f7-124">For example, the Data controller in Listing 1 exposes an action named `Index()` that returns the current time.</span></span> <span data-ttu-id="774f7-125">Cette action est décorée avec le filtre d’action `OutputCache`.</span><span class="sxs-lookup"><span data-stu-id="774f7-125">This action is decorated with the `OutputCache` action filter.</span></span> <span data-ttu-id="774f7-126">Ce filtre entraîne la mise en cache de la valeur retournée par l’action pendant 10 secondes.</span><span class="sxs-lookup"><span data-stu-id="774f7-126">This filter causes the value returned by the action to be cached for 10 seconds.</span></span>

<span data-ttu-id="774f7-127">**Liste 1 – `Controllers\DataController.cs`**</span><span class="sxs-lookup"><span data-stu-id="774f7-127">**Listing 1 – `Controllers\DataController.cs`**</span></span>

[!code-csharp[Main](understanding-action-filters-cs/samples/sample1.cs)]

<span data-ttu-id="774f7-128">Si vous appelez de manière répétée l’action `Index()` en entrant l’URL/Data/Index dans la barre d’adresses de votre navigateur et en appuyant plusieurs fois sur le bouton actualiser, vous verrez la même durée pendant 10 secondes.</span><span class="sxs-lookup"><span data-stu-id="774f7-128">If you repeatedly invoke the `Index()` action by entering the URL /Data/Index into the address bar of your browser and hitting the Refresh button multiple times, then you will see the same time for 10 seconds.</span></span> <span data-ttu-id="774f7-129">La sortie de l’action `Index()` est mise en cache pendant 10 secondes (voir la figure 1).</span><span class="sxs-lookup"><span data-stu-id="774f7-129">The output of the `Index()` action is cached for 10 seconds (see Figure 1).</span></span>

<span data-ttu-id="774f7-130">[temps mis en cache ![](understanding-action-filters-cs/_static/image2.png)](understanding-action-filters-cs/_static/image1.png)</span><span class="sxs-lookup"><span data-stu-id="774f7-130">[![Cached time](understanding-action-filters-cs/_static/image2.png)](understanding-action-filters-cs/_static/image1.png)</span></span>

<span data-ttu-id="774f7-131">**Figure 01**: temps mis en cache ([cliquez pour afficher l’image en taille réelle](understanding-action-filters-cs/_static/image3.png))</span><span class="sxs-lookup"><span data-stu-id="774f7-131">**Figure 01**: Cached time ([Click to view full-size image](understanding-action-filters-cs/_static/image3.png))</span></span>

<span data-ttu-id="774f7-132">Dans la liste 1, un filtre d’action unique, le filtre d’action `OutputCache`, est appliqué à la méthode `Index()`.</span><span class="sxs-lookup"><span data-stu-id="774f7-132">In Listing 1, a single action filter – the `OutputCache` action filter – is applied to the `Index()` method.</span></span> <span data-ttu-id="774f7-133">Si vous le souhaitez, vous pouvez appliquer plusieurs filtres d’action à la même action.</span><span class="sxs-lookup"><span data-stu-id="774f7-133">If you need, you can apply multiple action filters to the same action.</span></span> <span data-ttu-id="774f7-134">Par exemple, vous souhaiterez peut-être appliquer à la fois les filtres d’action `OutputCache` et `HandleError` à la même action.</span><span class="sxs-lookup"><span data-stu-id="774f7-134">For example, you might want to apply both the `OutputCache` and `HandleError` action filters to the same action.</span></span>

<span data-ttu-id="774f7-135">Dans la liste 1, le filtre d’action `OutputCache` est appliqué à l’action `Index()`.</span><span class="sxs-lookup"><span data-stu-id="774f7-135">In Listing 1, the `OutputCache` action filter is applied to the `Index()` action.</span></span> <span data-ttu-id="774f7-136">Vous pouvez également appliquer cet attribut à la classe `DataController` elle-même.</span><span class="sxs-lookup"><span data-stu-id="774f7-136">You also could apply this attribute to the `DataController` class itself.</span></span> <span data-ttu-id="774f7-137">Dans ce cas, le résultat retourné par une action exposée par le contrôleur est mis en cache pendant 10 secondes.</span><span class="sxs-lookup"><span data-stu-id="774f7-137">In that case, the result returned by any action exposed by the controller would be cached for 10 seconds.</span></span>

### <a name="the-different-types-of-filters"></a><span data-ttu-id="774f7-138">Les différents types de filtres</span><span class="sxs-lookup"><span data-stu-id="774f7-138">The Different Types of Filters</span></span>

<span data-ttu-id="774f7-139">L’infrastructure MVC ASP.NET prend en charge quatre types de filtres :</span><span class="sxs-lookup"><span data-stu-id="774f7-139">The ASP.NET MVC framework supports four different types of filters:</span></span>

1. <span data-ttu-id="774f7-140">Filtres d’autorisation : implémente l’attribut `IAuthorizationFilter`.</span><span class="sxs-lookup"><span data-stu-id="774f7-140">Authorization filters – Implements the `IAuthorizationFilter` attribute.</span></span>
2. <span data-ttu-id="774f7-141">Filtres d’action : implémente l’attribut `IActionFilter`.</span><span class="sxs-lookup"><span data-stu-id="774f7-141">Action filters – Implements the `IActionFilter` attribute.</span></span>
3. <span data-ttu-id="774f7-142">Filtres de résultats : implémente l’attribut `IResultFilter`.</span><span class="sxs-lookup"><span data-stu-id="774f7-142">Result filters – Implements the `IResultFilter` attribute.</span></span>
4. <span data-ttu-id="774f7-143">Filtres d’exception : implémente l’attribut `IExceptionFilter`.</span><span class="sxs-lookup"><span data-stu-id="774f7-143">Exception filters – Implements the `IExceptionFilter` attribute.</span></span>

<span data-ttu-id="774f7-144">Les filtres sont exécutés dans l’ordre indiqué ci-dessus.</span><span class="sxs-lookup"><span data-stu-id="774f7-144">Filters are executed in the order listed above.</span></span> <span data-ttu-id="774f7-145">Par exemple, les filtres d’autorisation sont toujours exécutés avant que les filtres d’action et les filtres d’exception soient toujours exécutés après chaque autre type de filtre.</span><span class="sxs-lookup"><span data-stu-id="774f7-145">For example, authorization filters are always executed before action filters and exception filters are always executed after every other type of filter.</span></span>

<span data-ttu-id="774f7-146">Les filtres d’autorisation sont utilisés pour implémenter l’authentification et l’autorisation pour les actions du contrôleur.</span><span class="sxs-lookup"><span data-stu-id="774f7-146">Authorization filters are used to implement authentication and authorization for controller actions.</span></span> <span data-ttu-id="774f7-147">Par exemple, le filtre authorize est un exemple de filtre d’autorisation.</span><span class="sxs-lookup"><span data-stu-id="774f7-147">For example, the Authorize filter is an example of an Authorization filter.</span></span>

<span data-ttu-id="774f7-148">Les filtres d’action contiennent une logique qui est exécutée avant et après l’exécution d’une action de contrôleur.</span><span class="sxs-lookup"><span data-stu-id="774f7-148">Action filters contain logic that is executed before and after a controller action executes.</span></span> <span data-ttu-id="774f7-149">Vous pouvez utiliser un filtre d’action, par exemple, pour modifier les données d’affichage retournées par une action de contrôleur.</span><span class="sxs-lookup"><span data-stu-id="774f7-149">You can use an action filter, for instance, to modify the view data that a controller action returns.</span></span>

<span data-ttu-id="774f7-150">Les filtres de résultats contiennent une logique qui est exécutée avant et après l’exécution d’un résultat de vue.</span><span class="sxs-lookup"><span data-stu-id="774f7-150">Result filters contain logic that is executed before and after a view result is executed.</span></span> <span data-ttu-id="774f7-151">Par exemple, vous souhaiterez peut-être modifier un résultat d’affichage juste avant que la vue ne soit rendue dans le navigateur.</span><span class="sxs-lookup"><span data-stu-id="774f7-151">For example, you might want to modify a view result right before the view is rendered to the browser.</span></span>

<span data-ttu-id="774f7-152">Les filtres d’exceptions sont le dernier type de filtre à exécuter.</span><span class="sxs-lookup"><span data-stu-id="774f7-152">Exception filters are the last type of filter to run.</span></span> <span data-ttu-id="774f7-153">Vous pouvez utiliser un filtre d’exception pour gérer les erreurs générées par les actions du contrôleur ou les résultats de l’action du contrôleur.</span><span class="sxs-lookup"><span data-stu-id="774f7-153">You can use an exception filter to handle errors raised by either your controller actions or controller action results.</span></span> <span data-ttu-id="774f7-154">Vous pouvez également utiliser des filtres d’exception pour consigner les erreurs.</span><span class="sxs-lookup"><span data-stu-id="774f7-154">You also can use exception filters to log errors.</span></span>

<span data-ttu-id="774f7-155">Chaque type de filtre est exécuté dans un ordre particulier.</span><span class="sxs-lookup"><span data-stu-id="774f7-155">Each different type of filter is executed in a particular order.</span></span> <span data-ttu-id="774f7-156">Si vous souhaitez contrôler l’ordre dans lequel les filtres du même type sont exécutés, vous pouvez définir la propriété d’ordre d’un filtre.</span><span class="sxs-lookup"><span data-stu-id="774f7-156">If you want to control the order in which filters of the same type are executed then you can set a filter's Order property.</span></span>

<span data-ttu-id="774f7-157">La classe de base pour tous les filtres d’action est la classe `System.Web.Mvc.FilterAttribute`.</span><span class="sxs-lookup"><span data-stu-id="774f7-157">The base class for all action filters is the `System.Web.Mvc.FilterAttribute` class.</span></span> <span data-ttu-id="774f7-158">Si vous souhaitez implémenter un type particulier de filtre, vous devez créer une classe qui hérite de la classe de filtre de base et implémente une ou plusieurs des interfaces `IAuthorizationFilter`, `IActionFilter`, `IResultFilter`ou `IExceptionFilter`.</span><span class="sxs-lookup"><span data-stu-id="774f7-158">If you want to implement a particular type of filter, then you need to create a class that inherits from the base Filter class and implements one or more of the `IAuthorizationFilter`, `IActionFilter`, `IResultFilter`, or `IExceptionFilter` interfaces.</span></span>

### <a name="the-base-actionfilterattribute-class"></a><span data-ttu-id="774f7-159">Classe de base ActionFilterAttribute</span><span class="sxs-lookup"><span data-stu-id="774f7-159">The Base ActionFilterAttribute Class</span></span>

<span data-ttu-id="774f7-160">Pour faciliter l’implémentation d’un filtre d’action personnalisé, l’infrastructure MVC ASP.NET comprend une classe de `ActionFilterAttribute` de base.</span><span class="sxs-lookup"><span data-stu-id="774f7-160">In order to make it easier for you to implement a custom action filter, the ASP.NET MVC framework includes a base `ActionFilterAttribute` class.</span></span> <span data-ttu-id="774f7-161">Cette classe implémente à la fois les interfaces `IActionFilter` et `IResultFilter` et hérite de la classe `Filter`.</span><span class="sxs-lookup"><span data-stu-id="774f7-161">This class implements both the `IActionFilter` and `IResultFilter` interfaces and inherits from the `Filter` class.</span></span>

<span data-ttu-id="774f7-162">La terminologie ici n’est pas entièrement cohérente.</span><span class="sxs-lookup"><span data-stu-id="774f7-162">The terminology here is not entirely consistent.</span></span> <span data-ttu-id="774f7-163">Techniquement, une classe qui hérite de la classe ActionFilterAttribute est à la fois un filtre d’action et un filtre de résultat.</span><span class="sxs-lookup"><span data-stu-id="774f7-163">Technically, a class that inherits from the ActionFilterAttribute class is both an action filter and a result filter.</span></span> <span data-ttu-id="774f7-164">Toutefois, dans le sens approximatif, le filtre d’action Word est utilisé pour faire référence à n’importe quel type de filtre dans l’infrastructure MVC ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="774f7-164">However, in the loose sense, the word action filter is used to refer to any type of filter in the ASP.NET MVC framework.</span></span>

<span data-ttu-id="774f7-165">La classe de base `ActionFilterAttribute` possède les méthodes suivantes que vous pouvez substituer :</span><span class="sxs-lookup"><span data-stu-id="774f7-165">The base `ActionFilterAttribute` class has the following methods that you can override:</span></span>

- <span data-ttu-id="774f7-166">OnActionExecuting : cette méthode est appelée avant l’exécution d’une action de contrôleur.</span><span class="sxs-lookup"><span data-stu-id="774f7-166">OnActionExecuting – This method is called before a controller action is executed.</span></span>
- <span data-ttu-id="774f7-167">OnActionExecuted : cette méthode est appelée après l’exécution d’une action de contrôleur.</span><span class="sxs-lookup"><span data-stu-id="774f7-167">OnActionExecuted – This method is called after a controller action is executed.</span></span>
- <span data-ttu-id="774f7-168">OnResultExecuting : cette méthode est appelée avant l’exécution d’un résultat d’action de contrôleur.</span><span class="sxs-lookup"><span data-stu-id="774f7-168">OnResultExecuting – This method is called before a controller action result is executed.</span></span>
- <span data-ttu-id="774f7-169">OnResultExecuted : cette méthode est appelée après l’exécution d’un résultat d’action de contrôleur.</span><span class="sxs-lookup"><span data-stu-id="774f7-169">OnResultExecuted – This method is called after a controller action result is executed.</span></span>

<span data-ttu-id="774f7-170">Dans la section suivante, nous verrons comment vous pouvez implémenter chacune de ces différentes méthodes.</span><span class="sxs-lookup"><span data-stu-id="774f7-170">In the next section, we'll see how you can implement each of these different methods.</span></span>

### <a name="creating-a-log-action-filter"></a><span data-ttu-id="774f7-171">Création d’un filtre d’action de journal</span><span class="sxs-lookup"><span data-stu-id="774f7-171">Creating a Log Action Filter</span></span>

<span data-ttu-id="774f7-172">Pour illustrer la façon dont vous pouvez créer un filtre d’action personnalisé, nous allons créer un filtre d’action personnalisé qui journalise les étapes de traitement d’une action de contrôleur dans la fenêtre sortie de Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="774f7-172">In order to illustrate how you can build a custom action filter, we'll create a custom action filter that logs the stages of processing a controller action to the Visual Studio Output window.</span></span> <span data-ttu-id="774f7-173">Notre `LogActionFilter` est contenu dans la liste 2.</span><span class="sxs-lookup"><span data-stu-id="774f7-173">Our `LogActionFilter` is contained in Listing 2.</span></span>

<span data-ttu-id="774f7-174">**Liste 2 – `ActionFilters\LogActionFilter.cs`**</span><span class="sxs-lookup"><span data-stu-id="774f7-174">**Listing 2 – `ActionFilters\LogActionFilter.cs`**</span></span>

[!code-csharp[Main](understanding-action-filters-cs/samples/sample2.cs)]

<span data-ttu-id="774f7-175">Dans la liste 2, les méthodes `OnActionExecuting()`, `OnActionExecuted()`, `OnResultExecuting()`et `OnResultExecuted()` appellent toutes la méthode `Log()`.</span><span class="sxs-lookup"><span data-stu-id="774f7-175">In Listing 2, the `OnActionExecuting()`, `OnActionExecuted()`, `OnResultExecuting()`, and `OnResultExecuted()` methods all call the `Log()` method.</span></span> <span data-ttu-id="774f7-176">Le nom de la méthode et les données d’itinéraire actuelles sont passés à la méthode `Log()`.</span><span class="sxs-lookup"><span data-stu-id="774f7-176">The name of the method and the current route data is passed to the `Log()` method.</span></span> <span data-ttu-id="774f7-177">La méthode `Log()` écrit un message dans la fenêtre sortie de Visual Studio (voir la figure 2).</span><span class="sxs-lookup"><span data-stu-id="774f7-177">The `Log()` method writes a message to the Visual Studio Output window (see Figure 2).</span></span>

<span data-ttu-id="774f7-178">[![l’écriture dans la fenêtre sortie de Visual Studio](understanding-action-filters-cs/_static/image5.png)](understanding-action-filters-cs/_static/image4.png)</span><span class="sxs-lookup"><span data-stu-id="774f7-178">[![Writing to the Visual Studio Output window](understanding-action-filters-cs/_static/image5.png)](understanding-action-filters-cs/_static/image4.png)</span></span>

<span data-ttu-id="774f7-179">**Figure 02**: écriture dans la fenêtre sortie de Visual Studio ([cliquez pour afficher l’image en taille réelle](understanding-action-filters-cs/_static/image6.png))</span><span class="sxs-lookup"><span data-stu-id="774f7-179">**Figure 02**: Writing to the Visual Studio Output window ([Click to view full-size image](understanding-action-filters-cs/_static/image6.png))</span></span>

<span data-ttu-id="774f7-180">Le contrôleur d’hébergement de la liste 3 montre comment vous pouvez appliquer le filtre d’action de journal à une classe de contrôleur entière.</span><span class="sxs-lookup"><span data-stu-id="774f7-180">The Home controller in Listing 3 illustrates how you can apply the Log action filter to an entire controller class.</span></span> <span data-ttu-id="774f7-181">Chaque fois que l’une des actions exposées par le contrôleur d’hébergement est appelée, soit la méthode `Index()` soit la méthode `About()` : les étapes de traitement de l’action sont enregistrées dans la fenêtre sortie de Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="774f7-181">Whenever any of the actions exposed by the Home controller are invoked – either the `Index()` method or the `About()` method – the stages of processing the action are logged to the Visual Studio Output window.</span></span>

<span data-ttu-id="774f7-182">**Liste 3 – `Controllers\HomeController.cs`**</span><span class="sxs-lookup"><span data-stu-id="774f7-182">**Listing 3 – `Controllers\HomeController.cs`**</span></span>

[!code-csharp[Main](understanding-action-filters-cs/samples/sample3.cs)]

### <a name="summary"></a><span data-ttu-id="774f7-183">Récapitulatif</span><span class="sxs-lookup"><span data-stu-id="774f7-183">Summary</span></span>

<span data-ttu-id="774f7-184">Dans ce didacticiel, vous avez introduit les filtres d’action MVC ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="774f7-184">In this tutorial, you were introduced to ASP.NET MVC action filters.</span></span> <span data-ttu-id="774f7-185">Vous avez appris les quatre types de filtres : les filtres d’autorisation, les filtres d’action, les filtres de résultats et les filtres d’exception.</span><span class="sxs-lookup"><span data-stu-id="774f7-185">You learned about the four different types of filters: authorization filters, action filters, result filters, and exception filters.</span></span> <span data-ttu-id="774f7-186">Vous avez également appris sur la classe de base `ActionFilterAttribute`.</span><span class="sxs-lookup"><span data-stu-id="774f7-186">You also learned about the base `ActionFilterAttribute` class.</span></span>

<span data-ttu-id="774f7-187">Enfin, vous avez appris à implémenter un filtre d’action simple.</span><span class="sxs-lookup"><span data-stu-id="774f7-187">Finally, you learned how to implement a simple action filter.</span></span> <span data-ttu-id="774f7-188">Nous avons créé un filtre d’action de journal qui journalise les étapes de traitement d’une action de contrôleur dans la fenêtre sortie de Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="774f7-188">We created a Log action filter that logs the stages of processing a controller action to the Visual Studio Output window.</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="774f7-189">[Précédent](asp-net-mvc-routing-overview-cs.md)
> [Suivant](improving-performance-with-output-caching-cs.md)</span><span class="sxs-lookup"><span data-stu-id="774f7-189">[Previous](asp-net-mvc-routing-overview-cs.md)
[Next](improving-performance-with-output-caching-cs.md)</span></span>
